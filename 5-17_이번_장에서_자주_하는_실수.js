var e=`# 5장. 데이터 재구조화 실습

## 5.17 이번 장에서 자주 하는 실수

데이터 재구조화 과정에서 자주 하는 실수를 정리합니다.

---

### 5.17.1 pivot과 pivot_table을 혼동하는 실수

\`pivot()\`은 중복 조합을 처리하지 못합니다.

주문 데이터처럼 같은 월과 카테고리 조합이 여러 번 나오는 데이터에는 \`pivot_table()\`을 사용해야 합니다.

\`\`\`python
pd.pivot_table(
    data=df,
    index="month",
    columns="category",
    values="sales",
    aggfunc="sum"
)
\`\`\`

---

### 5.17.2 fill_value를 지정하지 않아 결측치가 생기는 실수

피벗 테이블에서 특정 조합이 없으면 결측치가 생깁니다.

매출 합계에서 해당 조합이 없다는 뜻이라면 0으로 채우는 것이 자연스러울 수 있습니다.

\`\`\`python
fill_value=0
\`\`\`

단, 모든 결측치를 무조건 0으로 채워도 되는 것은 아닙니다.  
데이터 의미를 확인해야 합니다.

---

### 5.17.3 보고용 데이터와 시각화용 데이터를 혼동하는 실수

보고용 피벗 테이블을 그대로 seaborn 그래프에 사용하려고 하면 불편할 수 있습니다.

시각화에는 long format이 더 적합할 때가 많습니다.

\`\`\`python
monthly_category_long
\`\`\`

보고용 표와 시각화용 데이터는 따로 저장하는 것이 좋습니다.

---

### 5.17.4 melt할 때 id_vars를 잘못 지정하는 실수

\`id_vars\`는 유지할 컬럼입니다.

예를 들어 카테고리별 월별 매출 데이터를 melt할 때 \`category\`는 유지해야 합니다.

\`\`\`python
pd.melt(
    wide_sales,
    id_vars=["category"],
    var_name="year_month",
    value_name="total_sales"
)
\`\`\`

\`id_vars\`를 잘못 지정하면 원하는 구조가 나오지 않습니다.

---

### 5.17.5 explode 후 중복 집계를 조심하지 않는 실수

\`explode()\` 후에는 행 수가 늘어납니다.

상품 하나가 태그 3개를 가지면 explode 후 3행이 됩니다.  
이 상태에서 매출을 단순 합계하면 상품 매출이 태그 수만큼 중복 계산될 수 있습니다.

태그별 매출 분석에서는 중복 집계가 의도한 것인지 반드시 확인해야 합니다.

---

### 5.17.6 합계 행을 분석 데이터에 섞어 사용하는 실수

보고용 피벗 테이블에는 합계 행과 합계 열이 있을 수 있습니다.

하지만 이 데이터를 다시 분석에 사용하면 합계가 중복 계산될 수 있습니다.

예:

\`\`\`text
월별 매출 표에 "합계" 행이 포함되어 있는데 다시 sum()을 하면 전체 합계가 두 번 계산될 수 있다.
\`\`\`

합계 행이 있는 데이터는 보고용으로만 사용하고, 분석용 데이터와 구분해야 합니다.

---
`;export{e as default};