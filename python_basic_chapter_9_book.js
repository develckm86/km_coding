var e=`# 9장 외부 라이브러리 활용

## 이 장에서 배울 내용

지금까지 우리는 파이썬의 기본 문법, 조건문과 반복문, 자료구조, 함수, 예외 처리, 객체지향 프로그래밍, 모듈과 패키지를 배웠습니다. 이제 우리는 직접 코드를 작성하고, 파일을 나누고, 다른 파일의 코드를 가져와 사용할 수 있습니다.

하지만 실무에서 필요한 모든 기능을 직접 만들 수는 없습니다.

예를 들어 엑셀 파일을 읽는 기능, 웹 API에 요청을 보내는 기능, 표 형태 데이터를 분석하는 기능, HTML 문서를 해석하는 기능, 테스트를 자동으로 실행하는 기능을 모두 처음부터 만든다면 시간이 너무 오래 걸립니다. 게다가 이미 많은 개발자들이 검증해 둔 좋은 도구들이 있습니다.

이때 사용하는 것이 **외부 라이브러리**입니다.

외부 라이브러리는 파이썬에 기본으로 들어 있지는 않지만, 필요한 경우 설치해서 사용할 수 있는 코드 묶음입니다. 외부 라이브러리를 잘 활용하면 혼자서 모든 기능을 직접 만들지 않아도 됩니다. 이미 만들어진 도구를 가져와서 내가 해결하려는 문제에 집중할 수 있습니다.

이 장에서는 다음 내용을 배웁니다.

- 외부 라이브러리가 무엇인지 이해하기
- 표준 라이브러리와 외부 라이브러리의 차이 이해하기
- 모듈, 패키지, 라이브러리, 프레임워크의 관계 이해하기
- \`pip\`로 외부 라이브러리를 설치하고 관리하기
- 가상환경에서 라이브러리를 관리하는 이유 이해하기
- \`requirements.txt\`로 설치 목록을 저장하고 재현하기
- 외부 라이브러리를 선택할 때 확인해야 할 점 이해하기
- 대표적인 외부 라이브러리의 용도 살펴보기

이 장의 목표는 “외부 라이브러리를 무작정 설치하는 것”이 아닙니다. 목표는 **필요한 라이브러리를 안전하게 설치하고, 현재 프로젝트에 맞게 관리하며, 공식 문서를 기준으로 사용하는 습관을 갖는 것**입니다.

---

## 9.1 외부 라이브러리란?

### 9.1.1 라이브러리의 의미

라이브러리는 자주 필요한 기능을 미리 만들어 둔 코드 모음입니다.

우리가 프로그램을 만들 때 모든 기능을 직접 작성할 수도 있습니다. 하지만 실제로는 많은 기능이 반복해서 필요합니다.

예를 들어 다음과 같은 기능을 생각해 봅시다.

\`\`\`text
- 웹 API에 요청 보내기
- 엑셀 파일 읽기
- CSV 파일 분석하기
- 날짜와 시간 계산하기
- 그래프 그리기
- 테스트 자동 실행하기
- HTML 문서에서 원하는 정보 찾기
\`\`\`

이런 기능을 매번 직접 만들면 비효율적입니다. 이미 많은 사람들이 사용하고 검증한 코드가 있다면, 그것을 가져와 사용하는 편이 훨씬 낫습니다.

라이브러리는 이런 상황에서 사용하는 도구입니다. 라이브러리를 사용하면 직접 모든 코드를 작성하지 않고도 필요한 기능을 가져와 사용할 수 있습니다.

예를 들어 웹 API에 요청을 보내고 싶다면 \`requests\`라는 라이브러리를 사용할 수 있습니다.

\`\`\`python
import requests

response = requests.get("https://example.com")
print(response.status_code)
\`\`\`

위 코드에서 \`requests.get()\`은 웹 주소로 요청을 보내는 기능입니다. 이 기능을 직접 처음부터 만들려면 HTTP 통신에 대한 많은 내용을 알아야 합니다. 하지만 라이브러리를 사용하면 훨씬 간단하게 사용할 수 있습니다.

---

### 9.1.2 표준 라이브러리와 외부 라이브러리

파이썬에서 사용할 수 있는 라이브러리는 크게 두 종류로 나눌 수 있습니다.

첫 번째는 **표준 라이브러리**입니다.

표준 라이브러리는 파이썬을 설치하면 함께 제공되는 라이브러리입니다. 별도로 설치하지 않아도 사용할 수 있습니다.

예를 들어 다음과 같은 모듈은 표준 라이브러리에 포함됩니다.

\`\`\`python
import datetime
import os
import json
import csv
import math
import random
\`\`\`

이 모듈들은 파이썬을 설치하면 기본적으로 사용할 수 있습니다.

두 번째는 **외부 라이브러리**입니다.

외부 라이브러리는 파이썬에 기본으로 포함되어 있지 않습니다. 필요한 경우 별도로 설치해야 합니다.

예를 들어 다음과 같은 라이브러리는 보통 직접 설치해서 사용합니다.

\`\`\`python
import requests
import pandas
import openpyxl
import bs4
import pytest
\`\`\`

만약 설치하지 않은 외부 라이브러리를 import하면 에러가 발생합니다.

\`\`\`python
import pandas
\`\`\`

설치되어 있지 않은 경우 다음과 비슷한 에러를 볼 수 있습니다.

\`\`\`text
ModuleNotFoundError: No module named 'pandas'
\`\`\`

이 에러는 “파이썬이 \`pandas\`라는 모듈을 찾지 못했다”는 뜻입니다. 이때는 해당 라이브러리를 설치해야 합니다.

---

### 9.1.3 외부 라이브러리를 사용하는 이유

외부 라이브러리를 사용하는 이유는 단순히 코드를 짧게 쓰기 위해서만은 아닙니다. 실무에서는 다음과 같은 이유로 외부 라이브러리를 사용합니다.

첫째, 개발 시간을 줄일 수 있습니다.

예를 들어 엑셀 파일을 직접 분석하는 기능을 만들려면 파일 구조를 이해해야 하고, 셀과 시트 정보를 처리하는 코드도 직접 작성해야 합니다. 하지만 \`openpyxl\`을 사용하면 엑셀 파일을 읽고 쓸 수 있는 기능을 바로 사용할 수 있습니다.

둘째, 검증된 기능을 사용할 수 있습니다.

많은 사람이 사용하는 라이브러리는 다양한 상황에서 사용되면서 오류가 발견되고 개선됩니다. 물론 모든 라이브러리가 안전하다는 뜻은 아니지만, 혼자 만든 코드보다 더 많은 환경에서 검증된 경우가 많습니다.

셋째, 실무에서 통용되는 방식으로 작업할 수 있습니다.

데이터 분석을 할 때 \`pandas\`를 사용하고, API 요청을 할 때 \`requests\`를 사용하는 방식은 많은 파이썬 프로젝트에서 볼 수 있습니다. 이런 라이브러리를 익혀 두면 다른 사람의 코드를 읽거나 협업할 때도 도움이 됩니다.

넷째, 복잡한 문제를 더 높은 수준에서 다룰 수 있습니다.

예를 들어 표 형태 데이터를 분석할 때 모든 행과 열을 반복문으로 직접 처리할 수도 있습니다. 하지만 \`pandas\`를 사용하면 필터링, 정렬, 그룹화, 결측치 처리 같은 작업을 더 명확하게 표현할 수 있습니다.

---

### 9.1.4 모듈, 패키지, 라이브러리, 프레임워크

처음 외부 라이브러리를 배우면 비슷한 용어가 많이 나옵니다.

\`\`\`text
모듈
패키지
라이브러리
프레임워크
\`\`\`

이 용어들은 서로 겹쳐서 사용되는 경우도 있지만, 기초 단계에서는 다음처럼 이해하면 충분합니다.

| 용어 | 의미 | 예시 |
|---|---|---|
| 모듈 | 파이썬 파일 하나 | \`math\`, \`json\`, \`my_utils.py\` |
| 패키지 | 여러 모듈을 담은 폴더 구조 | \`pandas\`, \`requests\` |
| 라이브러리 | 특정 기능을 제공하는 코드 묶음 | \`openpyxl\`, \`pytest\` |
| 프레임워크 | 프로그램 구조까지 어느 정도 정해 주는 도구 | \`Django\`, \`FastAPI\` |

모듈은 가장 작은 코드 파일 단위입니다. 패키지는 여러 모듈을 폴더로 묶은 구조입니다. 라이브러리는 사용자가 필요한 기능을 가져와 쓸 수 있도록 제공되는 코드 묶음입니다. 프레임워크는 라이브러리보다 더 큰 개념으로, 프로그램을 어떤 구조로 만들지까지 어느 정도 정해 줍니다.

예를 들어 \`requests\`는 웹 요청을 보내는 데 사용하는 라이브러리이면서, 내부적으로는 여러 모듈과 패키지 구조를 가지고 있습니다.

기초 단계에서는 이 용어를 너무 엄격하게 구분하려고 하기보다, 다음 흐름을 이해하는 것이 더 중요합니다.

\`\`\`text
필요한 기능이 있다.
이미 만들어진 라이브러리를 찾는다.
설치한다.
import한다.
문서를 보며 사용한다.
\`\`\`

---

### 9.1.5 PyPI란?

외부 라이브러리는 보통 **PyPI**라는 저장소에서 설치합니다.

PyPI는 Python Package Index의 줄임말입니다. 파이썬 패키지를 찾아보고 설치할 수 있도록 제공되는 공식 패키지 저장소입니다.

우리가 터미널에서 다음 명령을 실행하면, 일반적으로 PyPI에서 해당 패키지를 내려받아 설치합니다.

\`\`\`bash
python -m pip install requests
\`\`\`

이 명령은 \`requests\`라는 패키지를 설치하라는 뜻입니다.

PyPI에는 매우 많은 패키지가 있습니다. 하지만 패키지가 많다는 것은 장점이면서 동시에 주의할 점이기도 합니다. 아무 패키지나 설치하면 안 됩니다. 비슷한 이름의 패키지, 오래 관리되지 않은 패키지, 사용자가 적은 패키지, 보안상 문제가 있을 수 있는 패키지도 존재할 수 있습니다.

따라서 외부 라이브러리를 사용할 때는 다음을 확인하는 습관이 필요합니다.

\`\`\`text
- 공식 문서가 있는가?
- 최근에도 관리되고 있는가?
- 설치 방법이 명확한가?
- 많은 사용자가 사용하는가?
- 내가 하려는 작업에 적합한가?
\`\`\`

---

### 9.1.6 설치와 import의 차이

외부 라이브러리를 사용할 때는 **설치**와 **import**를 구분해야 합니다.

설치는 내 컴퓨터 또는 현재 가상환경에 라이브러리를 내려받는 작업입니다.

\`\`\`bash
python -m pip install requests
\`\`\`

import는 파이썬 코드 안에서 설치된 라이브러리를 가져와 사용하는 작업입니다.

\`\`\`python
import requests
\`\`\`

설치는 터미널에서 실행합니다. import는 파이썬 코드 안에서 작성합니다.

이 둘을 헷갈리면 다음과 같은 상황이 생깁니다.

\`\`\`python
pip install requests
\`\`\`

위 코드는 파이썬 코드처럼 보이지만, 일반적인 파이썬 파일 안에 쓰면 안 됩니다. \`pip install\`은 파이썬 문법이 아니라 터미널 명령입니다.

정리하면 다음과 같습니다.

| 작업 | 어디에서 실행하는가 | 예시 |
|---|---|---|
| 설치 | 터미널 | \`python -m pip install requests\` |
| 사용 | 파이썬 코드 | \`import requests\` |

---

## 9.2 \`pip\` 사용법

### 9.2.1 \`pip\`란 무엇인가

\`pip\`는 파이썬 패키지를 설치하고 관리하는 도구입니다.

외부 라이브러리를 사용하려면 먼저 설치해야 합니다. 이때 가장 많이 사용하는 도구가 \`pip\`입니다.

예를 들어 \`requests\`를 설치하려면 터미널에서 다음 명령을 실행합니다.

\`\`\`bash
python -m pip install requests
\`\`\`

여기서 명령을 조금 나누어 보면 다음과 같습니다.

\`\`\`text
python -m pip    pip를 파이썬 모듈 방식으로 실행
install          설치하라는 명령
requests         설치할 패키지 이름
\`\`\`

\`pip install requests\`처럼 실행하는 방식도 많이 볼 수 있습니다. 하지만 기초 수업에서는 다음 형태를 권장합니다.

\`\`\`bash
python -m pip install 패키지이름
\`\`\`

이 방식은 현재 실행 중인 Python과 연결된 pip를 사용하기 때문에, 여러 버전의 Python이 설치된 환경에서 혼동을 줄이는 데 도움이 됩니다.

---

### 9.2.2 \`pip\` 버전 확인

먼저 \`pip\`가 정상적으로 동작하는지 확인해 봅시다.

\`\`\`bash
python -m pip --version
\`\`\`

실행하면 다음과 비슷한 결과가 나옵니다.

\`\`\`text
pip 26.1.2 from ... (python 3.x)
\`\`\`

중요한 것은 정확한 버전 숫자가 아닙니다. 현재 환경에서 \`pip\` 명령이 실행되는지 확인하는 것이 중요합니다.

만약 명령이 실행되지 않는다면 Python 설치 상태, 가상환경 활성화 여부, 터미널 설정을 확인해야 합니다.

---

### 9.2.3 라이브러리 설치하기

라이브러리를 설치하는 기본 명령은 다음과 같습니다.

\`\`\`bash
python -m pip install 패키지이름
\`\`\`

예를 들어 \`requests\`를 설치하려면 다음과 같이 입력합니다.

\`\`\`bash
python -m pip install requests
\`\`\`

설치가 끝난 뒤 파이썬 코드에서 import해 봅니다.

\`\`\`python
import requests

print(requests.__name__)
\`\`\`

실행 결과는 다음과 같습니다.

\`\`\`text
requests
\`\`\`

이렇게 import가 정상적으로 된다면 설치가 된 것입니다.

---

### 9.2.4 여러 라이브러리 한 번에 설치하기

여러 라이브러리를 한 번에 설치할 수도 있습니다.

\`\`\`bash
python -m pip install requests pandas openpyxl
\`\`\`

위 명령은 \`requests\`, \`pandas\`, \`openpyxl\`을 한 번에 설치합니다.

하지만 초보 단계에서는 처음부터 여러 개를 한 번에 설치하기보다, 하나씩 설치하고 import를 확인하는 방식이 안전합니다. 오류가 발생했을 때 어떤 라이브러리에서 문제가 생겼는지 찾기 쉽기 때문입니다.

---

### 9.2.5 설치된 라이브러리 확인하기

현재 환경에 설치된 패키지 목록은 다음 명령으로 확인합니다.

\`\`\`bash
python -m pip list
\`\`\`

결과는 다음과 비슷합니다.

\`\`\`text
Package      Version
------------ -------
requests     2.34.2
pandas       2.x.x
openpyxl     3.x.x
\`\`\`

여기에는 직접 설치한 패키지뿐 아니라, 다른 패키지가 내부적으로 필요로 해서 함께 설치된 패키지도 표시될 수 있습니다.

특정 패키지의 자세한 정보를 확인하려면 \`show\`를 사용합니다.

\`\`\`bash
python -m pip show requests
\`\`\`

이 명령은 패키지 버전, 설치 위치, 의존 패키지 등의 정보를 보여줍니다.

---

### 9.2.6 라이브러리 삭제하기

더 이상 필요 없는 라이브러리는 삭제할 수 있습니다.

\`\`\`bash
python -m pip uninstall requests
\`\`\`

실행하면 정말 삭제할 것인지 묻는 메시지가 나옵니다. 확인하면 해당 패키지가 제거됩니다.

주의할 점은 어떤 패키지는 다른 패키지가 의존하고 있을 수 있다는 것입니다. 무작정 삭제하면 다른 코드가 동작하지 않을 수 있습니다.

따라서 실무에서는 삭제하기 전에 다음을 확인하는 것이 좋습니다.

\`\`\`text
- 현재 프로젝트에서 사용 중인가?
- 다른 패키지가 필요로 하는가?
- requirements.txt에 포함되어 있는가?
- 삭제 후 테스트를 실행했는가?
\`\`\`

---

### 9.2.7 라이브러리 업데이트하기

설치된 라이브러리를 최신 버전으로 업데이트하려면 다음 명령을 사용합니다.

\`\`\`bash
python -m pip install --upgrade requests
\`\`\`

또는 짧게 쓸 수도 있습니다.

\`\`\`bash
python -m pip install -U requests
\`\`\`

업데이트는 항상 좋은 것처럼 보이지만, 실무에서는 조심해야 합니다. 라이브러리 버전이 바뀌면서 기존 코드와 맞지 않는 변경이 생길 수 있기 때문입니다.

그래서 운영 중인 프로젝트에서는 무작정 최신 버전으로 올리기보다 다음 흐름을 따르는 것이 좋습니다.

\`\`\`text
1. 현재 버전 확인
2. 변경 내용 확인
3. 테스트 환경에서 업데이트
4. 테스트 실행
5. 문제가 없으면 적용
\`\`\`

기초 수업에서는 “업데이트가 가능하다”는 사실을 이해하면 충분합니다. 중요한 프로젝트에서는 버전을 관리해야 한다는 점만 기억해 두면 됩니다.

---

### 9.2.8 특정 버전 설치하기

특정 버전의 라이브러리를 설치할 수도 있습니다.

\`\`\`bash
python -m pip install requests==2.34.2
\`\`\`

이 명령은 \`requests\`의 특정 버전을 설치하라는 뜻입니다.

버전을 고정하는 이유는 프로젝트 실행 환경을 일정하게 유지하기 위해서입니다. 개발할 때는 잘 동작했는데, 다른 사람 컴퓨터에서 실행했을 때 라이브러리 버전이 달라서 오류가 생기는 경우가 있습니다.

이런 문제를 줄이기 위해 프로젝트에서는 패키지 버전을 기록해 둡니다.

다만 수업 자료에서 특정 버전을 반드시 고정해야 하는 경우가 아니라면, 다음처럼 패키지 이름만 설치해도 됩니다.

\`\`\`bash
python -m pip install requests
\`\`\`

특정 버전 고정은 실무 프로젝트나 배포 환경에서 더 중요해집니다.

---

### 9.2.9 설치 오류가 날 때 확인할 것

외부 라이브러리를 설치하다 보면 오류가 발생할 수 있습니다. 대표적인 원인은 다음과 같습니다.

\`\`\`text
- 인터넷 연결 문제
- 패키지 이름 오타
- 가상환경이 활성화되지 않음
- Python 버전과 라이브러리 버전이 맞지 않음
- 권한 문제
- 운영체제별 설치 문제
\`\`\`

예를 들어 다음 명령은 패키지 이름이 잘못되어 실패할 수 있습니다.

\`\`\`bash
python -m pip install request
\`\`\`

우리가 설치하려는 라이브러리는 보통 \`requests\`입니다. 마지막에 \`s\`가 붙습니다.

패키지 이름이 헷갈릴 때는 공식 문서나 PyPI 페이지에서 설치 명령을 확인하는 것이 가장 좋습니다.

권한 문제가 발생하면 무작정 관리자 권한으로 실행하기보다, 먼저 가상환경을 사용하고 있는지 확인하는 습관이 좋습니다. 가상환경 안에서 설치하면 시스템 전체에 영향을 주지 않고 프로젝트별로 라이브러리를 관리할 수 있습니다.

---

## 9.3 가상환경과 라이브러리 관리

### 9.3.1 가상환경이 필요한 이유

가상환경은 프로젝트마다 독립된 파이썬 실행 환경을 만드는 기능입니다.

외부 라이브러리를 배우기 시작하면 가상환경이 왜 필요한지 잘 와닿지 않을 수 있습니다. 처음에는 그냥 설치하면 되는 것처럼 보입니다.

하지만 프로젝트가 여러 개가 되면 문제가 생깁니다.

예를 들어 다음과 같은 상황을 생각해 봅시다.

\`\`\`text
프로젝트 A: pandas 1.x 버전이 필요함
프로젝트 B: pandas 2.x 버전이 필요함
\`\`\`

두 프로젝트가 서로 다른 버전을 필요로 하는데, 컴퓨터 전체에 하나의 pandas만 설치되어 있다면 충돌이 생길 수 있습니다.

가상환경을 사용하면 프로젝트마다 독립적으로 라이브러리를 설치할 수 있습니다.

\`\`\`text
project_a/
  .venv/
  pandas 1.x

project_b/
  .venv/
  pandas 2.x
\`\`\`

이렇게 하면 프로젝트 A와 프로젝트 B가 서로 영향을 주지 않습니다.

---

### 9.3.2 가상환경 만들기

파이썬에는 \`venv\`라는 가상환경 도구가 기본으로 포함되어 있습니다.

프로젝트 폴더에서 다음 명령을 실행합니다.

\`\`\`bash
python -m venv .venv
\`\`\`

여기서 \`.venv\`는 가상환경 폴더 이름입니다. 꼭 \`.venv\`라는 이름을 써야 하는 것은 아니지만, 실무에서 자주 사용하는 이름입니다.

명령을 실행하면 프로젝트 폴더 안에 \`.venv\` 폴더가 생깁니다.

\`\`\`text
my_project/
  .venv/
  main.py
\`\`\`

\`.venv\` 폴더 안에는 해당 프로젝트에서 사용할 Python 실행 파일과 라이브러리 설치 공간이 들어 있습니다.

---

### 9.3.3 가상환경 활성화하기

가상환경을 만들었다고 해서 자동으로 사용되는 것은 아닙니다. 먼저 활성화해야 합니다.

Windows PowerShell에서는 보통 다음 명령을 사용합니다.

\`\`\`powershell
.venv\\Scripts\\Activate.ps1
\`\`\`

Windows 명령 프롬프트에서는 다음과 같이 사용할 수 있습니다.

\`\`\`cmd
.venv\\Scripts\\activate.bat
\`\`\`

macOS나 Linux에서는 다음 명령을 사용합니다.

\`\`\`bash
source .venv/bin/activate
\`\`\`

가상환경이 활성화되면 터미널 앞부분에 보통 \`(.venv)\`와 같은 표시가 나타납니다.

\`\`\`text
(.venv) my_project $
\`\`\`

이 상태에서 라이브러리를 설치하면 현재 프로젝트의 가상환경 안에 설치됩니다.

\`\`\`bash
python -m pip install requests
\`\`\`

---

### 9.3.4 가상환경 비활성화하기

가상환경 사용을 끝내려면 다음 명령을 입력합니다.

\`\`\`bash
deactivate
\`\`\`

그러면 터미널 앞의 \`(.venv)\` 표시가 사라집니다.

가상환경을 비활성화한다고 해서 설치한 라이브러리가 삭제되는 것은 아닙니다. 단지 현재 터미널에서 그 가상환경 사용을 중단하는 것입니다.

다시 사용하려면 다시 활성화하면 됩니다.

---

### 9.3.5 현재 어떤 Python을 사용 중인지 확인하기

가상환경을 사용할 때는 내가 어떤 Python을 사용하고 있는지 확인하는 것이 중요합니다.

다음 명령으로 Python 위치를 확인할 수 있습니다.

Windows에서는 다음 명령을 사용할 수 있습니다.

\`\`\`cmd
where python
\`\`\`

macOS나 Linux에서는 다음 명령을 사용할 수 있습니다.

\`\`\`bash
which python
\`\`\`

가상환경이 활성화된 상태라면 결과 경로에 \`.venv\`가 포함되어 있는지 확인합니다.

또한 파이썬 코드 안에서도 실행 파일 경로를 확인할 수 있습니다.

\`\`\`python
import sys

print(sys.executable)
\`\`\`

이 값이 현재 가상환경 안의 Python을 가리키고 있다면 가상환경을 제대로 사용하고 있는 것입니다.

---

### 9.3.6 가상환경 폴더는 공유하지 않는다

실무에서 프로젝트를 다른 사람과 공유할 때 \`.venv\` 폴더를 그대로 공유하지 않습니다.

가상환경 폴더는 운영체제, Python 경로, 설치된 바이너리 파일 등에 영향을 받습니다. 내 컴퓨터의 \`.venv\`를 그대로 다른 사람에게 보내도 정상적으로 동작하지 않을 수 있습니다.

대신 어떤 라이브러리가 필요한지 목록을 공유합니다. 이때 사용하는 파일이 \`requirements.txt\`입니다.

즉, 공유해야 하는 것은 \`.venv\` 폴더가 아니라 설치 목록입니다.

\`\`\`text
공유하지 않는 것: .venv 폴더
공유하는 것: requirements.txt
\`\`\`

Git을 사용한다면 보통 \`.gitignore\` 파일에 \`.venv/\`를 추가해 가상환경 폴더가 저장소에 올라가지 않도록 합니다.

---

## 9.4 \`requirements.txt\`

### 9.4.1 \`requirements.txt\`란?

\`requirements.txt\`는 프로젝트에 필요한 외부 라이브러리 목록을 적어 두는 파일입니다.

예를 들어 프로젝트에서 다음 라이브러리를 사용한다고 해봅시다.

\`\`\`text
requests
pandas
openpyxl
\`\`\`

그러면 \`requirements.txt\`에 다음처럼 적을 수 있습니다.

\`\`\`text
requests
pandas
openpyxl
\`\`\`

또는 버전을 함께 적을 수도 있습니다.

\`\`\`text
requests==2.34.2
pandas==2.3.3
openpyxl==3.1.4
\`\`\`

이 파일이 있으면 다른 사람이 같은 프로젝트를 실행할 때 필요한 라이브러리를 한 번에 설치할 수 있습니다.

\`\`\`bash
python -m pip install -r requirements.txt
\`\`\`

여기서 \`-r\`은 파일을 읽어서 그 안에 적힌 패키지들을 설치하라는 뜻입니다.

---

### 9.4.2 현재 설치 목록 저장하기

현재 가상환경에 설치된 패키지 목록을 파일로 저장하려면 다음 명령을 사용합니다.

\`\`\`bash
python -m pip freeze > requirements.txt
\`\`\`

이 명령은 현재 설치된 패키지와 버전을 \`requirements.txt\` 파일에 저장합니다.

예를 들어 파일 내용은 다음과 비슷할 수 있습니다.

\`\`\`text
certifi==2026.1.1
charset-normalizer==3.4.0
idna==3.10
requests==2.34.2
urllib3==2.5.0
\`\`\`

여기서 내가 직접 설치한 것은 \`requests\` 하나뿐일 수 있습니다. 하지만 \`requests\`가 내부적으로 필요로 하는 다른 패키지도 함께 기록될 수 있습니다.

이것을 **의존성**이라고 합니다.

---

### 9.4.3 \`pip freeze\` 결과를 이해하기

\`pip freeze\`를 실행하면 생각보다 많은 패키지가 보일 수 있습니다. 이것은 이상한 일이 아닙니다.

외부 라이브러리 하나가 다른 라이브러리에 의존하는 경우가 많기 때문입니다.

예를 들어 \`requests\`를 설치하면 \`urllib3\`, \`certifi\`, \`charset-normalizer\`, \`idna\` 같은 패키지가 함께 설치될 수 있습니다. \`requests\`가 HTTP 요청을 처리하기 위해 이런 패키지들의 도움을 받기 때문입니다.

기초 단계에서는 모든 의존 패키지를 외울 필요가 없습니다. 중요한 것은 다음입니다.

\`\`\`text
내가 설치한 라이브러리 외에도 함께 설치되는 패키지가 있을 수 있다.
requirements.txt에는 그런 패키지들도 기록될 수 있다.
\`\`\`

---

### 9.4.4 설치 목록으로 환경 재현하기

새로운 컴퓨터나 새로운 가상환경에서 같은 프로젝트를 실행하려면 먼저 가상환경을 만들고, \`requirements.txt\`로 필요한 패키지를 설치합니다.

\`\`\`bash
python -m venv .venv
\`\`\`

가상환경을 활성화한 뒤 다음 명령을 실행합니다.

\`\`\`bash
python -m pip install -r requirements.txt
\`\`\`

이렇게 하면 \`requirements.txt\`에 적힌 패키지들이 설치됩니다.

실무에서 프로젝트를 전달할 때는 보통 다음 파일들이 함께 필요합니다.

\`\`\`text
main.py
requirements.txt
README.md
\`\`\`

\`README.md\`에는 실행 방법을 적고, \`requirements.txt\`에는 필요한 라이브러리를 적습니다. 그러면 다른 사람이 프로젝트를 실행하기 쉬워집니다.

---

### 9.4.5 버전 고정의 장단점

\`requirements.txt\`에는 버전을 고정할 수도 있고, 고정하지 않을 수도 있습니다.

버전을 고정한 예시는 다음과 같습니다.

\`\`\`text
requests==2.34.2
pandas==2.3.3
\`\`\`

버전을 고정하지 않은 예시는 다음과 같습니다.

\`\`\`text
requests
pandas
\`\`\`

버전을 고정하면 같은 버전이 설치되므로 실행 환경을 재현하기 쉽습니다. 하지만 시간이 지나면 오래된 버전을 계속 쓰게 될 수 있습니다.

버전을 고정하지 않으면 설치 시점의 최신 버전이 설치될 수 있습니다. 하지만 최신 버전에서 기존 코드와 호환되지 않는 변경이 있으면 문제가 생길 수 있습니다.

실무에서는 프로젝트 성격에 따라 다르게 관리합니다.

\`\`\`text
학습용 프로젝트: 버전을 엄격하게 고정하지 않아도 됨
실무 운영 프로젝트: 버전 고정과 테스트가 중요함
협업 프로젝트: requirements.txt를 함께 관리하는 것이 좋음
\`\`\`

---

## 9.5 외부 라이브러리 사용 전 주의사항

### 9.5.1 공식 문서 확인하기

외부 라이브러리를 사용할 때 가장 먼저 확인해야 할 것은 공식 문서입니다.

블로그나 영상 강의도 도움이 되지만, 라이브러리 사용법은 버전에 따라 달라질 수 있습니다. 오래된 글을 그대로 따라 하면 현재 버전에서는 동작하지 않을 수도 있습니다.

공식 문서에서 확인할 내용은 다음과 같습니다.

\`\`\`text
- 설치 방법
- 기본 사용 예제
- 지원하는 Python 버전
- 주요 기능
- 주의사항
- 변경 사항
\`\`\`

특히 설치 명령은 공식 문서에 적힌 내용을 우선 확인하는 것이 좋습니다.

---

### 9.5.2 패키지 이름과 import 이름이 다를 수 있다

외부 라이브러리를 설치할 때 사용하는 이름과 import할 때 사용하는 이름이 항상 같지는 않습니다.

예를 들어 Beautiful Soup은 보통 다음 명령으로 설치합니다.

\`\`\`bash
python -m pip install beautifulsoup4
\`\`\`

하지만 파이썬 코드에서는 보통 이렇게 import합니다.

\`\`\`python
from bs4 import BeautifulSoup
\`\`\`

설치 이름은 \`beautifulsoup4\`이고, import 이름은 \`bs4\`입니다.

이처럼 패키지 이름과 import 이름이 다를 수 있으므로, 항상 공식 문서의 예제를 확인해야 합니다.

---

### 9.5.3 비슷한 이름의 패키지 주의하기

PyPI에는 매우 많은 패키지가 있습니다. 그래서 유명한 패키지와 비슷한 이름의 패키지도 있을 수 있습니다.

예를 들어 \`requests\`를 설치해야 하는데 이름을 잘못 입력하면 다른 패키지를 설치할 수도 있습니다.

\`\`\`bash
python -m pip install request
\`\`\`

위 명령은 우리가 흔히 사용하는 \`requests\`와 다릅니다. 패키지 이름이 정확한지 확인해야 합니다.

외부 라이브러리를 설치하기 전에는 다음을 확인하는 것이 좋습니다.

\`\`\`text
- 패키지 이름이 정확한가?
- 공식 문서에서 안내하는 설치 명령인가?
- PyPI 페이지의 프로젝트 설명이 내가 찾는 라이브러리와 맞는가?
\`\`\`

---

### 9.5.4 버전 차이 확인하기

라이브러리는 시간이 지나면서 계속 바뀝니다. 새로운 기능이 추가되기도 하고, 기존 기능 사용법이 바뀌기도 합니다.

예를 들어 어떤 블로그에서는 다음과 같은 코드를 사용한다고 해봅시다.

\`\`\`python
some_library.old_function()
\`\`\`

하지만 현재 버전에서는 이 함수가 사라졌을 수도 있습니다. 그러면 코드가 동작하지 않습니다.

따라서 오류가 발생했을 때는 다음을 확인해야 합니다.

\`\`\`text
- 내가 설치한 라이브러리 버전
- 예제가 작성된 시점
- 공식 문서의 현재 사용법
- 변경 내역
\`\`\`

버전을 확인하는 명령은 다음과 같습니다.

\`\`\`bash
python -m pip show 패키지이름
\`\`\`

파이썬 코드 안에서 확인할 수 있는 경우도 있습니다.

\`\`\`python
import requests

print(requests.__version__)
\`\`\`

다만 모든 라이브러리가 \`__version__\`을 제공하는 것은 아닙니다.

---

### 9.5.5 너무 많은 라이브러리에 의존하지 않기

외부 라이브러리는 유용하지만, 너무 많은 라이브러리에 의존하면 관리가 어려워집니다.

간단한 기능은 파이썬 기본 문법이나 표준 라이브러리로 충분히 해결할 수 있습니다. 예를 들어 단순한 날짜 계산은 \`datetime\`으로 처리할 수 있고, 간단한 JSON 파일 처리는 \`json\` 모듈로 처리할 수 있습니다.

외부 라이브러리를 설치하기 전에는 다음 질문을 해보는 것이 좋습니다.

\`\`\`text
- 이 기능이 정말 필요한가?
- 표준 라이브러리로 해결할 수 있는가?
- 이 라이브러리를 추가하면 관리 부담이 커지지 않는가?
- 팀원들도 쉽게 설치하고 사용할 수 있는가?
\`\`\`

라이브러리를 적게 쓰는 것이 항상 좋은 것은 아닙니다. 하지만 필요 없는 라이브러리를 많이 추가하는 것도 좋은 습관은 아닙니다.

---

### 9.5.6 보안과 민감 정보 주의하기

외부 라이브러리를 사용할 때는 보안도 중요합니다.

특히 API 키, 비밀번호, 토큰 같은 민감 정보를 코드에 직접 쓰지 않도록 주의해야 합니다.

나쁜 예시는 다음과 같습니다.

\`\`\`python
API_KEY = "my-secret-api-key"
\`\`\`

이런 값이 코드에 직접 들어 있으면, 파일을 공유하거나 저장소에 올렸을 때 민감 정보가 노출될 수 있습니다.

더 나은 방식은 환경 변수나 별도 설정 파일을 사용하는 것입니다. 이 내용은 12장에서 더 자세히 다룹니다.

외부 라이브러리를 설치할 때도 보안상 주의해야 합니다.

\`\`\`text
- 신뢰할 수 있는 패키지인지 확인한다.
- 비슷한 이름의 패키지를 조심한다.
- 오래 관리되지 않은 패키지는 주의한다.
- 민감 정보를 코드에 직접 쓰지 않는다.
\`\`\`

---

## 9.6 대표 외부 라이브러리 소개

### 9.6.1 \`requests\`

\`requests\`는 HTTP 요청을 보내는 데 사용하는 대표적인 라이브러리입니다.

API에서 데이터를 가져오거나, 웹 서버에 요청을 보낼 때 자주 사용합니다.

설치 명령은 다음과 같습니다.

\`\`\`bash
python -m pip install requests
\`\`\`

기본 사용 예시는 다음과 같습니다.

\`\`\`python
import requests

response = requests.get("https://example.com")

print(response.status_code)
print(response.text[:100])
\`\`\`

위 코드에서 \`requests.get()\`은 지정한 주소로 GET 요청을 보냅니다. 결과는 \`response\` 객체에 들어갑니다.

자주 사용하는 속성과 메서드는 다음과 같습니다.

| 사용법 | 의미 |
|---|---|
| \`response.status_code\` | 응답 상태 코드 |
| \`response.text\` | 응답 본문을 문자열로 확인 |
| \`response.json()\` | JSON 응답을 파이썬 데이터로 변환 |
| \`response.headers\` | 응답 헤더 확인 |

예를 들어 JSON API를 호출한다면 다음처럼 사용할 수 있습니다.

\`\`\`python
import requests

url = "https://api.example.com/users"
response = requests.get(url)

data = response.json()
print(data)
\`\`\`

다만 실제 API 주소는 서비스마다 다릅니다. API를 사용할 때는 해당 서비스의 문서를 확인해야 합니다.

\`requests\`는 11장에서 API 데이터 가져오기를 배울 때 다시 자세히 사용합니다.

---

### 9.6.2 \`pandas\`

\`pandas\`는 표 형태 데이터를 다루는 데 사용하는 대표적인 라이브러리입니다.

엑셀, CSV, 데이터베이스에서 가져온 데이터를 정리하고 분석할 때 많이 사용합니다.

설치 명령은 다음과 같습니다.

\`\`\`bash
python -m pip install pandas
\`\`\`

기본 사용 예시는 다음과 같습니다.

\`\`\`python
import pandas as pd

data = {
    "name": ["키보드", "마우스", "모니터"],
    "price": [30000, 15000, 200000],
}

df = pd.DataFrame(data)
print(df)
\`\`\`

실행 결과는 다음과 비슷합니다.

\`\`\`text
  name   price
0  키보드   30000
1  마우스   15000
2  모니터  200000
\`\`\`

\`pandas\`에서 가장 중요한 개념은 \`DataFrame\`입니다. \`DataFrame\`은 행과 열로 이루어진 표 형태 데이터입니다. 엑셀 시트와 비슷하다고 생각하면 이해하기 쉽습니다.

CSV 파일을 읽는 예시는 다음과 같습니다.

\`\`\`python
import pandas as pd

df = pd.read_csv("sales.csv")
print(df.head())
\`\`\`

엑셀 파일을 읽는 예시는 다음과 같습니다.

\`\`\`python
import pandas as pd

df = pd.read_excel("sales.xlsx")
print(df.head())
\`\`\`

\`pandas\`는 11장에서 데이터 처리 기초를 배울 때 본격적으로 다룹니다.

---

### 9.6.3 \`openpyxl\`

\`openpyxl\`은 엑셀 파일을 읽고 쓰는 데 사용하는 라이브러리입니다.

\`pandas\`도 엑셀 파일을 읽을 수 있지만, \`openpyxl\`은 셀 단위로 값을 읽고 쓰거나 엑셀 파일의 시트, 셀, 서식 등을 다룰 때 유용합니다.

설치 명령은 다음과 같습니다.

\`\`\`bash
python -m pip install openpyxl
\`\`\`

기본 사용 예시는 다음과 같습니다.

\`\`\`python
from openpyxl import Workbook

workbook = Workbook()
sheet = workbook.active

sheet["A1"] = "상품명"
sheet["B1"] = "가격"
sheet["A2"] = "키보드"
sheet["B2"] = 30000

workbook.save("products.xlsx")
\`\`\`

위 코드는 새 엑셀 파일을 만들고, 셀에 값을 입력한 뒤 \`products.xlsx\`로 저장합니다.

기존 엑셀 파일을 읽을 때는 다음처럼 사용할 수 있습니다.

\`\`\`python
from openpyxl import load_workbook

workbook = load_workbook("products.xlsx")
sheet = workbook.active

print(sheet["A1"].value)
print(sheet["B2"].value)
\`\`\`

\`openpyxl\`은 10장에서 엑셀 파일 다루기를 배울 때 다시 사용합니다.

---

### 9.6.4 \`beautifulsoup4\`

\`beautifulsoup4\`는 HTML이나 XML 문서를 분석해서 원하는 정보를 찾을 때 사용하는 라이브러리입니다.

웹 페이지에서 제목, 링크, 특정 태그의 텍스트를 추출할 때 자주 사용합니다.

설치 명령은 다음과 같습니다.

\`\`\`bash
python -m pip install beautifulsoup4
\`\`\`

설치 이름은 \`beautifulsoup4\`이지만, 코드에서는 보통 \`bs4\`에서 \`BeautifulSoup\`을 가져옵니다.

\`\`\`python
from bs4 import BeautifulSoup

html = """
<html>
  <body>
    <h1>공지사항</h1>
    <a href="https://example.com">자세히 보기</a>
  </body>
</html>
"""

soup = BeautifulSoup(html, "html.parser")

print(soup.find("h1").text)
print(soup.find("a")["href"])
\`\`\`

실행 결과는 다음과 같습니다.

\`\`\`text
공지사항
https://example.com
\`\`\`

웹 크롤링을 할 때는 단순히 기술적으로 가능하다고 해서 아무 사이트나 수집하면 안 됩니다. 사이트의 이용 약관, robots.txt, 저작권, 개인정보, 서버 부하 등을 고려해야 합니다.

이 수업에서는 \`beautifulsoup4\`를 대표 라이브러리로 소개만 하고, 본격적인 크롤링은 별도 수업에서 다루는 것이 좋습니다.

---

### 9.6.5 \`pytest\`

\`pytest\`는 파이썬 코드를 테스트하는 데 사용하는 라이브러리입니다.

함수가 의도대로 동작하는지 자동으로 확인할 수 있습니다.

설치 명령은 다음과 같습니다.

\`\`\`bash
python -m pip install pytest
\`\`\`

예를 들어 다음과 같은 함수가 있다고 해봅시다.

\`\`\`python
# calculator.py

def add(a, b):
    return a + b
\`\`\`

이 함수를 테스트하는 파일을 만들 수 있습니다.

\`\`\`python
# test_calculator.py

from calculator import add


def test_add():
    assert add(2, 3) == 5
\`\`\`

터미널에서 다음 명령을 실행합니다.

\`\`\`bash
pytest
\`\`\`

테스트가 통과하면 성공 메시지가 출력됩니다. 테스트가 실패하면 어떤 값이 예상과 달랐는지 알려줍니다.

\`pytest\`는 13장에서 테스트와 코드 검증을 배울 때 자세히 다룹니다.

---

### 9.6.6 대표 라이브러리 용도 정리

이 장에서 소개한 라이브러리를 정리하면 다음과 같습니다.

| 라이브러리 | 주 용도 | 나중에 자세히 다룰 장 |
|---|---|---|
| \`requests\` | API 요청, HTTP 통신 | 11장 |
| \`pandas\` | 표 형태 데이터 처리 | 11장 |
| \`openpyxl\` | 엑셀 파일 읽기와 쓰기 | 10장 |
| \`beautifulsoup4\` | HTML 문서 분석 | 별도 크롤링 수업 또는 심화 |
| \`pytest\` | 테스트 자동화 | 13장 |

지금 단계에서 모든 라이브러리의 사용법을 완전히 외울 필요는 없습니다. 중요한 것은 어떤 문제에 어떤 도구가 자주 쓰이는지 감을 잡는 것입니다.

---

## 9.7 외부 라이브러리 사용 흐름

### 9.7.1 라이브러리 사용의 기본 순서

외부 라이브러리를 사용하는 흐름은 보통 다음과 같습니다.

\`\`\`text
1. 해결하려는 문제를 정한다.
2. 필요한 라이브러리를 찾는다.
3. 공식 문서를 확인한다.
4. 가상환경을 만든다.
5. 라이브러리를 설치한다.
6. import해서 사용한다.
7. 간단한 예제로 동작을 확인한다.
8. requirements.txt에 기록한다.
\`\`\`

이 흐름을 습관으로 만들면 새로운 라이브러리를 배울 때도 덜 혼란스럽습니다.

---

### 9.7.2 예제 프로젝트 준비하기

간단한 예제 프로젝트를 만들어 보겠습니다.

프로젝트 폴더를 다음처럼 구성합니다.

\`\`\`text
library_practice/
  main.py
\`\`\`

먼저 프로젝트 폴더로 이동합니다.

\`\`\`bash
cd library_practice
\`\`\`

가상환경을 만듭니다.

\`\`\`bash
python -m venv .venv
\`\`\`

가상환경을 활성화합니다.

Windows PowerShell:

\`\`\`powershell
.venv\\Scripts\\Activate.ps1
\`\`\`

macOS 또는 Linux:

\`\`\`bash
source .venv/bin/activate
\`\`\`

그다음 \`requests\`를 설치합니다.

\`\`\`bash
python -m pip install requests
\`\`\`

설치 목록을 저장합니다.

\`\`\`bash
python -m pip freeze > requirements.txt
\`\`\`

이제 폴더 구조는 다음처럼 됩니다.

\`\`\`text
library_practice/
  .venv/
  main.py
  requirements.txt
\`\`\`

---

### 9.7.3 설치 확인 코드 작성하기

\`main.py\`에 다음 코드를 작성합니다.

\`\`\`python
import requests

print("requests 라이브러리를 정상적으로 불러왔습니다.")
print("라이브러리 이름:", requests.__name__)
\`\`\`

실행합니다.

\`\`\`bash
python main.py
\`\`\`

실행 결과는 다음과 같습니다.

\`\`\`text
requests 라이브러리를 정상적으로 불러왔습니다.
라이브러리 이름: requests
\`\`\`

이 예제는 실제 API 요청을 보내지는 않습니다. 하지만 설치와 import가 정상적으로 되는지 확인할 수 있습니다.

외부 라이브러리를 처음 설치했을 때는 이렇게 작은 코드로 먼저 확인하는 습관이 좋습니다.

---

### 9.7.4 설치 목록 확인하기

설치된 패키지를 확인해 봅니다.

\`\`\`bash
python -m pip list
\`\`\`

\`requests\`가 목록에 보이면 설치된 것입니다.

또한 \`requirements.txt\` 파일을 열어 보면 설치된 패키지와 버전이 기록되어 있습니다.

\`\`\`text
requests==2.34.2
...
\`\`\`

버전 번호는 실행 시점과 환경에 따라 다를 수 있습니다. 여기서 중요한 것은 파일에 필요한 패키지 목록이 기록되었다는 점입니다.

---

### 9.7.5 다른 환경에서 재현하기

다른 사람이 이 프로젝트를 받았다고 가정해 봅시다.

그 사람은 \`.venv\` 폴더를 받지 않습니다. 대신 다음 파일을 받습니다.

\`\`\`text
main.py
requirements.txt
\`\`\`

그리고 자신의 컴퓨터에서 가상환경을 새로 만들고, 필요한 라이브러리를 설치합니다.

\`\`\`bash
python -m venv .venv
\`\`\`

가상환경을 활성화한 다음 다음 명령을 실행합니다.

\`\`\`bash
python -m pip install -r requirements.txt
\`\`\`

이렇게 하면 프로젝트 실행에 필요한 라이브러리가 설치됩니다.

이 흐름이 실무에서 매우 중요합니다. “내 컴퓨터에서는 되는데 다른 사람 컴퓨터에서는 안 되는 문제”를 줄이는 기본 방법이기 때문입니다.

---

## 9.8 자주 발생하는 문제와 해결 방향

### 9.8.1 \`ModuleNotFoundError\`

가장 자주 만나는 에러 중 하나는 \`ModuleNotFoundError\`입니다.

\`\`\`text
ModuleNotFoundError: No module named 'requests'
\`\`\`

이 에러는 파이썬이 해당 모듈을 찾지 못했다는 뜻입니다.

확인할 내용은 다음과 같습니다.

\`\`\`text
- 라이브러리를 설치했는가?
- 현재 가상환경이 활성화되어 있는가?
- 설치한 환경과 실행한 환경이 같은가?
- 패키지 이름과 import 이름을 혼동하지 않았는가?
\`\`\`

예를 들어 \`beautifulsoup4\`는 설치 이름과 import 이름이 다릅니다.

\`\`\`bash
python -m pip install beautifulsoup4
\`\`\`

\`\`\`python
from bs4 import BeautifulSoup
\`\`\`

설치 이름을 그대로 import하려고 하면 에러가 발생할 수 있습니다.

---

### 9.8.2 설치했는데 import가 안 되는 경우

분명히 설치했는데 import가 안 되는 경우가 있습니다. 이때는 설치한 Python과 실행하는 Python이 다를 가능성이 있습니다.

다음 명령으로 설치된 패키지를 확인합니다.

\`\`\`bash
python -m pip list
\`\`\`

그리고 파이썬 실행 파일 경로를 확인합니다.

\`\`\`python
import sys

print(sys.executable)
\`\`\`

VS Code를 사용한다면 선택된 Python 인터프리터가 가상환경의 Python인지 확인해야 합니다.

자주 발생하는 상황은 다음과 같습니다.

\`\`\`text
터미널에서는 가상환경에 설치했는데,
VS Code는 다른 Python으로 코드를 실행하고 있음
\`\`\`

이 경우 VS Code에서 Python 인터프리터를 올바르게 선택해야 합니다.

---

### 9.8.3 권한 오류

라이브러리 설치 중 권한 오류가 발생할 수 있습니다.

이때 무조건 관리자 권한으로 설치하기보다, 먼저 가상환경을 사용하는 것이 좋습니다. 가상환경 안에서는 프로젝트 폴더 안에 라이브러리를 설치하므로 시스템 전체 권한 문제가 줄어듭니다.

권한 오류가 날 때 확인할 내용은 다음과 같습니다.

\`\`\`text
- 가상환경을 사용하고 있는가?
- 가상환경이 활성화되어 있는가?
- 회사 또는 학교 PC의 보안 정책이 있는가?
- 네트워크 제한이 있는가?
\`\`\`

특히 회사 PC에서는 보안 정책 때문에 패키지 설치가 제한될 수 있습니다. 이 경우 내부 정책을 따라야 합니다.

---

### 9.8.4 패키지 이름 오타

패키지 이름을 잘못 입력해 설치가 실패하거나 엉뚱한 패키지를 설치할 수 있습니다.

예를 들어 다음은 흔한 혼동입니다.

| 의도한 라이브러리 | 설치 명령 | import |
|---|---|---|
| Requests | \`python -m pip install requests\` | \`import requests\` |
| Beautiful Soup | \`python -m pip install beautifulsoup4\` | \`from bs4 import BeautifulSoup\` |
| OpenPyXL | \`python -m pip install openpyxl\` | \`import openpyxl\` |
| pytest | \`python -m pip install pytest\` | 보통 터미널에서 \`pytest\` 실행 |

설치하기 전에는 문서에서 정확한 이름을 확인하는 습관을 들이는 것이 좋습니다.

---

### 9.8.5 버전 충돌

버전 충돌은 여러 패키지가 서로 다른 버전을 요구할 때 발생할 수 있습니다.

예를 들어 어떤 패키지는 A 라이브러리의 1.x 버전이 필요하고, 다른 패키지는 A 라이브러리의 2.x 버전이 필요할 수 있습니다.

이런 충돌을 줄이기 위한 기본 방법은 다음과 같습니다.

\`\`\`text
- 프로젝트마다 가상환경을 분리한다.
- 불필요한 라이브러리를 많이 설치하지 않는다.
- requirements.txt를 관리한다.
- 새 라이브러리를 설치한 뒤 테스트한다.
\`\`\`

기초 단계에서 모든 충돌을 해결하는 방법을 외울 필요는 없습니다. 중요한 것은 “프로젝트마다 가상환경을 분리한다”는 습관입니다.

---

## 9.9 외부 라이브러리 활용 실습

### 9.9.1 실습 목표

이번 실습의 목표는 복잡한 라이브러리 기능을 깊게 사용하는 것이 아닙니다.

목표는 다음과 같습니다.

\`\`\`text
- 가상환경 만들기
- 외부 라이브러리 설치하기
- import 확인하기
- requirements.txt 만들기
- 설치 목록으로 환경 재현 흐름 이해하기
\`\`\`

---

### 9.9.2 실습 1: 프로젝트 폴더 만들기

먼저 실습용 폴더를 만듭니다.

\`\`\`text
chapter9_practice/
\`\`\`

폴더 안에 \`main.py\` 파일을 만듭니다.

\`\`\`text
chapter9_practice/
  main.py
\`\`\`

---

### 9.9.3 실습 2: 가상환경 만들기

프로젝트 폴더에서 다음 명령을 실행합니다.

\`\`\`bash
python -m venv .venv
\`\`\`

가상환경을 활성화합니다.

Windows PowerShell:

\`\`\`powershell
.venv\\Scripts\\Activate.ps1
\`\`\`

macOS 또는 Linux:

\`\`\`bash
source .venv/bin/activate
\`\`\`

터미널 앞에 \`(.venv)\` 표시가 보이는지 확인합니다.

---

### 9.9.4 실습 3: \`requests\` 설치하기

다음 명령으로 \`requests\`를 설치합니다.

\`\`\`bash
python -m pip install requests
\`\`\`

설치가 끝난 뒤 목록을 확인합니다.

\`\`\`bash
python -m pip list
\`\`\`

목록에 \`requests\`가 보이면 설치된 것입니다.

---

### 9.9.5 실습 4: import 확인하기

\`main.py\`에 다음 코드를 작성합니다.

\`\`\`python
import requests

print("외부 라이브러리 import 성공")
print("라이브러리 이름:", requests.__name__)
\`\`\`

실행합니다.

\`\`\`bash
python main.py
\`\`\`

실행 결과는 다음과 같습니다.

\`\`\`text
외부 라이브러리 import 성공
라이브러리 이름: requests
\`\`\`

---

### 9.9.6 실습 5: \`requirements.txt\` 만들기

현재 설치 목록을 저장합니다.

\`\`\`bash
python -m pip freeze > requirements.txt
\`\`\`

생성된 \`requirements.txt\` 파일을 열어 봅니다.

파일 안에는 \`requests\`와 관련 패키지들이 기록되어 있을 수 있습니다.

\`\`\`text
requests==2.34.2
urllib3==2.5.0
...
\`\`\`

버전은 환경에 따라 달라질 수 있습니다.

---

### 9.9.7 실습 6: 설치 명령 정리하기

최종적으로 이번 실습에서 사용한 명령을 정리하면 다음과 같습니다.

\`\`\`bash
python -m venv .venv
\`\`\`

\`\`\`bash
python -m pip install requests
\`\`\`

\`\`\`bash
python -m pip list
\`\`\`

\`\`\`bash
python -m pip freeze > requirements.txt
\`\`\`

\`\`\`bash
python main.py
\`\`\`

가상환경 활성화 명령은 운영체제에 따라 다릅니다.

Windows PowerShell:

\`\`\`powershell
.venv\\Scripts\\Activate.ps1
\`\`\`

macOS 또는 Linux:

\`\`\`bash
source .venv/bin/activate
\`\`\`

---

## 9장 핵심 정리

외부 라이브러리는 파이썬에 기본으로 포함되어 있지는 않지만, 필요한 경우 설치해서 사용할 수 있는 코드 묶음입니다. 실무에서는 모든 기능을 직접 만들기보다 검증된 라이브러리를 활용하는 경우가 많습니다.

표준 라이브러리는 파이썬 설치 시 함께 제공되는 라이브러리이고, 외부 라이브러리는 별도로 설치해야 하는 라이브러리입니다.

\`pip\`는 외부 라이브러리를 설치하고 관리하는 도구입니다. 기초 수업에서는 다음 형태의 명령을 권장합니다.

\`\`\`bash
python -m pip install 패키지이름
\`\`\`

가상환경은 프로젝트마다 독립된 파이썬 실행 환경을 만드는 기능입니다. 프로젝트마다 가상환경을 나누면 라이브러리 버전 충돌을 줄일 수 있습니다.

\`requirements.txt\`는 프로젝트에 필요한 라이브러리 목록을 기록하는 파일입니다. 현재 설치 목록은 다음 명령으로 저장할 수 있습니다.

\`\`\`bash
python -m pip freeze > requirements.txt
\`\`\`

다른 환경에서 같은 라이브러리를 설치하려면 다음 명령을 사용합니다.

\`\`\`bash
python -m pip install -r requirements.txt
\`\`\`

외부 라이브러리를 사용할 때는 공식 문서, 패키지 이름, 버전, 가상환경, 보안 문제를 함께 확인해야 합니다.

대표적인 외부 라이브러리에는 API 요청에 사용하는 \`requests\`, 표 형태 데이터 처리에 사용하는 \`pandas\`, 엑셀 파일 처리에 사용하는 \`openpyxl\`, HTML 문서 분석에 사용하는 \`beautifulsoup4\`, 테스트에 사용하는 \`pytest\`가 있습니다.

---

## 연습문제

### 문제 1

다음 중 외부 라이브러리에 대한 설명으로 가장 적절한 것은 무엇인가요?

A. 파이썬 문법 자체를 의미한다.  
B. 파이썬을 설치하면 무조건 함께 설치되는 운영체제다.  
C. 필요한 기능을 별도로 설치해서 사용할 수 있는 코드 묶음이다.  
D. 파이썬 파일을 삭제하는 명령어다.

---

### 문제 2

표준 라이브러리와 외부 라이브러리의 차이를 간단히 설명해 보세요.

---

### 문제 3

다음 중 라이브러리를 설치하는 터미널 명령으로 가장 적절한 것은 무엇인가요?

A. \`import requests\`  
B. \`python -m pip install requests\`  
C. \`requests.install()\`  
D. \`print install requests\`

---

### 문제 4

다음 코드에서 발생할 수 있는 에러 이름으로 가장 적절한 것은 무엇인가요?

\`\`\`python
import not_installed_library
\`\`\`

A. \`SyntaxError\`  
B. \`ModuleNotFoundError\`  
C. \`ZeroDivisionError\`  
D. \`IndexError\`

---

### 문제 5

가상환경을 사용하는 이유를 두 가지 적어 보세요.

---

### 문제 6

프로젝트 폴더에서 \`.venv\`라는 이름의 가상환경을 만드는 명령을 작성해 보세요.

---

### 문제 7

현재 설치된 패키지 목록을 확인하는 명령을 작성해 보세요.

---

### 문제 8

현재 설치된 패키지 목록을 \`requirements.txt\` 파일로 저장하는 명령을 작성해 보세요.

---

### 문제 9

\`requirements.txt\`에 적힌 패키지를 한 번에 설치하는 명령을 작성해 보세요.

---

### 문제 10

다음 중 패키지 설치 이름과 import 이름이 다른 대표적인 예로 알맞은 것은 무엇인가요?

A. 설치: \`beautifulsoup4\`, import: \`from bs4 import BeautifulSoup\`  
B. 설치: \`requests\`, import: \`import requests\`  
C. 설치: \`pandas\`, import: \`import pandas as pd\`  
D. 설치: \`openpyxl\`, import: \`import openpyxl\`

---

### 문제 11

다음 라이브러리의 주 용도를 연결해 보세요.

\`\`\`text
1. requests
2. pandas
3. openpyxl
4. pytest
\`\`\`

\`\`\`text
A. 테스트 자동화
B. 표 형태 데이터 처리
C. API 요청
D. 엑셀 파일 읽기와 쓰기
\`\`\`

---

### 문제 12

외부 라이브러리를 설치하기 전에 확인하면 좋은 항목을 세 가지 적어 보세요.

---

## 정답 및 해설

### 문제 1 정답

정답: **C**

외부 라이브러리는 파이썬에 기본으로 포함되어 있지 않지만, 필요한 기능을 별도로 설치해서 사용할 수 있는 코드 묶음입니다.

---

### 문제 2 정답

예시 답안:

\`\`\`text
표준 라이브러리는 파이썬을 설치하면 함께 제공되는 라이브러리이고,
외부 라이브러리는 필요할 때 pip 등을 사용해 별도로 설치해야 하는 라이브러리이다.
\`\`\`

해설:

\`datetime\`, \`json\`, \`os\` 같은 모듈은 표준 라이브러리입니다. \`requests\`, \`pandas\`, \`openpyxl\` 같은 라이브러리는 보통 별도로 설치해서 사용합니다.

---

### 문제 3 정답

정답: **B**

\`\`\`bash
python -m pip install requests
\`\`\`

해설:

\`import requests\`는 파이썬 코드 안에서 라이브러리를 가져와 사용하는 문장입니다. 설치 명령은 터미널에서 실행해야 합니다.

---

### 문제 4 정답

정답: **B**

설치되지 않았거나 찾을 수 없는 모듈을 import하려고 하면 보통 \`ModuleNotFoundError\`가 발생합니다.

---

### 문제 5 정답

예시 답안:

\`\`\`text
1. 프로젝트마다 필요한 라이브러리 버전을 분리하기 위해서
2. 다른 프로젝트와 라이브러리 충돌을 줄이기 위해서
\`\`\`

추가로 다음과 같은 답도 가능합니다.

\`\`\`text
- 시스템 Python 환경을 지저분하게 만들지 않기 위해서
- 협업 시 같은 실행 환경을 재현하기 위해서
- 프로젝트별 패키지 목록을 관리하기 위해서
\`\`\`

---

### 문제 6 정답

\`\`\`bash
python -m venv .venv
\`\`\`

해설:

\`venv\`는 파이썬 표준 라이브러리에 포함된 가상환경 도구입니다. \`.venv\`는 가상환경 폴더 이름입니다.

---

### 문제 7 정답

\`\`\`bash
python -m pip list
\`\`\`

해설:

현재 환경에 설치된 패키지 목록을 확인할 때 사용합니다.

---

### 문제 8 정답

\`\`\`bash
python -m pip freeze > requirements.txt
\`\`\`

해설:

현재 설치된 패키지와 버전 목록을 \`requirements.txt\` 파일에 저장합니다.

---

### 문제 9 정답

\`\`\`bash
python -m pip install -r requirements.txt
\`\`\`

해설:

\`-r\` 옵션은 파일을 읽어서 그 안에 적힌 패키지를 설치하라는 뜻입니다.

---

### 문제 10 정답

정답: **A**

\`\`\`bash
python -m pip install beautifulsoup4
\`\`\`

\`\`\`python
from bs4 import BeautifulSoup
\`\`\`

해설:

\`beautifulsoup4\`는 설치 이름과 import 이름이 다른 대표적인 예입니다. 다른 선택지도 올바른 설치와 import 예시이지만, 질문은 “설치 이름과 import 이름이 다른 예”를 묻고 있습니다.

---

### 문제 11 정답

\`\`\`text
1 - C
2 - B
3 - D
4 - A
\`\`\`

해설:

\`requests\`는 API 요청과 HTTP 통신에 사용합니다. \`pandas\`는 표 형태 데이터 처리에 사용합니다. \`openpyxl\`은 엑셀 파일 읽기와 쓰기에 사용합니다. \`pytest\`는 테스트 자동화에 사용합니다.

---

### 문제 12 정답

예시 답안:

\`\`\`text
1. 공식 문서가 있는지 확인한다.
2. 패키지 이름이 정확한지 확인한다.
3. 최근에도 관리되고 있는지 확인한다.
\`\`\`

추가로 다음과 같은 답도 가능합니다.

\`\`\`text
- 내가 사용하는 Python 버전과 호환되는지 확인한다.
- 사용자가 많은지 확인한다.
- 설치 명령이 공식 문서와 일치하는지 확인한다.
- 보안상 위험한 패키지는 아닌지 주의한다.
- 내가 하려는 작업에 적합한지 확인한다.
\`\`\`

---

## 다음 장 예고

다음 장에서는 **파일 다루기**를 배웁니다.

외부 라이브러리를 설치하고 관리하는 방법을 배웠으므로, 이제 실제 파일을 코드로 다루는 방법을 배울 수 있습니다. 텍스트 파일, CSV 파일, 엑셀 파일을 읽고 쓰는 방법을 배우고, 파일과 폴더를 자동으로 정리하는 방법도 살펴봅니다.

다음 장에서 다룰 내용은 다음과 같습니다.

\`\`\`text
- 텍스트 파일 읽고 쓰기
- CSV 파일 다루기
- openpyxl로 엑셀 파일 다루기
- os와 pathlib로 파일 경로 다루기
- 파일명 변경, 이동, 복사 자동화
- 파일 처리 중 발생하는 예외 처리
\`\`\`

9장에서 배운 외부 라이브러리 관리 방법은 10장부터 계속 사용됩니다. 특히 \`openpyxl\`을 설치하고 엑셀 파일을 처리하는 흐름에서 가상환경과 pip 사용법이 다시 등장합니다.
`;export{e as default};