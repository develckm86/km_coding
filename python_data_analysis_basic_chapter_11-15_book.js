var e=`<!-- 원본: python_data_analysis_basic_chapter_11_book.md / 세부 장: 11-15 -->

# 11.15 다음 장 예고

이번 장에서는 중복값과 이상값을 확인하고 처리하는 방법을 배웠습니다.

다음 장에서는 **문자열 데이터 처리**를 배웁니다.

실무 데이터에서 문자열 컬럼은 매우 자주 등장합니다.

예를 들어 다음과 같은 데이터가 모두 문자열입니다.

- 고객 이름
- 이메일
- 전화번호
- 주소
- 상품명
- 상품 코드
- 카테고리
- 리뷰 문장
- 로그 메시지

문자열 데이터는 공백, 대소문자, 특수문자, 구분자, 패턴 문제를 자주 포함합니다.

다음 장에서는 pandas의 \`.str\` 접근자를 사용해 문자열 컬럼을 정리하는 방법을 배웁니다.

다음 장의 주요 내용은 다음과 같습니다.

- 문자열 컬럼 다루기
- 앞뒤 공백 제거
- 대소문자 통일
- 문자열 분리
- 특정 문자열 포함 여부 확인
- 문자열 치환
- 정규표현식 기초 활용
- 이메일 도메인 추출
- 전화번호 형식 통일
- 상품 코드에서 카테고리 추출

문자열 처리는 데이터 클리닝에서 매우 중요한 단계입니다.  
결측치, 중복값, 이상값을 확인한 뒤 문자열 데이터를 정리하면 분석에 사용할 수 있는 데이터 품질이 크게 좋아집니다.

---

## 참고 문서

- pandas 공식 문서: \`DataFrame.duplicated\`
- pandas 공식 문서: \`DataFrame.drop_duplicates\`
- pandas 공식 문서: \`DataFrame.quantile\`
- pandas 공식 문서: \`DataFrame.describe\`
- pandas 공식 문서: \`DataFrame.boxplot\`
`;export{e as default};