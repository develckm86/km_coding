var e=`<!-- 원본: python_data_analysis_basic_chapter_11_book.md / 세부 장: 11-6 -->

# 11.6 이상값이란?

이상값은 일반적인 범위를 벗어난 값입니다.

예를 들어 주문 수량이 대부분 1개에서 10개 사이인데, 어떤 주문 수량이 10,000개라면 이상값일 수 있습니다.

하지만 이상값이 항상 오류는 아닙니다.

예를 들어 B2B 거래에서는 한 번에 10,000개를 주문할 수도 있습니다.  
고가 상품을 판매하는 쇼핑몰에서는 500만 원짜리 상품도 실제로 존재할 수 있습니다.

따라서 이상값은 먼저 **의심해야 하는 값**이지, 바로 삭제해야 하는 값은 아닙니다.

---

### 11.6.1 이상값의 종류

이상값은 크게 두 가지로 나눌 수 있습니다.

첫째, 명백한 오류값입니다.

\`\`\`text
나이: -10
주문 수량: -3
할인율: 150%
키: 999cm
날짜: 2099-99-99
\`\`\`

이 값들은 현실적으로 불가능하거나 데이터 형식상 잘못된 값입니다.

둘째, 드물지만 실제 가능한 값입니다.

\`\`\`text
주문 금액: 10,000,000원
구매 수량: 500개
방문 횟수: 1,000회
고객 연령: 95세
\`\`\`

이 값들은 드물지만 실제로 존재할 수 있습니다.  
이런 값은 무조건 제거하면 중요한 정보를 잃을 수 있습니다.

---

### 11.6.2 이상값이 생기는 이유

이상값은 다음과 같은 이유로 생깁니다.

| 원인 | 예시 |
|---|---|
| 입력 오류 | 가격에 0을 하나 더 입력 |
| 단위 오류 | 원 단위와 천 원 단위가 섞임 |
| 시스템 오류 | 센서가 비정상 값 기록 |
| 데이터 병합 오류 | 잘못된 값이 결합됨 |
| 실제 극단값 | 대량 주문, VIP 고객 구매 |
| 형 변환 오류 | 문자열이 잘못 숫자로 변환됨 |

이상값을 찾았을 때는 먼저 원인을 확인해야 합니다.

\`\`\`text
이 값은 실제로 가능한가?
단위가 잘못된 것은 아닌가?
입력 오류인가?
분석 목적상 포함해야 하는가?
\`\`\`

---

### 11.6.3 이상값이 분석에 미치는 영향

이상값은 특히 평균에 큰 영향을 줍니다.

다음 예를 보겠습니다.

\`\`\`python
sales = pd.Series([10000, 12000, 15000, 13000, 11000, 1000000])

sales.mean()
sales.median()
\`\`\`

대부분의 값은 1만 원대인데, 1,000,000원이 하나 들어가 있습니다.  
이 값 때문에 평균이 크게 올라갑니다.

반면 중앙값은 상대적으로 영향을 덜 받습니다.

이상값은 다음 분석에 큰 영향을 줄 수 있습니다.

- 평균
- 표준편차
- 상관관계
- 회귀분석
- 시각화 축 범위
- 예측 모델
- 고객 등급 분류
- 매출 상위 분석

기초 과정에서는 복잡한 통계 모델까지 다루지는 않지만, 이상값이 평균과 그래프 해석에 영향을 준다는 점은 꼭 이해해야 합니다.

---
`;export{e as default};