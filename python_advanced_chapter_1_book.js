var e=`# 1장. 고급 파이썬으로 들어가기

## 이 장에서 배울 내용

파이썬 기초 수업에서는 변수, 자료형, 조건문, 반복문, 함수, 객체지향, 파일 처리, 예외 처리와 같은 기본 문법을 배웠다. 이제 고급 파이썬 과정에서는 단순히 문법을 하나 더 배우는 것이 아니라, **파이썬 코드를 더 깊이 이해하고 더 좋은 구조로 작성하는 방법**을 배운다.

이 장에서는 고급 파이썬 과정이 무엇을 목표로 하는지, 기초 과정과 무엇이 다른지, 그리고 이후 데이터분석 기초·고급 과정으로 넘어가기 위해 왜 이 과정이 필요한지를 정리한다.

이 장을 마치면 다음을 설명할 수 있어야 한다.

- 고급 파이썬이 단순히 어려운 문법을 뜻하는 것이 아니라는 점
- 기초 문법을 아는 것과 실무형 코드를 작성하는 것의 차이
- 데이터분석 전에 고급 파이썬을 배우는 이유
- 좋은 파이썬 코드의 기준
- 파이썬다운 코드의 기본 방향
- 고급 파이썬 과정에서 배우게 될 핵심 주제

---

## 1.1 고급 파이썬이란?

파이썬을 처음 배울 때는 문법을 익히는 것이 가장 중요하다. 변수를 만들고, 조건문으로 흐름을 나누고, 반복문으로 여러 데이터를 처리하고, 함수를 만들어 코드를 재사용하는 법을 배운다. 이 단계에서는 “코드가 실행되는가?”가 가장 중요한 기준이다.

하지만 실무나 데이터분석으로 넘어가면 기준이 달라진다. 단순히 실행되는 코드만으로는 부족하다. 실행은 되지만 너무 복잡해서 수정하기 어렵거나, 작은 변경에도 전체 코드가 흔들리거나, 데이터가 조금만 커져도 느려지는 코드는 오래 사용하기 어렵다.

고급 파이썬은 이런 문제를 해결하기 위한 과정이다.

고급 파이썬은 다음과 같은 질문을 다룬다.

\`\`\`text
이 코드는 왜 이렇게 동작할까?
이 코드는 나중에 수정하기 쉬울까?
데이터가 커져도 이 방식이 괜찮을까?
함수와 클래스를 어떻게 나누는 것이 좋을까?
실패했을 때 어디서 원인을 찾을 수 있을까?
다른 사람이 읽어도 이해할 수 있을까?
\`\`\`

즉, 고급 파이썬은 “더 어려운 문법을 많이 아는 것”이 아니라, **파이썬을 더 정확하게 이해하고 안정적인 구조로 코드를 작성하는 능력**에 가깝다.

---

## 1.2 기초 파이썬과 고급 파이썬의 차이

기초 파이썬과 고급 파이썬은 완전히 다른 언어를 배우는 과정이 아니다. 같은 파이썬을 배우지만 관점이 달라진다.

기초 과정에서는 다음과 같은 내용을 배운다.

\`\`\`text
변수를 어떻게 만드는가?
리스트를 어떻게 사용하는가?
조건문을 어떻게 작성하는가?
함수는 어떻게 정의하는가?
클래스는 어떻게 만드는가?
파일은 어떻게 읽고 쓰는가?
\`\`\`

고급 과정에서는 같은 내용을 더 깊은 관점에서 다시 본다.

\`\`\`text
변수는 실제로 값을 담는 것인가, 객체를 가리키는 이름인가?
리스트를 복사할 때 왜 원본도 함께 바뀌는 것처럼 보일까?
함수를 값처럼 전달할 수 있다는 것은 어떤 의미일까?
반복문은 내부적으로 어떻게 동작할까?
클래스를 파이썬답게 설계하려면 무엇을 알아야 할까?
예외는 어디에서 처리하는 것이 좋을까?
테스트하기 좋은 코드는 어떤 구조를 가질까?
\`\`\`

두 과정의 차이를 표로 정리하면 다음과 같다.

| 구분 | 기초 파이썬 | 고급 파이썬 |
|---|---|---|
| 핵심 목표 | 문법을 익히고 코드를 실행한다 | 코드를 이해하고 구조화한다 |
| 주요 관심 | 변수, 조건문, 반복문, 함수, 클래스 | 객체 모델, 함수 심화, 이터레이터, 데코레이터, 설계 |
| 코드 기준 | 동작하는 코드 | 유지보수 가능한 코드 |
| 오류 대응 | 에러를 고친다 | 실패에 강한 구조를 만든다 |
| 데이터 처리 | 파일과 데이터를 읽고 쓴다 | 큰 데이터와 반복 처리 구조를 고려한다 |
| 실무 연결 | 간단한 자동화 | 재사용 가능한 도구와 프로젝트 구조 |

기초 과정에서 작성하는 코드는 보통 한 파일 안에서 동작해도 충분하다. 하지만 고급 과정에서는 코드를 여러 파일로 나누고, 함수와 클래스를 역할에 맞게 분리하며, 테스트와 로그까지 고려한다.

---

## 1.3 “문법을 아는 것”과 “코드를 설계하는 것”

문법을 아는 것과 코드를 설계하는 것은 다르다.

예를 들어 리스트를 알고, 반복문을 알고, 조건문을 안다면 아래와 같은 코드를 작성할 수 있다.

\`\`\`python
orders = [
    {"id": 1, "customer": "Kim", "amount": "12000", "status": "paid"},
    {"id": 2, "customer": "Lee", "amount": "", "status": "cancelled"},
    {"id": 3, "customer": "Park", "amount": "33000", "status": "paid"},
]

total = 0

for order in orders:
    if order["status"] == "paid":
        if order["amount"] != "":
            total += int(order["amount"])

print(total)
\`\`\`

이 코드는 실행된다. 결제 완료된 주문의 금액을 합산한다. 기초 문법만으로도 충분히 작성할 수 있다.

하지만 실무에서는 이런 질문을 하게 된다.

\`\`\`text
amount가 숫자로 변환할 수 없는 문자열이면 어떻게 할까?
status 값이 대문자로 들어오면 어떻게 할까?
주문 데이터가 10만 건이면 어떻게 처리할까?
같은 로직을 다른 파일에서도 사용해야 한다면 어떻게 할까?
이 계산이 맞는지 테스트하려면 어떻게 할까?
\`\`\`

이런 질문에 답하기 위해 코드를 조금 더 구조화할 수 있다.

\`\`\`python
def parse_amount(value: str) -> int:
    if not value:
        return 0
    return int(value)


def is_paid_order(order: dict) -> bool:
    return order.get("status") == "paid"


def calculate_paid_total(orders: list[dict]) -> int:
    total = 0

    for order in orders:
        if is_paid_order(order):
            total += parse_amount(order.get("amount", ""))

    return total


orders = [
    {"id": 1, "customer": "Kim", "amount": "12000", "status": "paid"},
    {"id": 2, "customer": "Lee", "amount": "", "status": "cancelled"},
    {"id": 3, "customer": "Park", "amount": "33000", "status": "paid"},
]

result = calculate_paid_total(orders)
print(result)
\`\`\`

두 번째 코드는 더 길어 보일 수 있다. 하지만 역할이 나뉘어 있다.

- \`parse_amount()\`는 금액 문자열을 정수로 바꾸는 역할을 한다.
- \`is_paid_order()\`는 결제 완료 주문인지 판단한다.
- \`calculate_paid_total()\`은 전체 합계를 계산한다.

이렇게 나누면 각 부분을 따로 테스트할 수 있고, 나중에 요구사항이 바뀌어도 수정할 위치를 찾기 쉽다.

고급 파이썬에서는 이런 관점을 중요하게 다룬다. 코드를 단순히 “한 번 실행되게” 만드는 것이 아니라, **읽고, 고치고, 확장하고, 검증할 수 있게** 만든다.

---

## 1.4 왜 데이터분석 전에 고급 파이썬을 배우는가?

이 과정 이후에는 데이터분석 기초와 데이터분석 고급 과정으로 이어진다. 그렇다면 바로 pandas나 시각화를 배우면 되지 않을까?

물론 작은 데이터로 간단한 분석을 할 때는 바로 pandas를 배워도 된다. 하지만 실무 데이터분석에서는 단순히 DataFrame 문법만 아는 것으로 부족한 경우가 많다.

실무 데이터분석에서는 다음과 같은 상황을 자주 만난다.

\`\`\`text
파일이 너무 커서 한 번에 열기 어렵다.
여러 API에서 데이터를 가져와야 한다.
응답 데이터가 중간에 실패할 수 있다.
수집한 데이터를 날짜별로 저장해야 한다.
분석 전에 데이터 형식을 검증해야 한다.
전처리 코드를 반복해서 사용해야 한다.
분석 결과가 맞는지 테스트해야 한다.
같은 분석 코드를 매주 또는 매월 다시 실행해야 한다.
\`\`\`

이런 상황에서는 파이썬 자체에 대한 이해가 필요하다.

예를 들어 큰 파일을 처리할 때는 제너레이터와 이터레이터를 이해하면 도움이 된다. API 요청을 반복할 때는 예외 처리, 재시도, 로깅이 필요하다. 분석 전처리 코드를 여러 번 사용할 때는 함수와 모듈 구조가 중요하다. 데이터 검증을 자동화하려면 테스트가 필요하다.

따라서 고급 파이썬 과정은 데이터분석 과정으로 가기 전의 중간 다리 역할을 한다.

\`\`\`text
파이썬 기초
  → 고급 파이썬
  → 데이터분석 기초
  → 데이터분석 고급
\`\`\`

이 흐름에서 고급 파이썬은 분석 도구 자체를 배우는 과정은 아니다. 대신 분석 도구를 더 잘 사용하기 위한 기반을 만든다.

---

## 1.5 이 과정에서 깊게 다루지 않는 것

고급 파이썬 과정은 데이터분석으로 이어지는 과정이지만, 데이터분석 자체를 깊게 다루지는 않는다. 이 구분이 중요하다.

이 과정에서는 다음 내용을 깊게 다루지 않는다.

\`\`\`text
pandas DataFrame 조작 심화
NumPy 배열 연산
통계 분석
데이터 시각화
머신러닝
시계열 분석
대시보드 제작
\`\`\`

이 내용들은 이후 데이터분석 기초 또는 데이터분석 고급 과정에서 다루는 것이 더 적절하다.

고급 파이썬 과정에서는 그 대신 다음과 같은 내용을 다룬다.

\`\`\`text
파이썬 객체 모델
함수 심화
이터레이터와 제너레이터
데코레이터
컨텍스트 매니저
객체지향 심화
타입 힌트
프로젝트 구조
예외 처리와 로깅
테스트
파일과 API 처리
데이터베이스 기초
동시성
성능 최적화
\`\`\`

즉, 이 과정의 목표는 분석 결과를 만드는 것이 아니라, **분석에 필요한 데이터를 안정적으로 준비하고, 처리 코드를 재사용 가능한 형태로 작성할 수 있게 하는 것**이다.

---

## 1.6 좋은 파이썬 코드의 기준

고급 파이썬을 배우기 전에 좋은 코드의 기준을 먼저 정리해야 한다. 좋은 코드의 기준을 알아야 왜 함수, 클래스, 타입 힌트, 테스트, 로깅을 배우는지 이해할 수 있다.

좋은 코드는 보통 다음과 같은 특징을 가진다.

---

### 1.6.1 읽기 쉬운 코드

코드는 컴퓨터만 읽는 것이 아니다. 사람도 읽는다. 실무에서는 미래의 나 자신도 코드를 다시 읽어야 한다.

읽기 쉬운 코드는 다음과 같은 특징이 있다.

\`\`\`text
변수와 함수 이름이 의미를 가진다.
한 함수가 너무 많은 일을 하지 않는다.
복잡한 조건문을 적절히 나눈다.
불필요하게 똑똑해 보이는 코드를 피한다.
주석 없이도 흐름이 어느 정도 읽힌다.
\`\`\`

예를 들어 다음 코드는 실행은 되지만 의미를 파악하기 어렵다.

\`\`\`python
def f(x):
    r = []
    for i in x:
        if i["s"] == "paid" and i["a"]:
            r.append(int(i["a"]))
    return sum(r)
\`\`\`

아래 코드는 같은 일을 하지만 조금 더 읽기 쉽다.

\`\`\`python
def calculate_paid_order_total(orders):
    paid_amounts = []

    for order in orders:
        if order["status"] == "paid" and order["amount"]:
            paid_amounts.append(int(order["amount"]))

    return sum(paid_amounts)
\`\`\`

좋은 코드는 짧은 코드가 아니라, **의도를 이해하기 쉬운 코드**다.

---

### 1.6.2 수정하기 쉬운 코드

실무 코드는 한 번 작성하고 끝나지 않는다. 요구사항은 계속 바뀐다.

예를 들어 처음에는 결제 완료 주문만 합산하면 되었지만, 나중에 다음 요구사항이 추가될 수 있다.

\`\`\`text
취소 주문은 제외한다.
환불 주문은 별도로 계산한다.
금액이 비어 있으면 0으로 처리한다.
특정 날짜 이후 주문만 계산한다.
VIP 고객 주문만 따로 계산한다.
\`\`\`

수정하기 쉬운 코드는 변경이 생겼을 때 수정할 위치가 분명하다. 반대로 모든 로직이 한 곳에 섞여 있으면 작은 변경도 어렵다.

수정하기 쉬운 코드를 만들려면 역할을 나누어야 한다.

\`\`\`python
def is_target_order(order):
    return order["status"] == "paid"


def get_order_amount(order):
    amount = order.get("amount")
    if not amount:
        return 0
    return int(amount)


def calculate_total(orders):
    total = 0

    for order in orders:
        if is_target_order(order):
            total += get_order_amount(order)

    return total
\`\`\`

이 구조에서는 주문 조건이 바뀌면 \`is_target_order()\`를 수정하면 되고, 금액 변환 방식이 바뀌면 \`get_order_amount()\`를 수정하면 된다.

---

### 1.6.3 재사용 가능한 코드

재사용 가능한 코드는 다른 곳에서도 다시 사용할 수 있는 코드다.

예를 들어 파일 경로에서 확장자를 확인하는 코드를 여러 곳에서 사용한다고 하자.

\`\`\`python
filename = "report.xlsx"

if filename.endswith(".xlsx"):
    print("엑셀 파일입니다.")
\`\`\`

이 로직이 여러 파일에 반복되면 나중에 수정하기 어렵다. 함수로 분리하면 재사용하기 쉬워진다.

\`\`\`python
def is_excel_file(filename: str) -> bool:
    return filename.endswith(".xlsx") or filename.endswith(".xls")
\`\`\`

이제 다른 곳에서도 같은 함수를 사용할 수 있다.

\`\`\`python
if is_excel_file("report.xlsx"):
    print("엑셀 파일입니다.")
\`\`\`

재사용 가능한 코드를 작성하려면 특정 상황에 너무 강하게 묶인 코드를 피해야 한다.

---

### 1.6.4 테스트 가능한 코드

테스트 가능한 코드는 입력과 출력이 분명하다.

아래 함수는 테스트하기 쉽다.

\`\`\`python
def calculate_discount_price(price: int, discount_rate: float) -> int:
    return int(price * (1 - discount_rate))
\`\`\`

입력값을 주면 결과가 반환된다.

\`\`\`python
result = calculate_discount_price(10000, 0.1)
print(result)  # 9000
\`\`\`

반면 아래 코드는 테스트하기 어렵다.

\`\`\`python
def calculate_discount_price():
    price = int(input("가격을 입력하세요: "))
    discount_rate = float(input("할인율을 입력하세요: "))
    print(int(price * (1 - discount_rate)))
\`\`\`

이 함수는 내부에서 직접 입력을 받고 직접 출력한다. 사람이 값을 입력해야 하므로 자동 테스트가 어렵다.

고급 파이썬에서는 테스트 가능한 구조를 중요하게 다룬다. 데이터분석에서도 전처리 함수가 맞게 동작하는지 검증해야 하기 때문이다.

---

### 1.6.5 실패에 강한 코드

실무 환경에서는 항상 정상 데이터만 들어오지 않는다.

\`\`\`text
파일이 없을 수 있다.
API 요청이 실패할 수 있다.
숫자여야 할 값이 문자열일 수 있다.
필수 컬럼이 누락될 수 있다.
날짜 형식이 잘못될 수 있다.
네트워크가 느릴 수 있다.
\`\`\`

실패에 강한 코드는 이런 상황을 예상하고 처리한다.

예를 들어 문자열을 정수로 변환할 때 실패할 가능성을 고려할 수 있다.

\`\`\`python
def safe_int(value: str, default: int = 0) -> int:
    try:
        return int(value)
    except ValueError:
        return default
\`\`\`

이 함수는 숫자로 바꿀 수 없는 값이 들어와도 프로그램 전체가 멈추지 않는다.

\`\`\`python
print(safe_int("100"))     # 100
print(safe_int("abc"))     # 0
\`\`\`

물론 모든 예외를 무조건 숨기면 안 된다. 중요한 것은 실패 가능성을 알고, 적절한 위치에서 처리하는 것이다.

---

### 1.6.6 성능을 고려한 코드

처리할 데이터가 작을 때는 대부분의 코드가 빠르게 실행된다. 하지만 데이터가 커지면 문제가 달라진다.

예를 들어 10개의 데이터에서는 어떤 방식이든 큰 차이가 없다. 하지만 100만 개의 데이터를 처리한다면 자료구조 선택, 반복 방식, 메모리 사용량이 중요해진다.

아래 코드는 리스트에서 값을 찾는다.

\`\`\`python
users = ["kim", "lee", "park", "choi"]

if "park" in users:
    print("존재합니다.")
\`\`\`

데이터가 아주 많고 포함 여부를 자주 확인해야 한다면 집합이 더 적합할 수 있다.

\`\`\`python
users = {"kim", "lee", "park", "choi"}

if "park" in users:
    print("존재합니다.")
\`\`\`

고급 과정에서는 성능 최적화를 무조건 많이 하는 것이 아니라, **언제 성능을 고려해야 하는지**와 **어디를 측정해야 하는지**를 배운다.

---

## 1.7 파이썬다운 코드란?

파이썬에는 “파이썬답다”는 표현이 있다. 영어로는 Pythonic이라고도 한다. 파이썬다운 코드는 파이썬의 문법과 철학을 자연스럽게 활용한 코드를 의미한다.

파이썬다운 코드는 무조건 짧은 코드가 아니다. 오히려 너무 짧아서 이해하기 어려운 코드는 좋은 코드가 아닐 수 있다.

파이썬다운 코드는 다음과 같은 방향을 가진다.

\`\`\`text
명시적인 코드
읽기 쉬운 코드
표준 기능을 적절히 활용한 코드
불필요한 복잡성을 피하는 코드
반복 가능한 구조를 자연스럽게 사용하는 코드
\`\`\`

---

### 1.7.1 명시적인 코드

파이썬에서는 의도가 분명한 코드를 선호한다.

예를 들어 비어 있는 리스트를 확인할 때 다음처럼 작성할 수 있다.

\`\`\`python
items = []

if len(items) == 0:
    print("비어 있습니다.")
\`\`\`

하지만 파이썬에서는 빈 리스트가 거짓처럼 평가되므로 다음과 같이 쓸 수 있다.

\`\`\`python
items = []

if not items:
    print("비어 있습니다.")
\`\`\`

두 번째 방식은 파이썬에서 자주 쓰이는 표현이다. 다만 초보자에게는 처음에 낯설 수 있으므로, 수업에서는 왜 이렇게 동작하는지 함께 이해해야 한다.

---

### 1.7.2 반복 가능한 구조 활용

파이썬은 반복 가능한 객체를 자연스럽게 다룬다.

아래 코드는 인덱스를 사용해 리스트를 반복한다.

\`\`\`python
names = ["Kim", "Lee", "Park"]

for i in range(len(names)):
    print(names[i])
\`\`\`

파이썬에서는 보통 다음과 같이 직접 값을 반복한다.

\`\`\`python
names = ["Kim", "Lee", "Park"]

for name in names:
    print(name)
\`\`\`

인덱스가 꼭 필요하다면 \`enumerate()\`를 사용할 수 있다.

\`\`\`python
names = ["Kim", "Lee", "Park"]

for index, name in enumerate(names, start=1):
    print(index, name)
\`\`\`

이런 방식은 파이썬의 반복 구조를 더 자연스럽게 사용하는 예다.

---

### 1.7.3 표준 라이브러리 활용

파이썬은 표준 라이브러리가 풍부하다. 이미 잘 만들어진 기능이 있다면 직접 구현하기보다 표준 라이브러리를 활용하는 것이 좋다.

예를 들어 파일 경로를 문자열로 직접 조합하는 방식은 운영체제마다 문제가 생길 수 있다.

\`\`\`python
folder = "data"
filename = "sales.csv"
path = folder + "/" + filename
\`\`\`

\`pathlib\`을 사용하면 더 안전하고 읽기 좋다.

\`\`\`python
from pathlib import Path

folder = Path("data")
filename = "sales.csv"
path = folder / filename
\`\`\`

고급 파이썬에서는 표준 라이브러리를 적절히 활용하는 습관도 중요하게 다룬다.

---

## 1.8 고급 과정에서 배우게 될 핵심 주제

이제 고급 파이썬 과정에서 어떤 내용을 배우게 될지 전체 흐름을 살펴보자.

---

### 1.8.1 파이썬 실행 구조와 객체 모델

파이썬에서 변수는 값을 담는 상자가 아니라 객체를 가리키는 이름이다. 이 개념을 이해하면 가변 객체, 불변 객체, 얕은 복사, 깊은 복사 같은 내용을 더 정확히 이해할 수 있다.

예를 들어 다음 코드는 처음 보면 이상해 보일 수 있다.

\`\`\`python
a = [1, 2, 3]
b = a

b.append(4)

print(a)
\`\`\`

결과는 다음과 같다.

\`\`\`text
[1, 2, 3, 4]
\`\`\`

왜 \`b\`를 수정했는데 \`a\`도 바뀐 것처럼 보일까? 고급 과정에서는 이런 동작을 객체와 참조 관점에서 설명한다.

---

### 1.8.2 함수 고급 활용

파이썬에서 함수는 객체다. 변수에 저장할 수도 있고, 다른 함수에 전달할 수도 있으며, 함수가 함수를 반환할 수도 있다.

\`\`\`python
def add_tax(price):
    return int(price * 1.1)


def apply_price_rule(price, rule):
    return rule(price)


result = apply_price_rule(10000, add_tax)
print(result)
\`\`\`

이런 개념은 고차 함수, 클로저, 데코레이터로 이어진다.

---

### 1.8.3 이터레이터와 제너레이터

이터레이터와 제너레이터는 반복 처리의 고급 개념이다. 특히 큰 파일이나 많은 데이터를 다룰 때 중요하다.

리스트는 모든 값을 한 번에 만든다.

\`\`\`python
numbers = [i for i in range(1_000_000)]
\`\`\`

제너레이터는 필요한 값을 하나씩 만든다.

\`\`\`python
numbers = (i for i in range(1_000_000))
\`\`\`

데이터분석 전처리에서는 큰 파일을 한 번에 메모리에 올리지 않고 처리해야 할 때가 있다. 이때 제너레이터 개념이 도움이 된다.

---

### 1.8.4 데코레이터

데코레이터는 기존 함수나 메서드에 추가 기능을 붙이는 문법이다.

\`\`\`python
def log_call(func):
    def wrapper(*args, **kwargs):
        print("함수를 실행합니다.")
        return func(*args, **kwargs)
    return wrapper


@log_call
def greet(name):
    print(f"안녕하세요, {name}님")


greet("Kim")
\`\`\`

처음에는 어렵게 느껴질 수 있지만, 데코레이터는 실무에서 자주 등장한다. \`@property\`, \`@classmethod\`, \`@dataclass\`, \`@pytest.fixture\` 같은 문법도 데코레이터다.

---

### 1.8.5 컨텍스트 매니저

컨텍스트 매니저는 \`with\` 문과 관련된 개념이다. 파일, 데이터베이스 연결, 네트워크 연결처럼 열고 닫아야 하는 자원을 안전하게 관리할 때 사용한다.

\`\`\`python
with open("data.txt", "r", encoding="utf-8") as file:
    text = file.read()
\`\`\`

고급 과정에서는 \`with\` 문이 내부적으로 어떻게 동작하는지, 직접 컨텍스트 매니저를 만드는 방법도 배운다.

---

### 1.8.6 객체지향 프로그래밍 심화

기초 과정에서 클래스와 객체를 배웠다면, 고급 과정에서는 객체지향 설계를 더 깊게 다룬다.

\`\`\`text
상속을 언제 사용할까?
상속보다 조합이 더 나은 경우는 언제일까?
추상 클래스는 왜 필요할까?
다형성은 실무에서 어떻게 쓰일까?
클래스의 책임은 어떻게 나눌까?
\`\`\`

데이터 처리 프로그램에서도 클래스는 유용하다. 예를 들어 API 클라이언트, 파일 저장기, 데이터 검증기, 보고서 생성기 등을 클래스로 설계할 수 있다.

---

### 1.8.7 파이썬 데이터 모델

파이썬 데이터 모델은 객체가 파이썬 문법과 어떻게 연결되는지를 설명한다.

예를 들어 \`len(obj)\`를 실행하면 내부적으로 \`obj.__len__()\`이 호출된다. \`str(obj)\`는 \`obj.__str__()\`와 연결된다.

이런 특수 메서드를 이해하면 내가 만든 객체도 리스트처럼, 숫자처럼, 함수처럼 동작하게 만들 수 있다.

\`\`\`python
class Cart:
    def __init__(self):
        self.items = []

    def __len__(self):
        return len(self.items)


cart = Cart()
print(len(cart))
\`\`\`

---

### 1.8.8 타입 힌트 심화

기초 과정에서는 함수 매개변수와 반환값에 타입을 표시하는 정도를 배웠다. 고급 과정에서는 더 복잡한 데이터 구조와 함수 타입까지 표현한다.

\`\`\`python
from typing import TypedDict


class Order(TypedDict):
    id: int
    customer: str
    amount: int
    status: str
\`\`\`

타입 힌트는 데이터분석 전처리 코드에서도 유용하다. 함수가 어떤 형태의 데이터를 받아 어떤 형태로 반환하는지 명확히 해주기 때문이다.

---

### 1.8.9 예외 처리와 로깅 심화

기초 과정에서는 \`try-except\`를 배웠다. 고급 과정에서는 예외를 어디서 처리해야 하는지, 어떤 예외를 직접 만들 수 있는지, 로그를 어떻게 남겨야 하는지를 배운다.

\`\`\`python
import logging

logging.basicConfig(level=logging.INFO)

try:
    value = int("abc")
except ValueError as error:
    logging.exception("숫자 변환에 실패했습니다.")
\`\`\`

실무에서는 오류가 발생했을 때 원인을 추적할 수 있어야 한다. 로깅은 자동화 프로그램과 데이터 처리 프로그램에서 매우 중요하다.

---

### 1.8.10 테스트 심화

기초 과정에서는 \`assert\`와 \`pytest\`의 기본을 배웠다. 고급 과정에서는 fixture, parametrization, mock 등을 사용해 더 체계적으로 테스트하는 방법을 배운다.

분석 코드도 테스트가 필요하다. 예를 들어 날짜 형식 변환 함수, 금액 정리 함수, 데이터 검증 함수는 테스트로 검증할 수 있다.

---

### 1.8.11 파일, API, 데이터베이스 처리

데이터분석 과정으로 이어지기 위해서는 데이터를 가져오고 저장하는 기본기가 필요하다.

고급 과정에서는 다음 내용을 다룬다.

\`\`\`text
대용량 파일을 한 줄씩 처리하기
JSON Lines 형식 이해하기
API 페이지네이션 처리하기
요청 실패 시 재시도하기
SQLite에 데이터 저장하기
분석용 CSV로 내보내기
\`\`\`

이 내용은 데이터분석의 전 단계에 해당한다. 분석을 하기 전에 데이터를 안정적으로 준비하는 능력을 기르는 것이다.

---

### 1.8.12 동시성과 성능

데이터가 많아지면 실행 시간이 길어질 수 있다. API 요청이 많거나 파일이 많을 때는 동시성도 고려해야 한다.

고급 과정에서는 다음을 배운다.

\`\`\`text
스레드와 프로세스의 차이
I/O 작업과 CPU 작업의 차이
async와 await의 기본 개념
코드 실행 시간 측정
프로파일링
자료구조 선택과 성능
제너레이터를 이용한 메모리 절약
\`\`\`

성능 최적화는 처음부터 하는 것이 아니라, 먼저 측정하고 병목 지점을 찾은 뒤 필요한 부분에 적용하는 것이 좋다.

---

## 1.9 고급 파이썬 학습 방법

고급 파이썬은 한 번에 이해하기 어려운 개념이 많다. 특히 이터레이터, 데코레이터, 컨텍스트 매니저, 데이터 모델은 처음에는 추상적으로 느껴질 수 있다.

따라서 다음과 같은 방식으로 학습하는 것이 좋다.

---

### 1.9.1 코드 결과를 먼저 확인한다

고급 개념은 설명만 읽으면 어렵다. 먼저 코드를 실행해보고 결과를 확인하는 것이 좋다.

\`\`\`python
a = [1, 2, 3]
b = a
b.append(4)
print(a)
\`\`\`

결과를 보고 “왜 이렇게 나왔을까?”를 질문하면 개념을 더 잘 이해할 수 있다.

---

### 1.9.2 작은 예제로 원리를 이해한다

처음부터 큰 프로젝트에 적용하려고 하면 어렵다. 작은 예제로 원리를 이해한 뒤 실무 예제로 확장하는 방식이 좋다.

예를 들어 데코레이터를 배울 때 처음부터 로그인 권한 검사 데코레이터를 만들기보다, 함수 실행 전후에 메시지를 출력하는 간단한 데코레이터부터 시작한다.

---

### 1.9.3 “왜 필요한가?”를 함께 본다

고급 문법은 필요성을 모르면 암기처럼 느껴진다.

예를 들어 제너레이터는 단순히 \`yield\` 문법을 배우는 것이 아니다. 큰 데이터를 한 번에 만들지 않고 필요한 만큼만 처리하기 위해 사용한다.

데코레이터도 단순히 \`@\` 문법을 배우는 것이 아니다. 기존 함수를 수정하지 않고 공통 기능을 추가하기 위해 사용한다.

고급 파이썬에서는 항상 다음 질문을 함께 해야 한다.

\`\`\`text
이 문법은 어떤 문제를 해결하려고 만들어졌을까?
언제 사용하면 좋을까?
언제 사용하지 않는 것이 좋을까?
\`\`\`

---

### 1.9.4 직접 리팩터링해본다

고급 파이썬은 새로운 코드를 작성하는 것뿐만 아니라 기존 코드를 개선하는 데에도 중요하다.

예를 들어 한 파일에 길게 작성된 코드를 다음과 같이 개선할 수 있다.

\`\`\`text
중복 코드를 함수로 분리한다.
관련 데이터를 클래스로 묶는다.
예외 처리 위치를 정리한다.
로그를 추가한다.
테스트를 작성한다.
파일 구조를 나눈다.
\`\`\`

리팩터링은 코드의 동작은 유지하면서 구조를 개선하는 작업이다. 고급 과정에서는 이런 연습이 중요하다.

---

## 1.10 고급 파이썬 과정과 데이터분석 과정의 연결

이 과정은 데이터분석 기초 과정과 자연스럽게 이어지도록 설계된다.

데이터분석 기초 과정에서는 보통 다음 내용을 다룬다.

\`\`\`text
NumPy 배열
pandas Series와 DataFrame
CSV와 엑셀 데이터 읽기
데이터 선택과 필터링
결측치 처리
그룹화와 집계
시각화 기초
탐색적 데이터 분석
\`\`\`

하지만 그 전에 다음 능력이 있으면 훨씬 수월하다.

\`\`\`text
파일 경로를 안정적으로 다룰 수 있다.
큰 파일을 메모리 부담 없이 처리할 수 있다.
API에서 데이터를 가져올 수 있다.
실패한 요청을 기록하고 재시도할 수 있다.
함수와 클래스로 전처리 코드를 정리할 수 있다.
데이터 검증 함수를 테스트할 수 있다.
로그를 남겨 실행 결과를 추적할 수 있다.
\`\`\`

고급 파이썬은 이 기반을 만든다.

분석을 잘하려면 pandas 문법도 중요하지만, 분석 전후의 코드 구조도 중요하다. 데이터를 가져오고, 검증하고, 정리하고, 저장하는 과정이 안정적이어야 분석 결과도 신뢰할 수 있다.

---

## 1.11 예제로 보는 고급 파이썬의 방향

다음은 기초 문법으로 작성한 간단한 데이터 처리 코드다.

\`\`\`python
rows = [
    {"name": "Kim", "amount": "10000"},
    {"name": "Lee", "amount": ""},
    {"name": "Park", "amount": "30000"},
]

result = []

for row in rows:
    if row["amount"] != "":
        row["amount"] = int(row["amount"])
        result.append(row)

print(result)
\`\`\`

이 코드는 빈 금액을 제외하고 금액을 정수로 바꾼다. 기초 과정 수준에서는 충분히 좋은 예제다.

하지만 고급 과정에서는 다음과 같이 생각해볼 수 있다.

\`\`\`text
원본 데이터를 직접 수정해도 괜찮을까?
amount가 "10,000"처럼 들어오면 어떻게 할까?
amount가 "abc"이면 어떻게 할까?
처리 실패한 행을 따로 기록해야 하지 않을까?
이 로직을 함수로 분리할 수 있을까?
테스트를 작성할 수 있을까?
\`\`\`

조금 더 구조화하면 다음과 같이 작성할 수 있다.

\`\`\`python
def parse_amount(value: str) -> int | None:
    if not value:
        return None

    cleaned = value.replace(",", "")

    try:
        return int(cleaned)
    except ValueError:
        return None


def clean_sales_rows(rows: list[dict]) -> list[dict]:
    cleaned_rows = []

    for row in rows:
        amount = parse_amount(row.get("amount", ""))

        if amount is None:
            continue

        cleaned_rows.append({
            "name": row.get("name", ""),
            "amount": amount,
        })

    return cleaned_rows
\`\`\`

이 코드는 더 안전하고 테스트하기 쉽다.

\`\`\`python
rows = [
    {"name": "Kim", "amount": "10,000"},
    {"name": "Lee", "amount": ""},
    {"name": "Park", "amount": "30000"},
]

print(clean_sales_rows(rows))
\`\`\`

고급 과정에서는 이런 식으로 코드를 바라본다. 실행되는 코드에서 멈추지 않고, 더 안정적인 구조로 개선한다.

---

## 1.12 이 장의 핵심 정리

고급 파이썬은 어려운 문법을 많이 외우는 과정이 아니다. 파이썬이 어떻게 동작하는지 이해하고, 코드를 더 안정적이고 유지보수하기 쉽게 작성하는 과정이다.

기초 파이썬에서는 문법을 익히는 것이 중요했다. 고급 파이썬에서는 같은 문법을 더 깊이 이해하고, 함수와 클래스, 모듈, 테스트, 로깅, 예외 처리, 성능까지 함께 고려한다.

데이터분석으로 넘어가기 전에 고급 파이썬을 배우면 분석 전 단계의 데이터 수집, 검증, 정리, 저장 작업을 더 안정적으로 처리할 수 있다.

좋은 파이썬 코드는 다음과 같은 특징을 가진다.

\`\`\`text
읽기 쉽다.
수정하기 쉽다.
재사용할 수 있다.
테스트할 수 있다.
실패에 강하다.
필요한 경우 성능을 고려한다.
\`\`\`

앞으로의 고급 과정에서는 파이썬 객체 모델, 함수 심화, 이터레이터와 제너레이터, 데코레이터, 컨텍스트 매니저, 객체지향 심화, 타입 힌트, 테스트, 로깅, 파일과 API 처리, 데이터베이스, 동시성, 성능 최적화 등을 배운다.

이 과정의 최종 목표는 다음과 같다.

\`\`\`text
파이썬 코드를 단순히 실행되게 만드는 수준을 넘어,
실무와 데이터분석에 사용할 수 있는 구조로 작성하는 것.
\`\`\`

---

# 연습문제

## 문제 1. 개념 이해

다음 중 고급 파이썬 과정의 목표로 가장 적절한 것은 무엇인가?

1. 파이썬 문법을 전혀 모르는 사람이 처음 코딩을 시작하는 것
2. 파이썬 코드를 더 안정적이고 유지보수하기 쉽게 작성하는 것
3. pandas의 모든 함수를 암기하는 것
4. 머신러닝 모델을 만드는 것

---

## 문제 2. 기초 과정과 고급 과정의 차이

다음 설명 중 기초 파이썬 과정에 더 가까운 것은 무엇인가?

1. 변수를 만들고 조건문을 작성하는 방법을 배운다.
2. 객체와 참조의 관계를 이해한다.
3. 데코레이터를 직접 구현한다.
4. 대용량 파일을 제너레이터로 처리한다.

---

## 문제 3. 좋은 코드의 기준

다음 중 좋은 코드의 특징으로 보기 어려운 것은 무엇인가?

1. 읽기 쉽다.
2. 수정하기 쉽다.
3. 테스트하기 어렵다.
4. 실패 상황을 고려한다.

---

## 문제 4. 코드 구조 이해

다음 두 코드 중 실무에서 더 수정하기 쉬운 구조는 어느 쪽인가?

### 코드 A

\`\`\`python
total = 0

for order in orders:
    if order["status"] == "paid" and order["amount"] != "":
        total += int(order["amount"])
\`\`\`

### 코드 B

\`\`\`python
def is_paid_order(order):
    return order.get("status") == "paid"


def get_order_amount(order):
    amount = order.get("amount")
    if not amount:
        return 0
    return int(amount)


def calculate_total(orders):
    total = 0

    for order in orders:
        if is_paid_order(order):
            total += get_order_amount(order)

    return total
\`\`\`

어느 코드가 더 수정하기 쉬운지 고르고, 그 이유를 간단히 설명하라.

---

## 문제 5. 데이터분석 전 고급 파이썬이 필요한 이유

데이터분석 전에 고급 파이썬을 배우면 좋은 이유를 두 가지 이상 적어보라.

---

## 문제 6. 파이썬다운 코드

다음 코드보다 파이썬에서 더 자주 쓰이는 표현으로 바꿔보라.

\`\`\`python
items = []

if len(items) == 0:
    print("비어 있습니다.")
\`\`\`

---

## 문제 7. 학습 주제 연결

다음 상황에 가장 관련 있는 고급 파이썬 주제를 연결하라.

| 상황 | 주제 |
|---|---|
| 큰 파일을 한 줄씩 처리하고 싶다 | ? |
| 함수 실행 시간을 자동으로 기록하고 싶다 | ? |
| 파일을 열고 자동으로 닫고 싶다 | ? |
| API 요청 실패를 기록하고 싶다 | ? |
| 내가 만든 객체를 \`len()\`과 함께 쓰고 싶다 | ? |

보기:

\`\`\`text
이터레이터와 제너레이터
데코레이터
컨텍스트 매니저
로깅
파이썬 데이터 모델
\`\`\`

---

## 문제 8. 서술형

“실행되는 코드”와 “좋은 코드”는 어떻게 다른가? 자신의 말로 설명하라.

---

# 정답 및 해설

## 문제 1 정답

정답: 2

고급 파이썬 과정은 단순히 더 어려운 문법을 배우는 과정이 아니라, 코드를 더 안정적이고 유지보수하기 쉽게 작성하는 방법을 배우는 과정이다.

---

## 문제 2 정답

정답: 1

변수, 조건문, 반복문, 함수와 같은 기본 문법은 기초 과정의 핵심 내용이다. 객체와 참조, 데코레이터, 제너레이터는 고급 과정에서 더 깊게 다룬다.

---

## 문제 3 정답

정답: 3

좋은 코드는 테스트하기 쉬워야 한다. 입력과 출력이 분명하고, 하나의 함수가 하나의 역할을 담당할수록 테스트하기 쉽다.

---

## 문제 4 정답 예시

코드 B가 더 수정하기 쉽다.

코드 B는 주문이 결제 완료 상태인지 판단하는 로직, 금액을 가져오는 로직, 전체 합계를 계산하는 로직이 함수로 나뉘어 있다. 따라서 주문 조건이 바뀌거나 금액 처리 방식이 바뀌어도 수정할 위치를 찾기 쉽다. 또한 각 함수를 따로 테스트할 수 있다.

---

## 문제 5 정답 예시

데이터분석 전에 고급 파이썬을 배우면 다음과 같은 장점이 있다.

\`\`\`text
큰 파일을 메모리에 한 번에 올리지 않고 처리할 수 있다.
API 요청 실패를 예외 처리와 로깅으로 관리할 수 있다.
데이터 전처리 코드를 함수와 모듈로 정리할 수 있다.
데이터 검증 함수를 테스트할 수 있다.
반복 실행 가능한 분석 전처리 구조를 만들 수 있다.
\`\`\`

---

## 문제 6 정답

\`\`\`python
items = []

if not items:
    print("비어 있습니다.")
\`\`\`

빈 리스트는 불리언 문맥에서 \`False\`처럼 평가된다. 따라서 \`not items\`는 리스트가 비어 있는지 확인하는 파이썬다운 표현이다.

---

## 문제 7 정답

| 상황 | 주제 |
|---|---|
| 큰 파일을 한 줄씩 처리하고 싶다 | 이터레이터와 제너레이터 |
| 함수 실행 시간을 자동으로 기록하고 싶다 | 데코레이터 |
| 파일을 열고 자동으로 닫고 싶다 | 컨텍스트 매니저 |
| API 요청 실패를 기록하고 싶다 | 로깅 |
| 내가 만든 객체를 \`len()\`과 함께 쓰고 싶다 | 파이썬 데이터 모델 |

---

## 문제 8 정답 예시

실행되는 코드는 당장 원하는 결과를 출력하는 코드다. 하지만 좋은 코드는 실행될 뿐 아니라 읽기 쉽고, 수정하기 쉽고, 재사용 가능하며, 테스트할 수 있고, 실패 상황도 고려한다. 실무에서는 코드를 한 번만 작성하고 끝내는 경우가 드물기 때문에 좋은 코드의 기준이 중요하다.

---

# 다음 장 예고

다음 장에서는 **파이썬 실행 구조와 객체 모델**을 배운다. 파이썬에서 변수와 객체가 어떤 관계를 가지는지, 가변 객체와 불변 객체는 어떻게 다른지, 얕은 복사와 깊은 복사가 왜 필요한지 살펴본다.

기초 과정에서는 변수를 값을 담는 상자처럼 이해해도 충분했다. 하지만 고급 과정에서는 변수를 객체를 가리키는 이름으로 이해해야 한다. 이 관점이 생기면 파이썬의 많은 동작이 훨씬 명확해진다.
`;export{e as default};