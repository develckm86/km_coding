var e=`<!-- 원본: python_basic_chapter_1_book.md / 세부 장: 1-4 -->

# 1.4 파이썬의 문법적 특징

## 들여쓰기로 코드 블록을 구분한다

파이썬의 대표적인 문법적 특징은 들여쓰기입니다.

많은 프로그래밍 언어는 중괄호 같은 기호로 코드의 범위를 구분합니다. 반면 파이썬은 들여쓰기를 사용해 코드 블록을 구분합니다. 코드 블록이란 특정 조건이나 반복, 함수 등에 속한 코드의 묶음을 의미합니다.

이 장에서는 아직 조건문이나 반복문 문법을 자세히 배우지 않았기 때문에, 들여쓰기의 정확한 사용법을 외울 필요는 없습니다. 지금은 파이썬에서 들여쓰기가 단순한 보기 좋게 만들기용 공백이 아니라, 문법적으로 의미가 있는 요소라는 점만 이해하면 됩니다.

들여쓰기를 중요하게 다루는 방식은 파이썬 코드가 일정한 모양을 유지하도록 도와줍니다. 그래서 여러 사람이 작성한 코드도 비교적 비슷한 구조로 읽을 수 있습니다.

## 불필요한 기호가 적다

파이썬 코드는 다른 언어에 비해 불필요한 기호가 적은 편입니다. 문장 끝에 세미콜론을 붙이지 않는 경우가 일반적이고, 코드 블록을 중괄호로 감싸지 않습니다.

이런 특징은 초보자가 코드의 핵심 의미에 집중하는 데 도움이 됩니다. 물론 기호가 적다고 해서 문법을 대충 작성해도 된다는 뜻은 아닙니다. 파이썬은 들여쓰기와 줄 구조를 엄격하게 해석하므로, 정해진 규칙을 지켜야 합니다.

## 동적 타입 언어다

파이썬은 동적 타입 언어입니다. 이것은 변수를 만들 때 자료형을 미리 고정해서 선언하지 않아도 된다는 뜻입니다.

예를 들어 어떤 값이 문자열인지, 숫자인지, 참과 거짓을 나타내는 값인지는 파이썬이 실행 중에 판단합니다. 이 덕분에 초보자는 비교적 간단하게 코드를 작성할 수 있습니다.

하지만 동적 타입 언어에는 주의할 점도 있습니다. 자료형을 명시하지 않는 만큼, 값이 어떤 종류인지 개발자가 잘 이해하고 있어야 합니다. 숫자인 줄 알았던 값이 문자열이면 계산할 때 오류가 발생할 수 있습니다. 그래서 이후 장에서 자료형을 정확히 이해하는 것이 중요합니다.

## 객체지향을 지원한다

파이썬은 객체지향 프로그래밍을 지원합니다. 객체지향 프로그래밍이란 데이터와 그 데이터를 다루는 기능을 하나의 단위로 묶어 프로그램을 구성하는 방식입니다.

예를 들어 고객, 상품, 주문 같은 개념을 각각 객체로 표현할 수 있습니다. 고객 객체는 이름, 이메일, 등급 같은 데이터를 가질 수 있고, 등급 변경이나 정보 출력 같은 기능을 가질 수 있습니다.

객체지향은 처음에는 어렵게 느껴질 수 있습니다. 하지만 프로그램의 규모가 커질수록 코드를 구조화하고 관리하기 위해 중요한 개념입니다. 이 수업에서는 예외 처리 이후에 객체지향 문법을 본격적으로 다룹니다.

## 다양한 프로그래밍 방식을 지원한다

파이썬은 한 가지 방식으로만 코드를 작성하도록 강제하지 않습니다. 간단한 스크립트처럼 순서대로 코드를 작성할 수도 있고, 함수를 만들어 기능을 나눌 수도 있으며, 클래스를 사용해 객체지향적으로 프로그램을 구성할 수도 있습니다.

이런 유연성은 파이썬의 장점입니다. 처음에는 간단한 코드로 시작하고, 프로그램이 복잡해지면 함수와 클래스를 사용해 구조를 개선할 수 있습니다.

## 풍부한 표준 라이브러리를 제공한다

파이썬은 설치할 때부터 다양한 표준 라이브러리를 함께 제공합니다. 표준 라이브러리는 파이썬에 기본으로 포함된 도구 모음입니다.

예를 들어 날짜와 시간을 다루는 기능, 파일 경로를 다루는 기능, JSON 데이터를 처리하는 기능, 난수를 만드는 기능 등이 표준 라이브러리에 포함되어 있습니다.

표준 라이브러리를 잘 활용하면 외부 도구를 설치하지 않고도 많은 작업을 처리할 수 있습니다. 이후 수업에서는 자주 쓰는 표준 라이브러리를 따로 정리합니다.

## 외부 라이브러리를 쉽게 사용할 수 있다

파이썬은 외부 라이브러리를 설치하고 사용하는 방식도 비교적 간단합니다. \`pip\`라는 도구를 사용하면 필요한 라이브러리를 설치할 수 있습니다.

예를 들어 엑셀 파일을 다루고 싶으면 관련 라이브러리를 설치해 사용할 수 있고, API 요청을 보내고 싶으면 그에 맞는 라이브러리를 설치해 사용할 수 있습니다.

외부 라이브러리는 파이썬을 실무에서 강력하게 만들어 주는 중요한 요소입니다. 다만 무분별하게 설치하기보다, 어떤 문제를 해결하기 위해 어떤 라이브러리가 필요한지 판단하는 습관이 필요합니다.

---
`;export{e as default};