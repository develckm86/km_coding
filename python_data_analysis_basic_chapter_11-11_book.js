var e=`<!-- 원본: python_data_analysis_basic_chapter_11_book.md / 세부 장: 11-11 -->

# 11.11 중복값과 이상값 처리 시 자주 하는 실수

이번 절에서는 초보자가 자주 하는 실수를 정리합니다.

---

### 11.11.1 기준 없이 중복을 제거하는 실수

다음 코드는 전체 행 기준으로 중복을 제거합니다.

\`\`\`python
df.drop_duplicates()
\`\`\`

하지만 실무에서는 특정 컬럼 기준으로 중복을 판단해야 할 때가 많습니다.

\`\`\`python
df.drop_duplicates(subset=["order_id"])
\`\`\`

주문 데이터에서는 \`order_id\`, 고객 데이터에서는 \`customer_id\`나 \`email\`, 상품 데이터에서는 \`product_id\`처럼 데이터의 의미에 맞는 기준을 정해야 합니다.

---

### 11.11.2 정상 중복을 오류로 착각하는 실수

고객명이 여러 번 나온다고 해서 무조건 중복 오류는 아닙니다.

\`\`\`text
민수 고객이 1월 1일, 1월 5일, 1월 10일에 주문했다.
\`\`\`

이것은 정상적인 재구매 데이터입니다.

중복값을 찾을 때는 “같은 값이 여러 번 나왔다”가 아니라, “중복되면 안 되는 기준 컬럼이 중복되었는가”를 봐야 합니다.

---

### 11.11.3 이상값을 무조건 삭제하는 실수

이상값은 검토 대상이지 삭제 대상이 아닙니다.

다음 값이 있다고 해서 바로 삭제하면 안 됩니다.

\`\`\`text
주문 금액: 5,000,000원
수량: 100개
방문 횟수: 1,000회
\`\`\`

이 값들은 실제 VIP 고객이나 기업 고객의 데이터일 수 있습니다.

이상값을 발견하면 먼저 원본과 업무 맥락을 확인해야 합니다.

---

### 11.11.4 평균만 보고 판단하는 실수

이상값이 있으면 평균이 크게 흔들릴 수 있습니다.

따라서 평균만 보지 말고 중앙값, 최솟값, 최댓값, 사분위수도 함께 확인해야 합니다.

\`\`\`python
df["total_price"].describe()
\`\`\`

또는 다음처럼 평균과 중앙값을 함께 비교합니다.

\`\`\`python
df["total_price"].mean()
df["total_price"].median()
\`\`\`

평균과 중앙값이 크게 다르면 분포가 치우쳐 있거나 이상값이 있을 가능성이 있습니다.

---

### 11.11.5 처리 전후를 비교하지 않는 실수

중복값이나 이상값을 처리한 뒤에는 반드시 처리 전후를 비교해야 합니다.

\`\`\`python
before_count = len(df)
after_count = len(df_clean)

before_count, after_count
\`\`\`

평균도 비교할 수 있습니다.

\`\`\`python
df["total_price"].mean()
df_clean["total_price"].mean()
\`\`\`

처리 전후를 비교하지 않으면 데이터가 얼마나 바뀌었는지 알 수 없습니다.

---

### 11.11.6 처리 기준을 기록하지 않는 실수

중복값과 이상값 처리는 분석 결과에 큰 영향을 줍니다.  
따라서 어떤 기준으로 처리했는지 반드시 기록해야 합니다.

최소한 다음 내용은 남겨야 합니다.

\`\`\`text
- 중복 판단 기준 컬럼
- 중복 행 개수
- 중복 처리 방식
- 이상값 탐지 기준
- 이상값 후보 개수
- 이상값 제거 여부
- 제거하지 않았다면 어떻게 표시했는지
\`\`\`

---
`;export{e as default};