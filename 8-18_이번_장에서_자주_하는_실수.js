var e=`# 8장. 시계열 매출 분석

## 8.18 이번 장에서 자주 하는 실수

시계열 분석에서 자주 하는 실수를 정리합니다.

---

### 8.18.1 날짜 컬럼을 datetime으로 변환하지 않는 실수

문자열 날짜로는 안정적인 시계열 분석이 어렵습니다.

\`\`\`python
df["date"] = pd.to_datetime(df["date"], errors="coerce")
\`\`\`

변환 후 실패한 값도 확인해야 합니다.

\`\`\`python
df[df["date"].isna()]
\`\`\`

---

### 8.18.2 날짜 정렬 없이 분석하는 실수

시계열 분석은 시간 순서가 중요합니다.

\`\`\`python
df = df.sort_values("date")
\`\`\`

날짜 인덱스를 사용할 때도 정렬합니다.

\`\`\`python
df = df.set_index("date").sort_index()
\`\`\`

---

### 8.18.3 일별 데이터에서 주문 없는 날짜를 놓치는 실수

주문이 없는 날짜가 빠져 있으면 일별 추세가 왜곡될 수 있습니다.

\`resample("D")\`를 사용하면 주문이 없는 날짜도 포함할 수 있습니다.

---

### 8.18.4 월별 분석에서 월만 사용하는 실수

여러 연도 데이터가 있을 때 월 숫자만 사용하면 서로 다른 연도의 같은 월이 합쳐질 수 있습니다.

나쁜 예:

\`\`\`python
df.groupby(df["date"].dt.month)["sales"].sum()
\`\`\`

좋은 예:

\`\`\`python
df["year_month"] = df["date"].dt.to_period("M")
df.groupby("year_month")["sales"].sum()
\`\`\`

---

### 8.18.5 성장률만 보고 판단하는 실수

성장률은 기준값이 작을 때 크게 보일 수 있습니다.

따라서 성장률은 증감액과 함께 봐야 합니다.

\`\`\`python
monthly_sales["sales_diff"]
monthly_sales["sales_growth_rate"]
\`\`\`

---

### 8.18.6 이벤트 전후 비교를 인과관계로 해석하는 실수

이벤트 후 매출이 증가했다고 해서 이벤트가 원인이라고 단정할 수 없습니다.

다음 요인도 함께 확인해야 합니다.

\`\`\`text
요일 구성
다른 프로모션
광고비
계절성
상품 출시
가격 변경
외부 이슈
\`\`\`

정확한 인과 효과 분석은 A/B 테스트나 더 엄밀한 분석 설계가 필요합니다.

---

### 8.18.7 요일 패턴을 짧은 기간으로 일반화하는 실수

분석 기간이 짧으면 요일별 패턴은 우연일 수 있습니다.

요일 패턴을 판단하려면 충분히 긴 기간의 데이터가 필요합니다.

---
`;export{e as default};