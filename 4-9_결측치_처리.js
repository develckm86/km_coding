var e=`# 4장. 분석용 데이터마트 만들기

## 4.9 결측치 처리

결측치 처리는 컬럼의 의미에 따라 다르게 해야 합니다.

---

### 4.9.1 주문 데이터 결측치 확인

\`\`\`python
orders.isna().sum()
\`\`\`

\`coupon_amount\`에 결측치가 있습니다.

---

### 4.9.2 쿠폰 금액 결측치 처리

이번 실습에서는 쿠폰 금액이 비어 있으면 쿠폰 미사용으로 보고 0으로 대체합니다.

\`\`\`python
orders["coupon_amount"] = orders["coupon_amount"].fillna(0)
\`\`\`

처리 후 확인합니다.

\`\`\`python
orders["coupon_amount"].isna().sum()
\`\`\`

---

### 4.9.3 고객 정보 결측치 처리 기준

고객 데이터 자체에는 결측치가 없을 수 있지만, 주문 데이터와 결합하면 고객 정보가 없는 주문이 생길 수 있습니다.

그 경우 다음 기준을 사용합니다.

\`\`\`text
customer_name이 없으면 "고객정보없음"
region이 없으면 "미상"
grade가 없으면 "미상"
visit_count가 없으면 결측치 유지
\`\`\`

이 처리는 고객 데이터 결합 후 수행합니다.

---

### 4.9.4 상품 정보 결측치 처리 기준

상품 정보가 없는 주문은 단가와 카테고리를 알 수 없습니다.  
따라서 매출 분석에서는 제외합니다.

하지만 제외된 주문은 별도로 저장합니다.

\`\`\`text
상품 정보 없음 → 매출 분석 제외
\`\`\`

이 처리는 상품 데이터 결합 후 수행합니다.

---
`;export{e as default};