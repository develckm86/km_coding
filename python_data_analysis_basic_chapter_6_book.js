var e=`# 6장. 데이터 확인과 기본 탐색

5장에서는 CSV, Excel, JSON 파일을 pandas로 불러오고 저장하는 방법을 배웠다. 파일을 성공적으로 불러왔다고 해서 바로 분석을 시작하면 안 된다. 데이터 분석에서 가장 흔한 실수 중 하나는 **데이터가 어떤 모양인지 확인하지 않은 채 계산부터 시작하는 것**이다.

예를 들어 매출 데이터를 불러왔다고 생각해 보자. 겉으로는 정상적인 표처럼 보이지만 실제로는 다음과 같은 문제가 숨어 있을 수 있다.

\`\`\`text
- 컬럼명이 예상과 다르다.
- 행 개수가 생각보다 적거나 많다.
- 숫자여야 할 매출 컬럼이 문자열로 들어왔다.
- 날짜 컬럼이 날짜형이 아니라 문자열로 들어왔다.
- 일부 컬럼에 결측치가 많다.
- 고객 등급 값이 예상보다 다양하다.
- 같은 카테고리인데 표기 방식이 다르다.
\`\`\`

이런 문제를 확인하지 않고 평균이나 합계를 계산하면 분석 결과가 잘못될 수 있다. 데이터 분석의 첫 단계는 복잡한 모델을 만드는 것이 아니라, **데이터를 관찰하고 이해하는 것**이다.

이 장에서는 pandas DataFrame을 불러온 뒤 가장 먼저 확인해야 하는 기본 탐색 방법을 배운다. \`head()\`, \`tail()\`, \`shape\`, \`columns\`, \`dtypes\`, \`info()\`, \`describe()\`, \`unique()\`, \`nunique()\`, \`value_counts()\` 같은 기능을 사용해 데이터의 크기, 구조, 자료형, 요약 통계, 고유값을 확인하는 방법을 익힌다.

---

## 이 장에서 배울 내용

이 장을 마치면 다음 내용을 할 수 있어야 한다.

- 데이터 일부를 확인해 파일이 제대로 불러와졌는지 점검할 수 있다.
- DataFrame의 행 개수와 열 개수를 확인할 수 있다.
- 컬럼명과 인덱스를 확인할 수 있다.
- 각 컬럼의 자료형을 확인하고 의미를 해석할 수 있다.
- \`info()\`로 결측치와 메모리 사용 정보를 대략 확인할 수 있다.
- \`describe()\`로 수치형 데이터의 기본 통계를 확인할 수 있다.
- 범주형 데이터의 고유값과 빈도를 확인할 수 있다.
- 데이터 분석을 시작하기 전 기본 점검 체크리스트를 만들 수 있다.

---

# 6.1 데이터 일부 확인

데이터를 불러온 뒤 가장 먼저 할 일은 데이터의 일부를 눈으로 확인하는 것이다. 전체 데이터가 수천 행, 수만 행이라면 모든 행을 한 번에 볼 수 없다. 대신 앞부분, 뒷부분, 임의의 일부를 확인한다.

pandas에서는 다음 메서드를 자주 사용한다.

\`\`\`text
head()    앞부분 확인
tail()    뒷부분 확인
sample()  임의의 일부 확인
\`\`\`

---

## 6.1.1 예제 데이터 준비

이 장에서는 쇼핑몰 주문 데이터를 예제로 사용한다. 실제 파일을 불러오지 않아도 실습할 수 있도록 DataFrame을 직접 만들어보자.

\`\`\`python
import pandas as pd

orders = pd.DataFrame({
    "order_id": [1001, 1002, 1003, 1004, 1005, 1006, 1007, 1008],
    "customer": ["김민수", "이지영", "박철수", "최유진", "김민수", "정하늘", "오세훈", "이지영"],
    "region": ["서울", "부산", "서울", "대구", "서울", "부산", "제주", "부산"],
    "category": ["전자기기", "생활용품", "전자기기", "도서", "생활용품", "도서", "전자기기", "생활용품"],
    "quantity": [1, 3, 2, 5, 2, 1, 1, 4],
    "price": [120000, 15000, 80000, 12000, 20000, 18000, 300000, 15000],
    "order_date": [
        "2026-01-03", "2026-01-05", "2026-01-08", "2026-01-10",
        "2026-01-12", "2026-01-15", "2026-01-18", "2026-01-20"
    ],
    "payment": ["카드", "계좌이체", "카드", "카드", "현금", "계좌이체", "카드", "카드"]
})

orders
\`\`\`

이 데이터는 주문번호, 고객명, 지역, 상품 카테고리, 수량, 가격, 주문일, 결제수단으로 구성되어 있다.

---

## 6.1.2 \`head()\`로 앞부분 확인하기

\`head()\`는 DataFrame의 앞부분을 보여준다. 기본값은 앞에서 5행이다.

\`\`\`python
orders.head()
\`\`\`

앞에서 3행만 보고 싶다면 괄호 안에 숫자를 넣는다.

\`\`\`python
orders.head(3)
\`\`\`

\`head()\`는 데이터를 불러온 직후 가장 자주 사용하는 메서드다. 파일을 제대로 읽었는지, 컬럼명이 맞는지, 값이 예상한 형태인지 빠르게 확인할 수 있다.

### 확인할 점

\`\`\`text
- 컬럼명이 제대로 들어왔는가?
- 첫 번째 행이 데이터인가, 잘못 들어온 헤더인가?
- 한글이 깨지지 않았는가?
- 값들이 예상한 위치에 들어왔는가?
- 날짜나 숫자가 이상하게 보이지 않는가?
\`\`\`

---

## 6.1.3 \`tail()\`로 뒷부분 확인하기

\`tail()\`은 DataFrame의 뒷부분을 보여준다. 기본값은 뒤에서 5행이다.

\`\`\`python
orders.tail()
\`\`\`

뒤에서 2행만 확인할 수도 있다.

\`\`\`python
orders.tail(2)
\`\`\`

데이터 앞부분은 정상인데 뒷부분에 합계 행, 주석 행, 빈 행이 섞여 있는 경우가 있다. 특히 Excel이나 CSV 파일에서 사람이 직접 만든 표를 읽을 때 이런 문제가 자주 생긴다.

### 확인할 점

\`\`\`text
- 마지막 행까지 실제 데이터인가?
- 합계 행이나 설명 행이 섞여 있지 않은가?
- 빈 행이 데이터로 들어오지 않았는가?
- 파일 끝부분의 형식도 앞부분과 같은가?
\`\`\`

---

## 6.1.4 \`sample()\`로 임의의 행 확인하기

\`sample()\`은 임의의 행을 보여준다.

\`\`\`python
orders.sample(3)
\`\`\`

데이터가 클수록 앞부분만 보는 것은 충분하지 않다. 앞부분 5행은 정상이어도 중간이나 뒤쪽에 이상한 값이 있을 수 있다. 이럴 때 \`sample()\`로 무작위 행을 확인하면 데이터 전체의 느낌을 더 잘 파악할 수 있다.

매번 같은 결과를 보고 싶다면 \`random_state\`를 지정한다.

\`\`\`python
orders.sample(3, random_state=42)
\`\`\`

\`random_state\`는 무작위 결과를 재현 가능하게 만드는 값이다. 보고서나 수업 자료에서 같은 결과를 보여주고 싶을 때 유용하다.

---

# 6.2 데이터 크기 확인

데이터를 일부 확인한 뒤에는 전체 데이터의 크기를 확인해야 한다. 데이터 크기는 분석 방향과 처리 방법에 영향을 준다.

예를 들어 100행짜리 데이터와 1,000만 행짜리 데이터는 다루는 방식이 달라진다. 작은 데이터는 화면으로 직접 확인할 수 있지만, 큰 데이터는 요약 정보와 집계 결과를 중심으로 확인해야 한다.

---

## 6.2.1 \`shape\`로 행과 열 개수 확인하기

\`shape\`는 DataFrame의 크기를 알려준다.

\`\`\`python
orders.shape
\`\`\`

실행 결과는 다음과 같은 튜플 형태다.

\`\`\`text
(8, 8)
\`\`\`

앞의 숫자는 행 개수, 뒤의 숫자는 열 개수다.

\`\`\`text
(행 개수, 열 개수)
\`\`\`

따라서 \`orders.shape\`가 \`(8, 8)\`이라면 8행 8열짜리 데이터라는 뜻이다.

---

## 6.2.2 행 개수만 확인하기

행 개수만 확인하고 싶다면 \`len()\`을 사용할 수 있다.

\`\`\`python
len(orders)
\`\`\`

또는 \`shape[0]\`을 사용할 수도 있다.

\`\`\`python
orders.shape[0]
\`\`\`

실무에서는 데이터를 불러온 뒤 예상한 행 개수와 실제 행 개수가 맞는지 확인하는 것이 중요하다.

\`\`\`python
expected_rows = 8
actual_rows = len(orders)

print("예상 행 개수:", expected_rows)
print("실제 행 개수:", actual_rows)
\`\`\`

파일을 잘못 읽거나 필터링 조건이 잘못되면 행 개수가 예상과 달라질 수 있다.

---

## 6.2.3 열 개수만 확인하기

열 개수는 \`shape[1]\`로 확인한다.

\`\`\`python
orders.shape[1]
\`\`\`

데이터를 불러올 때 구분자나 헤더가 잘못 설정되면 열 개수가 예상과 크게 달라질 수 있다.

예를 들어 콤마로 구분된 CSV 파일을 잘못된 구분자로 읽으면 전체 데이터가 하나의 컬럼으로 들어올 수 있다. 이 경우 열 개수가 1로 나타난다.

---

## 6.2.4 데이터 크기 확인이 중요한 이유

데이터 크기를 확인하면 다음과 같은 문제를 빠르게 발견할 수 있다.

\`\`\`text
- 데이터가 비어 있는지 확인할 수 있다.
- 일부 행만 잘못 불러온 것은 아닌지 확인할 수 있다.
- 컬럼이 제대로 분리되었는지 확인할 수 있다.
- 분석 가능한 규모인지 판단할 수 있다.
- 이후 처리 시간과 메모리 사용량을 예상할 수 있다.
\`\`\`

---

# 6.3 컬럼 확인

DataFrame에서 컬럼은 분석할 변수에 해당한다. 고객 데이터라면 이름, 지역, 나이, 가입일 같은 컬럼이 있을 수 있고, 주문 데이터라면 주문번호, 상품명, 수량, 가격, 주문일 같은 컬럼이 있을 수 있다.

데이터 분석에서는 컬럼명을 정확히 아는 것이 중요하다. 컬럼명을 잘못 입력하면 에러가 발생하거나 엉뚱한 데이터를 선택할 수 있다.

---

## 6.3.1 \`columns\`로 컬럼명 확인하기

DataFrame의 컬럼명은 \`columns\`로 확인한다.

\`\`\`python
orders.columns
\`\`\`

출력 결과는 다음과 비슷하다.

\`\`\`text
Index(['order_id', 'customer', 'region', 'category', 'quantity', 'price', 'order_date', 'payment'], dtype='object')
\`\`\`

컬럼명만 리스트로 보고 싶다면 다음처럼 변환할 수 있다.

\`\`\`python
list(orders.columns)
\`\`\`

---

## 6.3.2 컬럼명 확인 시 볼 것

컬럼명을 확인할 때는 다음을 살펴본다.

\`\`\`text
- 예상한 컬럼이 모두 있는가?
- 컬럼명이 너무 길거나 불명확하지 않은가?
- 컬럼명에 공백이 들어가 있지 않은가?
- 한글 컬럼명과 영어 컬럼명이 섞여 있지 않은가?
- 대소문자 표기가 일관적인가?
- 같은 의미의 컬럼명이 여러 방식으로 쓰이지 않았는가?
\`\`\`

예를 들어 다음과 같은 컬럼명은 이후 분석에서 불편할 수 있다.

\`\`\`text
주문 번호
Order Date
 product price 
고객명
\`\`\`

공백, 대소문자, 언어가 섞이면 코드를 작성할 때 실수하기 쉽다. 컬럼명 정리는 9장에서 본격적으로 다룬다.

---

## 6.3.3 특정 컬럼이 있는지 확인하기

특정 컬럼이 존재하는지 확인하려면 \`in\`을 사용한다.

\`\`\`python
"price" in orders.columns
\`\`\`

결과는 \`True\` 또는 \`False\`다.

여러 컬럼이 모두 있는지 확인할 때는 다음처럼 작성할 수 있다.

\`\`\`python
required_columns = ["order_id", "customer", "quantity", "price"]

for column in required_columns:
    print(column, column in orders.columns)
\`\`\`

실무에서는 분석에 반드시 필요한 컬럼이 누락되어 있는지 먼저 확인해야 한다.

---

## 6.3.4 인덱스 확인하기

컬럼과 함께 인덱스도 확인할 수 있다.

\`\`\`python
orders.index
\`\`\`

기본 DataFrame은 보통 0부터 시작하는 정수 인덱스를 가진다.

\`\`\`text
RangeIndex(start=0, stop=8, step=1)
\`\`\`

인덱스는 각 행을 식별하는 이름 또는 번호다. 데이터 분석 기초 단계에서는 기본 인덱스를 그대로 사용하는 경우가 많다. 나중에 날짜를 인덱스로 쓰거나, 특정 ID를 인덱스로 설정하는 경우도 있다.

---

# 6.4 데이터 타입 확인

데이터를 분석할 때는 값의 모양뿐 아니라 **자료형**을 확인해야 한다. 숫자처럼 보이는 값이 실제로는 문자열일 수 있고, 날짜처럼 보이는 값이 실제로는 문자열일 수 있다.

자료형이 맞지 않으면 계산, 정렬, 날짜 처리, 집계에서 문제가 생긴다.

---

## 6.4.1 \`dtypes\`로 컬럼별 자료형 확인하기

\`dtypes\`는 각 컬럼의 자료형을 보여준다.

\`\`\`python
orders.dtypes
\`\`\`

출력 예시는 다음과 같다.

\`\`\`text
order_id       int64
customer      object
region        object
category      object
quantity       int64
price          int64
order_date    object
payment       object
dtype: object
\`\`\`

여기서 \`int64\`는 정수형, \`object\`는 주로 문자열 데이터를 의미한다. \`order_date\`는 날짜처럼 보이지만 현재는 \`object\`다. 즉, 아직 날짜형으로 변환되지 않은 상태다.

---

## 6.4.2 pandas에서 자주 보는 자료형

pandas에서 자주 만나는 자료형은 다음과 같다.

| 자료형 | 의미 | 예시 |
|---|---|---|
| \`int64\` | 정수형 | 주문 수량, 나이, 개수 |
| \`float64\` | 실수형 | 평균, 비율, 결측치가 섞인 숫자 |
| \`object\` | 주로 문자열 | 이름, 지역, 카테고리 |
| \`bool\` | 참/거짓 | 구매 여부, 활성 여부 |
| \`datetime64\` | 날짜와 시간 | 주문일, 가입일 |
| \`category\` | 범주형 | 등급, 성별, 지역 |

기초 과정에서는 \`int64\`, \`float64\`, \`object\`, \`datetime64\`를 우선 이해하면 충분하다.

---

## 6.4.3 자료형이 잘못 들어오는 경우

CSV 파일을 불러오면 숫자 컬럼이 문자열로 들어오는 경우가 있다.

예를 들어 금액이 다음처럼 저장되어 있으면 숫자가 아니라 문자열로 인식될 수 있다.

\`\`\`text
"120,000"
"80,000"
"300,000"
\`\`\`

쉼표가 포함되어 있기 때문이다. 이런 경우 숫자 계산을 하려면 쉼표를 제거하고 숫자형으로 변환해야 한다.

날짜도 마찬가지다.

\`\`\`text
2026-01-03
2026/01/03
2026년 1월 3일
\`\`\`

이런 값은 사람이 보기에는 날짜지만 pandas가 자동으로 날짜형으로 인식하지 못할 수 있다. 날짜형 변환은 13장에서 자세히 다룬다.

---

## 6.4.4 자료형 확인이 중요한 이유

자료형을 확인하면 다음 문제를 미리 발견할 수 있다.

\`\`\`text
- 숫자 계산이 가능한 컬럼인지 확인할 수 있다.
- 날짜 분석이 가능한 컬럼인지 확인할 수 있다.
- 범주형 데이터인지 수치형 데이터인지 구분할 수 있다.
- 결측치 때문에 정수형이 실수형으로 바뀐 경우를 알 수 있다.
- 데이터 변환이 필요한 컬럼을 찾을 수 있다.
\`\`\`

---

# 6.5 요약 정보 확인

\`head()\`, \`shape\`, \`columns\`, \`dtypes\`를 각각 확인할 수도 있지만, 데이터 전체 요약을 한 번에 보고 싶을 때는 \`info()\`를 사용한다.

---

## 6.5.1 \`info()\`로 DataFrame 요약 확인하기

\`\`\`python
orders.info()
\`\`\`

\`info()\`는 대략 다음 정보를 보여준다.

\`\`\`text
- DataFrame의 행 개수
- 컬럼 개수
- 각 컬럼 이름
- 각 컬럼의 결측치가 아닌 값 개수
- 각 컬럼의 자료형
- 메모리 사용량
\`\`\`

출력 예시는 다음과 비슷하다.

\`\`\`text
<class 'pandas.core.frame.DataFrame'>
RangeIndex: 8 entries, 0 to 7
Data columns (total 8 columns):
 #   Column      Non-Null Count  Dtype 
---  ------      --------------  ----- 
 0   order_id    8 non-null      int64 
 1   customer    8 non-null      object
 2   region      8 non-null      object
 3   category    8 non-null      object
 4   quantity    8 non-null      int64 
 5   price       8 non-null      int64 
 6   order_date  8 non-null      object
 7   payment     8 non-null      object
dtypes: int64(3), object(5)
memory usage: ...
\`\`\`

\`info()\`는 데이터 분석을 시작하기 전에 거의 항상 실행하는 기본 점검 도구다.

---

## 6.5.2 Non-Null Count 이해하기

\`Non-Null Count\`는 비어 있지 않은 값의 개수를 의미한다.

예를 들어 전체 행이 8개인데 어떤 컬럼의 \`Non-Null Count\`가 6이라면, 그 컬럼에는 결측치가 2개 있다는 뜻이다.

\`\`\`text
전체 행 개수: 8
비어 있지 않은 값: 6
결측치: 2
\`\`\`

결측치 처리는 10장에서 자세히 다루지만, 이 장에서는 \`info()\`를 통해 결측치가 있는지 감지하는 데 집중한다.

---

## 6.5.3 \`info()\`를 볼 때 확인할 것

\`\`\`text
- 전체 행 개수가 예상과 맞는가?
- 컬럼 개수가 예상과 맞는가?
- 각 컬럼의 Non-Null Count가 전체 행 개수와 같은가?
- 숫자 컬럼이 숫자형으로 들어왔는가?
- 날짜 컬럼이 아직 문자열인지 확인했는가?
- 문자열 컬럼이 object로 들어왔는가?
\`\`\`

\`info()\`는 단순히 출력하고 끝내는 도구가 아니라, 데이터 상태를 읽는 도구다.

---

# 6.6 요약 통계 확인

수치형 데이터는 전체 분포를 숫자로 요약할 수 있다. pandas에서는 \`describe()\`를 사용해 기본 통계량을 확인한다.

---

## 6.6.1 \`describe()\` 기본 사용법

\`\`\`python
orders.describe()
\`\`\`

수치형 컬럼에 대해 다음과 같은 통계값을 보여준다.

| 항목 | 의미 |
|---|---|
| \`count\` | 값의 개수 |
| \`mean\` | 평균 |
| \`std\` | 표준편차 |
| \`min\` | 최솟값 |
| \`25%\` | 1사분위수 |
| \`50%\` | 중앙값 |
| \`75%\` | 3사분위수 |
| \`max\` | 최댓값 |

예를 들어 \`quantity\`와 \`price\` 컬럼의 평균, 최솟값, 최댓값을 빠르게 확인할 수 있다.

---

## 6.6.2 \`describe()\` 결과 해석하기

\`orders.describe()\` 결과에서 \`price\` 컬럼을 살펴보자.

\`\`\`text
count       8.000000
mean    72500.000000
std     ...
min     12000.000000
25%     ...
50%     ...
75%     ...
max    300000.000000
\`\`\`

이 결과를 통해 다음과 같은 질문을 할 수 있다.

\`\`\`text
- 주문 가격의 평균은 얼마인가?
- 가장 낮은 가격과 가장 높은 가격은 얼마인가?
- 평균과 중앙값의 차이가 큰가?
- 특정 값이 너무 크거나 작은 것은 아닌가?
\`\`\`

\`describe()\`는 분석의 결론을 바로 알려주는 함수가 아니다. 대신 데이터를 이해하기 위한 첫 번째 힌트를 제공한다.

---

## 6.6.3 특정 컬럼의 요약 통계 확인하기

특정 컬럼만 보고 싶다면 컬럼을 선택한 뒤 \`describe()\`를 사용할 수 있다.

\`\`\`python
orders["price"].describe()
\`\`\`

여러 수치형 컬럼만 선택해서 확인할 수도 있다.

\`\`\`python
orders[["quantity", "price"]].describe()
\`\`\`

---

## 6.6.4 범주형 데이터의 요약 정보 확인하기

\`describe()\`는 기본적으로 수치형 컬럼을 대상으로 한다. 범주형 데이터까지 포함해서 보고 싶다면 \`include="object"\` 또는 \`include="all"\`을 사용할 수 있다.

\`\`\`python
orders.describe(include="object")
\`\`\`

또는 전체 자료형을 포함한다.

\`\`\`python
orders.describe(include="all")
\`\`\`

범주형 데이터에 대해서는 다음과 같은 정보가 나온다.

| 항목 | 의미 |
|---|---|
| \`count\` | 비어 있지 않은 값 개수 |
| \`unique\` | 고유값 개수 |
| \`top\` | 가장 많이 등장한 값 |
| \`freq\` | 가장 많이 등장한 값의 빈도 |

예를 들어 \`region\` 컬럼에서 가장 많이 등장한 지역과 등장 횟수를 확인할 수 있다.

---

## 6.6.5 요약 통계로 알 수 있는 것과 없는 것

\`describe()\`는 데이터를 빠르게 요약하지만 모든 것을 알려주지는 않는다.

알 수 있는 것:

\`\`\`text
- 값의 개수
- 평균과 중앙값
- 최솟값과 최댓값
- 데이터가 대략 어떤 범위에 있는지
- 수치형 컬럼의 기본 분포
\`\`\`

알 수 없는 것:

\`\`\`text
- 왜 그런 값이 나왔는지
- 값이 올바른지
- 범주별 차이가 있는지
- 시간에 따른 변화가 있는지
- 두 변수 사이의 관계가 있는지
\`\`\`

따라서 \`describe()\`는 분석의 끝이 아니라 시작이다.

---

# 6.7 고유값 확인

범주형 데이터에서는 어떤 값들이 들어 있는지 확인하는 것이 중요하다. 예를 들어 지역 컬럼에는 서울, 부산, 대구 등이 들어 있을 수 있고, 결제수단 컬럼에는 카드, 현금, 계좌이체가 들어 있을 수 있다.

이런 값의 종류를 **고유값**이라고 한다.

---

## 6.7.1 \`unique()\`로 고유값 목록 확인하기

\`\`\`python
orders["region"].unique()
\`\`\`

결과는 다음과 비슷하다.

\`\`\`text
array(['서울', '부산', '대구', '제주'], dtype=object)
\`\`\`

\`unique()\`는 해당 컬럼에 어떤 값들이 있는지 목록으로 보여준다.

---

## 6.7.2 \`nunique()\`로 고유값 개수 확인하기

고유값의 개수만 알고 싶다면 \`nunique()\`를 사용한다.

\`\`\`python
orders["region"].nunique()
\`\`\`

여러 컬럼의 고유값 개수를 한 번에 확인할 수도 있다.

\`\`\`python
orders.nunique()
\`\`\`

\`nunique()\`는 데이터의 다양성을 파악할 때 유용하다. 예를 들어 고객 ID 컬럼의 고유값 개수는 고객 수를 파악하는 데 도움이 된다.

---

## 6.7.3 \`value_counts()\`로 값의 빈도 확인하기

\`value_counts()\`는 각 값이 몇 번 등장했는지 세어준다.

\`\`\`python
orders["region"].value_counts()
\`\`\`

출력 예시는 다음과 비슷하다.

\`\`\`text
region
부산    3
서울    3
대구    1
제주    1
Name: count, dtype: int64
\`\`\`

결제수단별 주문 건수도 확인할 수 있다.

\`\`\`python
orders["payment"].value_counts()
\`\`\`

---

## 6.7.4 비율로 확인하기

빈도 대신 비율을 보고 싶다면 \`normalize=True\`를 사용한다.

\`\`\`python
orders["payment"].value_counts(normalize=True)
\`\`\`

결과는 각 값의 비율로 나타난다.

\`\`\`text
카드      0.625
계좌이체   0.250
현금      0.125
Name: proportion, dtype: float64
\`\`\`

비율을 퍼센트로 보고 싶다면 100을 곱하면 된다.

\`\`\`python
orders["payment"].value_counts(normalize=True) * 100
\`\`\`

---

## 6.7.5 고유값 확인이 중요한 이유

고유값을 확인하면 다음 문제를 찾을 수 있다.

\`\`\`text
- 같은 값이 다르게 표기된 경우
- 오타가 있는 경우
- 예상하지 못한 범주가 있는 경우
- 범주 수가 너무 많은 경우
- 결측치나 빈 문자열이 섞여 있는 경우
\`\`\`

예를 들어 지역 컬럼에 다음 값들이 들어 있다면 문제가 있을 수 있다.

\`\`\`text
서울
서울시
seoul
SEOUL
\`\`\`

사람에게는 모두 서울처럼 보이지만 컴퓨터는 서로 다른 값으로 처리한다. 이런 문제는 12장 문자열 데이터 처리에서 정리하게 된다.

---

# 6.8 기본 탐색 체크리스트

데이터를 불러온 뒤에는 일정한 순서로 확인하는 습관을 들이는 것이 좋다. 매번 즉흥적으로 확인하면 중요한 문제를 놓치기 쉽다.

다음은 데이터 분석 초반에 사용할 수 있는 기본 체크리스트다.

\`\`\`python
# 1. 앞부분 확인
orders.head()

# 2. 뒷부분 확인
orders.tail()

# 3. 데이터 크기 확인
orders.shape

# 4. 컬럼명 확인
orders.columns

# 5. 자료형 확인
orders.dtypes

# 6. 전체 요약 정보 확인
orders.info()

# 7. 수치형 요약 통계 확인
orders.describe()

# 8. 범주형 값 빈도 확인
orders["region"].value_counts()
\`\`\`

이 순서는 절대적인 규칙은 아니지만, 초보자가 데이터 탐색을 빠뜨리지 않도록 도와준다.

---

## 6.8.1 실무용 기본 탐색 함수 만들기

자주 반복하는 확인 작업은 함수로 만들 수 있다.

\`\`\`python
def inspect_dataframe(df):
    print("[앞부분]")
    print(df.head())
    print()

    print("[크기]")
    print(df.shape)
    print()

    print("[컬럼]")
    print(df.columns)
    print()

    print("[자료형]")
    print(df.dtypes)
    print()

    print("[요약 정보]")
    df.info()
\`\`\`

사용 방법은 다음과 같다.

\`\`\`python
inspect_dataframe(orders)
\`\`\`

기초 과정에서는 이런 함수를 꼭 완벽하게 만들 필요는 없다. 중요한 것은 데이터 확인 작업을 일정한 절차로 반복하는 습관이다.

---

# 6.9 실무 예제: 주문 데이터 기본 탐색

이번에는 주문 데이터에 총 주문 금액 컬럼을 추가한 뒤 기본 탐색을 해보자.

\`\`\`python
orders["total_amount"] = orders["quantity"] * orders["price"]

orders.head()
\`\`\`

---

## 6.9.1 데이터 크기 확인

\`\`\`python
orders.shape
\`\`\`

이제 컬럼이 하나 추가되었으므로 결과는 다음과 같다.

\`\`\`text
(8, 9)
\`\`\`

8행 9열 데이터가 되었다.

---

## 6.9.2 자료형 확인

\`\`\`python
orders.dtypes
\`\`\`

\`total_amount\`는 수량과 가격을 곱해서 만든 값이므로 정수형으로 들어간다.

\`\`\`text
total_amount    int64
\`\`\`

---

## 6.9.3 수치형 요약 통계 확인

\`\`\`python
orders[["quantity", "price", "total_amount"]].describe()
\`\`\`

이 결과를 보면 수량, 단가, 총 주문 금액의 평균, 최솟값, 최댓값을 확인할 수 있다.

분석 질문으로 바꾸면 다음과 같다.

\`\`\`text
- 평균 주문 수량은 얼마인가?
- 가장 비싼 상품 가격은 얼마인가?
- 총 주문 금액이 가장 큰 주문은 어느 정도인가?
\`\`\`

---

## 6.9.4 범주형 데이터 확인

지역별 주문 건수를 확인한다.

\`\`\`python
orders["region"].value_counts()
\`\`\`

카테고리별 주문 건수도 확인한다.

\`\`\`python
orders["category"].value_counts()
\`\`\`

결제수단별 비율을 확인한다.

\`\`\`python
orders["payment"].value_counts(normalize=True) * 100
\`\`\`

이런 결과는 아직 최종 분석은 아니지만, 데이터의 기본 분포를 이해하는 데 도움이 된다.

---

## 6.9.5 탐색 결과를 문장으로 정리하기

데이터 탐색은 코드를 실행하는 것으로 끝나지 않는다. 결과를 짧은 문장으로 정리하는 습관이 중요하다.

예를 들어 다음처럼 정리할 수 있다.

\`\`\`text
주문 데이터는 8행 9열로 구성되어 있다.
주문번호, 고객명, 지역, 카테고리, 수량, 가격, 주문일, 결제수단, 총 주문 금액 컬럼이 있다.
수량과 가격, 총 주문 금액은 정수형이며, 주문일은 아직 문자열로 저장되어 있다.
지역은 서울, 부산, 대구, 제주로 구성되어 있고, 결제수단은 카드, 계좌이체, 현금이 있다.
\`\`\`

이처럼 데이터 상태를 말로 설명할 수 있어야 이후 분석 방향을 잡기 쉽다.

---

# 6.10 데이터 기본 탐색에서 자주 하는 실수

## 6.10.1 \`head()\`만 보고 전체를 안다고 생각하기

앞부분 5행이 정상이라고 해서 전체 데이터가 정상이라는 뜻은 아니다. \`tail()\`, \`sample()\`, \`info()\`, \`describe()\`를 함께 확인해야 한다.

---

## 6.10.2 숫자처럼 보이면 숫자형이라고 생각하기

화면에 \`120000\`처럼 보여도 실제 자료형이 문자열일 수 있다. 반드시 \`dtypes\`나 \`info()\`로 자료형을 확인해야 한다.

---

## 6.10.3 날짜처럼 보이면 날짜형이라고 생각하기

\`2026-01-03\`처럼 보여도 실제로는 문자열일 수 있다. 날짜 분석을 하려면 날짜형으로 변환해야 한다.

---

## 6.10.4 결측치 확인을 건너뛰기

결측치는 분석 결과에 큰 영향을 줄 수 있다. \`info()\`를 통해 비어 있지 않은 값의 개수를 확인하고, 필요하면 10장에서 다루는 결측치 처리 방법을 적용해야 한다.

---

## 6.10.5 범주형 값의 표기 차이를 무시하기

\`서울\`, \`서울시\`, \`seoul\`은 사람에게는 비슷해 보여도 pandas에서는 서로 다른 값이다. \`unique()\`나 \`value_counts()\`로 실제 값의 종류를 확인해야 한다.

---

# 6.11 미니 실습

다음 고객 데이터를 사용해 기본 탐색을 연습해보자.

\`\`\`python
customers = pd.DataFrame({
    "customer_id": [1, 2, 3, 4, 5, 6],
    "name": ["김민수", "이지영", "박철수", "최유진", "정하늘", "오세훈"],
    "age": [28, 34, 45, 29, 38, 41],
    "region": ["서울", "부산", "서울", "대구", "부산", "제주"],
    "grade": ["VIP", "일반", "VIP", "일반", "일반", "VIP"],
    "join_date": ["2025-01-10", "2025-02-15", "2025-03-20", "2025-04-01", "2025-05-12", "2025-06-30"]
})
\`\`\`

다음 작업을 수행해보자.

\`\`\`text
1. 앞에서 3행을 확인한다.
2. 뒤에서 2행을 확인한다.
3. 데이터의 행과 열 개수를 확인한다.
4. 컬럼명을 확인한다.
5. 컬럼별 자료형을 확인한다.
6. 나이 컬럼의 요약 통계를 확인한다.
7. 지역별 고객 수를 확인한다.
8. 등급별 고객 비율을 확인한다.
\`\`\`

예시 코드:

\`\`\`python
customers.head(3)
customers.tail(2)
customers.shape
customers.columns
customers.dtypes
customers["age"].describe()
customers["region"].value_counts()
customers["grade"].value_counts(normalize=True) * 100
\`\`\`

---

# 6장 핵심 정리

- 데이터를 불러온 뒤에는 바로 분석하지 말고 먼저 구조를 확인해야 한다.
- \`head()\`는 앞부분, \`tail()\`은 뒷부분, \`sample()\`은 임의의 일부를 보여준다.
- \`shape\`는 DataFrame의 행 개수와 열 개수를 알려준다.
- \`columns\`는 컬럼명을 확인할 때 사용한다.
- \`dtypes\`는 각 컬럼의 자료형을 확인할 때 사용한다.
- \`info()\`는 행 개수, 컬럼, 결측치가 아닌 값 개수, 자료형, 메모리 정보를 요약해서 보여준다.
- \`describe()\`는 수치형 데이터의 기본 통계량을 보여준다.
- \`unique()\`는 고유값 목록을 보여준다.
- \`nunique()\`는 고유값 개수를 보여준다.
- \`value_counts()\`는 각 값의 등장 횟수를 보여준다.
- 데이터 탐색 결과는 코드로만 확인하지 말고 문장으로 정리하는 습관이 중요하다.

---

# 연습문제

## 문제 1

DataFrame의 앞부분 5행을 확인하는 메서드를 쓰시오.

---

## 문제 2

DataFrame의 행 개수와 열 개수를 확인할 때 사용하는 속성을 쓰시오.

---

## 문제 3

다음 코드의 실행 결과가 의미하는 것을 설명하시오.

\`\`\`python
df.shape
\`\`\`

실행 결과:

\`\`\`text
(1200, 15)
\`\`\`

---

## 문제 4

DataFrame의 컬럼명을 확인하는 코드를 작성하시오.

DataFrame 이름은 \`sales\`라고 가정한다.

---

## 문제 5

DataFrame의 각 컬럼 자료형을 확인하는 코드를 작성하시오.

DataFrame 이름은 \`sales\`라고 가정한다.

---

## 문제 6

\`info()\`를 사용했을 때 확인할 수 있는 내용으로 적절한 것을 모두 고르시오.

\`\`\`text
A. 컬럼 이름
B. 컬럼별 자료형
C. 비어 있지 않은 값의 개수
D. 모든 행의 전체 값
\`\`\`

---

## 문제 7

다음 중 \`describe()\`의 기본 결과에 포함되는 값이 아닌 것은?

\`\`\`text
A. 평균
B. 최솟값
C. 표준편차
D. 파일 경로
\`\`\`

---

## 문제 8

\`region\` 컬럼에 어떤 값들이 있는지 고유값 목록을 확인하는 코드를 작성하시오.

DataFrame 이름은 \`customers\`라고 가정한다.

---

## 문제 9

\`grade\` 컬럼의 값별 등장 횟수를 확인하는 코드를 작성하시오.

DataFrame 이름은 \`customers\`라고 가정한다.

---

## 문제 10

다음 코드에서 \`normalize=True\`의 의미를 설명하시오.

\`\`\`python
customers["grade"].value_counts(normalize=True)
\`\`\`

---

## 문제 11

다음 주문 데이터에서 총 주문 금액 컬럼 \`total_amount\`를 추가하고, 수치형 컬럼의 요약 통계를 확인하는 코드를 작성하시오.

\`\`\`python
orders = pd.DataFrame({
    "quantity": [1, 3, 2],
    "price": [10000, 20000, 15000]
})
\`\`\`

---

## 문제 12

데이터를 불러온 직후 기본 탐색 순서로 적절한 것을 5개 이상 쓰시오.

---

# 정답 및 해설

## 문제 1 정답

\`\`\`python
df.head()
\`\`\`

해설:

\`head()\`는 DataFrame의 앞부분을 확인하는 메서드다. 기본값은 앞에서 5행이다.

---

## 문제 2 정답

\`\`\`python
df.shape
\`\`\`

해설:

\`shape\`는 \`(행 개수, 열 개수)\` 형태의 튜플을 반환한다.

---

## 문제 3 정답

1200행 15열짜리 DataFrame이라는 의미다.

해설:

\`shape\`의 첫 번째 값은 행 개수, 두 번째 값은 열 개수다.

---

## 문제 4 정답

\`\`\`python
sales.columns
\`\`\`

해설:

\`columns\`는 DataFrame의 컬럼명을 확인하는 속성이다.

---

## 문제 5 정답

\`\`\`python
sales.dtypes
\`\`\`

해설:

\`dtypes\`는 각 컬럼의 자료형을 보여준다.

---

## 문제 6 정답

\`\`\`text
A, B, C
\`\`\`

해설:

\`info()\`는 컬럼명, 컬럼별 자료형, 비어 있지 않은 값의 개수 등을 보여준다. 모든 행의 전체 값을 출력하는 도구는 아니다.

---

## 문제 7 정답

\`\`\`text
D. 파일 경로
\`\`\`

해설:

\`describe()\`는 count, mean, std, min, 25%, 50%, 75%, max 같은 통계량을 보여준다. 파일 경로는 보여주지 않는다.

---

## 문제 8 정답

\`\`\`python
customers["region"].unique()
\`\`\`

해설:

\`unique()\`는 해당 컬럼에 들어 있는 고유값 목록을 반환한다.

---

## 문제 9 정답

\`\`\`python
customers["grade"].value_counts()
\`\`\`

해설:

\`value_counts()\`는 각 값이 몇 번 등장했는지 세어준다.

---

## 문제 10 정답

\`normalize=True\`는 등장 횟수 대신 각 값의 비율을 반환하라는 의미다.

해설:

예를 들어 VIP가 전체의 절반이라면 결과가 \`0.5\`로 나타난다. 퍼센트로 보고 싶으면 100을 곱하면 된다.

---

## 문제 11 정답

\`\`\`python
import pandas as pd

orders = pd.DataFrame({
    "quantity": [1, 3, 2],
    "price": [10000, 20000, 15000]
})

orders["total_amount"] = orders["quantity"] * orders["price"]
orders.describe()
\`\`\`

해설:

수량과 가격을 곱해 총 주문 금액을 만든 뒤 \`describe()\`로 수치형 컬럼의 요약 통계를 확인한다.

---

## 문제 12 정답 예시

\`\`\`text
1. head()로 앞부분 확인
2. tail()로 뒷부분 확인
3. shape로 행과 열 개수 확인
4. columns로 컬럼명 확인
5. dtypes로 자료형 확인
6. info()로 전체 요약 정보 확인
7. describe()로 수치형 요약 통계 확인
8. value_counts()로 범주형 값 빈도 확인
\`\`\`

해설:

기본 탐색의 목적은 데이터가 예상한 구조로 들어왔는지, 분석 전에 수정해야 할 문제가 있는지 확인하는 것이다.

---

# 6장 마무리

이번 장에서는 데이터를 불러온 뒤 가장 먼저 확인해야 하는 기본 탐색 방법을 배웠다. 데이터 분석은 계산식이나 그래프를 만드는 일에서 시작하지 않는다. 먼저 데이터가 어떤 구조를 가지고 있는지, 컬럼은 무엇인지, 자료형은 적절한지, 값의 범위는 어떤지, 범주형 값은 어떻게 구성되어 있는지를 확인해야 한다.

기본 탐색은 단순한 준비 작업이 아니라 분석의 방향을 정하는 중요한 과정이다. 데이터의 크기와 자료형을 보면 어떤 전처리가 필요한지 알 수 있고, 요약 통계와 값의 빈도를 보면 어떤 질문을 던질 수 있는지 보이기 시작한다.

다음 장에서는 기본 탐색을 마친 데이터에서 필요한 행과 열을 선택하고, 조건에 맞는 데이터만 필터링하는 방법을 배운다. 분석에 필요한 데이터를 정확히 골라내는 능력은 pandas 활용의 핵심이다.

---

# 참고 문서

- pandas API Reference: \`DataFrame.head\`  
  https://pandas.pydata.org/docs/reference/api/pandas.DataFrame.head.html
- pandas API Reference: \`DataFrame.tail\`  
  https://pandas.pydata.org/docs/reference/api/pandas.DataFrame.tail.html
- pandas API Reference: \`DataFrame.info\`  
  https://pandas.pydata.org/docs/reference/api/pandas.DataFrame.info.html
- pandas API Reference: \`DataFrame.describe\`  
  https://pandas.pydata.org/docs/reference/api/pandas.DataFrame.describe.html
- pandas API Reference: \`Series.value_counts\`  
  https://pandas.pydata.org/docs/reference/api/pandas.Series.value_counts.html
- pandas API Reference: \`DataFrame.nunique\`  
  https://pandas.pydata.org/docs/reference/api/pandas.DataFrame.nunique.html
`;export{e as default};