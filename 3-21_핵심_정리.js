var e=`# 3장. 데이터 품질 진단 리포트 만들기

## 3.21 핵심 정리

이번 장에서는 데이터 품질 진단 리포트를 만드는 방법을 실습했습니다.

핵심 내용은 다음과 같습니다.

\`\`\`text
데이터 품질 진단은 분석 전에 반드시 수행해야 한다.
결측치, 중복값, key 매칭 실패, 날짜 오류, 이상값 후보를 체계적으로 확인해야 한다.
주문 ID, 고객 ID, 상품 ID 같은 key 컬럼은 중복 여부가 중요하다.
주문 데이터의 customer_id와 product_id가 기준표에 존재하는지 확인해야 한다.
날짜 변환 실패 값은 월별 분석, 코호트 분석, 리텐션 분석에 영향을 준다.
수량, 쿠폰 금액, 단가 같은 수치형 컬럼은 업무 규칙에 맞는지 확인해야 한다.
이상값 후보는 무조건 삭제하지 않고 원본 확인 후 처리해야 한다.
품질 진단 결과는 표와 Markdown 보고서로 문서화해야 한다.
\`\`\`

이번 장의 결과물은 다음 장에서 사용됩니다.

\`\`\`text
결측치 요약표
중복값 요약표
key 매칭 실패 목록
날짜 오류 목록
이상값 후보 목록
품질 이슈 목록
데이터 품질 진단 리포트
\`\`\`

다음 장에서는 이 진단 결과를 바탕으로 실제로 데이터를 정리하고, 분석용 데이터마트를 만듭니다.

---
`;export{e as default};