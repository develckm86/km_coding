var e=`# 19장. 대용량 데이터 처리 실습

## 19.0 들어가며

18장에서는 SQL 기반 분석을 실습했습니다.

DuckDB를 사용해 CSV와 pandas DataFrame을 SQL로 조회하고, 다음 작업을 수행했습니다.

\`\`\`text
SELECT, WHERE, ORDER BY
GROUP BY 집계
CASE WHEN 조건 분류
JOIN 데이터 결합
CTE로 쿼리 구조화
윈도우 함수
pandas 결과와 SQL 결과 비교
SQL 쿼리 파일 저장
\`\`\`

이번 19장에서는 **대용량 데이터 처리**를 실습합니다.

지금까지는 대부분 수십 행, 수백 행, 많아야 수천 행 수준의 예제 데이터를 사용했습니다.  
하지만 실무에서는 데이터가 훨씬 큽니다.

예를 들어 다음과 같은 데이터를 만날 수 있습니다.

\`\`\`text
주문 로그 500만 행
이벤트 로그 1억 행
광고 클릭 로그 3천만 행
앱 방문 로그 2억 행
상품 조회 로그 5억 행
\`\`\`

이런 데이터를 다룰 때는 단순히 다음처럼 읽는 방식이 문제가 될 수 있습니다.

\`\`\`python
df = pd.read_csv("large_orders.csv")
\`\`\`

데이터가 크면 다음 문제가 발생합니다.

\`\`\`text
파일 읽는 시간이 오래 걸린다.
메모리가 부족하다.
노트북 커널이 죽는다.
groupby가 너무 느리다.
중간 결과가 너무 커진다.
엑셀로 열 수 없다.
분석 결과 저장 시간이 오래 걸린다.
\`\`\`

대용량 데이터 처리에서 중요한 것은 무조건 빠른 컴퓨터를 쓰는 것이 아닙니다.  
먼저 데이터를 효율적으로 읽고, 줄이고, 저장하고, 집계하는 습관이 필요합니다.

이번 장에서는 다음 원칙을 중심으로 실습합니다.

\`\`\`text
필요한 컬럼만 읽는다.
자료형을 최적화한다.
문자열보다 category를 활용한다.
큰 CSV는 chunk 단위로 처리한다.
중간 결과는 Parquet으로 저장한다.
전체 원본을 pandas로 모두 읽지 않고 DuckDB로 집계한다.
pandas 결과와 DuckDB 결과를 비교해 검증한다.
메모리 사용량과 처리 시간을 기록한다.
\`\`\`

이번 장의 핵심은 “대용량 데이터를 무조건 pandas에 다 올리지 않는다”는 것입니다.

분석가는 다음 판단을 할 수 있어야 합니다.

\`\`\`text
이 데이터는 한 번에 읽어도 되는가?
필요한 컬럼만 읽을 수 있는가?
자료형을 줄이면 메모리가 얼마나 줄어드는가?
chunk로 처리해야 하는가?
DuckDB로 먼저 집계하는 것이 좋은가?
CSV보다 Parquet이 좋은가?
최종 분석에 필요한 결과만 저장하고 있는가?
\`\`\`

이번 장을 마치면 큰 데이터를 더 안전하게 다루는 기본기를 갖출 수 있습니다.

---
`;export{e as default};