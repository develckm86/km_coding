var e=`# 3장. 조건문과 반복문

## 이 장에서 배우는 것

2장에서는 변수, 문자열, 수 데이터, 연산자, 불리언 타입, 비교 연산자, 기본 입출력을 배웠다. 이제부터는 프로그램의 흐름을 직접 제어하는 방법을 배운다. 지금까지의 코드는 위에서 아래로 한 줄씩 실행되었다. 하지만 실제 프로그램은 항상 같은 순서로만 동작하지 않는다.

예를 들어 사용자의 나이가 19세 이상이면 “성인입니다”를 출력하고, 그렇지 않으면 “미성년자입니다”를 출력해야 할 수 있다. 주문 금액이 일정 금액 이상이면 배송비를 무료로 처리하고, 그렇지 않으면 배송비를 추가해야 할 수도 있다. 이처럼 조건에 따라 다른 코드를 실행할 때 조건문을 사용한다.

또한 프로그램에서는 같은 작업을 여러 번 반복해야 하는 경우가 많다. 고객 100명의 이름을 하나씩 출력하거나, 1부터 100까지의 합계를 구하거나, 여러 상품 가격을 차례대로 계산해야 할 수 있다. 이런 반복 작업을 사람이 직접 여러 줄로 작성하면 비효율적이고 실수하기 쉽다. 이때 반복문을 사용한다.

이 장에서는 다음 내용을 배운다.

- 조건문이 필요한 이유
- \`if\`, \`else\`, \`elif\` 문법
- 조건식과 들여쓰기
- 중첩 조건문
- \`for\` 반복문
- \`range()\`를 사용한 반복
- \`while\` 반복문
- \`break\`와 \`continue\`
- 반복문에서 자주 사용하는 실무 패턴
- 리스트 컴프리헨션 기초

이 장을 마치면 조건에 따라 다른 코드를 실행하고, 같은 작업을 여러 번 반복하는 프로그램을 작성할 수 있다. 조건문과 반복문은 이후 자료구조, 함수, 파일 처리, 데이터 분석을 배울 때 계속 사용되는 핵심 문법이다.

---

# 3.1 조건문

## 조건문이 필요한 이유

프로그램은 항상 같은 일을 하지 않는다. 상황에 따라 다른 판단을 해야 한다.

예를 들어 쇼핑몰에서 주문 금액이 50,000원 이상이면 무료배송을 적용한다고 생각해 보자. 주문 금액이 50,000원 이상일 때와 그렇지 않을 때의 처리 방식은 다르다.

사람의 말로 표현하면 다음과 같다.

\`\`\`text
주문 금액이 50,000원 이상이면 배송비는 0원이다.
그렇지 않으면 배송비는 3,000원이다.
\`\`\`

파이썬에서는 이런 판단을 조건문으로 표현한다.

\`\`\`python
order_price = 60000

if order_price >= 50000:
    delivery_fee = 0
else:
    delivery_fee = 3000

print(delivery_fee)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
0
\`\`\`

\`order_price\`가 \`50000\` 이상이므로 \`if\` 조건이 참이 된다. 따라서 \`delivery_fee = 0\`이 실행된다.

조건문은 실무 코드에서 매우 자주 사용된다. 입력값이 올바른지 검사하거나, 사용자 권한을 확인하거나, 데이터 상태에 따라 다른 작업을 수행할 때 조건문이 필요하다.

## \`if\` 문

가장 기본적인 조건문은 \`if\` 문이다.

\`\`\`python
if 조건식:
    조건식이 참일 때 실행할 코드
\`\`\`

\`if\` 뒤에는 조건식이 온다. 조건식은 결과가 \`True\` 또는 \`False\`로 판단될 수 있는 식이다. 조건식 뒤에는 콜론 \`:\`을 붙인다. 그리고 조건이 참일 때 실행할 코드는 들여쓰기해서 작성한다.

\`\`\`python
age = 20

if age >= 19:
    print("성인입니다.")
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
성인입니다.
\`\`\`

\`age >= 19\`의 결과는 \`True\`이다. 따라서 들여쓰기된 코드가 실행된다.

조건이 거짓이면 들여쓰기된 코드는 실행되지 않는다.

\`\`\`python
age = 15

if age >= 19:
    print("성인입니다.")

print("프로그램 종료")
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
프로그램 종료
\`\`\`

\`age >= 19\`가 거짓이므로 \`print("성인입니다.")\`는 실행되지 않는다. 하지만 조건문 밖에 있는 \`print("프로그램 종료")\`는 조건과 상관없이 실행된다.

## 들여쓰기

파이썬에서 들여쓰기는 매우 중요하다. 다른 언어에서는 중괄호 \`{}\`를 사용해 코드 블록을 구분하는 경우가 많지만, 파이썬은 들여쓰기로 코드 블록을 구분한다.

\`\`\`python
age = 20

if age >= 19:
    print("성인입니다.")
    print("입장할 수 있습니다.")
\`\`\`

위 코드에서 두 개의 \`print()\`는 모두 \`if\` 조건이 참일 때 실행된다. 두 줄이 같은 깊이로 들여쓰기되어 있기 때문이다.

실행 결과는 다음과 같다.

\`\`\`text
성인입니다.
입장할 수 있습니다.
\`\`\`

반면 아래 코드에서는 두 번째 \`print()\`가 조건문 밖에 있다.

\`\`\`python
age = 15

if age >= 19:
    print("성인입니다.")

print("입장 여부 확인 완료")
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
입장 여부 확인 완료
\`\`\`

\`print("입장 여부 확인 완료")\`는 들여쓰기되어 있지 않으므로 조건문에 포함되지 않는다. 따라서 조건이 참이든 거짓이든 항상 실행된다.

들여쓰기를 잘못하면 에러가 발생하거나, 코드가 의도와 다르게 실행된다.

\`\`\`python
age = 20

if age >= 19:
print("성인입니다.")
\`\`\`

위 코드는 \`if\` 문 다음 줄에 들여쓰기가 없기 때문에 에러가 발생한다.

조건문을 작성할 때는 다음 세 가지를 항상 확인해야 한다.

1. \`if\` 뒤에 조건식이 있는가?
2. 조건식 뒤에 콜론 \`:\`이 있는가?
3. 실행할 코드가 들여쓰기되어 있는가?

## 조건식 작성하기

조건식은 참 또는 거짓으로 판단되는 식이다. 2장에서 배운 비교 연산자와 논리 연산자를 조건식에서 자주 사용한다.

\`\`\`python
price = 30000

if price >= 20000:
    print("구매 가능 금액입니다.")
\`\`\`

비교 연산자는 다음과 같다.

| 연산자 | 의미 | 예시 |
|---|---|---|
| \`==\` | 같다 | \`age == 20\` |
| \`!=\` | 같지 않다 | \`status != "취소"\` |
| \`>\` | 크다 | \`price > 10000\` |
| \`<\` | 작다 | \`count < 5\` |
| \`>=\` | 크거나 같다 | \`age >= 19\` |
| \`<=\` | 작거나 같다 | \`score <= 100\` |

조건이 여러 개일 때는 논리 연산자를 사용할 수 있다.

\`\`\`python
age = 20
has_ticket = True

if age >= 19 and has_ticket:
    print("입장할 수 있습니다.")
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
입장할 수 있습니다.
\`\`\`

\`and\`는 양쪽 조건이 모두 참일 때 전체 조건이 참이 된다.

\`\`\`python
is_admin = False
is_manager = True

if is_admin or is_manager:
    print("관리자 페이지에 접근할 수 있습니다.")
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
관리자 페이지에 접근할 수 있습니다.
\`\`\`

\`or\`는 둘 중 하나라도 참이면 전체 조건이 참이 된다.

\`not\`은 참과 거짓을 반대로 바꾼다.

\`\`\`python
is_login = False

if not is_login:
    print("로그인이 필요합니다.")
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
로그인이 필요합니다.
\`\`\`

## \`else\` 문

\`if\` 문은 조건이 참일 때만 코드를 실행한다. 조건이 거짓일 때 실행할 코드를 작성하려면 \`else\`를 사용한다.

\`\`\`python
if 조건식:
    조건식이 참일 때 실행할 코드
else:
    조건식이 거짓일 때 실행할 코드
\`\`\`

예를 들어 나이에 따라 성인과 미성년자를 구분해 보자.

\`\`\`python
age = 16

if age >= 19:
    print("성인입니다.")
else:
    print("미성년자입니다.")
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
미성년자입니다.
\`\`\`

\`age >= 19\`가 거짓이므로 \`else\` 블록이 실행된다.

\`else\`는 단독으로 사용할 수 없다. 반드시 앞에 \`if\`가 있어야 한다.

\`\`\`python
score = 80

else:
    print("조건이 거짓입니다.")
\`\`\`

위 코드는 잘못된 코드이다. \`else\`는 어떤 조건이 거짓일 때 실행할지를 알아야 하므로, 항상 \`if\`와 함께 사용해야 한다.

## \`elif\` 문

조건이 두 가지보다 많을 때는 \`elif\`를 사용한다. \`elif\`는 “그렇지 않고 만약”이라는 의미로 이해하면 된다.

\`\`\`python
if 조건식1:
    조건식1이 참일 때 실행할 코드
elif 조건식2:
    조건식2가 참일 때 실행할 코드
else:
    모든 조건이 거짓일 때 실행할 코드
\`\`\`

예를 들어 점수에 따라 등급을 출력해 보자.

\`\`\`python
score = 85

if score >= 90:
    print("A등급")
elif score >= 80:
    print("B등급")
elif score >= 70:
    print("C등급")
else:
    print("D등급")
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
B등급
\`\`\`

\`score\`는 85이다. 첫 번째 조건 \`score >= 90\`은 거짓이다. 두 번째 조건 \`score >= 80\`은 참이다. 따라서 \`B등급\`이 출력되고, 그 아래의 조건은 더 이상 검사하지 않는다.

## 조건 검사 순서

\`if\`, \`elif\`, \`else\` 구조에서는 위에서 아래로 조건을 검사한다. 중간에 참인 조건을 만나면 그 블록을 실행하고 조건문 전체를 빠져나온다.

조건의 순서는 매우 중요하다. 다음 코드를 보자.

\`\`\`python
score = 95

if score >= 70:
    print("C등급 이상")
elif score >= 80:
    print("B등급 이상")
elif score >= 90:
    print("A등급")
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
C등급 이상
\`\`\`

\`score\`가 95이므로 세 조건 모두 참이 될 수 있다. 하지만 첫 번째 조건 \`score >= 70\`이 먼저 참이 되므로 그 블록만 실행된다. 따라서 원하는 결과와 다를 수 있다.

점수 등급처럼 범위가 겹치는 조건은 큰 범위부터 작은 범위로 검사해야 한다.

\`\`\`python
score = 95

if score >= 90:
    print("A등급")
elif score >= 80:
    print("B등급")
elif score >= 70:
    print("C등급")
else:
    print("D등급")
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
A등급
\`\`\`

조건문을 작성할 때는 “어떤 조건을 먼저 검사해야 하는가?”를 생각해야 한다.

## 중첩 조건문

조건문 안에 또 다른 조건문을 작성할 수 있다. 이것을 중첩 조건문이라고 한다.

예를 들어 로그인을 한 사용자 중에서 관리자만 관리자 페이지에 접근할 수 있다고 해 보자.

\`\`\`python
is_login = True
is_admin = False

if is_login:
    if is_admin:
        print("관리자 페이지에 접근할 수 있습니다.")
    else:
        print("관리자 권한이 없습니다.")
else:
    print("로그인이 필요합니다.")
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
관리자 권한이 없습니다.
\`\`\`

먼저 \`is_login\`이 참인지 확인한다. 로그인한 상태이므로 안쪽 조건문으로 들어간다. 그다음 \`is_admin\`이 참인지 확인한다. 관리자 권한이 없으므로 안쪽 \`else\`가 실행된다.

중첩 조건문은 필요한 경우 유용하지만, 너무 깊어지면 코드를 읽기 어렵다.

\`\`\`python
if condition1:
    if condition2:
        if condition3:
            print("처리")
\`\`\`

이런 코드가 많아지면 어떤 조건에서 어떤 코드가 실행되는지 파악하기 어렵다. 가능하면 논리 연산자나 함수 분리를 사용해 조건을 단순하게 만드는 것이 좋다. 함수 분리는 5장에서 자세히 배운다.

## 중첩 조건문 줄이기

중첩 조건문은 논리 연산자를 사용해 줄일 수 있는 경우가 있다.

아래 코드는 로그인 상태와 관리자 권한을 중첩 조건문으로 검사한다.

\`\`\`python
is_login = True
is_admin = True

if is_login:
    if is_admin:
        print("관리자 페이지에 접근할 수 있습니다.")
\`\`\`

위 코드는 \`and\`를 사용해 다음처럼 작성할 수 있다.

\`\`\`python
is_login = True
is_admin = True

if is_login and is_admin:
    print("관리자 페이지에 접근할 수 있습니다.")
\`\`\`

두 조건이 모두 참이어야 실행되는 상황이라면 \`and\`를 사용할 수 있다.

반대로 둘 중 하나만 참이어도 되는 상황이라면 \`or\`를 사용할 수 있다.

\`\`\`python
is_admin = False
is_manager = True

if is_admin or is_manager:
    print("관리 메뉴를 볼 수 있습니다.")
\`\`\`

조건문은 무조건 짧게 쓰는 것이 좋은 것은 아니다. 중요한 것은 읽기 쉬워야 한다는 점이다. 조건이 너무 복잡하면 여러 줄로 나누어 작성하는 편이 더 좋을 수 있다.

## 참과 거짓으로 평가되는 값

조건식에는 꼭 비교 연산자만 들어가는 것은 아니다. 파이썬에서는 값 자체도 참 또는 거짓으로 평가될 수 있다.

다음 값들은 거짓으로 평가된다.

- 빈 문자열 \`""\`
- 숫자 \`0\`
- \`None\`
- 빈 리스트 \`[]\`
- 빈 딕셔너리 \`{}\`
- 빈 집합 \`set()\`

아직 리스트, 딕셔너리, 집합은 4장에서 자세히 배운다. 지금은 “비어 있는 데이터는 보통 거짓으로 평가된다” 정도로 이해하면 충분하다.

빈 문자열을 검사하는 예를 보자.

\`\`\`python
name = ""

if name:
    print("이름이 입력되었습니다.")
else:
    print("이름이 비어 있습니다.")
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
이름이 비어 있습니다.
\`\`\`

빈 문자열은 거짓으로 평가되므로 \`else\` 블록이 실행된다.

반대로 문자열에 값이 있으면 참으로 평가된다.

\`\`\`python
name = "홍길동"

if name:
    print("이름이 입력되었습니다.")
else:
    print("이름이 비어 있습니다.")
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
이름이 입력되었습니다.
\`\`\`

실무에서는 입력값이 비어 있는지 검사할 때 이런 방식을 자주 사용한다.

\`\`\`python
email = input("이메일을 입력하세요: ")

if not email:
    print("이메일은 필수 입력값입니다.")
else:
    print("입력한 이메일:", email)
\`\`\`

\`not email\`은 \`email\`이 비어 있을 때 참이 된다.

## 조건문에서 자주 하는 실수

조건문을 처음 배울 때 자주 하는 실수가 있다.

첫 번째는 \`=\`와 \`==\`를 혼동하는 것이다.

\`\`\`python
age = 20

if age = 20:
    print("20살입니다.")
\`\`\`

위 코드는 잘못된 코드이다. \`=\`는 값을 저장할 때 사용한다. 값을 비교할 때는 \`==\`를 사용해야 한다.

\`\`\`python
age = 20

if age == 20:
    print("20살입니다.")
\`\`\`

두 번째는 콜론 \`:\`을 빠뜨리는 것이다.

\`\`\`python
age = 20

if age >= 19
    print("성인입니다.")
\`\`\`

\`if\` 조건식 뒤에는 반드시 콜론을 붙여야 한다.

\`\`\`python
age = 20

if age >= 19:
    print("성인입니다.")
\`\`\`

세 번째는 들여쓰기를 잘못하는 것이다.

\`\`\`python
age = 20

if age >= 19:
print("성인입니다.")
\`\`\`

\`if\` 문 아래에서 실행할 코드는 들여쓰기해야 한다.

\`\`\`python
age = 20

if age >= 19:
    print("성인입니다.")
\`\`\`

네 번째는 \`True\`, \`False\`의 대소문자를 잘못 쓰는 것이다.

\`\`\`python
is_login = true
\`\`\`

파이썬에서는 \`True\`와 \`False\`의 첫 글자를 반드시 대문자로 써야 한다.

\`\`\`python
is_login = True
\`\`\`

## 조건문 실무 예제 1: 무료배송 판단하기

주문 금액이 50,000원 이상이면 무료배송이고, 그렇지 않으면 배송비 3,000원을 적용해 보자.

\`\`\`python
order_price = 42000

if order_price >= 50000:
    delivery_fee = 0
else:
    delivery_fee = 3000

total_price = order_price + delivery_fee

print("주문 금액:", order_price)
print("배송비:", delivery_fee)
print("최종 결제 금액:", total_price)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
주문 금액: 42000
배송비: 3000
최종 결제 금액: 45000
\`\`\`

## 조건문 실무 예제 2: 회원 등급별 할인율 적용하기

회원 등급에 따라 할인율을 다르게 적용해 보자.

\`\`\`python
grade = "VIP"
price = 100000

if grade == "VIP":
    discount_rate = 0.2
elif grade == "GOLD":
    discount_rate = 0.1
elif grade == "SILVER":
    discount_rate = 0.05
else:
    discount_rate = 0

discount_price = price * discount_rate
final_price = price - discount_price

print("회원 등급:", grade)
print("상품 금액:", price)
print("할인 금액:", discount_price)
print("최종 금액:", final_price)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
회원 등급: VIP
상품 금액: 100000
할인 금액: 20000.0
최종 금액: 80000.0
\`\`\`

아직 실수와 정수의 출력 형태가 섞여 보일 수 있다. 나중에 숫자 포매팅을 사용하면 금액을 더 보기 좋게 표현할 수 있다.

## 조건문 실무 예제 3: 주문 상태에 따라 메시지 출력하기

주문 상태에 따라 사용자에게 보여줄 메시지를 다르게 출력해 보자.

\`\`\`python
order_status = "배송중"

if order_status == "결제완료":
    message = "결제가 완료되었습니다."
elif order_status == "상품준비중":
    message = "상품을 준비하고 있습니다."
elif order_status == "배송중":
    message = "상품이 배송 중입니다."
elif order_status == "배송완료":
    message = "배송이 완료되었습니다."
else:
    message = "주문 상태를 확인할 수 없습니다."

print(message)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
상품이 배송 중입니다.
\`\`\`

조건문은 사용자의 상태, 주문 상태, 데이터 상태에 따라 적절한 처리를 선택할 때 자주 사용된다.

---

# 3.2 반복문

## 반복문이 필요한 이유

반복문은 같은 작업을 여러 번 실행할 때 사용한다.

예를 들어 “안녕하세요”를 세 번 출력한다고 해 보자. 반복문을 사용하지 않으면 다음처럼 작성할 수 있다.

\`\`\`python
print("안녕하세요")
print("안녕하세요")
print("안녕하세요")
\`\`\`

세 번 정도는 직접 작성해도 큰 문제가 없어 보인다. 하지만 100번 출력해야 한다면 어떨까? 1,000개의 데이터를 처리해야 한다면 어떨까? 같은 코드를 계속 복사해서 붙여 넣는 방식은 비효율적이고, 수정할 때도 어렵다.

반복문을 사용하면 다음처럼 작성할 수 있다.

\`\`\`python
for number in range(3):
    print("안녕하세요")
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
안녕하세요
안녕하세요
안녕하세요
\`\`\`

반복문은 다음과 같은 상황에서 사용된다.

- 정해진 횟수만큼 같은 작업을 반복할 때
- 여러 데이터를 하나씩 처리할 때
- 조건이 만족될 때까지 계속 실행할 때
- 파일의 여러 줄을 차례대로 읽을 때
- 여러 고객, 여러 상품, 여러 주문을 처리할 때

## \`for\` 문

\`for\` 문은 여러 값을 차례대로 꺼내면서 반복할 때 사용한다.

기본 구조는 다음과 같다.

\`\`\`python
for 변수 in 반복할_대상:
    반복해서 실행할 코드
\`\`\`

가장 간단한 예로 \`range()\`와 함께 사용해 보자.

\`\`\`python
for number in range(5):
    print(number)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
0
1
2
3
4
\`\`\`

\`range(5)\`는 0부터 4까지의 값을 차례대로 만들어 낸다. \`for\` 문은 그 값을 하나씩 \`number\`에 넣고, 들여쓰기된 코드를 실행한다.

실행 흐름은 다음과 같다.

\`\`\`text
첫 번째 반복: number = 0
두 번째 반복: number = 1
세 번째 반복: number = 2
네 번째 반복: number = 3
다섯 번째 반복: number = 4
\`\`\`

\`for\` 문에서 사용하는 변수 이름은 마음대로 정할 수 있다.

\`\`\`python
for i in range(3):
    print(i)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
0
1
2
\`\`\`

반복 횟수를 나타내는 변수는 관습적으로 \`i\`를 사용하기도 한다. 하지만 실무에서는 가능하면 의미 있는 이름을 사용하는 것이 좋다.

\`\`\`python
for count in range(3):
    print("반복 중입니다.")
\`\`\`

위 코드에서는 반복 변수 \`count\`를 실제로 사용하지 않지만, 반복 횟수만큼 코드를 실행하기 위해 \`for\` 문을 사용한다.

## \`range()\`

\`range()\`는 일정한 범위의 정수를 만들어 주는 기능이다. 반복문에서 매우 자주 사용된다.

\`range()\`는 크게 세 가지 방식으로 사용할 수 있다.

\`\`\`python
range(끝값)
range(시작값, 끝값)
range(시작값, 끝값, 증가값)
\`\`\`

첫 번째 방식은 \`0\`부터 끝값 바로 전까지 값을 만든다.

\`\`\`python
for number in range(5):
    print(number)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
0
1
2
3
4
\`\`\`

\`range(5)\`는 0, 1, 2, 3, 4를 만든다. 끝값인 5는 포함되지 않는다.

두 번째 방식은 시작값부터 끝값 바로 전까지 값을 만든다.

\`\`\`python
for number in range(1, 6):
    print(number)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
1
2
3
4
5
\`\`\`

\`range(1, 6)\`은 1부터 5까지 값을 만든다.

세 번째 방식은 증가값을 지정한다.

\`\`\`python
for number in range(2, 11, 2):
    print(number)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
2
4
6
8
10
\`\`\`

\`range(2, 11, 2)\`는 2부터 시작해서 2씩 증가하며 10까지 값을 만든다.

감소하는 반복도 가능하다.

\`\`\`python
for number in range(5, 0, -1):
    print(number)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
5
4
3
2
1
\`\`\`

\`range()\`에서 끝값은 포함되지 않는다는 점을 꼭 기억해야 한다.

## 문자열 반복하기

문자열은 여러 문자가 순서대로 이어진 데이터이다. 따라서 \`for\` 문으로 문자열을 반복하면 문자 하나씩 꺼낼 수 있다.

\`\`\`python
text = "Python"

for char in text:
    print(char)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
P
y
t
h
o
n
\`\`\`

\`char\`는 character, 즉 문자를 의미하는 이름으로 자주 사용된다.

문자열 반복은 특정 문자가 포함되어 있는지 직접 검사하거나, 문자열에서 숫자만 세는 작업 등에 활용할 수 있다.

\`\`\`python
text = "A1B2C3"
count = 0

for char in text:
    if char.isdigit():
        count += 1

print("숫자 개수:", count)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
숫자 개수: 3
\`\`\`

\`isdigit()\`은 문자열이 숫자로만 이루어져 있는지 확인하는 함수이다. 문자 하나에도 사용할 수 있다.

## 여러 데이터 반복하기

여러 값을 한 번에 묶어 둔 데이터도 반복할 수 있다. 아래 코드에서는 대괄호 \`[]\` 안에 여러 상품명을 넣었다.

\`\`\`python
products = ["키보드", "마우스", "모니터"]

for product in products:
    print(product)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
키보드
마우스
모니터
\`\`\`

대괄호를 사용해 여러 값을 묶은 자료형을 리스트라고 한다. 리스트는 4장에서 자세히 배운다. 이 장에서는 “여러 값을 순서대로 담아 둔 데이터” 정도로만 이해하면 된다.

반복문은 리스트와 함께 사용할 때 매우 강력하다. 여러 상품, 여러 고객, 여러 파일명을 하나씩 처리할 수 있기 때문이다.

\`\`\`python
file_names = ["report.xlsx", "sales.csv", "memo.txt"]

for file_name in file_names:
    print("처리할 파일:", file_name)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
처리할 파일: report.xlsx
처리할 파일: sales.csv
처리할 파일: memo.txt
\`\`\`

## \`while\` 문

\`while\` 문은 조건이 참인 동안 반복한다.

기본 구조는 다음과 같다.

\`\`\`python
while 조건식:
    조건식이 참인 동안 반복할 코드
\`\`\`

예를 들어 1부터 5까지 출력해 보자.

\`\`\`python
count = 1

while count <= 5:
    print(count)
    count += 1
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
1
2
3
4
5
\`\`\`

실행 흐름은 다음과 같다.

1. \`count\`는 1이다.
2. \`count <= 5\`가 참이므로 반복문 안으로 들어간다.
3. \`count\`를 출력한다.
4. \`count += 1\`로 값을 1 증가시킨다.
5. 다시 조건을 검사한다.
6. \`count\`가 6이 되면 \`count <= 5\`가 거짓이므로 반복을 멈춘다.

\`while\` 문에서는 반복이 언젠가 끝나도록 조건을 바꾸는 코드가 필요하다. 위 예제에서는 \`count += 1\`이 그 역할을 한다.

## 무한 반복

\`while\` 문을 사용할 때 가장 조심해야 할 것은 무한 반복이다. 무한 반복은 반복문이 끝나지 않고 계속 실행되는 상황이다.

\`\`\`python
count = 1

while count <= 5:
    print(count)
\`\`\`

위 코드는 \`count\` 값이 변하지 않는다. 처음에 \`count\`는 1이고, 계속 1이다. 따라서 \`count <= 5\`는 항상 참이므로 반복문이 끝나지 않는다.

올바르게 작성하려면 반복문 안에서 \`count\`를 증가시켜야 한다.

\`\`\`python
count = 1

while count <= 5:
    print(count)
    count += 1
\`\`\`

무한 반복이 항상 나쁜 것은 아니다. 사용자에게 계속 입력을 받다가 특정 조건에서 종료하는 프로그램에서는 의도적으로 무한 반복을 사용하기도 한다.

\`\`\`python
while True:
    command = input("명령을 입력하세요. 종료하려면 q를 입력하세요: ")

    if command == "q":
        print("프로그램을 종료합니다.")
        break

    print("입력한 명령:", command)
\`\`\`

이 코드는 사용자가 \`q\`를 입력할 때까지 계속 실행된다. 이런 구조에서는 \`break\`가 반복을 멈추는 역할을 한다.

## \`break\`

\`break\`는 반복문을 즉시 종료할 때 사용한다.

\`\`\`python
for number in range(1, 11):
    if number == 5:
        break
    print(number)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
1
2
3
4
\`\`\`

\`number\`가 5가 되는 순간 \`break\`가 실행된다. 반복문은 즉시 종료되고, 5는 출력되지 않는다.

\`break\`는 원하는 값을 찾으면 더 이상 반복할 필요가 없을 때 자주 사용한다.

\`\`\`python
names = ["민수", "지영", "철수", "영희"]
target = "철수"

for name in names:
    if name == target:
        print("찾았습니다:", name)
        break
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
찾았습니다: 철수
\`\`\`

\`철수\`를 찾은 뒤에는 더 이상 반복할 필요가 없으므로 \`break\`로 반복을 종료한다.

## \`continue\`

\`continue\`는 현재 반복을 건너뛰고 다음 반복으로 넘어갈 때 사용한다.

\`\`\`python
for number in range(1, 6):
    if number == 3:
        continue
    print(number)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
1
2
4
5
\`\`\`

\`number\`가 3일 때 \`continue\`가 실행된다. 그러면 그 아래의 \`print(number)\`는 실행되지 않고 다음 반복으로 넘어간다.

\`continue\`는 특정 조건의 데이터만 제외하고 처리할 때 유용하다.

\`\`\`python
scores = [90, -1, 80, 75, -1, 100]

for score in scores:
    if score == -1:
        continue
    print("점수:", score)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
점수: 90
점수: 80
점수: 75
점수: 100
\`\`\`

여기서 \`-1\`은 아직 입력되지 않은 점수라고 가정했다. \`continue\`를 사용해 \`-1\`은 출력하지 않고 건너뛴다.

## \`for\` 문과 \`while\` 문의 차이

\`for\` 문과 \`while\` 문은 모두 반복문이지만 사용하는 상황이 다르다.

\`for\` 문은 반복할 대상이나 반복 횟수가 비교적 명확할 때 사용한다.

\`\`\`python
for number in range(10):
    print(number)
\`\`\`

또는 여러 데이터를 하나씩 처리할 때 사용한다.

\`\`\`python
products = ["키보드", "마우스", "모니터"]

for product in products:
    print(product)
\`\`\`

\`while\` 문은 조건이 만족되는 동안 반복해야 할 때 사용한다.

\`\`\`python
password = ""

while password != "1234":
    password = input("비밀번호를 입력하세요: ")

print("로그인 성공")
\`\`\`

반복 횟수가 정해져 있으면 보통 \`for\` 문이 더 적합하다. 반복 종료 조건이 상황에 따라 달라지면 \`while\` 문이 더 적합하다.

| 구분 | 주로 사용하는 상황 |
|---|---|
| \`for\` 문 | 정해진 횟수 반복, 여러 데이터 순회 |
| \`while\` 문 | 특정 조건이 만족될 때까지 반복 |

## 반복문 실무 예제 1: 여러 상품 가격 출력하기

\`\`\`python
prices = [12000, 35000, 28000]

for price in prices:
    print("상품 가격:", price)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
상품 가격: 12000
상품 가격: 35000
상품 가격: 28000
\`\`\`

## 반복문 실무 예제 2: 1부터 100까지의 합계 구하기

\`\`\`python
total = 0

for number in range(1, 101):
    total += number

print("합계:", total)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
합계: 5050
\`\`\`

\`total\`은 합계를 누적하기 위한 변수이다. 반복문을 돌면서 \`number\`를 계속 더한다.

## 반복문 실무 예제 3: 비밀번호 입력 기회 제한하기

비밀번호를 최대 3번까지 입력받는 프로그램을 만들어 보자.

\`\`\`python
correct_password = "1234"

for attempt in range(1, 4):
    password = input("비밀번호를 입력하세요: ")

    if password == correct_password:
        print("로그인 성공")
        break

    print("비밀번호가 틀렸습니다.")

    if attempt == 3:
        print("입력 횟수를 초과했습니다.")
\`\`\`

이 코드는 최대 3번 반복한다. 사용자가 올바른 비밀번호를 입력하면 \`break\`로 반복을 종료한다.

---

# 3.3 반복문 실무 패턴

반복문은 단순히 같은 문장을 여러 번 출력하는 데서 끝나지 않는다. 실무에서는 반복문을 사용해 합계를 구하고, 개수를 세고, 조건에 맞는 데이터를 찾고, 필요한 데이터만 추출한다.

이 절에서는 반복문에서 자주 사용하는 패턴을 정리한다. 아직 리스트를 자세히 배우지는 않았지만, 여러 값을 대괄호로 묶은 데이터라고 생각하고 예제를 따라오면 된다. 리스트 자체는 4장에서 자세히 배운다.

## 패턴 1: 누적 합계 구하기

여러 숫자의 합계를 구할 때는 합계를 저장할 변수를 먼저 만든다. 보통 초기값은 0으로 둔다.

\`\`\`python
prices = [10000, 20000, 15000]
total = 0

for price in prices:
    total += price

print("총 금액:", total)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
총 금액: 45000
\`\`\`

실행 흐름은 다음과 같다.

\`\`\`text
처음 total = 0
첫 번째 반복: total = 0 + 10000
두 번째 반복: total = 10000 + 20000
세 번째 반복: total = 30000 + 15000
최종 total = 45000
\`\`\`

이 패턴은 매출 합계, 포인트 합계, 주문 수량 합계처럼 여러 값을 더할 때 사용한다.

### 평균 구하기

합계를 구한 뒤 개수로 나누면 평균을 구할 수 있다.

\`\`\`python
scores = [80, 90, 70, 100]
total = 0

for score in scores:
    total += score

average = total / len(scores)

print("합계:", total)
print("평균:", average)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
합계: 340
평균: 85.0
\`\`\`

\`len(scores)\`는 데이터의 개수를 구한다. 이 예제에서는 점수가 4개이므로 \`len(scores)\`는 4이다.

## 패턴 2: 조건에 맞는 개수 세기

특정 조건에 맞는 데이터가 몇 개인지 세려면 개수를 저장할 변수를 만든다. 보통 초기값은 0으로 둔다.

\`\`\`python
scores = [90, 60, 75, 40, 100]
pass_count = 0

for score in scores:
    if score >= 70:
        pass_count += 1

print("통과한 사람 수:", pass_count)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
통과한 사람 수: 3
\`\`\`

점수가 70점 이상인 경우에만 \`pass_count\`를 1씩 증가시킨다.

이 패턴은 다음과 같은 상황에서 자주 사용된다.

- 특정 등급 이상의 고객 수 세기
- 완료된 주문 개수 세기
- 재고가 있는 상품 개수 세기
- 오류가 발생한 로그 개수 세기

## 패턴 3: 최댓값 찾기

여러 숫자 중 가장 큰 값을 찾는 방법을 알아보자. 가장 단순한 방법은 첫 번째 값을 기준값으로 잡고, 나머지 값과 비교하면서 더 큰 값이 나오면 기준값을 바꾸는 방식이다.

\`\`\`python
prices = [12000, 35000, 28000, 50000, 9000]
max_price = prices[0]

for price in prices:
    if price > max_price:
        max_price = price

print("가장 비싼 가격:", max_price)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
가장 비싼 가격: 50000
\`\`\`

\`prices[0]\`은 리스트의 첫 번째 값을 의미한다. 리스트 인덱싱은 4장에서 자세히 배우지만, 여기서는 첫 번째 값을 기준값으로 잡기 위해 사용했다.

실행 흐름은 다음과 같다.

\`\`\`text
처음 max_price = 12000
35000이 12000보다 크므로 max_price = 35000
28000은 35000보다 작으므로 그대로 유지
50000이 35000보다 크므로 max_price = 50000
9000은 50000보다 작으므로 그대로 유지
\`\`\`

물론 파이썬에는 \`max()\`라는 내장 함수가 있다.

\`\`\`python
prices = [12000, 35000, 28000, 50000, 9000]
print(max(prices))
\`\`\`

하지만 반복문을 배우는 단계에서는 내부적으로 어떤 흐름으로 최댓값을 찾는지 이해하는 것이 중요하다.

## 패턴 4: 최솟값 찾기

최솟값도 비슷한 방식으로 찾는다.

\`\`\`python
prices = [12000, 35000, 28000, 50000, 9000]
min_price = prices[0]

for price in prices:
    if price < min_price:
        min_price = price

print("가장 저렴한 가격:", min_price)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
가장 저렴한 가격: 9000
\`\`\`

최솟값을 찾을 때는 현재 기준값보다 더 작은 값이 나오면 기준값을 바꾼다.

## 패턴 5: 조건에 맞는 값만 모으기

여러 데이터 중에서 조건에 맞는 값만 따로 모아야 할 때가 있다. 예를 들어 30,000원 이상인 상품 가격만 모아 보자.

\`\`\`python
prices = [12000, 35000, 28000, 50000, 9000]
expensive_prices = []

for price in prices:
    if price >= 30000:
        expensive_prices.append(price)

print(expensive_prices)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
[35000, 50000]
\`\`\`

\`expensive_prices = []\`는 비어 있는 리스트를 만든다. \`append()\`는 리스트에 값을 추가하는 기능이다. 리스트와 \`append()\`는 4장에서 자세히 배우지만, 여기서는 “조건에 맞는 값을 모아 둔다”는 흐름을 이해하면 된다.

이 패턴은 필터링이라고도 한다. 실무에서는 매우 자주 사용된다.

- 특정 금액 이상 주문만 추출하기
- 특정 지역 고객만 추출하기
- 실패한 처리 결과만 모으기
- 특정 확장자의 파일명만 모으기

## 패턴 6: 조건에 맞는 값 찾기

여러 값 중에서 원하는 값을 찾는 경우도 많다. 값을 찾으면 더 이상 반복할 필요가 없으므로 \`break\`를 사용할 수 있다.

\`\`\`python
emails = ["a@example.com", "b@example.com", "admin@example.com"]
target_email = "admin@example.com"
found = False

for email in emails:
    if email == target_email:
        found = True
        break

if found:
    print("이메일을 찾았습니다.")
else:
    print("이메일을 찾지 못했습니다.")
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
이메일을 찾았습니다.
\`\`\`

\`found\`는 찾았는지 여부를 저장하는 변수이다. 이런 변수를 플래그 변수라고 부르기도 한다. 처음에는 \`False\`로 두고, 원하는 값을 찾으면 \`True\`로 바꾼다.

이 예제는 사실 \`in\` 연산자를 사용하면 더 간단하게 작성할 수 있다.

\`\`\`python
emails = ["a@example.com", "b@example.com", "admin@example.com"]
target_email = "admin@example.com"

if target_email in emails:
    print("이메일을 찾았습니다.")
else:
    print("이메일을 찾지 못했습니다.")
\`\`\`

하지만 반복문으로 직접 작성해 보면 검색 과정이 어떻게 이루어지는지 이해할 수 있다.

## 패턴 7: 건너뛰면서 처리하기

데이터 중 일부는 처리하지 않고 건너뛰어야 할 때가 있다. 이때 \`continue\`를 사용할 수 있다.

\`\`\`python
scores = [80, 0, 95, 70, 0, 60]

for score in scores:
    if score == 0:
        continue
    print("처리할 점수:", score)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
처리할 점수: 80
처리할 점수: 95
처리할 점수: 70
처리할 점수: 60
\`\`\`

여기서 0은 아직 점수가 입력되지 않은 상태라고 가정했다. 반복문은 0을 만나면 \`continue\`로 아래 코드를 건너뛰고 다음 값으로 넘어간다.

## 패턴 8: 중첩 반복문

반복문 안에 또 다른 반복문을 작성할 수 있다. 이것을 중첩 반복문이라고 한다.

가장 익숙한 예는 구구단이다.

\`\`\`python
for dan in range(2, 5):
    for number in range(1, 10):
        print(dan, "x", number, "=", dan * number)
    print("---")
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
2 x 1 = 2
2 x 2 = 4
2 x 3 = 6
2 x 4 = 8
2 x 5 = 10
2 x 6 = 12
2 x 7 = 14
2 x 8 = 16
2 x 9 = 18
---
3 x 1 = 3
3 x 2 = 6
3 x 3 = 9
3 x 4 = 12
3 x 5 = 15
3 x 6 = 18
3 x 7 = 21
3 x 8 = 24
3 x 9 = 27
---
4 x 1 = 4
4 x 2 = 8
4 x 3 = 12
4 x 4 = 16
4 x 5 = 20
4 x 6 = 24
4 x 7 = 28
4 x 8 = 32
4 x 9 = 36
---
\`\`\`

바깥쪽 반복문은 단을 바꾼다. 안쪽 반복문은 1부터 9까지 곱하는 수를 바꾼다.

중첩 반복문은 2차원 데이터나 표 형태 데이터를 처리할 때 자주 사용된다.

\`\`\`python
sales_table = [
    [10000, 20000, 15000],
    [30000, 25000, 40000],
    [12000, 18000, 22000]
]

for row in sales_table:
    row_total = 0

    for price in row:
        row_total += price

    print("행 합계:", row_total)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
행 합계: 45000
행 합계: 95000
행 합계: 52000
\`\`\`

위 예제의 대괄호 구조는 리스트 안에 리스트가 들어 있는 형태이다. 자세한 내용은 4장에서 다룬다. 지금은 “표의 각 행을 반복하고, 행 안의 값을 다시 반복한다”는 흐름을 이해하면 된다.

## 반복문을 작성할 때 생각할 질문

반복문을 작성하기 전에 다음 질문을 먼저 해 보면 좋다.

1. 무엇을 반복할 것인가?
2. 몇 번 반복할 것인가, 또는 어떤 조건까지 반복할 것인가?
3. 반복할 때마다 어떤 값이 바뀌는가?
4. 반복 결과를 저장할 변수가 필요한가?
5. 특정 조건에서 건너뛰거나 멈춰야 하는가?

예를 들어 여러 주문 금액 중 30,000원 이상인 주문의 합계를 구한다고 생각해 보자.

\`\`\`python
orders = [12000, 50000, 32000, 8000, 70000]
total = 0

for order in orders:
    if order >= 30000:
        total += order

print("30,000원 이상 주문 합계:", total)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
30,000원 이상 주문 합계: 152000
\`\`\`

이 코드는 다음 흐름으로 만들어졌다.

\`\`\`text
1. 주문 금액 목록을 반복한다.
2. 각 주문 금액이 30,000원 이상인지 확인한다.
3. 조건에 맞으면 total에 더한다.
4. 반복이 끝나면 total을 출력한다.
\`\`\`

반복문은 문법보다 사고 흐름이 중요하다. “어떤 데이터를 하나씩 보면서, 어떤 조건일 때, 어떤 작업을 할 것인가”를 먼저 말로 정리하면 코드로 옮기기 쉬워진다.

---

# 3.4 리스트 컴프리헨션 기초

## 리스트 컴프리헨션이란?

리스트 컴프리헨션은 반복문을 사용해 리스트를 간결하게 만드는 문법이다. 리스트는 4장에서 자세히 배우지만, 파이썬 코드에서 매우 자주 보이는 문법이므로 반복문을 배운 직후에 기본 형태를 먼저 살펴본다.

일반 반복문으로 새 리스트를 만드는 코드를 보자.

\`\`\`python
numbers = [1, 2, 3, 4, 5]
squares = []

for number in numbers:
    squares.append(number * number)

print(squares)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
[1, 4, 9, 16, 25]
\`\`\`

위 코드는 각 숫자의 제곱을 새 리스트에 담는다. 이 코드는 리스트 컴프리헨션을 사용해 다음처럼 줄일 수 있다.

\`\`\`python
numbers = [1, 2, 3, 4, 5]
squares = [number * number for number in numbers]

print(squares)
\`\`\`

실행 결과는 같다.

\`\`\`text
[1, 4, 9, 16, 25]
\`\`\`

리스트 컴프리헨션은 반복문을 한 줄로 줄이는 문법이다. 하지만 무조건 짧게 쓰는 것이 좋은 것은 아니다. 읽기 쉬울 때만 사용하는 것이 좋다.

## 기본 구조

리스트 컴프리헨션의 기본 구조는 다음과 같다.

\`\`\`python
[표현식 for 변수 in 반복할_대상]
\`\`\`

예를 들어 1부터 5까지 숫자를 리스트로 만들 수 있다.

\`\`\`python
numbers = [number for number in range(1, 6)]
print(numbers)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
[1, 2, 3, 4, 5]
\`\`\`

각 숫자에 10을 곱한 리스트도 만들 수 있다.

\`\`\`python
numbers = [1, 2, 3, 4, 5]
result = [number * 10 for number in numbers]

print(result)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
[10, 20, 30, 40, 50]
\`\`\`

## 조건문과 함께 사용하기

조건에 맞는 값만 리스트에 넣고 싶다면 \`if\`를 함께 사용할 수 있다.

\`\`\`python
numbers = [1, 2, 3, 4, 5, 6]
even_numbers = [number for number in numbers if number % 2 == 0]

print(even_numbers)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
[2, 4, 6]
\`\`\`

\`number % 2 == 0\`은 숫자가 짝수인지 확인하는 조건이다. 조건이 참인 값만 새 리스트에 포함된다.

일반 반복문으로 작성하면 다음과 같다.

\`\`\`python
numbers = [1, 2, 3, 4, 5, 6]
even_numbers = []

for number in numbers:
    if number % 2 == 0:
        even_numbers.append(number)

print(even_numbers)
\`\`\`

리스트 컴프리헨션은 이 코드를 한 줄로 표현한 것이다.

## 값을 변환하면서 조건 적용하기

조건에 맞는 값만 고르고, 동시에 값을 변환할 수도 있다.

\`\`\`python
prices = [10000, 25000, 30000, 5000]
discounted_prices = [price * 0.9 for price in prices if price >= 20000]

print(discounted_prices)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
[22500.0, 27000.0]
\`\`\`

이 코드는 20,000원 이상인 가격만 골라서 10% 할인한 값을 새 리스트로 만든다.

흐름을 풀어 쓰면 다음과 같다.

\`\`\`text
1. prices에서 가격을 하나씩 꺼낸다.
2. 가격이 20,000원 이상인지 확인한다.
3. 조건에 맞는 가격에 0.9를 곱한다.
4. 그 결과를 새 리스트에 담는다.
\`\`\`

## 조건에 따라 다른 값 넣기

리스트 컴프리헨션에서는 조건에 따라 다른 값을 넣을 수도 있다. 이때는 \`if-else\`의 위치가 조금 다르다.

\`\`\`python
scores = [90, 60, 75, 40]
results = ["통과" if score >= 70 else "탈락" for score in scores]

print(results)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
['통과', '탈락', '통과', '탈락']
\`\`\`

조건에 맞는 값만 고를 때는 \`if\`가 뒤에 온다.

\`\`\`python
[number for number in numbers if number % 2 == 0]
\`\`\`

조건에 따라 값을 다르게 만들 때는 \`if-else\`가 앞에 온다.

\`\`\`python
["짝수" if number % 2 == 0 else "홀수" for number in numbers]
\`\`\`

처음에는 이 차이가 헷갈릴 수 있다. 입문 단계에서는 먼저 “조건에 맞는 값만 고르는 형태”부터 익히면 충분하다.

## 문자열 리스트 다루기

문자열 데이터에서도 리스트 컴프리헨션을 사용할 수 있다.

\`\`\`python
emails = ["USER@EXAMPLE.COM", "ADMIN@EXAMPLE.COM", "TEST@EXAMPLE.COM"]
lower_emails = [email.lower() for email in emails]

print(lower_emails)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
['user@example.com', 'admin@example.com', 'test@example.com']
\`\`\`

이 코드는 이메일 주소를 모두 소문자로 바꾼다.

특정 도메인만 고를 수도 있다.

\`\`\`python
emails = ["a@example.com", "b@test.com", "c@example.com"]
example_emails = [email for email in emails if email.endswith("@example.com")]

print(example_emails)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
['a@example.com', 'c@example.com']
\`\`\`

## 리스트 컴프리헨션을 사용할 때 주의할 점

리스트 컴프리헨션은 코드를 짧게 만들 수 있지만, 너무 복잡하게 작성하면 오히려 읽기 어렵다.

예를 들어 다음 코드는 한 줄이지만 읽기 어렵다.

\`\`\`python
result = [price * 0.9 if price >= 30000 else price * 0.95 for price in prices if price > 0]
\`\`\`

이런 경우에는 일반 반복문을 사용하는 편이 더 낫다.

\`\`\`python
result = []

for price in prices:
    if price <= 0:
        continue

    if price >= 30000:
        result.append(price * 0.9)
    else:
        result.append(price * 0.95)
\`\`\`

코드가 길더라도 읽기 쉽고 수정하기 쉽다면 좋은 코드이다. 리스트 컴프리헨션은 간단한 변환이나 필터링에 사용하는 것이 좋다.

## 리스트 컴프리헨션 실무 예제 1: 할인 가격 목록 만들기

\`\`\`python
prices = [10000, 20000, 30000]
discounted_prices = [price * 0.9 for price in prices]

print(discounted_prices)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
[9000.0, 18000.0, 27000.0]
\`\`\`

## 리스트 컴프리헨션 실무 예제 2: 빈 값 제거하기

\`\`\`python
names = ["홍길동", "", "김철수", "", "이영희"]
valid_names = [name for name in names if name]

print(valid_names)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
['홍길동', '김철수', '이영희']
\`\`\`

빈 문자열은 거짓으로 평가되므로 \`if name\` 조건을 사용해 값이 있는 이름만 남길 수 있다.

## 리스트 컴프리헨션 실무 예제 3: 파일 확장자로 필터링하기

\`\`\`python
file_names = ["report.xlsx", "memo.txt", "sales.xlsx", "image.png"]
excel_files = [file_name for file_name in file_names if file_name.endswith(".xlsx")]

print(excel_files)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
['report.xlsx', 'sales.xlsx']
\`\`\`

파일 처리 자동화에서는 특정 확장자의 파일만 골라야 하는 경우가 많다. 이런 경우 리스트 컴프리헨션을 사용하면 간결하게 표현할 수 있다.

---

# 3장 핵심 정리

## 조건문 핵심 정리

조건문은 조건에 따라 다른 코드를 실행할 때 사용한다.

\`\`\`python
if 조건식:
    조건이 참일 때 실행할 코드
\`\`\`

조건이 거짓일 때 실행할 코드가 필요하면 \`else\`를 사용한다.

\`\`\`python
if 조건식:
    조건이 참일 때 실행할 코드
else:
    조건이 거짓일 때 실행할 코드
\`\`\`

조건이 여러 개라면 \`elif\`를 사용한다.

\`\`\`python
if 조건식1:
    코드1
elif 조건식2:
    코드2
else:
    코드3
\`\`\`

조건문에서는 들여쓰기가 매우 중요하다. \`if\`, \`elif\`, \`else\` 뒤에는 콜론 \`:\`을 붙이고, 실행할 코드는 들여쓰기해야 한다.

## 반복문 핵심 정리

반복문은 같은 작업을 여러 번 실행할 때 사용한다.

\`for\` 문은 반복할 대상이나 반복 횟수가 명확할 때 사용한다.

\`\`\`python
for 변수 in 반복할_대상:
    반복할 코드
\`\`\`

\`range()\`는 정해진 횟수만큼 반복할 때 자주 사용한다.

\`\`\`python
for number in range(1, 6):
    print(number)
\`\`\`

\`while\` 문은 조건이 참인 동안 반복한다.

\`\`\`python
while 조건식:
    반복할 코드
\`\`\`

\`break\`는 반복문을 종료하고, \`continue\`는 현재 반복을 건너뛴다.

## 반복문 실무 패턴 핵심 정리

반복문에서는 다음 패턴이 자주 사용된다.

- 합계 누적하기
- 조건에 맞는 개수 세기
- 최댓값 찾기
- 최솟값 찾기
- 조건에 맞는 값만 모으기
- 원하는 값을 찾으면 반복 종료하기
- 특정 값은 건너뛰기
- 중첩 반복문으로 표 형태 데이터 처리하기

## 리스트 컴프리헨션 핵심 정리

리스트 컴프리헨션은 반복문을 사용해 새 리스트를 간결하게 만드는 문법이다.

\`\`\`python
[표현식 for 변수 in 반복할_대상]
\`\`\`

조건을 붙일 수도 있다.

\`\`\`python
[표현식 for 변수 in 반복할_대상 if 조건식]
\`\`\`

리스트 컴프리헨션은 간단한 변환과 필터링에는 좋지만, 복잡한 조건이 많을 때는 일반 반복문을 사용하는 편이 좋다.

---

# 3장 연습문제

## 문제 1. 조건문 기본

다음 코드의 실행 결과를 예상해 보자.

\`\`\`python
age = 21

if age >= 19:
    print("성인")
else:
    print("미성년자")
\`\`\`

## 문제 2. 조건 검사 순서

다음 코드의 실행 결과를 예상해 보자.

\`\`\`python
score = 95

if score >= 70:
    print("C")
elif score >= 80:
    print("B")
elif score >= 90:
    print("A")
else:
    print("D")
\`\`\`

## 문제 3. 무료배송 조건

주문 금액이 50,000원 이상이면 \`"무료배송"\`, 그렇지 않으면 \`"배송비 3000원"\`을 출력하는 코드를 작성해 보자.

조건은 다음과 같다.

\`\`\`python
order_price = 48000
\`\`\`

## 문제 4. 회원 등급 조건

다음 조건에 따라 할인율을 출력하는 코드를 작성해 보자.

\`\`\`text
VIP: 20%
GOLD: 10%
SILVER: 5%
그 외: 0%
\`\`\`

주어진 값은 다음과 같다.

\`\`\`python
grade = "GOLD"
\`\`\`

## 문제 5. \`range()\` 실행 결과

다음 코드의 실행 결과를 예상해 보자.

\`\`\`python
for number in range(2, 8, 2):
    print(number)
\`\`\`

## 문제 6. 반복문 합계

\`1\`부터 \`10\`까지의 합계를 구하는 코드를 작성해 보자.

출력 예시는 다음과 같다.

\`\`\`text
합계: 55
\`\`\`

## 문제 7. 짝수만 출력하기

\`1\`부터 \`10\`까지의 숫자 중 짝수만 출력하는 코드를 작성해 보자.

출력 예시는 다음과 같다.

\`\`\`text
2
4
6
8
10
\`\`\`

## 문제 8. \`break\` 실행 결과

다음 코드의 실행 결과를 예상해 보자.

\`\`\`python
for number in range(1, 6):
    if number == 4:
        break
    print(number)
\`\`\`

## 문제 9. \`continue\` 실행 결과

다음 코드의 실행 결과를 예상해 보자.

\`\`\`python
for number in range(1, 6):
    if number == 3:
        continue
    print(number)
\`\`\`

## 문제 10. 조건에 맞는 개수 세기

다음 점수 목록에서 70점 이상인 점수의 개수를 세는 코드를 작성해 보자.

\`\`\`python
scores = [90, 65, 80, 50, 100]
\`\`\`

출력 예시는 다음과 같다.

\`\`\`text
통과한 개수: 3
\`\`\`

## 문제 11. 최댓값 찾기

다음 가격 목록에서 가장 큰 가격을 찾는 코드를 반복문으로 작성해 보자. \`max()\` 함수는 사용하지 않는다.

\`\`\`python
prices = [12000, 45000, 30000, 8000]
\`\`\`

출력 예시는 다음과 같다.

\`\`\`text
가장 큰 가격: 45000
\`\`\`

## 문제 12. 조건에 맞는 값 모으기

다음 가격 목록에서 30,000원 이상인 가격만 새 리스트에 모으는 코드를 작성해 보자.

\`\`\`python
prices = [12000, 45000, 30000, 8000, 70000]
\`\`\`

출력 예시는 다음과 같다.

\`\`\`text
[45000, 30000, 70000]
\`\`\`

## 문제 13. 문자열 반복

다음 문자열에서 숫자가 몇 개 들어 있는지 세는 코드를 작성해 보자.

\`\`\`python
text = "A1B2C3D"
\`\`\`

출력 예시는 다음과 같다.

\`\`\`text
숫자 개수: 3
\`\`\`

## 문제 14. 리스트 컴프리헨션 기본

다음 숫자 목록의 각 값에 10을 곱한 새 리스트를 리스트 컴프리헨션으로 만들어 보자.

\`\`\`python
numbers = [1, 2, 3, 4]
\`\`\`

출력 예시는 다음과 같다.

\`\`\`text
[10, 20, 30, 40]
\`\`\`

## 문제 15. 리스트 컴프리헨션 조건

다음 파일명 목록에서 \`.xlsx\`로 끝나는 파일명만 모아 보자.

\`\`\`python
file_names = ["report.xlsx", "memo.txt", "sales.xlsx", "image.png"]
\`\`\`

출력 예시는 다음과 같다.

\`\`\`text
['report.xlsx', 'sales.xlsx']
\`\`\`

## 문제 16. 오류 수정

다음 코드는 에러가 발생한다. 올바르게 수정해 보자.

\`\`\`python
age = 20

if age >= 19
    print("성인입니다.")
\`\`\`

## 문제 17. 오류 수정

다음 코드는 의도와 다르게 동작한다. \`number\`가 1부터 5까지 출력되도록 수정해 보자.

\`\`\`python
number = 1

while number <= 5:
    print(number)
\`\`\`

---

# 3장 연습문제 정답 및 해설

## 문제 1 정답

\`\`\`text
성인
\`\`\`

\`age\`가 21이므로 \`age >= 19\`는 참이다. 따라서 \`if\` 블록이 실행된다.

## 문제 2 정답

\`\`\`text
C
\`\`\`

\`score\`는 95이다. \`score >= 70\`, \`score >= 80\`, \`score >= 90\`이 모두 참이 될 수 있지만, 조건문은 위에서 아래로 검사한다. 첫 번째 조건 \`score >= 70\`이 먼저 참이 되므로 \`C\`가 출력된다. 등급을 정확히 나누려면 큰 점수 조건부터 검사해야 한다.

## 문제 3 정답

\`\`\`python
order_price = 48000

if order_price >= 50000:
    print("무료배송")
else:
    print("배송비 3000원")
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
배송비 3000원
\`\`\`

## 문제 4 정답

\`\`\`python
grade = "GOLD"

if grade == "VIP":
    print("20%")
elif grade == "GOLD":
    print("10%")
elif grade == "SILVER":
    print("5%")
else:
    print("0%")
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
10%
\`\`\`

## 문제 5 정답

\`\`\`text
2
4
6
\`\`\`

\`range(2, 8, 2)\`는 2부터 시작해서 2씩 증가한다. 끝값 8은 포함되지 않으므로 2, 4, 6이 출력된다.

## 문제 6 정답

\`\`\`python
total = 0

for number in range(1, 11):
    total += number

print("합계:", total)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
합계: 55
\`\`\`

## 문제 7 정답

\`\`\`python
for number in range(1, 11):
    if number % 2 == 0:
        print(number)
\`\`\`

\`number % 2 == 0\`은 숫자가 짝수인지 확인하는 조건이다.

다음처럼 \`range()\`의 증가값을 활용할 수도 있다.

\`\`\`python
for number in range(2, 11, 2):
    print(number)
\`\`\`

## 문제 8 정답

\`\`\`text
1
2
3
\`\`\`

\`number\`가 4가 되면 \`break\`가 실행되어 반복문이 종료된다. 따라서 4는 출력되지 않는다.

## 문제 9 정답

\`\`\`text
1
2
4
5
\`\`\`

\`number\`가 3일 때 \`continue\`가 실행되어 \`print(number)\`를 건너뛴다.

## 문제 10 정답

\`\`\`python
scores = [90, 65, 80, 50, 100]
pass_count = 0

for score in scores:
    if score >= 70:
        pass_count += 1

print("통과한 개수:", pass_count)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
통과한 개수: 3
\`\`\`

## 문제 11 정답

\`\`\`python
prices = [12000, 45000, 30000, 8000]
max_price = prices[0]

for price in prices:
    if price > max_price:
        max_price = price

print("가장 큰 가격:", max_price)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
가장 큰 가격: 45000
\`\`\`

첫 번째 값을 기준값으로 잡고, 더 큰 값이 나오면 기준값을 바꾼다.

## 문제 12 정답

\`\`\`python
prices = [12000, 45000, 30000, 8000, 70000]
result = []

for price in prices:
    if price >= 30000:
        result.append(price)

print(result)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
[45000, 30000, 70000]
\`\`\`

## 문제 13 정답

\`\`\`python
text = "A1B2C3D"
count = 0

for char in text:
    if char.isdigit():
        count += 1

print("숫자 개수:", count)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
숫자 개수: 3
\`\`\`

## 문제 14 정답

\`\`\`python
numbers = [1, 2, 3, 4]
result = [number * 10 for number in numbers]

print(result)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
[10, 20, 30, 40]
\`\`\`

## 문제 15 정답

\`\`\`python
file_names = ["report.xlsx", "memo.txt", "sales.xlsx", "image.png"]
excel_files = [file_name for file_name in file_names if file_name.endswith(".xlsx")]

print(excel_files)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
['report.xlsx', 'sales.xlsx']
\`\`\`

## 문제 16 정답

\`\`\`python
age = 20

if age >= 19:
    print("성인입니다.")
\`\`\`

\`if\` 조건식 뒤에는 콜론 \`:\`이 필요하다.

## 문제 17 정답

\`\`\`python
number = 1

while number <= 5:
    print(number)
    number += 1
\`\`\`

\`while\` 문에서는 반복이 끝날 수 있도록 조건에 사용되는 값을 바꾸어야 한다. 여기서는 \`number += 1\`을 추가해야 한다.

---

# 3장을 마치며

이 장에서는 조건문과 반복문을 배웠다. 조건문은 프로그램이 상황에 따라 다른 선택을 하도록 만든다. 반복문은 같은 작업을 여러 번 실행하거나 여러 데이터를 하나씩 처리하도록 만든다.

조건문과 반복문을 잘 다루면 프로그램이 훨씬 실용적으로 바뀐다. 단순히 값을 저장하고 출력하는 수준을 넘어, 입력값을 검사하고, 조건에 맞는 데이터를 처리하고, 반복되는 작업을 자동화할 수 있다.

다음 장에서는 여러 데이터를 더 체계적으로 관리하기 위한 자료구조를 배운다. 리스트, 튜플, 딕셔너리, 집합을 배우면 반복문과 함께 훨씬 다양한 데이터를 처리할 수 있다.
`;export{e as default};