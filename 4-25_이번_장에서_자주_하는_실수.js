var e=`# 4장. 분석용 데이터마트 만들기

## 4.25 이번 장에서 자주 하는 실수

분석용 데이터마트를 만들 때 자주 하는 실수를 정리합니다.

---

### 4.25.1 원본 데이터를 직접 수정하는 실수

원본 데이터는 직접 수정하지 않아야 합니다.

나쁜 예:

\`\`\`python
orders_raw["coupon_amount"] = orders_raw["coupon_amount"].fillna(0)
\`\`\`

좋은 예:

\`\`\`python
orders = orders_raw.copy()
orders["coupon_amount"] = orders["coupon_amount"].fillna(0)
\`\`\`

---

### 4.25.2 중복 마스터를 정리하지 않고 결합하는 실수

고객 데이터나 상품 데이터에 key 중복이 있으면 결합 후 행 수가 늘어납니다.

결합 전 반드시 확인합니다.

\`\`\`python
customers["customer_id"].is_unique
products["product_id"].is_unique
\`\`\`

---

### 4.25.3 결합 후 행 수를 확인하지 않는 실수

\`merge()\`가 오류 없이 실행되어도 결과가 맞는 것은 아닙니다.

\`\`\`python
len_before = len(orders)
len_after = len(orders_customer)
\`\`\`

\`left join\`에서는 행 수가 유지되는지 확인해야 합니다.

---

### 4.25.4 상품 정보가 없는 주문을 조용히 버리는 실수

상품 정보가 없어 매출 분석에서 제외한 주문은 반드시 별도 저장해야 합니다.

\`\`\`python
excluded_orders.to_csv("excluded_orders.csv", index=False)
\`\`\`

분석 제외 데이터가 얼마나 되는지 보고서에 적어야 합니다.

---

### 4.25.5 날짜 오류를 전체 분석에서 무조건 제외하는 실수

날짜가 오류인 주문은 월별 분석에는 사용할 수 없습니다.  
하지만 상품 정보와 금액이 정상이라면 전체 매출 분석에는 포함할 수 있습니다.

분석 목적에 따라 제외 여부가 달라집니다.

---

### 4.25.6 결측치를 모두 0으로 채우는 실수

쿠폰 금액 결측치는 0으로 채울 수 있을 수 있습니다.  
하지만 고객 ID, 상품 ID, 단가 결측치를 0으로 채우면 안 됩니다.

결측치 처리는 컬럼의 의미에 따라 달라야 합니다.

---

### 4.25.7 전처리 기준을 기록하지 않는 실수

전처리 기준을 기록하지 않으면 나중에 결과를 설명하기 어렵습니다.

반드시 전처리 로그를 작성해야 합니다.

\`\`\`text
무엇을 처리했는가?
왜 그렇게 처리했는가?
몇 건이 제외되었는가?
분석에 어떤 영향을 주는가?
\`\`\`

---
`;export{e as default};