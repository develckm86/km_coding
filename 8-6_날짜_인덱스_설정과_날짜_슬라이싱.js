var e=`# 8장. 시계열 매출 분석

## 8.6 날짜 인덱스 설정과 날짜 슬라이싱

시계열 분석에서는 날짜 컬럼을 인덱스로 설정하면 편리합니다.

---

### 8.6.1 날짜 인덱스 설정

\`\`\`python
orders_ts = orders_mart.set_index("order_date_dt").sort_index()

orders_ts.head()
\`\`\`

\`order_date_dt\`가 인덱스가 되었습니다.

---

### 8.6.2 특정 월 데이터 선택

날짜 인덱스를 사용하면 특정 월 데이터를 쉽게 선택할 수 있습니다.

\`\`\`python
orders_ts.loc["2026-01"]
\`\`\`

이 코드는 2026년 1월 주문을 선택합니다.

---

### 8.6.3 특정 기간 데이터 선택

\`\`\`python
orders_ts.loc["2026-02-01":"2026-03-31"]
\`\`\`

이 코드는 2026년 2월 1일부터 2026년 3월 31일까지의 주문을 선택합니다.

---

### 8.6.4 날짜 인덱스 사용 시 주의점

날짜 인덱스를 사용하려면 인덱스가 날짜형이어야 합니다.

\`\`\`python
orders_ts.index
\`\`\`

또한 날짜 인덱스는 정렬되어 있어야 기간 선택이 안정적으로 동작합니다.

\`\`\`python
orders_ts = orders_ts.sort_index()
\`\`\`

---
`;export{e as default};