var e=`<!-- 원본: python_advanced_chapter_2_book.md / 세부 장: 2-2 -->

# 2.2 객체와 참조

### 2.2.1 파이썬의 모든 값은 객체다

파이썬에서는 정수, 실수, 문자열, 리스트, 딕셔너리, 함수, 클래스, 모듈까지 모두 객체다.

다음 값들은 모두 객체다.

\`\`\`python
10
3.14
"Python"
[1, 2, 3]
{"name": "Kim"}
print
\`\`\`

객체는 단순한 값만 의미하지 않는다. 객체는 다음과 같은 정보를 함께 가진다.

\`\`\`text
1. 값
2. 자료형
3. 고유한 식별 정보
4. 사용할 수 있는 동작
\`\`\`

예를 들어 문자열 객체는 문자열 값을 가지고 있고, \`upper()\`, \`lower()\`, \`split()\` 같은 메서드를 사용할 수 있다.

\`\`\`python
text = "python"
print(text.upper())
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
PYTHON
\`\`\`

---

### 2.2.2 변수는 상자가 아니라 이름이다

많은 입문 설명에서는 변수를 “값을 담는 상자”라고 표현한다. 초급 단계에서는 이 비유가 도움이 된다. 그러나 파이썬을 깊게 이해하려면 이 비유에서 벗어나야 한다.

파이썬에서 변수는 값을 직접 담는 상자라기보다 **객체에 붙는 이름**에 가깝다.

\`\`\`python
a = 10
\`\`\`

이 코드는 다음처럼 이해할 수 있다.

\`\`\`text
정수 객체 10이 있다.
a라는 이름이 그 객체를 가리킨다.
\`\`\`

다음 코드를 보자.

\`\`\`python
a = 10
b = a
\`\`\`

이 코드는 \`a\`의 값이 복사되어 \`b\`라는 상자에 들어간다는 의미가 아니다. 더 정확히는 다음과 같다.

\`\`\`text
a가 정수 객체 10을 가리킨다.
b도 같은 정수 객체 10을 가리킨다.
\`\`\`

이처럼 이름이 객체를 가리키는 관계를 **참조(reference)**라고 한다.

---

### 2.2.3 \`id()\`로 객체 확인하기

파이썬에서는 \`id()\` 함수를 사용해 객체의 식별값을 확인할 수 있다.

\`\`\`python
a = [1, 2, 3]
b = a

print(id(a))
print(id(b))
\`\`\`

두 값은 같다. 실제 숫자는 실행 환경마다 다르다.

\`\`\`text
140000000000000
140000000000000
\`\`\`

\`a\`와 \`b\`의 \`id()\`가 같다는 것은 두 이름이 같은 리스트 객체를 가리킨다는 뜻이다.

이번에는 다음 코드를 보자.

\`\`\`python
a = [1, 2, 3]
b = [1, 2, 3]

print(id(a))
print(id(b))
\`\`\`

두 리스트의 내용은 같지만 서로 다른 객체다. 따라서 \`id()\`는 다르게 나온다.

\`\`\`text
140000000000000
140000000000128
\`\`\`

이 차이는 \`==\`와 \`is\`의 차이로 이어진다.

---

### 2.2.4 \`==\`와 \`is\`

\`==\`는 두 객체의 **값이 같은지** 비교한다.  
\`is\`는 두 이름이 **같은 객체를 가리키는지** 비교한다.

\`\`\`python
a = [1, 2, 3]
b = [1, 2, 3]

print(a == b)
print(a is b)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
True
False
\`\`\`

두 리스트는 값은 같지만 같은 객체는 아니다.

반면 다음 코드는 다르다.

\`\`\`python
a = [1, 2, 3]
b = a

print(a == b)
print(a is b)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
True
True
\`\`\`

\`b = a\`는 새 리스트를 만드는 것이 아니라, \`a\`가 가리키던 리스트 객체에 \`b\`라는 이름을 하나 더 붙이는 것이다.

실무에서는 값을 비교할 때 대부분 \`==\`를 사용한다. \`is\`는 주로 \`None\`과 비교할 때 사용한다.

\`\`\`python
value = None

if value is None:
    print("값이 없습니다.")
\`\`\`

---

### 2.2.5 재할당

변수에 새 값을 대입하면 기존 객체가 바뀌는 것이 아니라, 이름이 다른 객체를 가리키게 된다.

\`\`\`python
a = 10
print(id(a))

a = 20
print(id(a))
\`\`\`

\`a = 20\`은 정수 객체 10을 20으로 수정하는 것이 아니다. \`a\`라는 이름이 이제 정수 객체 20을 가리키도록 바뀌는 것이다. 이것을 **재할당**이라고 한다.

문자열도 마찬가지다.

\`\`\`python
text = "python"
text = text.upper()

print(text)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
PYTHON
\`\`\`

\`upper()\`가 기존 문자열 객체를 직접 바꾼 것이 아니다. 대문자로 바뀐 새 문자열 객체를 만들고, \`text\`라는 이름이 그 새 객체를 가리키게 된 것이다.

---
`;export{e as default};