var e=`# 16장. 기본 통계 이해

## 16.0 들어가며

데이터를 불러오고, 정리하고, 결합했다면 이제 데이터를 이해해야 합니다.  
데이터를 이해하는 가장 기본적인 방법 중 하나가 **통계값을 확인하는 것**입니다.

예를 들어 쇼핑몰 주문 데이터가 있다고 생각해봅시다.

\`\`\`text
주문 금액: 10,000원
주문 금액: 15,000원
주문 금액: 20,000원
주문 금액: 300,000원
\`\`\`

이 데이터를 보고 다음과 같은 질문을 할 수 있습니다.

\`\`\`text
평균 주문 금액은 얼마인가?
가장 작은 주문 금액과 가장 큰 주문 금액은 얼마인가?
주문 금액은 대체로 어느 정도 범위에 있는가?
일부 큰 주문이 평균을 크게 올리고 있지는 않은가?
고객 나이와 구매 금액 사이에 관계가 있는가?
지역별 고객 수는 어떻게 다른가?
\`\`\`

이런 질문에 답하기 위해 사용하는 것이 기본 통계입니다.

이번 장에서는 데이터 분석에서 가장 자주 사용하는 기초 통계 개념을 배웁니다.

- 평균
- 중앙값
- 최빈값
- 최솟값
- 최댓값
- 범위
- 분산
- 표준편차
- 사분위수
- 상관관계
- 빈도와 비율

이 장의 목적은 복잡한 수식을 외우는 것이 아닙니다.  
데이터를 분석할 때 통계값을 어떻게 읽고, 어떤 상황에서 어떤 값을 봐야 하는지 이해하는 것입니다.

통계값은 데이터의 특징을 짧게 요약해줍니다.  
하지만 통계값 하나만 보고 결론을 내리면 위험할 수 있습니다.

예를 들어 평균만 보면 데이터가 어느 정도인지 알 수 있지만, 이상값이 있으면 평균이 왜곡될 수 있습니다.  
상관계수가 높다고 해서 한 변수가 다른 변수를 원인으로 만든다고 단정할 수도 없습니다.

따라서 이 장에서는 통계값을 계산하는 방법뿐 아니라, 통계값을 해석할 때 주의해야 할 점도 함께 다룹니다.

---

## 16.1 기술통계란?

기술통계는 데이터를 요약해서 설명하는 통계입니다.

데이터가 많아지면 모든 행을 하나씩 보는 것은 어렵습니다.  
예를 들어 주문 데이터가 10건이면 직접 볼 수 있지만, 10만 건이라면 모든 행을 눈으로 확인하기 어렵습니다.

이럴 때 우리는 데이터를 요약합니다.

\`\`\`text
전체 주문 수는 몇 건인가?
총매출은 얼마인가?
평균 주문 금액은 얼마인가?
가장 큰 주문 금액은 얼마인가?
주문 금액의 분포는 넓은가, 좁은가?
\`\`\`

이런 요약을 통해 데이터의 전체적인 특징을 파악할 수 있습니다.

---

### 16.1.1 기술통계와 추론통계

통계는 크게 기술통계와 추론통계로 나눌 수 있습니다.

| 구분 | 의미 | 예시 |
|---|---|---|
| 기술통계 | 가지고 있는 데이터를 요약하고 설명 | 평균, 중앙값, 표준편차, 빈도 |
| 추론통계 | 표본을 바탕으로 더 큰 집단을 추정 | 신뢰구간, 가설검정, 회귀분석 |

이 기초 과정에서는 주로 기술통계를 다룹니다.

추론통계, 가설검정, 회귀분석 같은 내용은 데이터 분석 고급 과정에서 다루는 것이 좋습니다.

기초 과정에서 중요한 것은 다음입니다.

\`\`\`text
지금 가지고 있는 데이터를 어떻게 요약하고 이해할 것인가?
\`\`\`

---

### 16.1.2 기술통계가 필요한 이유

기술통계는 데이터 분석의 출발점입니다.

데이터를 본격적으로 분석하기 전에 다음을 확인해야 합니다.

\`\`\`text
데이터 크기는 어느 정도인가?
수치형 컬럼의 평균과 범위는 어떤가?
이상하게 큰 값이나 작은 값은 없는가?
범주형 값은 어떤 종류가 있는가?
특정 값이 너무 많이 등장하지는 않는가?
두 수치형 변수 사이에 관계가 있는가?
\`\`\`

기술통계를 확인하면 데이터의 기본 상태를 빠르게 파악할 수 있습니다.

pandas에서는 \`describe()\`를 사용해 기본 통계값을 한 번에 확인할 수 있습니다.

\`\`\`python
df.describe()
\`\`\`

하지만 \`describe()\` 결과를 단순히 보는 것에서 끝나면 안 됩니다.  
각 통계값이 무엇을 의미하는지 알아야 데이터 해석이 가능합니다.

---

## 16.2 예제 데이터 준비

이번 장에서는 고객 주문 데이터를 사용합니다.

\`\`\`python
import pandas as pd
import numpy as np

orders = pd.DataFrame({
    "order_id": [1001, 1002, 1003, 1004, 1005, 1006, 1007, 1008, 1009, 1010],
    "customer_id": [1, 2, 3, 1, 4, 2, 5, 6, 7, 8],
    "region": ["서울", "부산", "서울", "서울", "대전", "부산", "부산", "서울", "대전", "서울"],
    "grade": ["VIP", "일반", "일반", "VIP", "일반", "일반", "VIP", "일반", "일반", "VIP"],
    "age": [34, 28, 41, 34, 25, 28, 39, 45, 31, 52],
    "quantity": [1, 3, 2, 1, 2, 4, 1, 2, 5, 1],
    "total_price": [300000, 45000, 50000, 220000, 35000, 60000, 180000, 30000, 75000, 260000],
    "visit_count": [12, 5, 7, 12, 3, 5, 10, 4, 6, 15],
    "satisfaction": [5, 4, 3, 5, 4, 3, 5, 3, 4, 5]
})

orders
\`\`\`

이 데이터에는 다음 컬럼이 있습니다.

| 컬럼 | 의미 |
|---|---|
| \`order_id\` | 주문 ID |
| \`customer_id\` | 고객 ID |
| \`region\` | 지역 |
| \`grade\` | 고객 등급 |
| \`age\` | 고객 나이 |
| \`quantity\` | 주문 수량 |
| \`total_price\` | 주문 금액 |
| \`visit_count\` | 방문 횟수 |
| \`satisfaction\` | 만족도 점수 |

이번 장에서는 \`age\`, \`quantity\`, \`total_price\`, \`visit_count\`, \`satisfaction\` 같은 수치형 컬럼을 중심으로 통계값을 계산합니다.  
또한 \`region\`, \`grade\` 같은 범주형 컬럼의 빈도와 비율도 확인합니다.

---

## 16.3 데이터 크기와 기본 구조 확인

통계값을 계산하기 전에 먼저 데이터의 크기와 구조를 확인합니다.

---

### 16.3.1 행과 열 개수 확인

\`\`\`python
orders.shape
\`\`\`

\`shape\`는 행 개수와 열 개수를 튜플로 보여줍니다.

\`\`\`text
(10, 9)
\`\`\`

이 결과는 10행, 9열이라는 뜻입니다.

행 수는 데이터의 규모를 이해하는 데 중요합니다.  
데이터가 너무 적으면 평균이나 비율을 일반화하기 어렵습니다.  
데이터가 너무 많으면 성능과 처리 방식도 고려해야 합니다.

---

### 16.3.2 자료형 확인

\`\`\`python
orders.dtypes
\`\`\`

수치형 컬럼인지, 문자열 컬럼인지 확인할 수 있습니다.

수치형 컬럼은 평균, 표준편차 같은 통계값을 계산할 수 있습니다.  
문자열 또는 범주형 컬럼은 빈도와 비율을 확인하는 것이 일반적입니다.

---

### 16.3.3 기본 정보 확인

\`\`\`python
orders.info()
\`\`\`

\`info()\`는 각 컬럼의 결측치 여부와 자료형을 확인하는 데 유용합니다.

데이터 분석을 시작할 때는 다음 순서로 확인하는 습관이 좋습니다.

\`\`\`python
orders.head()
orders.shape
orders.info()
orders.describe()
\`\`\`

---

## 16.4 수치형 데이터 요약: \`describe()\`

수치형 데이터의 기본 통계값은 \`describe()\`로 확인할 수 있습니다.

\`\`\`python
orders.describe()
\`\`\`

결과에는 다음 값들이 포함됩니다.

| 항목 | 의미 |
|---|---|
| \`count\` | 결측치가 아닌 값의 개수 |
| \`mean\` | 평균 |
| \`std\` | 표준편차 |
| \`min\` | 최솟값 |
| \`25%\` | 1사분위수 |
| \`50%\` | 중앙값 |
| \`75%\` | 3사분위수 |
| \`max\` | 최댓값 |

\`describe()\`는 데이터의 전체적인 모습을 빠르게 확인할 때 매우 유용합니다.

---

### 16.4.1 특정 컬럼만 요약하기

주문 금액만 요약해보겠습니다.

\`\`\`python
orders["total_price"].describe()
\`\`\`

여러 컬럼만 선택해 요약할 수도 있습니다.

\`\`\`python
orders[["age", "quantity", "total_price", "visit_count", "satisfaction"]].describe()
\`\`\`

실무에서는 모든 컬럼을 한 번에 보기보다, 분석 목적에 맞는 컬럼을 선택해 보는 것이 더 좋습니다.

---

### 16.4.2 \`describe()\` 결과 해석하기

예를 들어 \`total_price\`의 \`describe()\` 결과를 해석한다고 해봅시다.

\`\`\`text
count    10
mean     125500
std      ...
min      30000
25%      ...
50%      ...
75%      ...
max      300000
\`\`\`

이 결과에서 확인할 수 있는 것은 다음과 같습니다.

\`\`\`text
주문 금액 데이터는 10개가 있다.
평균 주문 금액은 약 125,500원이다.
가장 작은 주문 금액은 30,000원이다.
가장 큰 주문 금액은 300,000원이다.
중앙값과 평균을 비교해 분포가 치우쳤는지 볼 수 있다.
\`\`\`

평균이 중앙값보다 훨씬 크다면 일부 큰 주문이 평균을 끌어올리고 있을 수 있습니다.

---

## 16.5 중심 경향: 평균, 중앙값, 최빈값

중심 경향은 데이터가 대체로 어디에 모여 있는지를 나타냅니다.

대표적인 중심 경향 지표는 다음 세 가지입니다.

\`\`\`text
평균
중앙값
최빈값
\`\`\`

---

### 16.5.1 평균: \`mean()\`

평균은 모든 값을 더한 뒤 개수로 나눈 값입니다.

\`\`\`python
orders["total_price"].mean()
\`\`\`

평균은 가장 익숙한 대표값입니다.  
예를 들어 평균 주문 금액은 전체 주문 금액의 일반적인 수준을 보여줍니다.

\`\`\`python
avg_order_price = orders["total_price"].mean()

avg_order_price
\`\`\`

평균은 다음과 같은 질문에 답할 때 사용합니다.

\`\`\`text
평균 주문 금액은 얼마인가?
고객의 평균 나이는 몇 세인가?
평균 방문 횟수는 몇 회인가?
평균 만족도는 몇 점인가?
\`\`\`

---

### 16.5.2 평균의 장점과 단점

평균은 계산하기 쉽고 이해하기 쉽습니다.  
하지만 이상값의 영향을 크게 받습니다.

다음 예를 보겠습니다.

\`\`\`python
sales = pd.Series([10000, 12000, 15000, 13000, 11000, 1000000])

sales.mean()
\`\`\`

대부분의 값은 1만 원대이지만, 1,000,000원이 하나 있어서 평균이 크게 올라갑니다.

이런 경우 평균만 보면 일반적인 주문 금액을 잘못 이해할 수 있습니다.

---

### 16.5.3 중앙값: \`median()\`

중앙값은 데이터를 크기순으로 정렬했을 때 가운데에 있는 값입니다.

\`\`\`python
orders["total_price"].median()
\`\`\`

중앙값은 이상값의 영향을 평균보다 덜 받습니다.

위에서 본 \`sales\` 데이터의 중앙값을 계산해봅시다.

\`\`\`python
sales.median()
\`\`\`

평균과 중앙값을 비교해보면 차이를 알 수 있습니다.

\`\`\`python
sales.mean()
sales.median()
\`\`\`

일부 큰 값이 있을 때는 평균보다 중앙값이 일반적인 수준을 더 잘 보여줄 수 있습니다.

---

### 16.5.4 평균과 중앙값 비교

주문 금액에서 평균과 중앙값을 함께 계산해보겠습니다.

\`\`\`python
mean_price = orders["total_price"].mean()
median_price = orders["total_price"].median()

mean_price, median_price
\`\`\`

두 값의 차이를 해석할 수 있습니다.

\`\`\`text
평균이 중앙값보다 크다 → 일부 큰 값이 평균을 끌어올릴 수 있다.
평균과 중앙값이 비슷하다 → 값들이 비교적 균형 있게 분포할 수 있다.
평균이 중앙값보다 작다 → 일부 작은 값이 평균을 낮출 수 있다.
\`\`\`

분석 보고서에서는 평균과 중앙값을 함께 보여주는 것이 좋습니다.

\`\`\`python
orders["total_price"].agg(["mean", "median"])
\`\`\`

---

### 16.5.5 최빈값: \`mode()\`

최빈값은 가장 자주 등장하는 값입니다.

\`\`\`python
orders["region"].mode()
\`\`\`

수치형 데이터에서도 사용할 수 있지만, 범주형 데이터에서 특히 유용합니다.

예를 들어 가장 많이 등장한 지역을 확인할 수 있습니다.

\`\`\`python
orders["region"].mode()
\`\`\`

고객 등급의 최빈값도 확인할 수 있습니다.

\`\`\`python
orders["grade"].mode()
\`\`\`

\`mode()\`는 결과가 여러 개일 수 있으므로 Series 형태로 반환됩니다.  
가장 첫 번째 최빈값만 사용하려면 \`[0]\`을 붙일 수 있습니다.

\`\`\`python
orders["region"].mode()[0]
\`\`\`

---

### 16.5.6 실무 해석 예시

다음 통계값을 계산해봅시다.

\`\`\`python
summary_center = orders["total_price"].agg(["mean", "median", "min", "max"])

summary_center
\`\`\`

해석 예시는 다음과 같습니다.

\`\`\`text
주문 금액의 평균은 중앙값보다 높다.
이는 일부 고가 주문이 평균을 끌어올리고 있을 가능성을 의미한다.
최솟값과 최댓값의 차이가 크므로 주문 금액의 분포가 넓다.
따라서 평균만으로 주문 금액을 설명하기보다 중앙값과 분위수도 함께 확인하는 것이 좋다.
\`\`\`

---

## 16.6 산포: 데이터가 얼마나 퍼져 있는가

중심 경향은 데이터가 어디에 모여 있는지 보여줍니다.  
하지만 그것만으로는 충분하지 않습니다.

예를 들어 두 데이터가 있다고 해봅시다.

\`\`\`text
A: 90, 100, 110
B: 10, 100, 190
\`\`\`

두 데이터의 평균은 모두 100입니다.  
하지만 A는 값들이 100 근처에 모여 있고, B는 매우 넓게 퍼져 있습니다.

이처럼 데이터가 얼마나 퍼져 있는지를 나타내는 개념이 **산포**입니다.

대표적인 산포 지표는 다음과 같습니다.

\`\`\`text
범위
분산
표준편차
사분위수
IQR
\`\`\`

---

### 16.6.1 최솟값과 최댓값

가장 간단한 산포 확인 방법은 최솟값과 최댓값을 보는 것입니다.

\`\`\`python
orders["total_price"].min()
orders["total_price"].max()
\`\`\`

최솟값과 최댓값을 함께 확인합니다.

\`\`\`python
orders["total_price"].agg(["min", "max"])
\`\`\`

최솟값과 최댓값은 데이터의 전체 범위를 빠르게 보여줍니다.

---

### 16.6.2 범위

범위는 최댓값에서 최솟값을 뺀 값입니다.

\`\`\`python
price_range = orders["total_price"].max() - orders["total_price"].min()

price_range
\`\`\`

범위가 크면 값의 차이가 크다는 의미입니다.

하지만 범위는 최솟값과 최댓값만 사용하기 때문에 이상값의 영향을 크게 받습니다.

---

### 16.6.3 분산: \`var()\`

분산은 값들이 평균에서 얼마나 떨어져 있는지를 나타내는 지표입니다.

\`\`\`python
orders["total_price"].var()
\`\`\`

분산이 크면 값들이 평균에서 멀리 퍼져 있다는 뜻입니다.  
분산이 작으면 값들이 평균 근처에 모여 있다는 뜻입니다.

다만 분산은 단위가 제곱되어 해석이 직관적이지 않습니다.  
예를 들어 금액 데이터의 분산은 원의 제곱 단위가 됩니다.

그래서 실무에서는 표준편차를 더 자주 봅니다.

---

### 16.6.4 표준편차: \`std()\`

표준편차는 분산의 제곱근입니다.  
원래 데이터와 같은 단위를 가지므로 해석이 더 쉽습니다.

\`\`\`python
orders["total_price"].std()
\`\`\`

표준편차가 크면 값들이 평균 주변에서 넓게 퍼져 있습니다.  
표준편차가 작으면 값들이 평균 주변에 비교적 모여 있습니다.

예를 들어 다음 두 데이터의 표준편차를 비교해봅시다.

\`\`\`python
a = pd.Series([90, 100, 110])
b = pd.Series([10, 100, 190])

a.mean(), b.mean()
a.std(), b.std()
\`\`\`

두 데이터의 평균은 같지만 표준편차는 다릅니다.  
B의 표준편차가 훨씬 큽니다.

---

### 16.6.5 표준편차 해석 시 주의점

표준편차는 데이터가 평균 주변에서 얼마나 퍼져 있는지를 보여줍니다.  
하지만 이상값이 있으면 표준편차도 크게 커질 수 있습니다.

또한 평균과 마찬가지로 데이터 분포에 영향을 받습니다.  
따라서 표준편차만 보는 것보다 사분위수와 박스플롯도 함께 확인하는 것이 좋습니다.

기초 분석에서는 표준편차를 다음처럼 해석하면 충분합니다.

\`\`\`text
표준편차가 작다 → 값들이 비슷하다.
표준편차가 크다 → 값들의 차이가 크다.
표준편차가 매우 크다 → 이상값이나 큰 편차가 있을 수 있다.
\`\`\`

---

## 16.7 사분위수와 IQR

사분위수는 데이터를 크기순으로 정렬했을 때 4등분하는 기준값입니다.

대표적으로 다음 값이 있습니다.

| 이름 | 의미 |
|---|---|
| Q1 | 25% 지점 |
| Q2 | 50% 지점, 중앙값 |
| Q3 | 75% 지점 |

pandas에서는 \`quantile()\`을 사용합니다.

---

### 16.7.1 사분위수 계산

\`\`\`python
q1 = orders["total_price"].quantile(0.25)
q2 = orders["total_price"].quantile(0.50)
q3 = orders["total_price"].quantile(0.75)

q1, q2, q3
\`\`\`

여러 분위수를 한 번에 확인할 수도 있습니다.

\`\`\`python
orders["total_price"].quantile([0.25, 0.5, 0.75])
\`\`\`

\`describe()\`의 \`25%\`, \`50%\`, \`75%\`도 사분위수입니다.

\`\`\`python
orders["total_price"].describe()
\`\`\`

---

### 16.7.2 IQR

IQR은 Interquartile Range의 약자입니다.  
Q3에서 Q1을 뺀 값입니다.

\`\`\`python
iqr = q3 - q1

iqr
\`\`\`

IQR은 데이터의 가운데 50%가 어느 정도 범위에 퍼져 있는지를 보여줍니다.

이상값을 찾을 때도 IQR을 사용할 수 있습니다.

\`\`\`python
lower_bound = q1 - 1.5 * iqr
upper_bound = q3 + 1.5 * iqr

lower_bound, upper_bound
\`\`\`

이 범위를 벗어난 값은 이상값 후보로 볼 수 있습니다.

\`\`\`python
outliers = orders[
    (orders["total_price"] < lower_bound) |
    (orders["total_price"] > upper_bound)
]

outliers
\`\`\`

이상값 처리는 11장에서 이미 다루었습니다.  
이번 장에서는 IQR이 통계적으로 데이터의 퍼짐을 이해하는 데도 사용된다는 점을 기억하면 됩니다.

---

### 16.7.3 분위수로 데이터 해석하기

분위수는 평균보다 데이터의 분포를 더 자세히 보여줍니다.

예를 들어 다음 결과를 생각해봅시다.

\`\`\`text
25%: 47,500
50%: 67,500
75%: 210,000
\`\`\`

이 결과는 다음처럼 해석할 수 있습니다.

\`\`\`text
주문 금액의 25%는 47,500원 이하이다.
주문 금액의 절반은 67,500원 이하이다.
주문 금액의 75%는 210,000원 이하이다.
상위 25% 주문은 210,000원보다 크다.
\`\`\`

분위수는 “상위 몇 %”, “하위 몇 %”를 이해하는 데 유용합니다.

---

## 16.8 분포 확인

분포는 데이터 값들이 어떻게 퍼져 있는지를 의미합니다.

평균과 표준편차만으로는 분포의 모양을 완전히 알 수 없습니다.  
데이터가 한쪽에 몰려 있는지, 양쪽으로 넓게 퍼져 있는지, 이상값이 있는지 확인하려면 분포를 봐야 합니다.

---

### 16.8.1 히스토그램

히스토그램은 수치형 데이터의 분포를 확인하는 대표적인 그래프입니다.

\`\`\`python
import matplotlib.pyplot as plt

orders["total_price"].plot(kind="hist", bins=5)

plt.title("Total Price Distribution")
plt.xlabel("Total Price")
plt.ylabel("Count")
plt.show()
\`\`\`

히스토그램은 값의 구간별 빈도를 보여줍니다.

기초 과정에서는 다음 질문을 중심으로 히스토그램을 해석하면 됩니다.

\`\`\`text
값들이 어느 구간에 많이 몰려 있는가?
오른쪽이나 왼쪽으로 치우쳐 있는가?
멀리 떨어진 값이 있는가?
분포가 넓은가, 좁은가?
\`\`\`

그래프 자체는 17장에서 더 자세히 다룹니다.  
이번 장에서는 통계값을 해석할 때 분포도 함께 확인해야 한다는 점이 중요합니다.

---

### 16.8.2 박스플롯

박스플롯은 중앙값, 사분위수, 이상값 후보를 한눈에 보여줍니다.

\`\`\`python
orders["total_price"].plot(kind="box")

plt.title("Total Price Boxplot")
plt.ylabel("Total Price")
plt.show()
\`\`\`

박스플롯에서는 다음을 확인할 수 있습니다.

\`\`\`text
중앙값은 어디에 있는가?
데이터의 가운데 50% 범위는 얼마나 넓은가?
이상값 후보가 있는가?
분포가 한쪽으로 치우쳐 있는가?
\`\`\`

박스플롯은 평균보다 중앙값과 사분위수 중심으로 분포를 보여주기 때문에 이상값 확인에 유용합니다.

---

### 16.8.3 왜도 기초

왜도는 데이터가 어느 한쪽으로 치우쳐 있는 정도를 나타냅니다.  
기초 과정에서는 수식보다 개념을 이해하면 충분합니다.

\`\`\`text
오른쪽으로 긴 꼬리 → 큰 값이 일부 존재
왼쪽으로 긴 꼬리 → 작은 값이 일부 존재
\`\`\`

주문 금액이나 소득 데이터는 오른쪽으로 긴 꼬리를 가지는 경우가 많습니다.  
대부분의 값은 낮거나 중간 수준이지만, 일부 매우 큰 값이 있기 때문입니다.

pandas에서는 \`skew()\`로 왜도를 계산할 수 있습니다.

\`\`\`python
orders["total_price"].skew()
\`\`\`

하지만 기초 과정에서는 왜도 숫자 자체보다 히스토그램과 평균·중앙값 비교를 통해 치우침을 이해하는 것이 더 중요합니다.

\`\`\`python
orders["total_price"].mean()
orders["total_price"].median()
\`\`\`

평균이 중앙값보다 크면 오른쪽으로 치우친 분포일 가능성이 있습니다.

---

## 16.9 범주형 데이터 요약

수치형 데이터는 평균, 중앙값, 표준편차로 요약할 수 있습니다.  
하지만 지역, 등급, 카테고리 같은 범주형 데이터는 다른 방식으로 요약해야 합니다.

범주형 데이터에서는 주로 빈도와 비율을 봅니다.

---

### 16.9.1 빈도 확인: \`value_counts()\`

지역별 데이터 개수를 확인해보겠습니다.

\`\`\`python
orders["region"].value_counts()
\`\`\`

고객 등급별 개수도 확인합니다.

\`\`\`python
orders["grade"].value_counts()
\`\`\`

\`value_counts()\`는 범주형 데이터의 분포를 빠르게 파악하는 데 매우 유용합니다.

---

### 16.9.2 비율 확인

비율로 보고 싶다면 \`normalize=True\`를 사용합니다.

\`\`\`python
orders["region"].value_counts(normalize=True)
\`\`\`

백분율로 표현하려면 100을 곱합니다.

\`\`\`python
region_ratio = orders["region"].value_counts(normalize=True) * 100

region_ratio.round(1)
\`\`\`

이 결과는 각 지역이 전체에서 몇 %를 차지하는지 보여줍니다.

---

### 16.9.3 결측치 포함 여부

\`value_counts()\`는 기본적으로 결측치를 제외합니다.

결측치도 함께 보고 싶다면 \`dropna=False\`를 사용합니다.

\`\`\`python
orders["region"].value_counts(dropna=False)
\`\`\`

실무에서는 결측치가 하나의 품질 문제일 수 있으므로 범주형 데이터 분포를 볼 때 결측치 포함 여부를 확인하는 것이 좋습니다.

---

### 16.9.4 범주형 데이터 요약표 만들기

지역별 빈도와 비율을 함께 보고 싶다면 DataFrame으로 만들 수 있습니다.

\`\`\`python
region_count = orders["region"].value_counts()
region_ratio = orders["region"].value_counts(normalize=True) * 100

region_summary = pd.DataFrame({
    "count": region_count,
    "ratio_percent": region_ratio.round(1)
})

region_summary
\`\`\`

이런 요약표는 보고서에 넣기 좋습니다.

---

### 16.9.5 교차표로 두 범주형 변수 요약

두 범주형 변수의 관계를 보고 싶다면 \`pd.crosstab()\`을 사용할 수 있습니다.

\`\`\`python
pd.crosstab(orders["region"], orders["grade"])
\`\`\`

지역별 고객 등급 분포를 비율로 보고 싶다면 다음처럼 작성합니다.

\`\`\`python
pd.crosstab(
    orders["region"],
    orders["grade"],
    normalize="index"
).round(2)
\`\`\`

교차표는 14장에서 다룬 내용입니다.  
기본 통계에서도 범주형 데이터 요약을 위해 자주 사용됩니다.

---

## 16.10 상관관계 기초

상관관계는 두 수치형 변수 사이의 관계를 나타냅니다.

예를 들어 다음 질문을 생각해봅시다.

\`\`\`text
방문 횟수가 많을수록 주문 금액도 큰가?
나이가 많을수록 만족도가 높은가?
주문 수량이 많을수록 주문 금액이 큰가?
\`\`\`

이런 질문은 두 변수 사이의 관계를 확인하는 질문입니다.

pandas에서는 \`corr()\`을 사용해 상관계수를 계산할 수 있습니다.

---

### 16.10.1 상관계수란?

상관계수는 두 변수의 관계 방향과 강도를 나타내는 값입니다.

일반적으로 -1에서 1 사이의 값을 가집니다.

| 상관계수 | 의미 |
|---:|---|
| 1에 가까움 | 강한 양의 상관관계 |
| 0에 가까움 | 선형 관계가 약함 |
| -1에 가까움 | 강한 음의 상관관계 |

양의 상관관계는 한 변수가 커질 때 다른 변수도 커지는 경향을 의미합니다.

\`\`\`text
방문 횟수가 많을수록 구매 금액이 커진다.
\`\`\`

음의 상관관계는 한 변수가 커질 때 다른 변수는 작아지는 경향을 의미합니다.

\`\`\`text
가격이 높을수록 구매 수량이 줄어든다.
\`\`\`

상관계수가 0에 가깝다는 것은 두 변수 사이에 직선적인 관계가 약하다는 뜻입니다.

---

### 16.10.2 상관계수 계산

수치형 컬럼 간 상관관계를 계산해보겠습니다.

\`\`\`python
orders[["age", "quantity", "total_price", "visit_count", "satisfaction"]].corr()
\`\`\`

결과는 상관계수 행렬입니다.

각 변수 쌍의 상관계수를 보여줍니다.

특정 두 컬럼의 상관계수만 보고 싶다면 다음처럼 작성할 수 있습니다.

\`\`\`python
orders["visit_count"].corr(orders["total_price"])
\`\`\`

---

### 16.10.3 상관관계 해석

상관계수를 해석할 때는 절댓값의 크기를 참고할 수 있습니다.

| 절댓값 | 해석 예시 |
|---:|---|
| 0.0 ~ 0.2 | 매우 약한 관계 |
| 0.2 ~ 0.4 | 약한 관계 |
| 0.4 ~ 0.6 | 중간 정도 관계 |
| 0.6 ~ 0.8 | 비교적 강한 관계 |
| 0.8 ~ 1.0 | 매우 강한 관계 |

이 기준은 절대적인 규칙은 아닙니다.  
분석 분야와 데이터 특성에 따라 다르게 해석해야 합니다.

기초 과정에서는 다음 정도로 이해하면 충분합니다.

\`\`\`text
양수이면 함께 증가하는 경향이 있다.
음수이면 한쪽이 증가할 때 다른 쪽이 감소하는 경향이 있다.
0에 가까우면 직선적 관계가 약하다.
\`\`\`

---

### 16.10.4 상관관계와 산점도

상관관계를 해석할 때는 산점도를 함께 보는 것이 좋습니다.

\`\`\`python
orders.plot(
    kind="scatter",
    x="visit_count",
    y="total_price"
)

plt.title("Visit Count vs Total Price")
plt.xlabel("Visit Count")
plt.ylabel("Total Price")
plt.show()
\`\`\`

산점도는 두 변수의 관계를 시각적으로 보여줍니다.

상관계수가 하나의 숫자로 관계를 요약한다면, 산점도는 데이터가 실제로 어떻게 퍼져 있는지 보여줍니다.

상관계수만 보고 판단하기보다 산점도를 함께 확인하는 습관이 중요합니다.

---

### 16.10.5 상관관계와 인과관계의 차이

상관관계가 있다고 해서 인과관계가 있다는 뜻은 아닙니다.

예를 들어 방문 횟수와 구매 금액 사이에 양의 상관관계가 있다고 해봅시다.

\`\`\`text
방문 횟수가 많을수록 구매 금액도 높다.
\`\`\`

이 결과만 보고 다음처럼 단정하면 안 됩니다.

\`\`\`text
방문 횟수가 구매 금액을 증가시켰다.
\`\`\`

왜냐하면 다른 가능성도 있기 때문입니다.

\`\`\`text
원래 구매 의향이 높은 고객이 더 많이 방문했을 수 있다.
VIP 고객이 방문도 많고 구매도 많을 수 있다.
특정 이벤트가 방문과 구매를 동시에 증가시켰을 수 있다.
\`\`\`

상관관계는 두 변수 사이에 함께 움직이는 경향이 있음을 보여줄 뿐, 원인을 증명하지는 않습니다.

인과관계를 확인하려면 실험 설계, 통제 변수, 가설검정 등 더 깊은 분석이 필요합니다.  
이 내용은 데이터 분석 고급 과정에서 다루는 것이 좋습니다.

---

### 16.10.6 상관관계 해석 시 주의점

상관관계를 해석할 때는 다음을 주의해야 합니다.

\`\`\`text
상관관계는 인과관계가 아니다.
이상값이 상관계수를 크게 바꿀 수 있다.
비선형 관계는 상관계수로 잘 드러나지 않을 수 있다.
범주형 변수에는 일반적인 corr()를 바로 적용하기 어렵다.
데이터 수가 너무 적으면 상관계수 해석이 불안정하다.
\`\`\`

특히 데이터 수가 적은 예제에서는 상관계수를 일반화하면 안 됩니다.  
상관계수는 충분한 데이터와 함께 해석해야 합니다.

---

## 16.11 그룹별 통계

전체 데이터의 평균도 중요하지만, 그룹별 평균과 분포를 보는 것도 중요합니다.

예를 들어 전체 평균 주문 금액이 125,500원이라고 해도, VIP 고객과 일반 고객의 평균이 크게 다를 수 있습니다.

---

### 16.11.1 고객 등급별 평균 주문 금액

\`\`\`python
grade_price_summary = (
    orders
    .groupby("grade")
    .agg(
        order_count=("order_id", "count"),
        total_sales=("total_price", "sum"),
        avg_order_price=("total_price", "mean"),
        median_order_price=("total_price", "median")
    )
    .reset_index()
)

grade_price_summary
\`\`\`

이 결과를 보면 VIP와 일반 고객의 주문 금액 차이를 확인할 수 있습니다.

평균과 중앙값을 함께 보는 것이 중요합니다.  
일부 큰 주문이 평균을 끌어올릴 수 있기 때문입니다.

---

### 16.11.2 지역별 통계 요약

지역별 주문 금액 통계를 계산해보겠습니다.

\`\`\`python
region_price_summary = (
    orders
    .groupby("region")
    .agg(
        order_count=("order_id", "count"),
        avg_order_price=("total_price", "mean"),
        median_order_price=("total_price", "median"),
        min_order_price=("total_price", "min"),
        max_order_price=("total_price", "max")
    )
    .reset_index()
)

region_price_summary
\`\`\`

이 요약표는 지역별 주문 금액의 특징을 보여줍니다.

---

### 16.11.3 그룹별 표준편차

그룹별로 값이 얼마나 퍼져 있는지 확인하려면 표준편차를 계산할 수 있습니다.

\`\`\`python
region_std_summary = (
    orders
    .groupby("region")["total_price"]
    .std()
    .reset_index(name="std_order_price")
)

region_std_summary
\`\`\`

표준편차가 큰 지역은 주문 금액의 차이가 큰 지역입니다.  
표준편차가 작은 지역은 주문 금액이 비교적 비슷한 지역입니다.

다만 그룹별 데이터 수가 너무 적으면 표준편차 해석이 불안정할 수 있습니다.

---

## 16.12 실무 예제 1: 주문 금액 요약 분석

주문 금액을 중심으로 기본 통계 분석을 해보겠습니다.

---

### 16.12.1 기본 통계 계산

\`\`\`python
price_summary = orders["total_price"].agg([
    "count",
    "mean",
    "median",
    "min",
    "max",
    "std"
])

price_summary
\`\`\`

범위도 추가해보겠습니다.

\`\`\`python
price_range = orders["total_price"].max() - orders["total_price"].min()

price_range
\`\`\`

사분위수도 계산합니다.

\`\`\`python
price_quantiles = orders["total_price"].quantile([0.25, 0.5, 0.75])

price_quantiles
\`\`\`

---

### 16.12.2 요약표 만들기

\`\`\`python
price_report = pd.DataFrame({
    "metric": [
        "count",
        "mean",
        "median",
        "min",
        "max",
        "range",
        "std",
        "q1",
        "q3",
        "iqr"
    ],
    "value": [
        orders["total_price"].count(),
        orders["total_price"].mean(),
        orders["total_price"].median(),
        orders["total_price"].min(),
        orders["total_price"].max(),
        orders["total_price"].max() - orders["total_price"].min(),
        orders["total_price"].std(),
        orders["total_price"].quantile(0.25),
        orders["total_price"].quantile(0.75),
        orders["total_price"].quantile(0.75) - orders["total_price"].quantile(0.25)
    ]
})

price_report
\`\`\`

이 표는 주문 금액을 이해하는 기본 요약 리포트입니다.

---

### 16.12.3 해석 예시

분석 결과를 다음처럼 해석할 수 있습니다.

\`\`\`text
주문 금액의 평균과 중앙값을 비교했을 때 평균이 더 높다면, 일부 고가 주문이 평균을 끌어올리고 있을 가능성이 있다.
최댓값과 최솟값의 차이가 크다면 주문 금액의 범위가 넓다.
IQR을 통해 주문 금액의 가운데 50%가 어느 범위에 있는지 확인할 수 있다.
표준편차가 크다면 주문 금액 간 차이가 크다는 의미이다.
\`\`\`

숫자를 계산하는 것만큼 중요한 것은 해석입니다.

---

## 16.13 실무 예제 2: 고객 등급별 구매 특성 분석

이번에는 고객 등급별 구매 특성을 분석해보겠습니다.

\`\`\`python
grade_summary = (
    orders
    .groupby("grade")
    .agg(
        order_count=("order_id", "count"),
        unique_customers=("customer_id", "nunique"),
        total_sales=("total_price", "sum"),
        avg_order_price=("total_price", "mean"),
        median_order_price=("total_price", "median"),
        avg_visit_count=("visit_count", "mean"),
        avg_satisfaction=("satisfaction", "mean")
    )
    .reset_index()
)

grade_summary
\`\`\`

평균값을 보기 좋게 반올림합니다.

\`\`\`python
grade_summary = grade_summary.round({
    "avg_order_price": 0,
    "median_order_price": 0,
    "avg_visit_count": 1,
    "avg_satisfaction": 1
})

grade_summary
\`\`\`

해석 예시는 다음과 같습니다.

\`\`\`text
VIP 고객은 일반 고객보다 평균 주문 금액이 높게 나타난다.
VIP 고객은 평균 방문 횟수와 만족도도 높을 수 있다.
다만 데이터 수가 적다면 이 결과를 일반화하기 어렵다.
추가 데이터가 필요하며, 실제 분석에서는 기간과 표본 수를 함께 확인해야 한다.
\`\`\`

---

## 16.14 실무 예제 3: 상관관계 분석

방문 횟수, 주문 금액, 만족도 사이의 관계를 확인해보겠습니다.

\`\`\`python
corr_data = orders[["age", "quantity", "total_price", "visit_count", "satisfaction"]]

corr_matrix = corr_data.corr()

corr_matrix
\`\`\`

특정 변수와 주문 금액의 상관관계를 정렬해볼 수 있습니다.

\`\`\`python
corr_with_price = corr_matrix["total_price"].sort_values(ascending=False)

corr_with_price
\`\`\`

자기 자신과의 상관관계는 항상 1입니다.  
실제로는 다른 변수와의 관계를 봐야 합니다.

\`\`\`python
corr_with_price.drop("total_price")
\`\`\`

해석 예시는 다음과 같습니다.

\`\`\`text
visit_count와 total_price의 상관계수가 양수라면, 방문 횟수가 많은 고객일수록 주문 금액이 큰 경향이 있을 수 있다.
하지만 이것이 방문 횟수가 주문 금액을 증가시킨다는 의미는 아니다.
고객 등급, 상품 종류, 이벤트 참여 여부 같은 다른 요인이 영향을 줄 수 있다.
\`\`\`

---

## 16.15 실무 미니 프로젝트: 주문 데이터 기본 통계 리포트 만들기

이번 장에서 배운 내용을 하나로 묶어 기본 통계 리포트를 만들어보겠습니다.

목표는 다음과 같습니다.

\`\`\`text
1. 주문 데이터를 준비한다.
2. 수치형 컬럼의 기본 통계를 확인한다.
3. 주문 금액의 평균, 중앙값, 표준편차, 사분위수를 계산한다.
4. 지역별 주문 수와 비율을 계산한다.
5. 고객 등급별 주문 금액 요약표를 만든다.
6. 주요 수치형 변수의 상관관계를 확인한다.
7. 분석 결과를 간단히 해석한다.
\`\`\`

---

### 16.15.1 데이터 준비

\`\`\`python
import pandas as pd
import numpy as np

orders = pd.DataFrame({
    "order_id": [1001, 1002, 1003, 1004, 1005, 1006, 1007, 1008, 1009, 1010],
    "customer_id": [1, 2, 3, 1, 4, 2, 5, 6, 7, 8],
    "region": ["서울", "부산", "서울", "서울", "대전", "부산", "부산", "서울", "대전", "서울"],
    "grade": ["VIP", "일반", "일반", "VIP", "일반", "일반", "VIP", "일반", "일반", "VIP"],
    "age": [34, 28, 41, 34, 25, 28, 39, 45, 31, 52],
    "quantity": [1, 3, 2, 1, 2, 4, 1, 2, 5, 1],
    "total_price": [300000, 45000, 50000, 220000, 35000, 60000, 180000, 30000, 75000, 260000],
    "visit_count": [12, 5, 7, 12, 3, 5, 10, 4, 6, 15],
    "satisfaction": [5, 4, 3, 5, 4, 3, 5, 3, 4, 5]
})

orders
\`\`\`

---

### 16.15.2 수치형 컬럼 기본 통계

\`\`\`python
numeric_columns = ["age", "quantity", "total_price", "visit_count", "satisfaction"]

numeric_summary = orders[numeric_columns].describe()

numeric_summary
\`\`\`

이 표를 통해 수치형 컬럼의 평균, 표준편차, 최솟값, 사분위수, 최댓값을 확인합니다.

---

### 16.15.3 주문 금액 상세 요약

\`\`\`python
q1 = orders["total_price"].quantile(0.25)
q3 = orders["total_price"].quantile(0.75)
iqr = q3 - q1

price_summary = pd.DataFrame({
    "metric": [
        "주문 수",
        "평균 주문 금액",
        "중앙값",
        "최솟값",
        "최댓값",
        "범위",
        "표준편차",
        "1사분위수",
        "3사분위수",
        "IQR"
    ],
    "value": [
        orders["total_price"].count(),
        orders["total_price"].mean(),
        orders["total_price"].median(),
        orders["total_price"].min(),
        orders["total_price"].max(),
        orders["total_price"].max() - orders["total_price"].min(),
        orders["total_price"].std(),
        q1,
        q3,
        iqr
    ]
})

price_summary
\`\`\`

---

### 16.15.4 지역별 빈도와 비율

\`\`\`python
region_count = orders["region"].value_counts()
region_ratio = orders["region"].value_counts(normalize=True) * 100

region_summary = pd.DataFrame({
    "count": region_count,
    "ratio_percent": region_ratio.round(1)
})

region_summary
\`\`\`

---

### 16.15.5 고객 등급별 주문 금액 요약

\`\`\`python
grade_summary = (
    orders
    .groupby("grade")
    .agg(
        order_count=("order_id", "count"),
        unique_customers=("customer_id", "nunique"),
        total_sales=("total_price", "sum"),
        avg_order_price=("total_price", "mean"),
        median_order_price=("total_price", "median"),
        std_order_price=("total_price", "std"),
        avg_satisfaction=("satisfaction", "mean")
    )
    .reset_index()
)

grade_summary = grade_summary.round({
    "avg_order_price": 0,
    "median_order_price": 0,
    "std_order_price": 0,
    "avg_satisfaction": 1
})

grade_summary
\`\`\`

---

### 16.15.6 상관관계 확인

\`\`\`python
corr_matrix = orders[numeric_columns].corr()

corr_matrix
\`\`\`

주문 금액과 다른 변수의 상관관계를 따로 확인합니다.

\`\`\`python
corr_with_price = (
    corr_matrix["total_price"]
    .drop("total_price")
    .sort_values(ascending=False)
)

corr_with_price
\`\`\`

---

### 16.15.7 결과 해석 예시

분석 결과를 다음과 같이 정리할 수 있습니다.

\`\`\`text
주문 금액의 평균과 중앙값을 비교해보면 평균이 더 높게 나타난다.
이는 일부 고가 주문이 평균을 끌어올리고 있을 가능성을 의미한다.

지역별 주문 분포를 보면 서울 주문 비중이 가장 높다.
다만 예제 데이터의 행 수가 적으므로 실제 의사결정에는 더 많은 데이터가 필요하다.

고객 등급별 요약 결과, VIP 고객의 평균 주문 금액이 일반 고객보다 높게 나타난다.
또한 VIP 고객의 평균 만족도도 높게 나타날 수 있다.

방문 횟수와 주문 금액의 상관관계가 양수라면 방문 횟수가 많은 고객일수록 구매 금액이 높은 경향이 있을 수 있다.
하지만 상관관계는 인과관계를 의미하지 않으므로 추가 분석이 필요하다.
\`\`\`

---

### 16.15.8 처리 기준 문서화

분석 노트북이나 보고서에는 다음처럼 기준을 남길 수 있습니다.

\`\`\`text
기본 통계 분석 기준
- 수치형 컬럼은 age, quantity, total_price, visit_count, satisfaction으로 정의했다.
- 주문 금액은 평균, 중앙값, 표준편차, 사분위수, IQR로 요약했다.
- 지역 분포는 value_counts와 normalize=True를 사용해 빈도와 비율을 계산했다.
- 고객 등급별 비교는 grade 기준 groupby로 수행했다.
- 상관관계는 수치형 컬럼을 대상으로 corr()를 사용해 계산했다.
- 상관관계는 인과관계로 해석하지 않았다.
\`\`\`

---

## 16.16 기본 통계 해석 시 자주 하는 실수

기본 통계는 쉽고 강력하지만 잘못 해석하기도 쉽습니다.

---

### 16.16.1 평균만 보고 판단하는 실수

평균은 이상값의 영향을 크게 받습니다.

\`\`\`python
sales = pd.Series([10000, 12000, 15000, 13000, 11000, 1000000])

sales.mean()
sales.median()
\`\`\`

평균이 높다고 해서 대부분의 값이 높은 것은 아닐 수 있습니다.  
주문 금액, 소득, 방문 횟수처럼 치우친 데이터에서는 중앙값도 함께 확인해야 합니다.

---

### 16.16.2 데이터 수를 확인하지 않는 실수

데이터가 너무 적으면 평균이나 비율을 일반화하기 어렵습니다.

예를 들어 VIP 고객 주문이 2건뿐인데 평균 주문 금액이 높다고 해서 VIP 전체의 특성이라고 단정할 수 없습니다.

항상 데이터 개수도 함께 확인해야 합니다.

\`\`\`python
orders.groupby("grade")["order_id"].count()
\`\`\`

---

### 16.16.3 비율과 개수를 함께 보지 않는 실수

비율만 보면 착각할 수 있습니다.

예를 들어 어떤 지역의 VIP 비율이 100%라고 해도, 그 지역 데이터가 1건뿐이라면 의미가 약합니다.

따라서 비율과 개수를 함께 봐야 합니다.

\`\`\`python
pd.crosstab(orders["region"], orders["grade"])
pd.crosstab(orders["region"], orders["grade"], normalize="index")
\`\`\`

---

### 16.16.4 상관관계를 인과관계로 해석하는 실수

상관관계는 두 변수가 함께 움직이는 경향을 나타냅니다.  
하지만 원인을 증명하지는 않습니다.

\`\`\`text
방문 횟수와 주문 금액의 상관관계가 높다.
\`\`\`

이것은 방문 횟수가 주문 금액을 증가시켰다는 뜻이 아닙니다.

다른 요인이 두 변수에 동시에 영향을 주고 있을 수 있습니다.  
인과관계를 확인하려면 별도의 분석 설계가 필요합니다.

---

### 16.16.5 통계값만 보고 분포를 보지 않는 실수

평균, 중앙값, 표준편차만으로 데이터의 모양을 모두 알 수 없습니다.

가능하면 히스토그램이나 박스플롯을 함께 확인해야 합니다.

\`\`\`python
orders["total_price"].plot(kind="hist", bins=5)
orders["total_price"].plot(kind="box")
\`\`\`

시각화는 17장에서 더 자세히 다룹니다.

---

### 16.16.6 결측치와 이상값 처리 전 통계값을 그대로 믿는 실수

결측치, 중복값, 이상값이 정리되지 않은 상태에서 계산한 통계값은 왜곡될 수 있습니다.

기본 통계 분석 전에 다음을 먼저 확인하는 것이 좋습니다.

\`\`\`python
df.isna().sum()
df.duplicated().sum()
df.describe()
\`\`\`

기초 통계는 데이터 품질 점검과 함께 사용해야 합니다.

---

## 16.17 핵심 정리

이번 장에서는 데이터 분석에서 자주 사용하는 기본 통계를 배웠습니다.

기술통계는 가지고 있는 데이터를 요약하고 설명하는 통계입니다.  
복잡한 모델을 만들기 전에 데이터의 기본 상태를 파악하는 데 사용합니다.

수치형 데이터의 기본 통계는 \`describe()\`로 확인할 수 있습니다.

\`\`\`python
df.describe()
\`\`\`

중심 경향을 나타내는 대표값에는 평균, 중앙값, 최빈값이 있습니다.

\`\`\`python
df["col"].mean()
df["col"].median()
df["col"].mode()
\`\`\`

평균은 이해하기 쉽지만 이상값의 영향을 크게 받습니다.  
따라서 주문 금액이나 소득처럼 치우친 데이터에서는 중앙값도 함께 확인하는 것이 좋습니다.

데이터의 퍼짐 정도를 확인할 때는 범위, 분산, 표준편차, 사분위수, IQR을 사용할 수 있습니다.

\`\`\`python
df["col"].min()
df["col"].max()
df["col"].std()
df["col"].quantile([0.25, 0.5, 0.75])
\`\`\`

범주형 데이터는 빈도와 비율로 요약합니다.

\`\`\`python
df["category"].value_counts()
df["category"].value_counts(normalize=True)
\`\`\`

두 수치형 변수 사이의 관계는 상관계수로 확인할 수 있습니다.

\`\`\`python
df[["x", "y"]].corr()
\`\`\`

하지만 상관관계는 인과관계가 아닙니다.  
상관계수가 높다고 해서 한 변수가 다른 변수의 원인이라고 단정하면 안 됩니다.

기본 통계는 데이터 분석의 출발점입니다.  
통계값을 계산하는 것에서 끝나지 말고, 데이터의 맥락과 함께 해석하는 습관이 필요합니다.

---

## 16.18 연습문제

### 문제 1. 개념 확인

기술통계에 대한 설명으로 가장 적절한 것은 무엇인가요?

A. 표본으로 모집단을 추정하는 통계이다.  
B. 데이터를 요약하고 설명하는 통계이다.  
C. 데이터를 무작위로 섞는 방법이다.  
D. 결측치를 제거하는 방법이다.

---

### 문제 2. 코드 작성

다음 데이터에서 \`sales\` 컬럼의 평균을 구하세요.

\`\`\`python
df = pd.DataFrame({
    "sales": [10000, 20000, 30000, 40000]
})
\`\`\`

---

### 문제 3. 코드 작성

다음 데이터에서 \`sales\` 컬럼의 중앙값을 구하세요.

\`\`\`python
df = pd.DataFrame({
    "sales": [10000, 20000, 30000, 1000000]
})
\`\`\`

---

### 문제 4. 개념 확인

평균과 중앙값 중 이상값의 영향을 더 크게 받는 것은 무엇인가요?

---

### 문제 5. 코드 작성

다음 데이터에서 \`score\` 컬럼의 최솟값, 최댓값, 표준편차를 구하세요.

\`\`\`python
df = pd.DataFrame({
    "score": [70, 80, 90, 100]
})
\`\`\`

---

### 문제 6. 코드 작성

다음 데이터에서 \`amount\` 컬럼의 1사분위수, 중앙값, 3사분위수를 구하세요.

\`\`\`python
df = pd.DataFrame({
    "amount": [10000, 15000, 20000, 25000, 30000, 100000]
})
\`\`\`

---

### 문제 7. 코드 작성

다음 데이터에서 \`region\` 컬럼의 빈도와 비율을 구하세요.

\`\`\`python
df = pd.DataFrame({
    "region": ["서울", "부산", "서울", "대전", "서울", "부산"]
})
\`\`\`

---

### 문제 8. 코드 작성

다음 데이터에서 \`visit_count\`와 \`total_price\`의 상관계수를 구하세요.

\`\`\`python
df = pd.DataFrame({
    "visit_count": [1, 2, 3, 4, 5],
    "total_price": [10000, 20000, 25000, 40000, 50000]
})
\`\`\`

---

### 문제 9. 개념 확인

상관관계와 인과관계의 차이를 설명하세요.

---

### 문제 10. 실무형 문제

다음 주문 데이터에서 기본 통계 리포트를 작성하세요.

처리 기준은 다음과 같습니다.

\`\`\`text
- total_price의 평균, 중앙값, 최솟값, 최댓값, 표준편차를 계산한다.
- grade별 주문 수와 평균 주문 금액을 계산한다.
- region별 빈도와 비율을 계산한다.
- visit_count와 total_price의 상관계수를 계산한다.
\`\`\`

\`\`\`python
orders = pd.DataFrame({
    "grade": ["VIP", "일반", "일반", "VIP", "일반", "VIP"],
    "region": ["서울", "부산", "서울", "서울", "대전", "부산"],
    "visit_count": [10, 3, 5, 12, 4, 9],
    "total_price": [300000, 40000, 50000, 250000, 30000, 180000]
})
\`\`\`

---

## 16.19 정답 및 해설

### 문제 1 정답

정답: B

기술통계는 가지고 있는 데이터를 요약하고 설명하는 통계입니다.  
평균, 중앙값, 표준편차, 빈도, 비율 등이 대표적인 기술통계입니다.

---

### 문제 2 정답

\`\`\`python
df["sales"].mean()
\`\`\`

평균은 모든 값을 더한 뒤 개수로 나눈 값입니다.

---

### 문제 3 정답

\`\`\`python
df["sales"].median()
\`\`\`

중앙값은 데이터를 크기순으로 정렬했을 때 가운데에 있는 값입니다.  
이상값이 있을 때 평균보다 일반적인 수준을 더 잘 보여줄 수 있습니다.

---

### 문제 4 정답

정답: 평균

평균은 모든 값을 계산에 사용하므로 매우 큰 값이나 작은 값의 영향을 크게 받습니다.  
중앙값은 가운데 위치의 값이므로 이상값의 영향을 상대적으로 덜 받습니다.

---

### 문제 5 정답

\`\`\`python
df["score"].min()
df["score"].max()
df["score"].std()
\`\`\`

또는 한 번에 확인할 수도 있습니다.

\`\`\`python
df["score"].agg(["min", "max", "std"])
\`\`\`

---

### 문제 6 정답

\`\`\`python
df["amount"].quantile([0.25, 0.5, 0.75])
\`\`\`

\`0.25\`는 1사분위수, \`0.5\`는 중앙값, \`0.75\`는 3사분위수입니다.

---

### 문제 7 정답

빈도:

\`\`\`python
df["region"].value_counts()
\`\`\`

비율:

\`\`\`python
df["region"].value_counts(normalize=True)
\`\`\`

백분율로 보고 싶다면 다음처럼 작성할 수 있습니다.

\`\`\`python
(df["region"].value_counts(normalize=True) * 100).round(1)
\`\`\`

---

### 문제 8 정답

\`\`\`python
df["visit_count"].corr(df["total_price"])
\`\`\`

또는 두 컬럼의 상관행렬을 볼 수도 있습니다.

\`\`\`python
df[["visit_count", "total_price"]].corr()
\`\`\`

---

### 문제 9 정답

상관관계는 두 변수가 함께 움직이는 경향을 의미합니다.  
예를 들어 방문 횟수가 많을수록 구매 금액이 큰 경향이 있다면 양의 상관관계가 있다고 할 수 있습니다.

하지만 인과관계는 한 변수가 다른 변수의 원인이라는 뜻입니다.  
상관관계가 있다고 해서 반드시 인과관계가 있는 것은 아닙니다.

예를 들어 방문 횟수와 구매 금액의 상관관계가 높아도, 방문 횟수가 구매 금액을 증가시켰다고 단정할 수는 없습니다.  
고객 등급이나 구매 의향 같은 다른 요인이 영향을 주었을 수 있습니다.

---

### 문제 10 정답

\`\`\`python
price_summary = orders["total_price"].agg([
    "mean",
    "median",
    "min",
    "max",
    "std"
])

price_summary
\`\`\`

고객 등급별 주문 수와 평균 주문 금액:

\`\`\`python
grade_summary = (
    orders
    .groupby("grade")
    .agg(
        order_count=("total_price", "count"),
        avg_order_price=("total_price", "mean")
    )
    .reset_index()
)

grade_summary
\`\`\`

지역별 빈도와 비율:

\`\`\`python
region_count = orders["region"].value_counts()
region_ratio = orders["region"].value_counts(normalize=True) * 100

region_summary = pd.DataFrame({
    "count": region_count,
    "ratio_percent": region_ratio.round(1)
})

region_summary
\`\`\`

방문 횟수와 주문 금액의 상관계수:

\`\`\`python
visit_price_corr = orders["visit_count"].corr(orders["total_price"])

visit_price_corr
\`\`\`

이 분석 결과를 해석할 때는 평균과 중앙값의 차이, 등급별 데이터 수, 지역별 표본 수, 상관관계와 인과관계의 차이를 함께 고려해야 합니다.

---

## 16.20 다음 장 예고

이번 장에서는 기본 통계를 사용해 데이터를 요약하고 해석하는 방법을 배웠습니다.

다음 장에서는 **데이터 시각화 기초**를 배웁니다.

통계값은 데이터를 숫자로 요약해줍니다.  
하지만 숫자만으로는 데이터의 분포나 패턴을 직관적으로 이해하기 어려울 때가 많습니다.

예를 들어 평균과 표준편차를 보는 것보다 히스토그램을 보면 데이터가 어느 구간에 몰려 있는지 더 쉽게 알 수 있습니다.  
상관계수만 보는 것보다 산점도를 보면 두 변수의 관계를 더 명확히 볼 수 있습니다.

다음 장에서는 다음 내용을 다룹니다.

- 시각화가 필요한 이유
- matplotlib 기초
- pandas plot
- seaborn 기초
- 선 그래프
- 막대 그래프
- 히스토그램
- 박스플롯
- 산점도
- 히트맵
- 그래프 선택 기준
- 시각화 실무 주의점

기본 통계가 데이터를 숫자로 요약하는 방법이라면, 시각화는 데이터를 그림으로 이해하고 전달하는 방법입니다.

---

## 참고 문서

- pandas 공식 문서: \`DataFrame.describe\`
- pandas 공식 문서: \`Series.mean\`
- pandas 공식 문서: \`Series.median\`
- pandas 공식 문서: \`Series.mode\`
- pandas 공식 문서: \`Series.std\`
- pandas 공식 문서: \`Series.quantile\`
- pandas 공식 문서: \`Series.value_counts\`
- pandas 공식 문서: \`DataFrame.corr\`
`;export{e as default};