var e=`<!-- 원본: python_data_analysis_basic_chapter_3_book.md / 세부 장: 3-10 -->

# 3.10 조건 기반 선택

데이터 분석에서는 전체 데이터보다 조건에 맞는 데이터만 보고 싶을 때가 많다.

예를 들어 다음과 같은 질문을 할 수 있다.

- 80점 이상인 점수만 보고 싶다.
- 매출이 100,000 이상인 값만 찾고 싶다.
- 온도가 30도 이상인 날만 선택하고 싶다.
- 결측치가 아닌 값만 분석하고 싶다.

NumPy에서는 조건식을 사용해 이런 작업을 할 수 있다.

---

## 3.10.1 조건식 만들기

다음 배열을 보자.

\`\`\`python
scores = np.array([80, 90, 75, 100, 60])

print(scores >= 80)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
[ True  True False  True False]
\`\`\`

각 값이 80 이상인지 검사한 결과가 불리언 배열로 반환되었다.

\`\`\`text
80  >= 80 → True
90  >= 80 → True
75  >= 80 → False
100 >= 80 → True
60  >= 80 → False
\`\`\`

---

## 3.10.2 Boolean indexing

조건식의 결과를 사용하면 조건에 맞는 값만 선택할 수 있다.

\`\`\`python
scores = np.array([80, 90, 75, 100, 60])

high_scores = scores[scores >= 80]

print(high_scores)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
[ 80  90 100]
\`\`\`

80 이상인 값만 선택되었다. 이런 방식을 **Boolean indexing**이라고 한다.

---

## 3.10.3 여러 조건 사용하기

여러 조건을 함께 사용할 때는 \`&\`와 \`|\`를 사용한다.

- \`&\` : 그리고
- \`|\` : 또는

조건마다 괄호를 감싸야 한다.

\`\`\`python
scores = np.array([80, 90, 75, 100, 60])

result = scores[(scores >= 80) & (scores <= 95)]

print(result)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
[80 90]
\`\`\`

80 이상이면서 95 이하인 값만 선택했다.

또는 조건을 사용해 보자.

\`\`\`python
result = scores[(scores < 70) | (scores >= 95)]

print(result)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
[100  60]
\`\`\`

70 미만이거나 95 이상인 값이 선택되었다.

주의할 점은 파이썬의 일반 논리 연산자인 \`and\`, \`or\`를 배열 조건에 그대로 사용하면 안 된다는 것이다. NumPy 배열에서는 조건 결합에 \`&\`, \`|\`를 사용한다.

---

## 3.10.4 조건에 맞는 값 변경하기

조건에 맞는 값만 바꿀 수도 있다.

\`\`\`python
scores = np.array([80, 90, 75, 100, 60])

scores[scores < 70] = 0

print(scores)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
[ 80  90  75 100   0]
\`\`\`

70점 미만인 값이 0으로 바뀌었다.

이런 방식은 데이터 전처리에서 자주 사용된다. 예를 들어 음수 매출을 0으로 바꾸거나, 기준을 넘는 값을 별도로 표시할 수 있다.

---

## 3.10.5 \`np.where()\`

\`np.where()\`는 조건에 따라 다른 값을 선택할 때 사용한다.

\`\`\`python
scores = np.array([80, 90, 75, 100, 60])

result = np.where(scores >= 80, "합격", "불합격")

print(result)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
['합격' '합격' '불합격' '합격' '불합격']
\`\`\`

구조는 다음과 같다.

\`\`\`python
np.where(조건, 조건이 참일 때 값, 조건이 거짓일 때 값)
\`\`\`

숫자 값을 바꾸는 데도 사용할 수 있다.

\`\`\`python
prices = np.array([10000, 20000, 30000])

sale_prices = np.where(prices >= 20000, prices * 0.9, prices)

print(sale_prices)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
[10000. 18000. 27000.]
\`\`\`

가격이 20,000 이상인 상품에만 10% 할인을 적용했다.

---

## 3.10.6 조건 기반 선택 실무 예제

다음은 주문 금액 배열이다.

\`\`\`python
order_amounts = np.array([12000, 55000, 32000, 8000, 76000, 23000])
\`\`\`

30,000원 이상인 주문만 선택해 보자.

\`\`\`python
large_orders = order_amounts[order_amounts >= 30000]

print(large_orders)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
[55000 32000 76000]
\`\`\`

무료배송 여부를 만들어 보자.

\`\`\`python
shipping = np.where(order_amounts >= 30000, "무료배송", "배송비 부과")

print(shipping)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
['배송비 부과' '무료배송' '무료배송' '배송비 부과' '무료배송' '배송비 부과']
\`\`\`

이 방식은 나중에 pandas에서 조건 기반 컬럼을 만들 때도 자주 사용된다.

---
`;export{e as default};