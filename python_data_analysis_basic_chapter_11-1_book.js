var e=`<!-- 원본: python_data_analysis_basic_chapter_11_book.md / 세부 장: 11-1 -->

## 11.0 들어가며

데이터 분석에서 결측치만큼 자주 만나는 문제가 있습니다.  
바로 **중복값**과 **이상값**입니다.

중복값은 같은 데이터가 여러 번 들어간 상태를 말합니다.  
예를 들어 같은 주문번호가 두 번 들어가 있거나, 같은 고객 정보가 여러 번 저장되어 있을 수 있습니다.

이상값은 일반적인 범위를 벗어난 값을 말합니다.  
예를 들어 상품 가격이 대부분 1만 원에서 20만 원 사이인데, 어떤 상품 가격이 9,999,999원으로 들어가 있다면 이상값일 가능성이 있습니다.

중복값과 이상값은 분석 결과에 큰 영향을 줄 수 있습니다.

예를 들어 같은 주문이 두 번 들어가 있으면 매출이 실제보다 크게 계산됩니다.  
고객 데이터가 중복되어 있으면 고객 수가 실제보다 많게 계산됩니다.  
비정상적으로 큰 주문 금액이 있으면 평균 주문 금액이 왜곡될 수 있습니다.

데이터 분석에서 중요한 것은 단순히 코드를 실행하는 것이 아닙니다.  
데이터가 신뢰할 수 있는 상태인지 확인하고, 문제가 있는 값을 어떻게 처리할지 판단하는 과정이 필요합니다.

이번 장에서는 pandas를 사용해 중복값과 이상값을 확인하고 처리하는 기본 방법을 배웁니다.

---

# 11.1 중복값이란?

중복값은 같은 데이터가 두 번 이상 존재하는 상태입니다.

중복값은 여러 형태로 나타납니다.

첫째, 모든 컬럼의 값이 완전히 같은 행이 있을 수 있습니다.

\`\`\`text
order_id | customer | total_price
1001     | 민수      | 30000
1001     | 민수      | 30000
\`\`\`

둘째, 특정 컬럼만 중복될 수도 있습니다.

\`\`\`text
customer_id | name | email
1           | 민수  | minsu@example.com
1           | 김민수 | minsu@example.com
\`\`\`

셋째, 실제로는 같은 데이터인데 표기 방식이 달라 중복으로 바로 잡히지 않는 경우도 있습니다.

\`\`\`text
서울
서울시
서울특별시
\`\`\`

또는 다음처럼 공백이나 대소문자 차이 때문에 서로 다른 값으로 보일 수 있습니다.

\`\`\`text
user@example.com
User@example.com
user@example.com 
\`\`\`

이번 장에서는 먼저 pandas가 바로 찾을 수 있는 명확한 중복값을 다룹니다.  
표기 차이 때문에 발생하는 복잡한 중복 문제는 문자열 처리와 데이터 클리닝 과정에서 함께 다루면 좋습니다.

---

### 11.1.1 중복값이 생기는 이유

중복값은 다양한 이유로 생깁니다.

| 원인 | 예시 |
|---|---|
| 데이터 입력 실수 | 같은 고객을 두 번 입력 |
| 파일 병합 문제 | 같은 월 데이터 파일을 두 번 합침 |
| API 중복 수집 | 같은 페이지를 여러 번 요청 |
| 시스템 오류 | 주문 데이터가 중복 저장 |
| 기준 컬럼 누락 | 같은 고객인데 ID가 다르게 부여됨 |
| 데이터 결합 문제 | 잘못된 key로 merge하여 행이 늘어남 |

중복값이 항상 나쁜 것은 아닙니다.  
예를 들어 같은 고객이 여러 번 주문한 것은 정상입니다.

\`\`\`text
고객명: 민수
주문 1: 2026-01-01
주문 2: 2026-01-05
주문 3: 2026-01-10
\`\`\`

이 경우 고객명이 여러 번 등장한다고 해서 중복 오류는 아닙니다.  
같은 고객이 여러 번 주문한 정상 데이터입니다.

따라서 중복값을 확인할 때는 반드시 기준을 정해야 합니다.

\`\`\`text
무엇을 기준으로 중복이라고 볼 것인가?
\`\`\`

주문 데이터에서는 \`order_id\`가 같으면 중복일 수 있습니다.  
고객 데이터에서는 \`customer_id\`나 \`email\`이 같으면 중복일 수 있습니다.  
상품 데이터에서는 \`product_id\`가 같으면 중복일 수 있습니다.

---

### 11.1.2 중복값이 분석에 미치는 영향

중복값은 분석 결과를 왜곡할 수 있습니다.

예를 들어 주문 데이터에서 같은 주문이 두 번 들어가 있으면 총 매출이 실제보다 크게 계산됩니다.

\`\`\`python
import pandas as pd

orders = pd.DataFrame({
    "order_id": [1001, 1002, 1002, 1003],
    "customer": ["민수", "지영", "지영", "철수"],
    "total_price": [30000, 50000, 50000, 20000]
})

orders["total_price"].sum()
\`\`\`

\`order_id\` 1002가 중복되었기 때문에 매출 합계가 실제보다 50,000원 더 크게 계산됩니다.

고객 데이터에서도 비슷한 문제가 생깁니다.

\`\`\`python
customers = pd.DataFrame({
    "customer_id": [1, 2, 2, 3],
    "name": ["민수", "지영", "지영", "철수"]
})

customers["customer_id"].nunique()
\`\`\`

고객 수를 고유 ID 기준으로 계산하면 3명입니다.  
하지만 단순히 행 개수를 세면 4명으로 잘못 볼 수 있습니다.

중복값은 특히 다음 분석에 큰 영향을 줍니다.

- 총합
- 평균
- 개수
- 고객 수
- 주문 수
- 상품 수
- 전환율
- 재구매율
- 중복 제거 후 고유 개수 분석

중복값을 처리하기 전에 먼저 “중복이 분석 목적상 문제인지” 판단해야 합니다.

---
`;export{e as default};