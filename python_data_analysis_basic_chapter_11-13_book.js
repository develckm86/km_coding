var e=`<!-- 원본: python_data_analysis_basic_chapter_11_book.md / 세부 장: 11-13 -->

# 11.13 연습문제

### 문제 1. 개념 확인

중복값에 대한 설명으로 가장 적절한 것은 무엇인가요?

A. 항상 결측치를 의미한다.  
B. 같은 데이터가 두 번 이상 존재하는 상태를 말한다.  
C. 평균보다 작은 값을 말한다.  
D. 정렬 후 첫 번째 행을 말한다.

---

### 문제 2. 코드 작성

다음 DataFrame에서 전체 행 기준으로 중복 여부를 확인하는 코드를 작성하세요.

\`\`\`python
df = pd.DataFrame({
    "id": [1, 2, 2, 3],
    "value": ["A", "B", "B", "C"]
})
\`\`\`

---

### 문제 3. 코드 작성

위 데이터에서 \`id\` 컬럼 기준으로 중복된 모든 행을 확인하는 코드를 작성하세요.  
첫 번째 중복 행도 함께 표시되어야 합니다.

---

### 문제 4. 코드 작성

다음 고객 데이터에서 \`customer_id\` 기준으로 중복을 제거하세요.  
첫 번째 행을 남기면 됩니다.

\`\`\`python
customers = pd.DataFrame({
    "customer_id": [1, 2, 2, 3],
    "name": ["민수", "지영", "지영", "철수"]
})
\`\`\`

---

### 문제 5. 개념 확인

\`drop_duplicates(subset=["email"], keep="last")\`의 의미를 설명하세요.

---

### 문제 6. 코드 작성

다음 주문 데이터에서 \`amount\` 컬럼의 최솟값, 최댓값, 평균, 중앙값을 확인하는 코드를 작성하세요.

\`\`\`python
orders = pd.DataFrame({
    "order_id": [1, 2, 3, 4, 5],
    "amount": [10000, 12000, 15000, 11000, 1000000]
})
\`\`\`

---

### 문제 7. 코드 작성

위 주문 데이터에서 \`amount\` 기준으로 큰 값부터 정렬해 상위 3개 행을 확인하세요.

---

### 문제 8. 코드 작성

다음 코드의 빈칸을 채워 IQR 기준 이상값 후보를 찾으세요.

\`\`\`python
q1 = orders["amount"].quantile(0.25)
q3 = orders["amount"].quantile(0.75)
iqr = q3 - q1

lower_bound = q1 - 1.5 * iqr
upper_bound = q3 + 1.5 * iqr

outliers = orders[
    (orders["amount"] < ________) |
    (orders["amount"] > ________)
]
\`\`\`

---

### 문제 9. 개념 확인

IQR 기준으로 이상값 후보로 탐지된 값은 반드시 삭제해야 하나요?  
그 이유를 설명하세요.

---

### 문제 10. 실무형 문제

다음 주문 데이터에서 중복값과 이상값을 점검하세요.

처리 기준은 다음과 같습니다.

\`\`\`text
- order_id 기준으로 중복을 확인한다.
- 중복된 order_id는 첫 번째 행만 남긴다.
- amount 컬럼을 기준으로 IQR 방식의 이상값 후보를 찾는다.
- 이상값 후보는 삭제하지 않고 is_outlier 컬럼으로 표시한다.
\`\`\`

\`\`\`python
orders = pd.DataFrame({
    "order_id": [1, 2, 2, 3, 4, 5],
    "customer": ["A", "B", "B", "C", "D", "E"],
    "amount": [10000, 20000, 20000, 15000, 12000, 1000000]
})
\`\`\`

---
`;export{e as default};