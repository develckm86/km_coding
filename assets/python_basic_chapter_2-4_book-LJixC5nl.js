var e=`<!-- 원본: python_basic_chapter_2_book.md / 세부 장: 2-4 -->

# 2.4 수 데이터

## 수 데이터란 무엇인가

수 데이터는 숫자를 표현하는 데이터이다. 프로그램에서는 가격, 수량, 나이, 점수, 할인율, 평균, 좌표처럼 많은 숫자를 다룬다.

파이썬의 대표적인 수 자료형은 정수와 실수이다.

| 자료형 | 의미 | 예시 |
|---|---|---|
| \`int\` | 정수 | \`10\`, \`0\`, \`-5\` |
| \`float\` | 실수 | \`3.14\`, \`0.5\`, \`-2.7\` |

정수는 소수점이 없는 수이고, 실수는 소수점이 있는 수이다.

\`\`\`python
count = 10
rate = 0.15

print(type(count))
print(type(rate))
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
<class 'int'>
<class 'float'>
\`\`\`

## 정수 \`int\`

정수는 소수점이 없는 수이다. 양수, 음수, 0이 모두 정수이다.

\`\`\`python
age = 25
count = 100
zero = 0
temperature = -5
\`\`\`

정수는 개수, 수량, 인원, 순번, 페이지 번호처럼 딱 떨어지는 값을 표현할 때 사용한다.

\`\`\`python
product_count = 35
page_number = 2
member_count = 150
\`\`\`

## 정수 선언법

정수는 숫자를 그대로 쓰면 된다.

\`\`\`python
price = 10000
quantity = 3
\`\`\`

음수는 앞에 \`-\`를 붙인다.

\`\`\`python
temperature = -10
\`\`\`

큰 숫자는 읽기 좋게 밑줄 \`_\`을 넣어 표현할 수 있다.

\`\`\`python
sales = 1_000_000
print(sales)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
1000000
\`\`\`

밑줄은 숫자를 읽기 좋게 만들 뿐 실제 값에는 영향을 주지 않는다.

## 정수 선언 방식

일반적으로 사용하는 숫자는 10진수이다.

\`\`\`python
decimal_number = 100
\`\`\`

파이썬에서는 2진수, 8진수, 16진수도 표현할 수 있다.

\`\`\`python
binary_number = 0b1010
octal_number = 0o12
hex_number = 0xA

print(binary_number)
print(octal_number)
print(hex_number)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
10
10
10
\`\`\`

- \`0b\`로 시작하면 2진수이다.
- \`0o\`로 시작하면 8진수이다.
- \`0x\`로 시작하면 16진수이다.

처음 파이썬을 배울 때 진법 표현을 많이 사용할 일은 많지 않다. 하지만 색상 코드, 권한 값, 네트워크, 하드웨어 관련 작업을 하다 보면 16진수나 2진수를 만날 수 있다.

## 파이썬 정수의 크기

파이썬의 정수는 매우 큰 수를 다룰 수 있다. 다른 언어에서는 정수의 크기에 제한이 있는 경우가 많지만, 파이썬은 메모리가 허용하는 범위 안에서 큰 정수를 표현할 수 있다.

\`\`\`python
big_number = 123456789012345678901234567890
print(big_number)
\`\`\`

실행 결과는 입력한 큰 숫자가 그대로 출력된다.

이 특징은 편리하지만, 숫자가 너무 커지면 계산에 시간과 메모리가 많이 필요할 수 있다.

## 실수 \`float\`

실수는 소수점이 있는 수이다.

\`\`\`python
height = 175.5
discount_rate = 0.15
temperature = -3.7
\`\`\`

실수는 비율, 평균, 온도, 좌표, 할인율처럼 소수점이 필요한 값을 표현할 때 사용한다.

\`\`\`python
average_score = 87.5
latitude = 37.5665
exchange_rate = 1380.25
\`\`\`

## 실수 선언법

실수는 소수점을 포함해서 작성한다.

\`\`\`python
pi = 3.14
rate = 0.05
value = 10.0
\`\`\`

\`10\`은 정수이고, \`10.0\`은 실수이다.

\`\`\`python
print(type(10))
print(type(10.0))
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
<class 'int'>
<class 'float'>
\`\`\`

## 지수 표기법

매우 크거나 작은 실수는 지수 표기법으로 표현할 수 있다.

\`\`\`python
value1 = 1.5e3
value2 = 2.5e-2

print(value1)
print(value2)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
1500.0
0.025
\`\`\`

\`1.5e3\`은 \`1.5 × 10³\`을 의미하고, \`2.5e-2\`는 \`2.5 × 10⁻²\`를 의미한다.

데이터 분석이나 과학 계산에서는 지수 표기법을 자주 볼 수 있다.

## 실수 계산의 오차

실수를 사용할 때는 계산 오차에 주의해야 한다. 컴퓨터는 일부 소수를 정확하게 표현하지 못한다.

\`\`\`python
print(0.1 + 0.2)
\`\`\`

실행 결과는 다음과 비슷하게 나올 수 있다.

\`\`\`text
0.30000000000000004
\`\`\`

사람이 보기에는 \`0.1 + 0.2\`가 당연히 \`0.3\`이지만, 컴퓨터 내부 표현 방식 때문에 아주 작은 오차가 생길 수 있다.

실무에서 금액을 계산할 때는 이런 오차가 문제가 될 수 있다. 예를 들어 원 단위 금액은 가능하면 정수로 다루는 편이 안전하다. 더 정밀한 소수 계산이 필요할 때는 별도의 방법을 사용해야 한다. 이 수업의 기초 단계에서는 “실수 계산에는 미세한 오차가 생길 수 있다”는 점을 기억하면 충분하다.

## 정수와 실수의 변환

정수를 실수로 바꿀 때는 \`float()\`을 사용한다.

\`\`\`python
number = 10
result = float(number)

print(result)
print(type(result))
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
10.0
<class 'float'>
\`\`\`

실수를 정수로 바꿀 때는 \`int()\`를 사용한다.

\`\`\`python
value = 3.99
result = int(value)

print(result)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
3
\`\`\`

\`int()\`는 반올림하지 않고 소수점 아래를 버린다. 반올림을 하고 싶다면 \`round()\`를 사용해야 한다.

문자열로 된 숫자를 정수나 실수로 바꿀 수도 있다.

\`\`\`python
price_text = "10000"
rate_text = "0.15"

price = int(price_text)
rate = float(rate_text)

print(price)
print(rate)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
10000
0.15
\`\`\`

하지만 숫자로 바꿀 수 없는 문자열을 변환하려고 하면 오류가 발생한다.

\`\`\`python
price_text = "10,000"
price = int(price_text)
\`\`\`

이 코드는 쉼표 때문에 바로 정수로 변환할 수 없다. 먼저 쉼표를 제거해야 한다.

\`\`\`python
price_text = "10,000"
price = int(price_text.replace(",", ""))

print(price)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
10000
\`\`\`

이처럼 문자열 처리와 숫자 변환은 실무 데이터 처리에서 함께 사용되는 경우가 많다.

## 2.4 핵심 정리

- 수 데이터는 계산 가능한 숫자 데이터이다.
- 정수 \`int\`는 소수점이 없는 수이다.
- 실수 \`float\`는 소수점이 있는 수이다.
- 정수는 개수, 수량, 순번 등에 사용한다.
- 실수는 비율, 평균, 좌표, 온도 등에 사용한다.
- 정수는 10진수 외에 2진수, 8진수, 16진수로도 표현할 수 있다.
- 실수 계산에는 미세한 오차가 생길 수 있다.
- \`int()\`와 \`float()\`으로 숫자 자료형을 변환할 수 있다.

---
`;export{e as default};