var e=`<!-- 원본: python_basic_chapter_2_book.md / 세부 장: 2-2 -->

# 2.2 문자열

## 문자열은 문자의 열이다

문자열은 말 그대로 문자의 열이다. 하나의 문자들이 순서대로 이어져 있는 데이터를 문자열이라고 한다.

예를 들어 \`"Python"\`이라는 문자열은 다음 문자들이 순서대로 이어진 데이터이다.

\`\`\`text
P y t h o n
\`\`\`

파이썬에서 문자열은 \`str\` 자료형이다.

\`\`\`python
language = "Python"
print(type(language))
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
<class 'str'>
\`\`\`

문자열은 이름, 이메일, 주소, 파일 경로, 설명 문장, 상품 코드처럼 문자로 표현되는 값을 다룰 때 사용한다.

\`\`\`python
name = "홍길동"
email = "hong@example.com"
address = "서울시 강남구"
product_code = "A-001"
\`\`\`

## 문자 하나와 문자열

일부 프로그래밍 언어에서는 문자 하나와 문자열을 서로 다른 자료형으로 구분한다. 하지만 파이썬에서는 문자 하나도 문자열이다.

\`\`\`python
letter = "A"
word = "Apple"

print(type(letter))
print(type(word))
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
<class 'str'>
<class 'str'>
\`\`\`

\`"A"\`처럼 문자 하나만 있어도 파이썬에서는 문자열로 다룬다.

## 문자열은 순서가 있는 데이터다

문자열은 여러 문자가 순서대로 나열된 데이터이다. 따라서 첫 번째 문자, 두 번째 문자처럼 위치를 기준으로 접근할 수 있다. 이 위치를 인덱스라고 한다.

\`\`\`python
text = "Python"
\`\`\`

이 문자열의 문자는 다음과 같은 순서를 가진다.

| 문자 | P | y | t | h | o | n |
|---|---|---|---|---|---|---|
| 위치 | 0 | 1 | 2 | 3 | 4 | 5 |

파이썬의 위치 번호는 1이 아니라 0부터 시작한다. 인덱싱과 슬라이싱은 다음 절에서 자세히 다룬다. 여기서는 문자열이 순서를 가진 데이터라는 점만 기억하면 된다.

## 문자열을 선언하는 방법

파이썬에서 문자열은 따옴표로 감싸서 만든다. 작은따옴표와 큰따옴표 모두 사용할 수 있다.

\`\`\`python
name1 = 'Python'
name2 = "Python"
\`\`\`

두 방식은 같은 문자열을 만든다.

작은따옴표를 사용할지 큰따옴표를 사용할지는 상황에 따라 선택할 수 있다. 문자열 안에 작은따옴표가 들어가야 한다면 바깥쪽에는 큰따옴표를 쓰면 편하다.

\`\`\`python
message = "I'm learning Python."
\`\`\`

반대로 문자열 안에 큰따옴표가 들어가야 한다면 바깥쪽에는 작은따옴표를 쓸 수 있다.

\`\`\`python
message = '그는 "안녕하세요"라고 말했다.'
\`\`\`

## 빈 문자열

아무 문자도 들어 있지 않은 문자열을 빈 문자열이라고 한다.

\`\`\`python
empty_text = ""
\`\`\`

빈 문자열은 길이가 0인 문자열이다. 값이 없다는 뜻의 \`None\`과는 다르다.

\`\`\`python
empty_text = ""
empty_value = None

print(type(empty_text))
print(type(empty_value))
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
<class 'str'>
<class 'NoneType'>
\`\`\`

빈 문자열은 사용자가 아직 아무 글자도 입력하지 않았거나, 문자열 처리 결과가 비어 있을 때 나타날 수 있다.

## 여러 줄 문자열

여러 줄의 문자열을 만들 때는 따옴표 세 개를 사용한다.

\`\`\`python
message = """안녕하세요.
파이썬 기초 수업입니다.
오늘은 문자열을 배웁니다."""

print(message)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
안녕하세요.
파이썬 기초 수업입니다.
오늘은 문자열을 배웁니다.
\`\`\`

여러 줄 문자열은 안내문, 긴 설명, SQL 문장, 템플릿 문장 등을 작성할 때 사용할 수 있다.

다만 따옴표 세 개로 만든 문자열을 코드 중간에 아무 곳에나 두면, 주석처럼 보이지만 실제로는 문자열 객체가 만들어진다. 파이썬에서 여러 줄 설명을 남길 때는 보통 \`#\` 주석을 사용하고, 함수나 클래스 설명에는 문서 문자열을 사용한다. 문서 문자열은 나중에 함수와 클래스를 배울 때 다시 만난다.

## 이스케이프 문자

문자열 안에서 줄바꿈이나 탭처럼 직접 입력하기 어려운 문자를 표현할 때 이스케이프 문자를 사용한다. 이스케이프 문자는 역슬래시 \`\\\`로 시작한다.

\`\`\`python
text = "Hello\\nPython"
print(text)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
Hello
Python
\`\`\`

자주 사용하는 이스케이프 문자는 다음과 같다.

| 이스케이프 문자 | 의미 |
|---|---|
| \`\\n\` | 줄바꿈 |
| \`\\t\` | 탭 |
| \`\\\\\` | 역슬래시 자체 |
| \`\\'\` | 작은따옴표 |
| \`\\"\` | 큰따옴표 |

예를 들어 Windows 파일 경로에는 역슬래시가 들어가는 경우가 많다.

\`\`\`python
path = "C:\\\\Users\\\\admin\\\\Desktop"
print(path)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
C:\\Users\\admin\\Desktop
\`\`\`

역슬래시를 그대로 표현하려면 \`\\\\\`처럼 두 번 써야 한다.

## raw string

역슬래시가 많이 들어가는 문자열에서는 raw string을 사용할 수 있다. raw string은 문자열 앞에 \`r\`을 붙여 만든다.

\`\`\`python
path = r"C:\\Users\\admin\\Desktop"
print(path)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
C:\\Users\\admin\\Desktop
\`\`\`

raw string은 파일 경로, 정규표현식 패턴처럼 역슬래시가 자주 들어가는 문자열에서 유용하다. 정규표현식은 뒤의 데이터 처리 장에서 다시 배운다.

## 문자열은 불변 데이터다

파이썬의 문자열은 불변 데이터이다. 불변 데이터란 한 번 만들어진 값을 내부에서 직접 바꿀 수 없는 데이터를 말한다.

다음 코드는 문자열의 첫 글자를 바꾸려고 한다.

\`\`\`python
text = "python"
text[0] = "P"
\`\`\`

이 코드는 실행하면 오류가 발생한다.

\`\`\`text
TypeError: 'str' object does not support item assignment
\`\`\`

문자열의 일부를 직접 바꿀 수 없기 때문이다.

그렇다면 문자열을 대문자로 바꾸는 코드는 어떻게 동작할까?

\`\`\`python
text = "python"
text = text.upper()

print(text)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
PYTHON
\`\`\`

겉으로 보면 기존 문자열이 바뀐 것처럼 보인다. 하지만 실제로는 \`"python"\`이라는 문자열 자체를 바꾼 것이 아니라, \`text.upper()\`가 \`"PYTHON"\`이라는 새 문자열을 만들고, 그 새 문자열을 다시 \`text\`라는 변수에 저장한 것이다.

문자열이 불변이라는 특징은 처음에는 불편해 보일 수 있지만, 프로그램의 안정성과 예측 가능성을 높이는 데 도움이 된다.

## 문자와 인코딩

컴퓨터는 문자를 문자 그대로 저장하지 않는다. 컴퓨터는 모든 데이터를 숫자로 저장한다. 문자도 내부적으로는 숫자 코드로 표현된다.

예를 들어 문자 \`A\`는 특정 숫자 코드와 연결되어 있다. 파이썬에서는 \`ord()\` 함수를 사용해 문자의 코드 값을 확인할 수 있다.

\`\`\`python
print(ord("A"))
print(ord("가"))
\`\`\`

실행 결과는 환경과 문자 체계에 따라 정해진 유니코드 코드 포인트를 보여준다.

\`\`\`text
65
44032
\`\`\`

반대로 숫자 코드를 문자로 바꿀 때는 \`chr()\`을 사용할 수 있다.

\`\`\`python
print(chr(65))
print(chr(44032))
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
A
가
\`\`\`

이 내용을 모두 외울 필요는 없다. 중요한 것은 컴퓨터가 문자를 숫자 규칙에 따라 저장하고 해석한다는 점이다.

## 유니코드와 UTF-8

문자를 표현하기 위한 규칙에는 여러 종류가 있다. 그중 전 세계의 다양한 문자를 표현하기 위해 만들어진 표준이 유니코드이다. 파이썬 3의 문자열은 유니코드 기반으로 동작한다.

하지만 파일에 저장하거나 네트워크로 전송할 때는 문자열을 바이트 데이터로 바꾸어야 한다. 이때 사용하는 변환 규칙이 인코딩이다. UTF-8은 가장 널리 쓰이는 인코딩 방식 중 하나이다.

정리하면 다음과 같다.

- 유니코드: 문자를 표현하기 위한 표준 체계
- 인코딩: 문자열을 바이트로 바꾸는 과정
- 디코딩: 바이트를 문자열로 바꾸는 과정
- UTF-8: 많이 사용되는 인코딩 방식

## 문자열과 bytes

문자열을 바이트로 바꿀 때는 \`encode()\`를 사용한다.

\`\`\`python
text = "안녕하세요"
data = text.encode("utf-8")

print(data)
print(type(data))
\`\`\`

실행 결과는 다음과 비슷하다.

\`\`\`text
b'\\xec\\x95\\x88\\xeb\\x85\\x95\\xed\\x95\\x98\\xec\\x84\\xb8\\xec\\x9a\\x94'
<class 'bytes'>
\`\`\`

바이트 데이터를 다시 문자열로 바꿀 때는 \`decode()\`를 사용한다.

\`\`\`python
text = "안녕하세요"
data = text.encode("utf-8")
restored_text = data.decode("utf-8")

print(restored_text)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
안녕하세요
\`\`\`

파일을 읽을 때 한글이 깨지는 이유는 대부분 인코딩과 디코딩 방식이 맞지 않기 때문이다. 예를 들어 UTF-8로 저장된 파일을 다른 방식으로 읽으면 글자가 깨질 수 있다. 파일 처리 장에서는 \`encoding="utf-8"\` 같은 옵션을 실제로 사용하게 된다.

## 2.2 핵심 정리

- 문자열은 문자의 열이다.
- 파이썬에서 문자열은 \`str\` 자료형이다.
- 문자 하나도 파이썬에서는 문자열이다.
- 문자열은 순서가 있는 데이터다.
- 문자열은 작은따옴표나 큰따옴표로 선언한다.
- 여러 줄 문자열은 따옴표 세 개로 만들 수 있다.
- 이스케이프 문자는 줄바꿈, 탭, 역슬래시 등을 표현할 때 사용한다.
- 문자열은 불변 데이터이다.
- 컴퓨터는 문자를 숫자 규칙으로 저장하고, 파일과 네트워크에서는 인코딩이 중요하다.

---
`;export{e as default};