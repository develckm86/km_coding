var e=`<!-- 원본: python_basic_chapter_2_book.md / 세부 장: 2-1 -->

# 2.1 변수와 자료형

## 변수란 무엇인가

변수는 값을 저장하기 위해 붙이는 이름이다. 프로그램을 작성하다 보면 이름, 나이, 가격, 수량, 할인율, 로그인 여부처럼 여러 값을 다루게 된다. 이 값을 매번 직접 쓰면 코드를 읽기 어렵고, 같은 값을 여러 번 사용할 때도 불편하다.

예를 들어 고객의 이름을 프로그램에서 사용한다고 생각해 보자. 이름을 변수에 저장하면 이후 코드에서 그 이름을 다시 사용할 수 있다.

\`\`\`python
name = "홍길동"
\`\`\`

위 코드는 \`name\`이라는 이름에 \`"홍길동"\`이라는 값을 연결한다. 이후 \`name\`을 사용하면 \`"홍길동"\`이라는 값을 사용하는 것과 같다.

\`\`\`python
name = "홍길동"
print(name)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
홍길동
\`\`\`

변수를 처음 배울 때는 변수를 “값을 담는 상자”라고 설명하기도 한다. 이 비유는 입문 단계에서는 도움이 된다. 다만 파이썬에서는 변수가 실제 값을 직접 담는다기보다, 값에 붙어 있는 이름표에 가깝다고 이해하는 편이 더 정확하다. 지금은 깊게 이해할 필요는 없지만, 같은 값에 여러 이름이 붙을 수 있다는 점은 나중에 리스트 복사나 객체를 배울 때 중요해진다.

## 변수에 값을 저장하기

파이썬에서 변수에 값을 저장할 때는 등호 \`=\`를 사용한다.

\`\`\`python
price = 10000
quantity = 3
\`\`\`

여기서 \`=\`는 수학에서 말하는 “같다”의 의미가 아니다. 프로그래밍에서 \`=\`는 오른쪽 값을 왼쪽 변수 이름에 저장한다는 의미이다.

\`\`\`python
total = price * quantity
\`\`\`

위 코드는 \`price * quantity\`의 계산 결과를 \`total\`이라는 변수에 저장한다.

\`\`\`python
price = 10000
quantity = 3
total = price * quantity

print(total)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
30000
\`\`\`

## 변수 값 다시 할당하기

변수에 저장된 값은 다시 바꿀 수 있다.

\`\`\`python
count = 1
print(count)

count = 2
print(count)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
1
2
\`\`\`

처음에는 \`count\`에 \`1\`이 연결되어 있었다. 이후 \`count = 2\`를 실행하면 \`count\`는 더 이상 \`1\`이 아니라 \`2\`를 가리키게 된다. 이것을 재할당이라고 한다.

재할당은 실무 코드에서 자주 사용된다. 예를 들어 처음에는 주문 수량이 1개였지만, 사용자가 수량을 변경하면 변수에 새로운 값을 저장할 수 있다.

\`\`\`python
quantity = 1
quantity = 5
\`\`\`

## 변수 이름 짓기 규칙

변수 이름은 아무렇게나 지을 수 없다. 파이썬에는 변수 이름을 만들 때 지켜야 하는 기본 규칙이 있다.

변수 이름에 사용할 수 있는 것은 다음과 같다.

- 영문자
- 숫자
- 밑줄 \`_\`
- 일부 유니코드 문자

하지만 숫자로 시작할 수는 없다.

\`\`\`python
name = "홍길동"
user_name = "hong"
age2 = 20
\`\`\`

위 변수 이름은 사용할 수 있다.

반대로 아래와 같은 이름은 사용할 수 없다.

\`\`\`python
2age = 20
user-name = "hong"
class = "A"
\`\`\`

\`2age\`는 숫자로 시작하기 때문에 사용할 수 없다. \`user-name\`은 가운데의 \`-\`가 뺄셈 연산자로 해석되기 때문에 사용할 수 없다. \`class\`는 파이썬에서 이미 특별한 의미로 사용하는 예약어이기 때문에 변수 이름으로 사용할 수 없다.

예약어는 파이썬 문법에서 이미 정해진 의미를 가진 단어다. 예를 들어 \`if\`, \`for\`, \`while\`, \`def\`, \`class\`, \`return\`, \`True\`, \`False\`, \`None\` 같은 단어는 변수 이름으로 사용하면 안 된다.

## 좋은 변수 이름

변수 이름은 규칙만 지키면 끝나는 것이 아니다. 좋은 변수 이름은 코드의 의미를 잘 드러내야 한다.

\`\`\`python
a = 10000
b = 3
c = a * b
\`\`\`

위 코드는 실행은 되지만, \`a\`, \`b\`, \`c\`가 무엇을 의미하는지 알기 어렵다.

다음처럼 작성하면 훨씬 이해하기 쉽다.

\`\`\`python
price = 10000
quantity = 3
total_price = price * quantity
\`\`\`

\`price\`는 가격, \`quantity\`는 수량, \`total_price\`는 총금액이라는 의미가 드러난다.

파이썬이 변수 이름을 자동으로 좋게 만들어 주는 것은 아니다. 좋은 이름을 짓는 것은 개발자의 습관이다. 다만 파이썬은 간결한 문법을 가지고 있기 때문에, 이름만 잘 지어도 코드가 문장처럼 자연스럽게 읽히는 경우가 많다.

## 파이썬의 변수 이름 스타일

파이썬에서는 일반적으로 변수 이름을 소문자와 밑줄로 작성한다.

\`\`\`python
user_name = "홍길동"
total_price = 30000
is_active = True
\`\`\`

이런 방식을 스네이크 케이스라고 부른다. 단어 사이를 밑줄로 연결한 모양이 뱀처럼 보인다고 해서 붙은 이름이다.

다음처럼 작성할 수도 있지만, 파이썬 코드에서는 일반 변수명으로는 권장되지 않는다.

\`\`\`python
userName = "홍길동"
TotalPrice = 30000
\`\`\`

파이썬 코드에서는 일관성이 중요하다. 변수 이름을 정할 때는 하나의 스타일을 정하고 계속 유지하는 것이 좋다.

## 자료형이란 무엇인가

자료형은 값의 종류를 의미한다. 파이썬은 값이 어떤 종류인지에 따라 다르게 처리한다.

예를 들어 \`100\`은 숫자이고, \`"100"\`은 문자열이다. 화면에 보기에는 비슷해 보이지만, 파이썬에서는 전혀 다른 데이터로 다룬다.

\`\`\`python
number = 100
text = "100"
\`\`\`

\`number\`는 계산할 수 있는 숫자 데이터이다. 반면 \`text\`는 문자 데이터이다. 숫자처럼 생겼지만 따옴표 안에 들어 있으므로 문자열이다.

파이썬의 기본 자료형에는 다음과 같은 것들이 있다.

| 자료형 | 의미 | 예시 |
|---|---|---|
| \`str\` | 문자열 | \`"Python"\`, \`"홍길동"\` |
| \`int\` | 정수 | \`10\`, \`0\`, \`-5\` |
| \`float\` | 실수 | \`3.14\`, \`0.5\`, \`-2.7\` |
| \`bool\` | 참 또는 거짓 | \`True\`, \`False\` |
| \`NoneType\` | 값이 없음을 나타냄 | \`None\` |

## \`type()\`으로 자료형 확인하기

값의 자료형을 확인할 때는 \`type()\` 함수를 사용할 수 있다.

\`\`\`python
name = "홍길동"
age = 25
height = 175.5
is_member = True
empty_value = None

print(type(name))
print(type(age))
print(type(height))
print(type(is_member))
print(type(empty_value))
\`\`\`

실행 결과는 다음과 비슷하다.

\`\`\`text
<class 'str'>
<class 'int'>
<class 'float'>
<class 'bool'>
<class 'NoneType'>
\`\`\`

\`type()\`은 지금 다루는 값이 어떤 종류인지 확인할 때 유용하다. 특히 사용자 입력이나 파일에서 읽은 데이터가 예상한 자료형인지 확인할 때 자주 사용된다.

## 파이썬은 동적 타입 언어다

파이썬에서는 변수를 만들 때 자료형을 미리 적지 않는다.

\`\`\`python
name = "홍길동"
age = 25
\`\`\`

다른 언어에서는 변수를 만들 때 “이 변수에는 문자열이 들어간다”, “이 변수에는 정수가 들어간다”라고 미리 선언해야 하는 경우가 있다. 하지만 파이썬은 변수에 저장된 값을 보고 자료형을 판단한다.

그래서 같은 변수에 나중에 다른 자료형의 값을 다시 저장할 수도 있다.

\`\`\`python
value = 100
print(type(value))

value = "100"
print(type(value))
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
<class 'int'>
<class 'str'>
\`\`\`

이런 특징은 편리하지만, 실무에서는 주의가 필요하다. 같은 변수에 여러 종류의 값을 계속 바꾸어 저장하면 코드를 읽기 어렵고 오류가 생기기 쉽다. 변수의 의미가 일정하게 유지되도록 작성하는 습관이 중요하다.

## \`None\`은 값이 없음을 나타낸다

파이썬에는 \`None\`이라는 특별한 값이 있다. \`None\`은 값이 아직 없거나, 의도적으로 비워 두었다는 것을 나타낼 때 사용한다.

\`\`\`python
email = None
\`\`\`

위 코드는 \`email\` 변수에 아직 이메일 값이 없다는 의미로 볼 수 있다.

\`None\`은 빈 문자열 \`""\`이나 숫자 \`0\`과 다르다.

\`\`\`python
empty_text = ""
zero = 0
empty_value = None
\`\`\`

- \`""\`는 길이가 0인 문자열이다.
- \`0\`은 숫자 값이다.
- \`None\`은 값이 없음을 나타내는 특별한 값이다.

이 차이는 조건 판단과 데이터 처리에서 중요하다.

## 자료형 변환 기초

값의 자료형을 바꿀 때는 변환 함수를 사용한다.

\`\`\`python
price_text = "10000"
price = int(price_text)

print(price)
print(type(price))
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
10000
<class 'int'>
\`\`\`

문자열 \`"10000"\`은 계산할 수 없지만, 정수 \`10000\`으로 바꾸면 계산할 수 있다.

자주 사용하는 변환 함수는 다음과 같다.

| 함수 | 의미 | 예시 |
|---|---|---|
| \`str()\` | 문자열로 변환 | \`str(100)\` |
| \`int()\` | 정수로 변환 | \`int("100")\` |
| \`float()\` | 실수로 변환 | \`float("3.14")\` |
| \`bool()\` | 불리언으로 변환 | \`bool(1)\` |

자료형 변환은 입력값을 처리하거나 파일에서 읽은 데이터를 계산할 때 자주 사용된다.

## 2.1 핵심 정리

- 변수는 값을 저장하기 위해 붙이는 이름이다.
- \`=\`는 오른쪽 값을 왼쪽 변수에 저장한다는 뜻이다.
- 변수 이름은 의미가 드러나도록 작성하는 것이 좋다.
- 자료형은 값의 종류를 의미한다.
- 파이썬의 기본 자료형에는 문자열, 정수, 실수, 불리언, \`None\`이 있다.
- \`type()\`으로 자료형을 확인할 수 있다.
- \`str()\`, \`int()\`, \`float()\`, \`bool()\`로 자료형을 변환할 수 있다.

---
`;export{e as default};