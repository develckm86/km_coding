var e=`<!-- 원본: python_basic_chapter_2_book.md / 세부 장: 2-5 -->

# 2.5 연산자

## 연산자란 무엇인가

연산자는 값을 계산하거나 비교하거나 결합할 때 사용하는 기호이다. 수 데이터에서 가장 먼저 배우는 연산자는 산술 연산자이다.

예를 들어 \`+\`는 더하기, \`*\`는 곱하기를 의미한다.

\`\`\`python
price = 10000
quantity = 3

total = price * quantity
print(total)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
30000
\`\`\`

연산자는 단순한 계산뿐 아니라 실무 프로그램에서 매우 자주 사용된다. 매출 합계, 할인 금액, 부가세, 평균 점수, 페이지 수, 남은 재고 등을 계산할 때 필요하다.

## 산술 연산자

파이썬의 기본 산술 연산자는 다음과 같다.

| 연산자 | 의미 | 예시 | 결과 |
|---|---|---|---|
| \`+\` | 더하기 | \`10 + 3\` | \`13\` |
| \`-\` | 빼기 | \`10 - 3\` | \`7\` |
| \`*\` | 곱하기 | \`10 * 3\` | \`30\` |
| \`/\` | 나누기 | \`10 / 3\` | \`3.333...\` |
| \`//\` | 몫 | \`10 // 3\` | \`3\` |
| \`%\` | 나머지 | \`10 % 3\` | \`1\` |
| \`**\` | 거듭제곱 | \`10 ** 3\` | \`1000\` |

예시는 다음과 같다.

\`\`\`python
a = 10
b = 3

print(a + b)
print(a - b)
print(a * b)
print(a / b)
print(a // b)
print(a % b)
print(a ** b)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
13
7
30
3.3333333333333335
3
1
1000
\`\`\`

## 나눗셈 연산 이해하기

파이썬에서 \`/\`는 일반 나눗셈이다. 결과는 실수로 나온다.

\`\`\`python
print(10 / 2)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
5.0
\`\`\`

\`10\`을 \`2\`로 나누면 수학적으로는 \`5\`이지만, \`/\` 연산의 결과는 실수 \`5.0\`이다.

몫만 구하고 싶을 때는 \`//\`를 사용한다.

\`\`\`python
print(10 // 3)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
3
\`\`\`

나머지를 구하고 싶을 때는 \`%\`를 사용한다.

\`\`\`python
print(10 % 3)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
1
\`\`\`

나머지 연산은 짝수와 홀수를 판단할 때 자주 사용된다.

\`\`\`python
number = 7
remainder = number % 2

print(remainder)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
1
\`\`\`

2로 나누었을 때 나머지가 0이면 짝수이고, 1이면 홀수이다. 조건에 따라 실제로 다르게 처리하는 방법은 다음 장의 조건문에서 배운다.

## 거듭제곱

거듭제곱은 \`**\`를 사용한다.

\`\`\`python
print(2 ** 3)
print(10 ** 2)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
8
100
\`\`\`

제곱 계산, 면적 계산, 지수 계산 등에서 사용할 수 있다.

## 대입 연산자

변수에 값을 저장할 때 사용하는 \`=\`도 연산자이다. 이것을 대입 연산자라고 한다.

\`\`\`python
count = 10
\`\`\`

기존 값에 어떤 계산을 적용해서 다시 저장할 때는 복합 대입 연산자를 사용할 수 있다.

\`\`\`python
count = 10
count += 1

print(count)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
11
\`\`\`

\`count += 1\`은 다음 코드와 같은 의미이다.

\`\`\`python
count = count + 1
\`\`\`

자주 사용하는 복합 대입 연산자는 다음과 같다.

| 연산자 | 의미 | 같은 표현 |
|---|---|---|
| \`+=\` | 더해서 다시 저장 | \`a = a + b\` |
| \`-=\` | 빼서 다시 저장 | \`a = a - b\` |
| \`*=\` | 곱해서 다시 저장 | \`a = a * b\` |
| \`/=\` | 나누어서 다시 저장 | \`a = a / b\` |
| \`//=\` | 몫을 다시 저장 | \`a = a // b\` |
| \`%=\` | 나머지를 다시 저장 | \`a = a % b\` |

예시는 다음과 같다.

\`\`\`python
point = 1000
point += 500
point -= 200

print(point)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
1300
\`\`\`

복합 대입 연산자는 포인트 누적, 재고 차감, 합계 계산에서 자주 사용된다.

## 연산자 우선순위

파이썬도 수학처럼 연산자 우선순위가 있다. 곱셈과 나눗셈은 덧셈과 뺄셈보다 먼저 계산된다.

\`\`\`python
result = 10 + 2 * 3
print(result)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
16
\`\`\`

\`2 * 3\`이 먼저 계산되고, 그 결과에 \`10\`을 더한다.

계산 순서를 명확히 하고 싶을 때는 괄호를 사용한다.

\`\`\`python
result = (10 + 2) * 3
print(result)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
36
\`\`\`

실무 코드에서는 복잡한 계산식일수록 괄호를 사용해 의도를 명확히 하는 것이 좋다.

\`\`\`python
price = 10000
quantity = 3
discount_rate = 0.1

total = price * quantity * (1 - discount_rate)
print(total)
\`\`\`

이 코드는 가격과 수량을 곱한 뒤 할인율을 적용한다.

## 반올림과 숫자 처리

실수 계산 결과를 반올림할 때는 \`round()\`를 사용한다.

\`\`\`python
value = 3.14159
print(round(value))
print(round(value, 2))
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
3
3.14
\`\`\`

두 번째 인자로 소수점 자리수를 지정할 수 있다.

평균 점수를 계산하는 예시를 보자.

\`\`\`python
score1 = 80
score2 = 90
score3 = 85

average = (score1 + score2 + score3) / 3
print(round(average, 1))
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
85.0
\`\`\`

금액 계산에서는 소수점이 필요 없을 때 정수로 변환할 수도 있다.

\`\`\`python
price = 9900
tax_rate = 0.1

tax = price * tax_rate
print(int(tax))
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
990
\`\`\`

다만 \`int()\`는 소수점 아래를 버린다. 반올림이 필요한지, 버림이 필요한지는 업무 규칙에 따라 결정해야 한다.

## 문자열과 \`+\`, \`*\`

\`+\`와 \`*\`는 숫자뿐 아니라 문자열에도 사용할 수 있다.

문자열끼리 \`+\`를 사용하면 문자열이 연결된다.

\`\`\`python
first_name = "길동"
last_name = "홍"

full_name = last_name + first_name
print(full_name)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
홍길동
\`\`\`

문자열에 정수를 곱하면 문자열이 반복된다.

\`\`\`python
line = "-" * 10
print(line)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
----------
\`\`\`

하지만 문자열과 숫자를 바로 더할 수는 없다.

\`\`\`python
age = 25
message = "나이: " + age
\`\`\`

이 코드는 오류가 발생한다. 숫자를 문자열로 바꾸거나 f-string을 사용해야 한다.

\`\`\`python
age = 25
message = "나이: " + str(age)
print(message)
\`\`\`

또는 다음처럼 쓸 수 있다.

\`\`\`python
age = 25
message = f"나이: {age}"
print(message)
\`\`\`

## 2.5 핵심 정리

- 연산자는 값을 계산하거나 처리하는 기호이다.
- 산술 연산자에는 \`+\`, \`-\`, \`*\`, \`/\`, \`//\`, \`%\`, \`**\`가 있다.
- \`/\`의 결과는 실수이다.
- \`//\`는 몫을 구하고, \`%\`는 나머지를 구한다.
- 복합 대입 연산자로 기존 값을 계산해서 다시 저장할 수 있다.
- 계산 순서가 헷갈릴 때는 괄호를 사용한다.
- \`round()\`로 반올림할 수 있다.
- 문자열에서도 \`+\`와 \`*\`를 사용할 수 있지만, 문자열과 숫자를 바로 더할 수는 없다.

---
`;export{e as default};