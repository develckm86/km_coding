var e=`<!-- 원본: python_data_analysis_basic_chapter_11_book.md / 세부 장: 11-5 -->

# 11.5 중복값 처리 기준 세우기

중복값은 무조건 제거하는 것이 아닙니다.  
중복이 정상인지 오류인지 판단해야 합니다.

---

### 11.5.1 정상 중복과 오류 중복

다음은 정상 중복일 수 있습니다.

\`\`\`text
같은 고객이 여러 번 주문했다.
같은 상품이 여러 주문에 등장했다.
같은 지역에 여러 고객이 있다.
같은 카테고리에 여러 상품이 있다.
\`\`\`

반면 다음은 오류 중복일 가능성이 있습니다.

\`\`\`text
같은 주문번호가 두 번 들어갔다.
같은 고객 ID가 같은 정보로 두 번 저장됐다.
같은 상품 ID가 같은 가격과 이름으로 두 번 저장됐다.
같은 파일을 두 번 합쳐서 같은 행이 반복됐다.
\`\`\`

중복 판단 기준은 데이터 종류에 따라 달라집니다.

| 데이터 | 중복 판단 기준 예시 |
|---|---|
| 주문 데이터 | \`order_id\` |
| 고객 데이터 | \`customer_id\`, \`email\` |
| 상품 데이터 | \`product_id\` |
| 로그 데이터 | \`timestamp\`, \`user_id\`, \`event_type\` 조합 |
| 설문 데이터 | \`respondent_id\` |

---

### 11.5.2 중복 처리 전 확인할 질문

중복값을 처리하기 전에 다음 질문을 확인합니다.

\`\`\`text
1. 어떤 컬럼을 기준으로 중복을 판단할 것인가?
2. 중복이 실제 오류인가, 정상 데이터인가?
3. 중복된 행 중 어떤 행을 남길 것인가?
4. 최신 행을 남길 것인가, 최초 행을 남길 것인가?
5. 중복을 제거하면 분석 결과가 어떻게 바뀌는가?
6. 중복 처리 기준을 기록했는가?
\`\`\`

중복 제거는 분석 결과에 직접 영향을 주므로 처리 기준을 명확히 남겨야 합니다.

---

### 11.5.3 중복 처리 기록 예시

분석 보고서나 노트북에는 다음과 같이 기록할 수 있습니다.

\`\`\`text
중복값 처리 기준
- 주문 데이터에서는 order_id를 기준으로 중복을 확인했다.
- 중복된 order_id가 1건 발견되었다.
- 완전히 동일한 중복 행이므로 첫 번째 행만 남기고 제거했다.
- 고객 데이터에서는 customer_id 기준으로 중복을 확인했다.
- 고객 정보가 중복된 경우 updated_at이 가장 최근인 행을 남겼다.
\`\`\`

이런 기록은 나중에 분석 결과를 설명할 때 매우 중요합니다.

---
`;export{e as default};