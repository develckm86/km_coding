var e=`<!-- 원본: python_data_analysis_basic_chapter_3_book.md / 세부 장: 3-9 -->

# 3.9 기본 통계 함수

데이터 분석에서는 데이터를 요약하는 일이 많다. NumPy는 합계, 평균, 중앙값, 최솟값, 최댓값, 표준편차, 분산 같은 기본 통계 함수를 제공한다.

---

## 3.9.1 합계 \`sum()\`

배열의 합계를 구할 때는 \`sum()\`을 사용한다.

\`\`\`python
sales = np.array([100000, 120000, 90000, 150000])

print(sales.sum())
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
460000
\`\`\`

다음처럼 NumPy 함수로도 사용할 수 있다.

\`\`\`python
print(np.sum(sales))
\`\`\`

둘 다 같은 결과를 얻을 수 있다.

---

## 3.9.2 평균 \`mean()\`

평균은 \`mean()\`으로 구한다.

\`\`\`python
scores = np.array([80, 90, 75, 100])

print(scores.mean())
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
86.25
\`\`\`

NumPy 함수로도 사용할 수 있다.

\`\`\`python
print(np.mean(scores))
\`\`\`

---

## 3.9.3 중앙값 \`median()\`

중앙값은 데이터를 크기순으로 정렬했을 때 가운데에 있는 값이다.

\`\`\`python
values = np.array([10, 20, 30, 1000])

print(np.mean(values))
print(np.median(values))
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
265.0
25.0
\`\`\`

평균은 1000이라는 큰 값의 영향을 많이 받았다. 반면 중앙값은 비교적 덜 영향을 받는다. 이상값이 있는 데이터에서는 평균과 중앙값을 함께 확인하는 것이 좋다.

---

## 3.9.4 최솟값과 최댓값

최솟값은 \`min()\`, 최댓값은 \`max()\`로 구한다.

\`\`\`python
prices = np.array([15000, 23000, 9900, 12000])

print(prices.min())
print(prices.max())
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
9900
23000
\`\`\`

최솟값과 최댓값은 데이터 범위를 파악할 때 유용하다.

---

## 3.9.5 표준편차와 분산

표준편차는 데이터가 평균을 기준으로 얼마나 퍼져 있는지를 나타낸다.

\`\`\`python
scores = np.array([80, 90, 75, 100])

print(scores.std())
print(scores.var())
\`\`\`

실행 결과는 다음과 비슷하다.

\`\`\`text
9.013878188659973
81.25
\`\`\`

\`std()\`는 표준편차, \`var()\`는 분산이다.

기초 단계에서는 표준편차를 다음처럼 이해하면 된다.

- 표준편차가 작으면 값들이 평균 근처에 모여 있다.
- 표준편차가 크면 값들이 평균에서 많이 퍼져 있다.

---

## 3.9.6 2차원 배열의 통계

2차원 배열에서도 통계 계산을 할 수 있다.

\`\`\`python
sales = np.array([
    [100, 120, 130],
    [80, 95, 110],
    [150, 160, 170]
])

print(sales.sum())
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
1115
\`\`\`

전체 합계가 계산되었다.

---

## 3.9.7 \`axis\` 이해하기

2차원 배열에서는 행 방향 또는 열 방향으로 계산할 수 있다. 이때 사용하는 것이 \`axis\`다.

다음 배열을 다시 보자.

\`\`\`python
sales = np.array([
    [100, 120, 130],
    [80, 95, 110],
    [150, 160, 170]
])
\`\`\`

행별 합계를 구하려면 \`axis=1\`을 사용한다.

\`\`\`python
print(sales.sum(axis=1))
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
[350 285 480]
\`\`\`

각 행의 합계가 계산되었다.

| 상품 | 1월 | 2월 | 3월 | 합계 |
|---|---:|---:|---:|---:|
| 상품 A | 100 | 120 | 130 | 350 |
| 상품 B | 80 | 95 | 110 | 285 |
| 상품 C | 150 | 160 | 170 | 480 |

열별 합계를 구하려면 \`axis=0\`을 사용한다.

\`\`\`python
print(sales.sum(axis=0))
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
[330 375 410]
\`\`\`

각 열의 합계가 계산되었다.

| 월 | 합계 |
|---|---:|
| 1월 | 330 |
| 2월 | 375 |
| 3월 | 410 |

처음에는 \`axis\`가 헷갈릴 수 있다. 기초 단계에서는 다음처럼 기억하면 좋다.

\`\`\`text
axis=0  → 행 방향으로 내려가며 열별 계산
axis=1  → 열 방향으로 가며 행별 계산
\`\`\`

더 쉽게 말하면 다음과 같다.

\`\`\`text
axis=0  → 각 열의 결과
axis=1  → 각 행의 결과
\`\`\`

---

## 3.9.8 통계 함수 실무 예제

다음은 네 상품의 3개월 매출 데이터다.

\`\`\`python
sales = np.array([
    [120000, 135000, 150000],
    [98000, 110000, 125000],
    [200000, 210000, 230000],
    [87000, 92000, 95000]
])

print("전체 매출 합계:", sales.sum())
print("상품별 매출 합계:", sales.sum(axis=1))
print("월별 매출 합계:", sales.sum(axis=0))
print("전체 평균 매출:", sales.mean())
print("최고 매출:", sales.max())
print("최저 매출:", sales.min())
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
전체 매출 합계: 1652000
상품별 매출 합계: [405000 333000 640000 274000]
월별 매출 합계: [505000 547000 600000]
전체 평균 매출: 137666.66666666666
최고 매출: 230000
최저 매출: 87000
\`\`\`

이 간단한 계산만으로도 다음을 알 수 있다.

- 전체 매출은 1,652,000이다.
- 상품별로는 세 번째 상품의 매출이 가장 높다.
- 월별로는 세 번째 달의 매출이 가장 높다.
- 전체 데이터의 평균 매출은 약 137,667이다.

---
`;export{e as default};