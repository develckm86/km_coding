var e=`<!-- 원본: python_advanced_chapter_18_book.md / 세부 장: 18-8 -->

# 18.8 CLI 프로그램 설계 체크리스트

실무 CLI 프로그램을 만들 때는 다음 항목을 확인하자.

### 실행 관련

- 프로그램 실행 명령이 명확한가?
- \`--help\` 메시지가 충분한가?
- 필수 인수와 선택 옵션이 잘 구분되어 있는가?
- 잘못된 인수가 들어왔을 때 적절한 메시지가 나오는가?
- 종료 코드가 성공과 실패를 구분하는가?

### 입력 관련

- 입력 파일 또는 폴더가 존재하는지 확인하는가?
- 파일인지 폴더인지 검사하는가?
- 인코딩을 지정할 수 있는가?
- 필요한 컬럼이나 필드를 검증하는가?
- 잘못된 입력값을 초기에 막는가?

### 처리 관련

- 실제 작업 로직이 함수로 분리되어 있는가?
- \`main()\`이 너무 많은 일을 하지 않는가?
- 일부 데이터 실패 시 전체 중단 여부가 명확한가?
- \`--dry-run\`이 필요한 작업인가?
- 테스트 가능한 구조인가?

### 출력 관련

- 결과를 화면에 적절히 출력하는가?
- 결과 파일을 저장하는가?
- 실패 목록을 따로 저장하는가?
- 로그 파일이 남는가?
- 자동화 시스템이 결과를 확인할 수 있는가?

### 유지보수 관련

- 설정값이 코드 안에 흩어져 있지 않은가?
- 경로 처리는 \`Path\`로 통일되어 있는가?
- 로그 레벨을 조정할 수 있는가?
- 함수 이름이 역할을 잘 드러내는가?
- 테스트를 추가하기 쉬운가?

---
`;export{e as default};