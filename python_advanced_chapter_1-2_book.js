var e=`<!-- 원본: python_advanced_chapter_1_book.md / 세부 장: 1-2 -->

# 1.2 기초 파이썬과 고급 파이썬의 차이

기초 파이썬과 고급 파이썬은 완전히 다른 언어를 배우는 과정이 아니다. 같은 파이썬을 배우지만 관점이 달라진다.

기초 과정에서는 다음과 같은 내용을 배운다.

\`\`\`text
변수를 어떻게 만드는가?
리스트를 어떻게 사용하는가?
조건문을 어떻게 작성하는가?
함수는 어떻게 정의하는가?
클래스는 어떻게 만드는가?
파일은 어떻게 읽고 쓰는가?
\`\`\`

고급 과정에서는 같은 내용을 더 깊은 관점에서 다시 본다.

\`\`\`text
변수는 실제로 값을 담는 것인가, 객체를 가리키는 이름인가?
리스트를 복사할 때 왜 원본도 함께 바뀌는 것처럼 보일까?
함수를 값처럼 전달할 수 있다는 것은 어떤 의미일까?
반복문은 내부적으로 어떻게 동작할까?
클래스를 파이썬답게 설계하려면 무엇을 알아야 할까?
예외는 어디에서 처리하는 것이 좋을까?
테스트하기 좋은 코드는 어떤 구조를 가질까?
\`\`\`

두 과정의 차이를 표로 정리하면 다음과 같다.

| 구분 | 기초 파이썬 | 고급 파이썬 |
|---|---|---|
| 핵심 목표 | 문법을 익히고 코드를 실행한다 | 코드를 이해하고 구조화한다 |
| 주요 관심 | 변수, 조건문, 반복문, 함수, 클래스 | 객체 모델, 함수 심화, 이터레이터, 데코레이터, 설계 |
| 코드 기준 | 동작하는 코드 | 유지보수 가능한 코드 |
| 오류 대응 | 에러를 고친다 | 실패에 강한 구조를 만든다 |
| 데이터 처리 | 파일과 데이터를 읽고 쓴다 | 큰 데이터와 반복 처리 구조를 고려한다 |
| 실무 연결 | 간단한 자동화 | 재사용 가능한 도구와 프로젝트 구조 |

기초 과정에서 작성하는 코드는 보통 한 파일 안에서 동작해도 충분하다. 하지만 고급 과정에서는 코드를 여러 파일로 나누고, 함수와 클래스를 역할에 맞게 분리하며, 테스트와 로그까지 고려한다.

---
`;export{e as default};