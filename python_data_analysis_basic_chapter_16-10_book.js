var e=`<!-- 원본: python_data_analysis_basic_chapter_16_book.md / 세부 장: 16-10 -->

# 16.10 상관관계 기초

상관관계는 두 수치형 변수 사이의 관계를 나타냅니다.

예를 들어 다음 질문을 생각해봅시다.

\`\`\`text
방문 횟수가 많을수록 주문 금액도 큰가?
나이가 많을수록 만족도가 높은가?
주문 수량이 많을수록 주문 금액이 큰가?
\`\`\`

이런 질문은 두 변수 사이의 관계를 확인하는 질문입니다.

pandas에서는 \`corr()\`을 사용해 상관계수를 계산할 수 있습니다.

---

### 16.10.1 상관계수란?

상관계수는 두 변수의 관계 방향과 강도를 나타내는 값입니다.

일반적으로 -1에서 1 사이의 값을 가집니다.

| 상관계수 | 의미 |
|---:|---|
| 1에 가까움 | 강한 양의 상관관계 |
| 0에 가까움 | 선형 관계가 약함 |
| -1에 가까움 | 강한 음의 상관관계 |

양의 상관관계는 한 변수가 커질 때 다른 변수도 커지는 경향을 의미합니다.

\`\`\`text
방문 횟수가 많을수록 구매 금액이 커진다.
\`\`\`

음의 상관관계는 한 변수가 커질 때 다른 변수는 작아지는 경향을 의미합니다.

\`\`\`text
가격이 높을수록 구매 수량이 줄어든다.
\`\`\`

상관계수가 0에 가깝다는 것은 두 변수 사이에 직선적인 관계가 약하다는 뜻입니다.

---

### 16.10.2 상관계수 계산

수치형 컬럼 간 상관관계를 계산해보겠습니다.

\`\`\`python
orders[["age", "quantity", "total_price", "visit_count", "satisfaction"]].corr()
\`\`\`

결과는 상관계수 행렬입니다.

각 변수 쌍의 상관계수를 보여줍니다.

특정 두 컬럼의 상관계수만 보고 싶다면 다음처럼 작성할 수 있습니다.

\`\`\`python
orders["visit_count"].corr(orders["total_price"])
\`\`\`

---

### 16.10.3 상관관계 해석

상관계수를 해석할 때는 절댓값의 크기를 참고할 수 있습니다.

| 절댓값 | 해석 예시 |
|---:|---|
| 0.0 ~ 0.2 | 매우 약한 관계 |
| 0.2 ~ 0.4 | 약한 관계 |
| 0.4 ~ 0.6 | 중간 정도 관계 |
| 0.6 ~ 0.8 | 비교적 강한 관계 |
| 0.8 ~ 1.0 | 매우 강한 관계 |

이 기준은 절대적인 규칙은 아닙니다.  
분석 분야와 데이터 특성에 따라 다르게 해석해야 합니다.

기초 과정에서는 다음 정도로 이해하면 충분합니다.

\`\`\`text
양수이면 함께 증가하는 경향이 있다.
음수이면 한쪽이 증가할 때 다른 쪽이 감소하는 경향이 있다.
0에 가까우면 직선적 관계가 약하다.
\`\`\`

---

### 16.10.4 상관관계와 산점도

상관관계를 해석할 때는 산점도를 함께 보는 것이 좋습니다.

\`\`\`python
orders.plot(
    kind="scatter",
    x="visit_count",
    y="total_price"
)

plt.title("Visit Count vs Total Price")
plt.xlabel("Visit Count")
plt.ylabel("Total Price")
plt.show()
\`\`\`

산점도는 두 변수의 관계를 시각적으로 보여줍니다.

상관계수가 하나의 숫자로 관계를 요약한다면, 산점도는 데이터가 실제로 어떻게 퍼져 있는지 보여줍니다.

상관계수만 보고 판단하기보다 산점도를 함께 확인하는 습관이 중요합니다.

---

### 16.10.5 상관관계와 인과관계의 차이

상관관계가 있다고 해서 인과관계가 있다는 뜻은 아닙니다.

예를 들어 방문 횟수와 구매 금액 사이에 양의 상관관계가 있다고 해봅시다.

\`\`\`text
방문 횟수가 많을수록 구매 금액도 높다.
\`\`\`

이 결과만 보고 다음처럼 단정하면 안 됩니다.

\`\`\`text
방문 횟수가 구매 금액을 증가시켰다.
\`\`\`

왜냐하면 다른 가능성도 있기 때문입니다.

\`\`\`text
원래 구매 의향이 높은 고객이 더 많이 방문했을 수 있다.
VIP 고객이 방문도 많고 구매도 많을 수 있다.
특정 이벤트가 방문과 구매를 동시에 증가시켰을 수 있다.
\`\`\`

상관관계는 두 변수 사이에 함께 움직이는 경향이 있음을 보여줄 뿐, 원인을 증명하지는 않습니다.

인과관계를 확인하려면 실험 설계, 통제 변수, 가설검정 등 더 깊은 분석이 필요합니다.  
이 내용은 데이터 분석 고급 과정에서 다루는 것이 좋습니다.

---

### 16.10.6 상관관계 해석 시 주의점

상관관계를 해석할 때는 다음을 주의해야 합니다.

\`\`\`text
상관관계는 인과관계가 아니다.
이상값이 상관계수를 크게 바꿀 수 있다.
비선형 관계는 상관계수로 잘 드러나지 않을 수 있다.
범주형 변수에는 일반적인 corr()를 바로 적용하기 어렵다.
데이터 수가 너무 적으면 상관계수 해석이 불안정하다.
\`\`\`

특히 데이터 수가 적은 예제에서는 상관계수를 일반화하면 안 됩니다.  
상관계수는 충분한 데이터와 함께 해석해야 합니다.

---
`;export{e as default};