var e=`# 6장. 고급 groupby 분석 리포트

## 6.0 들어가며

5장에서는 데이터 재구조화 실습을 진행했습니다.  
\`pivot_table\`, \`melt\`, \`stack\`, \`unstack\`, \`explode\`를 사용해 같은 데이터를 보고용 표와 시각화용 데이터로 바꾸는 방법을 배웠습니다.

이번 장에서는 pandas 분석에서 가장 자주 쓰이는 기능 중 하나인 \`groupby\`를 더 깊게 다룹니다.

데이터 분석 기초 과정에서도 \`groupby()\`를 사용했습니다.  
기초 과정에서는 주로 다음 작업을 했습니다.

\`\`\`python
df.groupby("category")["sales"].sum()
df.groupby("grade")["sales"].mean()
df.groupby("region")["order_id"].count()
\`\`\`

이 정도만으로도 기본적인 요약표를 만들 수 있습니다.

하지만 실무 분석에서는 더 복잡한 질문이 자주 등장합니다.

\`\`\`text
카테고리별 총매출, 주문 수, 평균 주문 금액, 고유 고객 수를 한 번에 보고 싶다.
각 상품의 매출이 자기 카테고리 안에서 몇 위인지 알고 싶다.
지역별 매출 중 각 카테고리가 차지하는 비중을 보고 싶다.
고객별 구매 금액이 전체 평균보다 높은지 알고 싶다.
주문 수가 일정 이상인 고객만 따로 분석하고 싶다.
월별 카테고리 매출 비중이 어떻게 달라졌는지 보고 싶다.
\`\`\`

이런 질문에는 단순한 \`sum()\`이나 \`mean()\`만으로는 부족합니다.

이번 장에서는 다음 기능을 중심으로 실습합니다.

\`\`\`python
groupby().agg()
groupby().transform()
groupby().filter()
groupby().rank()
groupby().cumcount()
groupby().ngroup()
\`\`\`

이번 장의 목표는 단순히 고급 문법을 배우는 것이 아닙니다.  
**실무 분석 리포트에 바로 사용할 수 있는 요약 테이블을 만드는 것**입니다.

이번 장의 최종 결과물은 다음과 같습니다.

\`\`\`text
카테고리별 매출 요약 리포트
고객별 구매 요약 리포트
카테고리 내 상품 순위 리포트
지역별 카테고리 매출 비중 리포트
월별 카테고리 매출 비중 리포트
고객별 평균 대비 구매 금액 비교 리포트
고급 groupby 분석 보고서
\`\`\`

---
`;export{e as default};