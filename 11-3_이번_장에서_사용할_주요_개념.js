var e=`# 11장. 통계적 비교 실습

## 11.3 이번 장에서 사용할 주요 개념

이번 장은 실습 중심이지만, 최소한의 통계 개념을 알고 있어야 합니다.

---

### 11.3.1 평균

평균은 전체 값을 더한 뒤 개수로 나눈 값입니다.

\`\`\`python
df["value"].mean()
\`\`\`

평균은 대표값으로 자주 사용되지만 이상값에 민감합니다.

---

### 11.3.2 중앙값

중앙값은 데이터를 크기순으로 정렬했을 때 가운데 값입니다.

\`\`\`python
df["value"].median()
\`\`\`

중앙값은 이상값의 영향을 평균보다 덜 받습니다.

---

### 11.3.3 표준편차

표준편차는 값들이 평균에서 얼마나 퍼져 있는지 보여줍니다.

\`\`\`python
df["value"].std()
\`\`\`

표준편차가 크면 집단 내부의 차이가 크다는 뜻입니다.

---

### 11.3.4 신뢰구간

신뢰구간은 추정값의 불확실성을 범위로 표현한 것입니다.

예를 들어 평균 차이의 95% 신뢰구간이 다음과 같다고 합시다.

\`\`\`text
평균 차이 95% 신뢰구간: 50,000원 ~ 200,000원
\`\`\`

이는 평균 차이가 어느 정도 범위에 있을 가능성이 높은지를 보여줍니다.

이 수업에서는 복잡한 수식 대신 부트스트랩으로 신뢰구간을 직접 만들어봅니다.

---

### 11.3.5 p-value

p-value는 관찰된 차이가 우연히 나왔을 가능성을 판단할 때 사용하는 값입니다.

보통 p-value가 0.05보다 작으면 통계적으로 유의하다고 표현합니다.

하지만 p-value는 다음처럼 조심해서 해석해야 합니다.

\`\`\`text
p-value가 작다 → 차이가 우연히 발생했을 가능성이 낮다고 볼 수 있다.
p-value가 크다 → 차이가 없다고 증명하는 것은 아니다.
p-value는 실무적 중요도를 알려주지 않는다.
\`\`\`

이번 장에서는 t-test 결과를 읽는 수준으로 다룹니다.

---

### 11.3.6 부트스트랩

부트스트랩은 가지고 있는 데이터에서 반복적으로 표본을 다시 뽑아 통계량의 분포를 확인하는 방법입니다.

예를 들어 VIP 고객과 일반 고객의 평균 구매액 차이를 알고 싶다면 다음을 반복합니다.

\`\`\`text
VIP 고객 데이터에서 복원추출
일반 고객 데이터에서 복원추출
각 집단 평균 계산
평균 차이 계산
이 과정을 1,000번 반복
\`\`\`

그러면 평균 차이가 얼마나 흔들리는지 볼 수 있습니다.

부트스트랩은 수식을 많이 몰라도 직접 실습하기 좋습니다.

---
`;export{e as default};