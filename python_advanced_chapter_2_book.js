var e=`# 2장. 파이썬 실행 구조와 객체 모델

## 들어가며

파이썬 기초 과정에서는 변수를 만들고, 리스트에 값을 넣고, 함수를 호출하고, 클래스로 객체를 만드는 방법을 배웠다. 그때의 관심사는 주로 “어떻게 쓰는가”였다. 고급 파이썬에서는 한 걸음 더 들어가 **그 코드가 내부적으로 어떻게 동작하는가**를 이해해야 한다.

예를 들어 다음 코드를 보자.

\`\`\`python
a = [1, 2, 3]
b = a
b.append(4)

print(a)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
[1, 2, 3, 4]
\`\`\`

\`b\`에 값을 추가했는데 \`a\`도 바뀌었다. 반면 다음 코드는 다르게 동작한다.

\`\`\`python
x = 10
y = x
y = y + 1

print(x)
print(y)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
10
11
\`\`\`

이번에는 \`y\`를 바꿨지만 \`x\`는 바뀌지 않았다. 리스트 예제와 숫자 예제는 왜 다르게 동작할까? 이 질문에 답하려면 파이썬의 **객체, 참조, 가변 객체, 불변 객체**를 이해해야 한다.

이번 장에서는 파이썬 코드의 실행 흐름, 객체와 참조, 가변성과 불변성, 네임스페이스와 스코프, 메모리 관리의 기초를 다룬다. 이 내용은 이후 함수 심화, 데코레이터, 객체지향 심화, 성능 최적화로 이어지는 기반이 된다.

---

## 2.1 파이썬 코드는 어떻게 실행되는가

### 2.1.1 소스 코드와 인터프리터

우리가 작성하는 \`.py\` 파일은 사람이 읽을 수 있는 텍스트 파일이다. 이런 파일을 **소스 코드**라고 한다.

예를 들어 \`hello.py\`라는 파일에 다음 코드를 작성했다고 하자.

\`\`\`python
print("Hello, Python")
\`\`\`

터미널에서 다음과 같이 실행할 수 있다.

\`\`\`bash
python hello.py
\`\`\`

이때 \`python\` 명령어는 파이썬 인터프리터를 실행한다. 인터프리터는 우리가 작성한 코드를 읽고 실행한다.

단순하게 표현하면 흐름은 다음과 같다.

\`\`\`text
소스 코드 작성 → 파이썬 인터프리터 실행 → 코드 해석 → 결과 실행
\`\`\`

파이썬은 보통 인터프리터 언어라고 설명한다. 이는 작성한 코드를 빠르게 실행하고 결과를 확인할 수 있다는 뜻이다. 하지만 내부적으로는 소스 코드를 그대로 한 글자씩 읽어 실행하는 것은 아니다. 파이썬은 코드를 실행하기 전에 내부 명령 형태로 변환한다.

---

### 2.1.2 바이트코드 개념

파이썬 코드는 실행될 때 내부적으로 **바이트코드**라는 중간 형태로 변환된다. 바이트코드는 사람이 직접 작성하는 코드는 아니다. 파이썬 인터프리터가 실행하기 쉽도록 만든 내부 명령이라고 이해하면 된다.

흐름을 조금 더 자세히 쓰면 다음과 같다.

\`\`\`text
.py 소스 코드
   ↓
바이트코드
   ↓
파이썬 가상 머신이 실행
\`\`\`

파이썬 파일을 실행하거나 모듈을 import하면 프로젝트 폴더에 \`__pycache__\`라는 폴더가 생길 수 있다.

\`\`\`text
project/
  app.py
  __pycache__/
    app.cpython-312.pyc
\`\`\`

\`__pycache__\` 폴더 안의 \`.pyc\` 파일은 파이썬이 만들어 둔 바이트코드 캐시 파일이다. 우리가 직접 수정하는 파일이 아니다. 초급 단계에서는 몰라도 되는 내용이지만, 고급 과정에서는 파이썬이 실행을 위해 내부적으로 여러 단계를 거친다는 사실을 이해하는 것이 중요하다.

---

### 2.1.3 런타임의 의미

**런타임(runtime)**은 프로그램이 실제로 실행되는 시간을 의미한다. 파이썬 코드가 실행되는 동안에는 변수, 객체, 함수, 모듈, 예외, 메모리 등이 계속 만들어지고 사라진다.

다음 코드를 보자.

\`\`\`python
price = 10000
quantity = 3

total = price * quantity
print(total)
\`\`\`

이 코드를 실행하면 런타임 중에 다음과 같은 일이 일어난다.

\`\`\`text
1. 정수 객체 10000이 만들어진다.
2. price라는 이름이 그 객체를 가리킨다.
3. 정수 객체 3이 만들어진다.
4. quantity라는 이름이 그 객체를 가리킨다.
5. 곱셈 결과인 정수 객체 30000이 만들어진다.
6. total이라는 이름이 그 객체를 가리킨다.
7. print 함수가 total이 가리키는 값을 출력한다.
\`\`\`

초급 과정에서는 “변수에 값을 저장한다”고 표현했다. 고급 과정에서는 조금 더 정확하게 **이름이 객체를 가리킨다**고 이해하는 것이 좋다.

---

### 2.1.4 스크립트 실행과 모듈 import의 차이

파이썬 파일은 직접 실행할 수도 있고, 다른 파일에서 import할 수도 있다.

다음과 같은 파일이 있다고 하자.

\`\`\`python
# greeting.py
print("greeting 모듈이 실행되었습니다.")

name = "Python"

def say_hello():
    print("Hello")
\`\`\`

이 파일을 직접 실행하면 \`print()\`가 실행된다.

\`\`\`bash
python greeting.py
\`\`\`

다른 파일에서 import해도 모듈의 최상위 코드는 한 번 실행된다.

\`\`\`python
# main.py
import greeting

print(greeting.name)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
greeting 모듈이 실행되었습니다.
Python
\`\`\`

파이썬에서 \`import\`는 단순히 함수 목록만 가져오는 동작이 아니다. 해당 모듈 파일을 실행하고, 그 안에 정의된 이름을 사용할 수 있게 만드는 동작이다.

그래서 실행용 코드와 import용 코드를 구분할 때 다음 구조를 자주 사용한다.

\`\`\`python
def main():
    print("프로그램을 실행합니다.")

if __name__ == "__main__":
    main()
\`\`\`

이 구조는 파일이 직접 실행될 때만 \`main()\`을 호출한다. 다른 파일에서 import할 때는 \`main()\`이 자동으로 실행되지 않는다.

---

## 2.2 객체와 참조

### 2.2.1 파이썬의 모든 값은 객체다

파이썬에서는 정수, 실수, 문자열, 리스트, 딕셔너리, 함수, 클래스, 모듈까지 모두 객체다.

다음 값들은 모두 객체다.

\`\`\`python
10
3.14
"Python"
[1, 2, 3]
{"name": "Kim"}
print
\`\`\`

객체는 단순한 값만 의미하지 않는다. 객체는 다음과 같은 정보를 함께 가진다.

\`\`\`text
1. 값
2. 자료형
3. 고유한 식별 정보
4. 사용할 수 있는 동작
\`\`\`

예를 들어 문자열 객체는 문자열 값을 가지고 있고, \`upper()\`, \`lower()\`, \`split()\` 같은 메서드를 사용할 수 있다.

\`\`\`python
text = "python"
print(text.upper())
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
PYTHON
\`\`\`

---

### 2.2.2 변수는 상자가 아니라 이름이다

많은 입문 설명에서는 변수를 “값을 담는 상자”라고 표현한다. 초급 단계에서는 이 비유가 도움이 된다. 그러나 파이썬을 깊게 이해하려면 이 비유에서 벗어나야 한다.

파이썬에서 변수는 값을 직접 담는 상자라기보다 **객체에 붙는 이름**에 가깝다.

\`\`\`python
a = 10
\`\`\`

이 코드는 다음처럼 이해할 수 있다.

\`\`\`text
정수 객체 10이 있다.
a라는 이름이 그 객체를 가리킨다.
\`\`\`

다음 코드를 보자.

\`\`\`python
a = 10
b = a
\`\`\`

이 코드는 \`a\`의 값이 복사되어 \`b\`라는 상자에 들어간다는 의미가 아니다. 더 정확히는 다음과 같다.

\`\`\`text
a가 정수 객체 10을 가리킨다.
b도 같은 정수 객체 10을 가리킨다.
\`\`\`

이처럼 이름이 객체를 가리키는 관계를 **참조(reference)**라고 한다.

---

### 2.2.3 \`id()\`로 객체 확인하기

파이썬에서는 \`id()\` 함수를 사용해 객체의 식별값을 확인할 수 있다.

\`\`\`python
a = [1, 2, 3]
b = a

print(id(a))
print(id(b))
\`\`\`

두 값은 같다. 실제 숫자는 실행 환경마다 다르다.

\`\`\`text
140000000000000
140000000000000
\`\`\`

\`a\`와 \`b\`의 \`id()\`가 같다는 것은 두 이름이 같은 리스트 객체를 가리킨다는 뜻이다.

이번에는 다음 코드를 보자.

\`\`\`python
a = [1, 2, 3]
b = [1, 2, 3]

print(id(a))
print(id(b))
\`\`\`

두 리스트의 내용은 같지만 서로 다른 객체다. 따라서 \`id()\`는 다르게 나온다.

\`\`\`text
140000000000000
140000000000128
\`\`\`

이 차이는 \`==\`와 \`is\`의 차이로 이어진다.

---

### 2.2.4 \`==\`와 \`is\`

\`==\`는 두 객체의 **값이 같은지** 비교한다.  
\`is\`는 두 이름이 **같은 객체를 가리키는지** 비교한다.

\`\`\`python
a = [1, 2, 3]
b = [1, 2, 3]

print(a == b)
print(a is b)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
True
False
\`\`\`

두 리스트는 값은 같지만 같은 객체는 아니다.

반면 다음 코드는 다르다.

\`\`\`python
a = [1, 2, 3]
b = a

print(a == b)
print(a is b)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
True
True
\`\`\`

\`b = a\`는 새 리스트를 만드는 것이 아니라, \`a\`가 가리키던 리스트 객체에 \`b\`라는 이름을 하나 더 붙이는 것이다.

실무에서는 값을 비교할 때 대부분 \`==\`를 사용한다. \`is\`는 주로 \`None\`과 비교할 때 사용한다.

\`\`\`python
value = None

if value is None:
    print("값이 없습니다.")
\`\`\`

---

### 2.2.5 재할당

변수에 새 값을 대입하면 기존 객체가 바뀌는 것이 아니라, 이름이 다른 객체를 가리키게 된다.

\`\`\`python
a = 10
print(id(a))

a = 20
print(id(a))
\`\`\`

\`a = 20\`은 정수 객체 10을 20으로 수정하는 것이 아니다. \`a\`라는 이름이 이제 정수 객체 20을 가리키도록 바뀌는 것이다. 이것을 **재할당**이라고 한다.

문자열도 마찬가지다.

\`\`\`python
text = "python"
text = text.upper()

print(text)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
PYTHON
\`\`\`

\`upper()\`가 기존 문자열 객체를 직접 바꾼 것이 아니다. 대문자로 바뀐 새 문자열 객체를 만들고, \`text\`라는 이름이 그 새 객체를 가리키게 된 것이다.

---

## 2.3 가변 객체와 불변 객체 심화

### 2.3.1 가변 객체와 불변 객체

파이썬 객체는 크게 **가변 객체**와 **불변 객체**로 나눌 수 있다.

가변 객체는 만들어진 뒤 내부 값을 바꿀 수 있는 객체다.

\`\`\`text
list
set
dict
\`\`\`

불변 객체는 만들어진 뒤 내부 값을 바꿀 수 없는 객체다.

\`\`\`text
int
float
str
tuple
bool
None
\`\`\`

리스트는 가변 객체다.

\`\`\`python
numbers = [1, 2, 3]
numbers.append(4)

print(numbers)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
[1, 2, 3, 4]
\`\`\`

문자열은 불변 객체다.

\`\`\`python
text = "python"
# text[0] = "P"  # TypeError 발생
\`\`\`

문자열 일부를 직접 바꿀 수 없다. 바꾸고 싶다면 새 문자열을 만들어야 한다.

\`\`\`python
text = "Python" + text[1:]
print(text)
\`\`\`

---

### 2.3.2 가변 객체에서 주의할 점

가변 객체는 여러 이름이 같은 객체를 참조할 때 특히 주의해야 한다.

\`\`\`python
a = [1, 2, 3]
b = a

b.append(4)

print(a)
print(b)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
[1, 2, 3, 4]
[1, 2, 3, 4]
\`\`\`

\`a\`와 \`b\`는 같은 리스트 객체를 가리킨다. 따라서 \`b.append(4)\`는 \`b\`만 바꾸는 것이 아니라, 두 이름이 함께 가리키는 리스트 객체 자체를 바꾼다.

반면 숫자는 불변 객체다.

\`\`\`python
x = 10
y = x

y = y + 1

print(x)
print(y)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
10
11
\`\`\`

\`y = y + 1\`은 정수 객체 10을 11로 고친 것이 아니다. 정수 객체 11이 새로 만들어지고, \`y\`가 그 객체를 가리키게 된 것이다.

---

### 2.3.3 함수 인자로 가변 객체를 넘길 때

함수에 리스트를 전달할 때도 같은 문제가 생길 수 있다.

\`\`\`python
def add_item(items, item):
    items.append(item)

cart = ["keyboard", "mouse"]
add_item(cart, "monitor")

print(cart)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
['keyboard', 'mouse', 'monitor']
\`\`\`

함수 안에서 \`items.append(item)\`을 실행했는데 함수 밖의 \`cart\`도 바뀌었다. \`items\`와 \`cart\`가 같은 리스트 객체를 가리키기 때문이다.

이것이 항상 나쁜 것은 아니다. 함수가 외부 리스트를 변경하는 것이 의도라면 괜찮다. 하지만 의도하지 않은 변경이라면 문제가 된다.

외부 리스트를 바꾸지 않고 새 리스트를 반환하고 싶다면 다음처럼 작성할 수 있다.

\`\`\`python
def add_item_safely(items, item):
    new_items = items.copy()
    new_items.append(item)
    return new_items

cart = ["keyboard", "mouse"]
new_cart = add_item_safely(cart, "monitor")

print(cart)
print(new_cart)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
['keyboard', 'mouse']
['keyboard', 'mouse', 'monitor']
\`\`\`

실무에서는 함수가 전달받은 데이터를 직접 바꾸는지, 새 데이터를 반환하는지 명확히 설계해야 한다.

---

### 2.3.4 기본값 인자와 가변 객체 함정

파이썬 함수에서 가장 자주 만나는 실수 중 하나는 가변 객체를 기본값 인자로 사용하는 것이다.

\`\`\`python
def add_task(task, tasks=[]):
    tasks.append(task)
    return tasks

print(add_task("메일 보내기"))
print(add_task("보고서 작성"))
print(add_task("회의 준비"))
\`\`\`

많은 사람은 각각 새 리스트가 만들어질 것이라고 예상한다. 하지만 실행 결과는 다음과 같다.

\`\`\`text
['메일 보내기']
['메일 보내기', '보고서 작성']
['메일 보내기', '보고서 작성', '회의 준비']
\`\`\`

기본값 인자는 함수가 호출될 때마다 새로 만들어지는 것이 아니라, 함수가 정의될 때 한 번 만들어진다. 그래서 \`tasks=[]\` 리스트가 계속 재사용된다.

안전한 방식은 \`None\`을 기본값으로 사용하고, 함수 안에서 새 리스트를 만드는 것이다.

\`\`\`python
def add_task(task, tasks=None):
    if tasks is None:
        tasks = []
    tasks.append(task)
    return tasks
\`\`\`

이 패턴은 실무에서 매우 중요하다. 기본값 인자에 리스트, 딕셔너리, 집합 같은 가변 객체를 직접 넣는 것은 피하는 것이 좋다.

---

### 2.3.5 얕은 복사와 깊은 복사

리스트를 복사할 때 \`copy()\`를 사용할 수 있다.

\`\`\`python
a = [1, 2, 3]
b = a.copy()

b.append(4)

print(a)
print(b)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
[1, 2, 3]
[1, 2, 3, 4]
\`\`\`

겉으로 보기에는 복사가 잘 된 것 같다. 그러나 중첩 리스트에서는 주의해야 한다.

\`\`\`python
a = [[1, 2], [3, 4]]
b = a.copy()

b[0].append(99)

print(a)
print(b)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
[[1, 2, 99], [3, 4]]
[[1, 2, 99], [3, 4]]
\`\`\`

\`copy()\`는 바깥 리스트만 새로 만든다. 안쪽 리스트는 여전히 같은 객체를 참조한다. 이런 복사를 **얕은 복사**라고 한다.

중첩된 내부 객체까지 모두 새로 복사하려면 \`copy\` 모듈의 \`deepcopy()\`를 사용할 수 있다.

\`\`\`python
import copy

a = [[1, 2], [3, 4]]
b = copy.deepcopy(a)

b[0].append(99)

print(a)
print(b)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
[[1, 2], [3, 4]]
[[1, 2, 99], [3, 4]]
\`\`\`

실무에서 중첩 딕셔너리, JSON 응답, 설정값 객체를 복사할 때 이 차이가 중요하다.

---

## 2.4 네임스페이스와 스코프

### 2.4.1 네임스페이스란 무엇인가

**네임스페이스(namespace)**는 이름과 객체의 연결을 저장하는 공간이다.

다음 코드를 보자.

\`\`\`python
x = 10
name = "Python"

def hello():
    print("Hello")
\`\`\`

이 코드가 실행되면 현재 모듈의 네임스페이스에는 다음과 같은 이름이 등록된다.

\`\`\`text
x     → 정수 객체 10
name  → 문자열 객체 "Python"
hello → 함수 객체
\`\`\`

네임스페이스는 이름 충돌을 줄이고, 코드가 어느 범위에서 어떤 이름을 사용할 수 있는지 결정하는 역할을 한다.

---

### 2.4.2 지역, 전역, 내장 네임스페이스

파이썬에는 여러 종류의 네임스페이스가 있다.

\`\`\`text
지역 네임스페이스: 함수 안에서 만들어지는 이름
전역 네임스페이스: 모듈 전체에서 사용하는 이름
내장 네임스페이스: print, len, sum 같은 기본 이름
\`\`\`

예를 들어 다음 코드를 보자.

\`\`\`python
x = 10

def show():
    y = 20
    print(x)
    print(y)

show()
\`\`\`

\`x\`는 전역 네임스페이스에 있다. \`y\`는 \`show()\` 함수의 지역 네임스페이스에 있다.

함수 밖에서 \`y\`를 사용하면 에러가 발생한다.

\`\`\`python
print(y)  # NameError
\`\`\`

---

### 2.4.3 LEGB 규칙

파이썬은 이름을 찾을 때 다음 순서로 찾는다.

\`\`\`text
L: Local       지역 범위
E: Enclosing   바깥 함수 범위
G: Global      전역 범위
B: Built-in    내장 범위
\`\`\`

이 순서를 **LEGB 규칙**이라고 한다.

\`\`\`python
x = "global"

def outer():
    x = "enclosing"

    def inner():
        x = "local"
        print(x)

    inner()

outer()
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
local
\`\`\`

\`inner()\` 안에 지역 변수 \`x\`가 있기 때문에 가장 먼저 그 값을 사용한다.

이번에는 지역 변수 \`x\`를 제거해보자.

\`\`\`python
x = "global"

def outer():
    x = "enclosing"

    def inner():
        print(x)

    inner()

outer()
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
enclosing
\`\`\`

\`inner()\` 안에 \`x\`가 없으므로 바깥 함수인 \`outer()\`의 \`x\`를 찾는다.

---

### 2.4.4 \`global\`

함수 안에서 전역 변수를 읽는 것은 가능하다.

\`\`\`python
count = 0

def show_count():
    print(count)

show_count()
\`\`\`

하지만 함수 안에서 전역 변수에 새 값을 할당하려고 하면 주의해야 한다.

\`\`\`python
count = 0

def increase():
    count = count + 1

increase()
\`\`\`

이 코드는 에러가 발생한다. 함수 안에서 \`count = ...\` 형태의 할당이 있기 때문에 파이썬은 \`count\`를 지역 변수로 판단한다. 그런데 오른쪽의 \`count + 1\`을 계산하려면 아직 만들어지지 않은 지역 변수 \`count\`를 읽어야 하므로 문제가 된다.

전역 변수를 함수 안에서 수정하려면 \`global\`을 사용할 수 있다.

\`\`\`python
count = 0

def increase():
    global count
    count = count + 1

increase()
print(count)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
1
\`\`\`

하지만 \`global\`은 남용하지 않는 것이 좋다. 전역 상태가 많아지면 코드 흐름을 추적하기 어렵고 테스트하기도 어려워진다. 보통은 값을 함수에 전달하고 결과를 반환하는 방식이 더 안전하다.

\`\`\`python
def increase(count):
    return count + 1

count = 0
count = increase(count)
\`\`\`

---

### 2.4.5 \`nonlocal\`

\`nonlocal\`은 중첩 함수에서 바깥 함수의 변수를 수정할 때 사용한다.

\`\`\`python
def make_counter():
    count = 0

    def increase():
        nonlocal count
        count += 1
        return count

    return increase

counter = make_counter()

print(counter())
print(counter())
print(counter())
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
1
2
3
\`\`\`

\`increase()\` 함수는 \`make_counter()\`의 지역 변수였던 \`count\`를 기억하고 있다. 이런 구조는 다음 장에서 다룰 클로저와 데코레이터의 기반이 된다.

---

### 2.4.6 내장 이름 가리기 주의

파이썬에는 \`list\`, \`dict\`, \`str\`, \`sum\`, \`len\` 같은 내장 이름이 있다. 이런 이름을 변수명으로 사용하면 내장 함수를 가리게 된다.

\`\`\`python
list = [1, 2, 3]

numbers = list((4, 5, 6))  # TypeError 발생 가능
\`\`\`

\`list\`라는 이름에 리스트 객체를 할당했기 때문에, 원래의 내장 함수 \`list()\`를 사용할 수 없게 된다.

다음과 같은 변수명은 피하는 것이 좋다.

\`\`\`python
list = []
dict = {}
str = "hello"
sum = 100
max = 10
min = 1
\`\`\`

실무에서는 \`items\`, \`data\`, \`text\`, \`total\`, \`max_value\`, \`min_value\`처럼 의미가 드러나는 이름을 사용하는 것이 좋다.

---

## 2.5 메모리 관리 기초

### 2.5.1 객체는 언제 사라지는가

파이썬에서는 객체를 직접 삭제하는 경우가 많지 않다. 대부분 파이썬이 자동으로 메모리를 관리한다.

다음 코드를 보자.

\`\`\`python
a = [1, 2, 3]
b = a

print(id(a))
print(id(b))
\`\`\`

리스트 객체는 \`a\`와 \`b\` 두 이름에서 참조되고 있다. 만약 \`a\`를 삭제해도 객체가 바로 사라지는 것은 아니다.

\`\`\`python
del a
print(b)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
[1, 2, 3]
\`\`\`

\`del a\`는 리스트 객체를 직접 삭제하는 것이 아니라, \`a\`라는 이름과 객체의 연결을 끊는 것이다. \`b\`가 여전히 리스트 객체를 참조하고 있으므로 객체는 계속 남아 있다.

객체를 참조하는 이름이 더 이상 없으면 파이썬은 그 객체를 메모리에서 정리할 수 있다.

---

### 2.5.2 참조 카운트 개념

가장 널리 쓰이는 파이썬 구현체인 CPython은 객체가 몇 군데에서 참조되고 있는지 세는 방식을 사용한다. 이를 **참조 카운트**라고 한다.

\`\`\`python
a = [1, 2, 3]
b = a
c = a
\`\`\`

위 코드에서 리스트 객체는 \`a\`, \`b\`, \`c\` 세 이름에서 참조된다. 이름이 사라지거나 다른 객체를 가리키게 되면 참조 카운트가 줄어든다.

\`\`\`python
del b
c = None
\`\`\`

이제 리스트 객체를 참조하는 이름은 \`a\`만 남는다.

개념적으로는 다음과 같다.

\`\`\`text
참조하는 이름이 많다 → 객체가 살아 있다
참조하는 이름이 없다 → 객체를 정리할 수 있다
\`\`\`

참조 카운트의 세부 동작을 직접 제어할 필요는 없다. 중요한 것은 파이썬 객체가 이름과 참조 관계를 통해 생명 주기를 가진다는 점이다.

---

### 2.5.3 순환 참조와 가비지 컬렉션

참조 카운트만으로 해결하기 어려운 경우가 있다. 대표적인 예가 **순환 참조**다.

\`\`\`python
a = []
b = []

a.append(b)
b.append(a)
\`\`\`

\`a\`가 가리키는 리스트는 \`b\`가 가리키는 리스트를 포함하고, \`b\`가 가리키는 리스트는 다시 \`a\`가 가리키는 리스트를 포함한다.

이런 구조에서는 외부 이름이 사라져도 객체끼리 서로를 참조하고 있을 수 있다.

\`\`\`python
del a
del b
\`\`\`

이 경우 파이썬은 가비지 컬렉션을 통해 더 이상 사용할 수 없는 객체를 찾아 정리한다.

초급 단계에서는 가비지 컬렉션을 직접 다룰 필요가 거의 없다. 하지만 큰 데이터를 처리하거나 오래 실행되는 프로그램을 만들 때는 순환 참조와 메모리 증가 문제를 의식해야 한다.

---

### 2.5.4 메모리를 많이 사용하는 코드의 특징

데이터분석이나 자동화 작업에서는 대량 데이터를 다룰 수 있다. 다음과 같은 코드는 메모리를 많이 사용할 수 있다.

\`\`\`python
lines = open("large_log.txt", encoding="utf-8").readlines()
\`\`\`

이 코드는 파일의 모든 줄을 한 번에 리스트로 읽는다. 파일이 작으면 괜찮지만, 매우 큰 파일이라면 메모리를 많이 사용할 수 있다.

더 안전한 방식은 한 줄씩 처리하는 것이다.

\`\`\`python
with open("large_log.txt", encoding="utf-8") as file:
    for line in file:
        process(line)
\`\`\`

이 코드는 파일 전체를 메모리에 올리지 않고 한 줄씩 처리한다. 이후 이터레이터와 제너레이터를 배우면 이런 방식의 장점을 더 명확히 이해할 수 있다.

---

### 2.5.5 불필요한 복사를 줄이기

대용량 리스트나 딕셔너리를 다룰 때는 불필요한 복사가 성능과 메모리에 영향을 줄 수 있다.

\`\`\`python
def normalize_items(items):
    new_items = items.copy()
    for item in new_items:
        item["name"] = item["name"].strip()
    return new_items
\`\`\`

이 함수는 바깥 리스트만 복사한다. 하지만 리스트 안의 딕셔너리 객체는 여전히 원본과 공유된다. 그래서 원본 데이터가 의도치 않게 바뀔 수 있다.

\`\`\`python
items = [
    {"name": "  keyboard  "},
    {"name": "  mouse  "},
]

new_items = normalize_items(items)

print(items)
\`\`\`

이런 경우에는 함수가 원본을 수정하는 함수인지, 새 데이터를 반환하는 함수인지 명확히 정해야 한다. 대용량 데이터에서는 깊은 복사를 무조건 사용하는 것도 좋은 선택이 아닐 수 있다. 깊은 복사는 안전하지만 메모리 사용량과 실행 시간이 늘어날 수 있다.

실무에서는 다음 기준을 세우는 것이 좋다.

\`\`\`text
원본을 수정해도 되는가?
원본을 반드시 보존해야 하는가?
데이터가 얼마나 큰가?
중첩 구조가 있는가?
함수 이름이 동작을 명확히 설명하는가?
\`\`\`

---

## 2.6 실무에서 꼭 기억해야 할 객체 모델 규칙

### 2.6.1 할당은 복사가 아니다

다음 코드는 리스트를 복사하지 않는다.

\`\`\`python
b = a
\`\`\`

이 코드는 \`a\`가 가리키는 객체에 \`b\`라는 이름을 하나 더 붙이는 것이다.

리스트를 복사하려면 명시적으로 복사해야 한다.

\`\`\`python
b = a.copy()
\`\`\`

중첩 구조까지 복사해야 한다면 \`deepcopy()\`를 검토한다.

\`\`\`python
import copy
b = copy.deepcopy(a)
\`\`\`

---

### 2.6.2 변경과 재할당을 구분한다

다음은 리스트 객체 자체를 변경한다.

\`\`\`python
items.append("new")
\`\`\`

다음은 이름이 다른 객체를 가리키게 하는 재할당이다.

\`\`\`python
items = items + ["new"]
\`\`\`

두 코드는 결과가 비슷해 보일 수 있지만 동작 방식은 다르다. 같은 리스트를 다른 이름도 참조하고 있다면 차이가 드러난다.

\`\`\`python
a = [1, 2]
b = a

a.append(3)
print(b)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
[1, 2, 3]
\`\`\`

이번에는 재할당을 보자.

\`\`\`python
a = [1, 2]
b = a

a = a + [3]
print(b)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
[1, 2]
\`\`\`

\`append()\`는 기존 리스트 객체를 변경하고, \`a = a + [3]\`은 새 리스트 객체를 만들어 \`a\`가 그 객체를 가리키게 한다.

---

### 2.6.3 함수가 입력 데이터를 바꾸는지 명확히 한다

함수 설계에서 다음 두 방식은 구분해야 한다.

첫 번째는 입력 리스트를 직접 바꾸는 방식이다.

\`\`\`python
def remove_empty_values(values):
    while "" in values:
        values.remove("")
\`\`\`

두 번째는 새 리스트를 반환하는 방식이다.

\`\`\`python
def get_non_empty_values(values):
    return [value for value in values if value != ""]
\`\`\`

첫 번째 함수는 원본을 변경한다. 두 번째 함수는 원본을 변경하지 않는다.

실무에서는 함수 이름에 의도를 드러내는 것이 좋다.

\`\`\`text
remove_...   → 원본을 변경할 가능성이 있음
get_...      → 값을 계산해 반환하는 느낌
create_...   → 새 객체 생성
normalize_... → 정규화 처리, 원본 변경 여부를 문서화해야 함
\`\`\`

---

## 2장 핵심 정리

이번 장에서 배운 핵심은 다음과 같다.

1. 파이썬 코드는 소스 코드에서 바이트코드로 변환된 뒤 실행된다.
2. \`import\`는 모듈을 한 번 실행하고 그 안의 이름을 사용할 수 있게 한다.
3. 파이썬의 모든 값은 객체다.
4. 변수는 값을 담는 상자가 아니라 객체를 가리키는 이름이다.
5. \`==\`는 값 비교이고, \`is\`는 같은 객체인지 비교한다.
6. 리스트, 딕셔너리, 집합은 가변 객체다.
7. 정수, 실수, 문자열, 튜플은 불변 객체다.
8. 가변 객체를 함수 인자로 넘기면 함수 안에서 원본이 바뀔 수 있다.
9. 기본값 인자에 리스트나 딕셔너리 같은 가변 객체를 직접 쓰면 위험하다.
10. 얕은 복사는 바깥 객체만 복사하고, 깊은 복사는 중첩 객체까지 복사한다.
11. 파이썬은 LEGB 규칙에 따라 이름을 찾는다.
12. \`global\`과 \`nonlocal\`은 필요한 경우에만 신중하게 사용한다.
13. \`del\`은 객체를 직접 지우는 것이 아니라 이름과 객체의 연결을 끊는다.
14. 큰 데이터를 다룰 때는 한 번에 모두 메모리에 올리지 않는 방식이 중요하다.

---

## 연습문제

### 문제 1. 실행 결과 예측하기

다음 코드의 실행 결과를 예측하시오.

\`\`\`python
a = [1, 2, 3]
b = a
b.append(4)

print(a)
\`\`\`

---

### 문제 2. \`==\`와 \`is\`

다음 코드의 실행 결과를 예측하시오.

\`\`\`python
a = [1, 2]
b = [1, 2]

print(a == b)
print(a is b)
\`\`\`

---

### 문제 3. 불변 객체

다음 코드의 실행 결과를 예측하시오.

\`\`\`python
x = 10
y = x
y = y + 5

print(x)
print(y)
\`\`\`

---

### 문제 4. 함수와 가변 객체

다음 코드의 실행 결과를 예측하시오.

\`\`\`python
def add_value(values):
    values.append(100)

numbers = [1, 2, 3]
add_value(numbers)

print(numbers)
\`\`\`

---

### 문제 5. 안전한 함수로 수정하기

다음 함수는 원본 리스트를 변경한다. 원본을 변경하지 않고 새 리스트를 반환하도록 수정하시오.

\`\`\`python
def add_value(values, value):
    values.append(value)
    return values
\`\`\`

---

### 문제 6. 기본값 인자 문제

다음 코드의 문제점을 설명하고 수정하시오.

\`\`\`python
def add_log(message, logs=[]):
    logs.append(message)
    return logs
\`\`\`

---

### 문제 7. 얕은 복사

다음 코드의 실행 결과를 예측하시오.

\`\`\`python
a = [[1, 2], [3, 4]]
b = a.copy()

b[0].append(99)

print(a)
\`\`\`

---

### 문제 8. LEGB 규칙

다음 코드의 실행 결과를 예측하시오.

\`\`\`python
x = "global"

def outer():
    x = "enclosing"

    def inner():
        print(x)

    inner()

outer()
\`\`\`

---

### 문제 9. 내장 이름 가리기

다음 코드에서 문제가 될 수 있는 부분을 찾고, 더 나은 변수명으로 수정하시오.

\`\`\`python
list = [10, 20, 30]
sum = 0

for number in list:
    sum += number

print(sum)
\`\`\`

---

### 문제 10. 변경과 재할당

다음 두 코드의 차이를 설명하시오.

\`\`\`python
# 코드 A
a = [1, 2]
b = a
a.append(3)
print(b)
\`\`\`

\`\`\`python
# 코드 B
a = [1, 2]
b = a
a = a + [3]
print(b)
\`\`\`

---

## 정답 및 해설

### 문제 1 정답

\`\`\`text
[1, 2, 3, 4]
\`\`\`

\`a\`와 \`b\`는 같은 리스트 객체를 가리킨다. \`b.append(4)\`는 그 리스트 객체 자체를 변경하므로 \`a\`로 출력해도 변경된 결과가 보인다.

---

### 문제 2 정답

\`\`\`text
True
False
\`\`\`

\`a\`와 \`b\`는 값은 같지만 서로 다른 리스트 객체다. 따라서 \`==\`는 \`True\`, \`is\`는 \`False\`다.

---

### 문제 3 정답

\`\`\`text
10
15
\`\`\`

정수는 불변 객체다. \`y = y + 5\`는 기존 정수 객체를 바꾸는 것이 아니라 새 정수 객체 \`15\`를 만들고 \`y\`가 그 객체를 가리키게 한다. \`x\`는 여전히 \`10\`을 가리킨다.

---

### 문제 4 정답

\`\`\`text
[1, 2, 3, 100]
\`\`\`

리스트는 가변 객체다. 함수의 매개변수 \`values\`와 함수 밖의 \`numbers\`는 같은 리스트 객체를 가리킨다. 함수 안에서 \`append()\`를 실행하면 원본 리스트가 변경된다.

---

### 문제 5 정답 예시

\`\`\`python
def add_value(values, value):
    new_values = values.copy()
    new_values.append(value)
    return new_values
\`\`\`

원본 리스트를 직접 변경하지 않으려면 먼저 복사본을 만들고, 복사본에 값을 추가한 뒤 반환한다.

---

### 문제 6 정답 예시

문제점은 기본값 인자로 빈 리스트를 직접 사용했다는 것이다. 이 리스트는 함수가 호출될 때마다 새로 만들어지지 않고, 함수 정의 시 한 번 만들어진 뒤 계속 재사용된다.

수정 예시는 다음과 같다.

\`\`\`python
def add_log(message, logs=None):
    if logs is None:
        logs = []
    logs.append(message)
    return logs
\`\`\`

---

### 문제 7 정답

\`\`\`text
[[1, 2, 99], [3, 4]]
\`\`\`

\`copy()\`는 바깥 리스트만 복사한다. 안쪽 리스트는 원본과 복사본이 함께 참조한다. 따라서 \`b[0]\`을 변경하면 \`a[0]\`도 변경된 것처럼 보인다.

---

### 문제 8 정답

\`\`\`text
enclosing
\`\`\`

\`inner()\` 안에는 \`x\`가 없다. 파이썬은 LEGB 규칙에 따라 바깥 함수인 \`outer()\`의 \`x\`를 찾고 출력한다.

---

### 문제 9 정답 예시

\`list\`와 \`sum\`은 파이썬의 내장 이름이다. 변수명으로 사용하면 내장 함수 \`list()\`와 \`sum()\`을 가리게 된다.

수정 예시는 다음과 같다.

\`\`\`python
numbers = [10, 20, 30]
total = 0

for number in numbers:
    total += number

print(total)
\`\`\`

---

### 문제 10 정답

코드 A의 실행 결과는 다음과 같다.

\`\`\`text
[1, 2, 3]
\`\`\`

\`a.append(3)\`은 기존 리스트 객체를 변경한다. \`a\`와 \`b\`가 같은 리스트를 가리키므로 \`b\`로 출력해도 변경된 결과가 보인다.

코드 B의 실행 결과는 다음과 같다.

\`\`\`text
[1, 2]
\`\`\`

\`a = a + [3]\`은 새 리스트를 만들고 \`a\`가 그 새 리스트를 가리키게 한다. \`b\`는 여전히 원래 리스트 \`[1, 2]\`를 가리킨다.

---

## 다음 장으로

이번 장에서는 파이썬의 실행 구조와 객체 모델을 살펴보았다. 이제 변수와 객체의 관계, 가변성과 불변성, 스코프, 메모리 관리의 기본 흐름을 이해했다.

다음 장에서는 이 이해를 바탕으로 **함수 고급 활용**을 배운다. 함수도 객체라는 사실을 이용해 함수를 인자로 넘기고, 함수를 반환하고, 클로저를 만들고, 더 유연한 함수 설계 패턴을 익힐 것이다.
`;export{e as default};