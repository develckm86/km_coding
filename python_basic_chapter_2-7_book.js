var e=`<!-- 원본: python_basic_chapter_2_book.md / 세부 장: 2-7 -->

# 2.7 기본 입출력

## 입출력이란 무엇인가

입력은 프로그램 밖에서 값을 받아오는 것이다. 출력은 프로그램이 처리한 결과를 밖으로 보여주는 것이다.

가장 기본적인 출력 함수는 \`print()\`이고, 가장 기본적인 입력 함수는 \`input()\`이다.

\`\`\`python
print("안녕하세요")
\`\`\`

위 코드는 화면에 문자열을 출력한다.

\`\`\`python
name = input("이름을 입력하세요: ")
\`\`\`

위 코드는 사용자에게 이름을 입력받는다.

입출력은 프로그램이 사용자와 대화하는 가장 기본적인 방법이다.

## 출력 함수 \`print()\`

\`print()\`는 괄호 안의 값을 화면에 출력한다.

\`\`\`python
print("Hello, Python!")
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
Hello, Python!
\`\`\`

여러 값을 쉼표로 구분해서 출력할 수도 있다.

\`\`\`python
name = "홍길동"
age = 25

print(name, age)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
홍길동 25
\`\`\`

쉼표로 여러 값을 출력하면 기본적으로 값 사이에 공백이 들어간다.

## \`sep\` 옵션

\`sep\`은 여러 값을 출력할 때 값 사이에 넣을 문자열을 지정하는 옵션이다.

\`\`\`python
print("2026", "06", "15", sep="-")
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
2026-06-15
\`\`\`

기본값은 공백이다.

\`\`\`python
print("홍길동", "25", "서울")
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
홍길동 25 서울
\`\`\`

쉼표로 구분된 형태로 출력하고 싶다면 \`sep=","\`를 사용할 수 있다.

\`\`\`python
print("홍길동", "25", "서울", sep=",")
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
홍길동,25,서울
\`\`\`

## \`end\` 옵션

\`print()\`는 기본적으로 출력이 끝난 뒤 줄바꿈을 한다. 이 줄 끝 처리를 바꾸고 싶을 때 \`end\` 옵션을 사용한다.

\`\`\`python
print("Hello", end=" ")
print("Python")
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
Hello Python
\`\`\`

첫 번째 \`print()\`가 줄바꿈 대신 공백으로 끝났기 때문에, 두 번째 출력이 같은 줄에 이어서 나온다.

\`end\` 옵션은 진행 상황을 한 줄에 표시하거나, 출력 모양을 조정할 때 사용할 수 있다.

## 입력 함수 \`input()\`

\`input()\`은 사용자에게 값을 입력받을 때 사용한다.

\`\`\`python
name = input("이름을 입력하세요: ")
print(name)
\`\`\`

프로그램을 실행하면 다음과 같이 입력을 기다린다.

\`\`\`text
이름을 입력하세요: 
\`\`\`

사용자가 \`홍길동\`이라고 입력하면 \`name\` 변수에 \`"홍길동"\`이 저장된다.

\`\`\`text
이름을 입력하세요: 홍길동
홍길동
\`\`\`

\`input()\`의 괄호 안에 넣은 문자열은 입력 안내 문구로 표시된다.

## \`input()\`의 결과는 항상 문자열이다

\`input()\`으로 받은 값은 항상 문자열이다. 사용자가 숫자를 입력해도 문자열로 저장된다.

\`\`\`python
age = input("나이를 입력하세요: ")

print(age)
print(type(age))
\`\`\`

사용자가 \`25\`를 입력하면 실행 결과는 다음과 같다.

\`\`\`text
25
<class 'str'>
\`\`\`

화면에는 \`25\`처럼 보이지만 실제 자료형은 문자열이다. 따라서 숫자 계산을 하려면 정수나 실수로 변환해야 한다.

## 입력값을 정수로 변환하기

나이, 수량, 가격처럼 정수로 계산해야 하는 값은 \`int()\`로 변환한다.

\`\`\`python
age_text = input("나이를 입력하세요: ")
age = int(age_text)

print(age + 1)
\`\`\`

사용자가 \`25\`를 입력하면 실행 결과는 다음과 같다.

\`\`\`text
26
\`\`\`

다음처럼 한 줄로 작성할 수도 있다.

\`\`\`python
age = int(input("나이를 입력하세요: "))
print(age + 1)
\`\`\`

하지만 처음 배울 때는 두 단계로 나누어 작성하는 것이 이해하기 쉽다.

\`\`\`python
age_text = input("나이를 입력하세요: ")
age = int(age_text)
\`\`\`

첫 번째 줄은 문자열 입력을 받고, 두 번째 줄은 그 문자열을 정수로 변환한다.

## 입력값을 실수로 변환하기

키, 몸무게, 할인율처럼 소수점이 필요한 값은 \`float()\`으로 변환한다.

\`\`\`python
height_text = input("키를 입력하세요: ")
height = float(height_text)

print(height)
print(type(height))
\`\`\`

사용자가 \`175.5\`를 입력하면 실행 결과는 다음과 같다.

\`\`\`text
175.5
<class 'float'>
\`\`\`

## 입력값 변환 시 주의할 점

숫자로 바꿀 수 없는 값을 \`int()\`나 \`float()\`으로 변환하려고 하면 오류가 발생한다.

\`\`\`python
age_text = "스무살"
age = int(age_text)
\`\`\`

이 코드는 실행하면 오류가 발생한다.

\`\`\`text
ValueError: invalid literal for int() with base 10
\`\`\`

사용자 입력은 항상 예상한 대로 들어온다고 보장할 수 없다. 사용자가 숫자 대신 문자를 입력할 수도 있고, 아무것도 입력하지 않을 수도 있다.

이런 상황을 안전하게 처리하는 방법은 예외 처리 장에서 자세히 배운다. 지금은 \`input()\`으로 받은 값이 문자열이고, 숫자 계산을 하려면 변환이 필요하다는 점을 이해하면 된다.

## 입력값 검증 기초

입력값을 실제로 처리하기 전에 문자열 메서드로 간단히 확인할 수 있다.

\`\`\`python
age_text = "25"
print(age_text.isdigit())
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
True
\`\`\`

숫자가 아닌 문자열은 다음과 같이 나온다.

\`\`\`python
age_text = "스무살"
print(age_text.isdigit())
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
False
\`\`\`

아직 조건문을 배우지 않았으므로, \`True\`일 때는 어떻게 하고 \`False\`일 때는 어떻게 하는지 코드를 작성하지는 않는다. 다음 장에서 조건문을 배우면 이 결과를 이용해 입력값을 검사하는 프로그램을 만들 수 있다.

## 입력과 출력 실무 예시

상품 가격과 수량을 입력받아 총 금액을 출력하는 예시를 보자.

\`\`\`python
price_text = input("상품 가격을 입력하세요: ")
quantity_text = input("수량을 입력하세요: ")

price = int(price_text)
quantity = int(quantity_text)

total_price = price * quantity

print(f"총 금액은 {total_price:,}원입니다.")
\`\`\`

사용자가 상품 가격으로 \`15000\`, 수량으로 \`3\`을 입력하면 다음과 같이 출력된다.

\`\`\`text
총 금액은 45,000원입니다.
\`\`\`

이 예제에는 지금까지 배운 내용이 함께 사용된다.

- \`input()\`으로 사용자 입력을 받는다.
- 입력값은 문자열이므로 \`int()\`로 변환한다.
- 숫자끼리 곱해 총 금액을 계산한다.
- f-string으로 보기 좋은 메시지를 만든다.
- \`:,\`를 사용해 숫자에 쉼표를 넣어 출력한다.

## 2.7 핵심 정리

- \`print()\`는 값을 화면에 출력한다.
- \`print()\`의 \`sep\` 옵션은 값 사이의 구분자를 지정한다.
- \`print()\`의 \`end\` 옵션은 출력 끝 문자를 지정한다.
- \`input()\`은 사용자 입력을 받는다.
- \`input()\`의 결과는 항상 문자열이다.
- 숫자 계산을 하려면 \`int()\`나 \`float()\`으로 변환해야 한다.
- 입력값 변환에 실패하면 오류가 발생할 수 있다.
- 입력값 검증은 조건문과 예외 처리에서 더 자세히 배운다.

---

# 2장 마무리

## 2장에서 배운 내용

이번 장에서는 파이썬 기본 문법의 가장 중요한 출발점을 배웠다. 변수에 값을 저장하고, 값의 종류인 자료형을 구분하고, 문자열과 수 데이터를 다루는 방법을 익혔다. 또한 연산자로 숫자를 계산하고, 비교 연산자로 참과 거짓을 판단하는 방법도 살펴보았다. 마지막으로 \`print()\`와 \`input()\`을 사용해 가장 기본적인 입출력을 다루었다.

이 장의 핵심은 다음과 같다.

- 변수는 값을 저장하기 위한 이름이다.
- 자료형은 값의 종류이다.
- 문자열은 문자의 열이며, 불변 데이터이다.
- 문자열 메서드로 공백 제거, 대소문자 변환, 검색, 치환, 분리, 결합을 할 수 있다.
- 정수와 실수는 서로 다른 수 자료형이다.
- 연산자를 사용해 숫자를 계산할 수 있다.
- 비교 연산자는 결과로 \`True\` 또는 \`False\`를 반환한다.
- \`input()\`으로 받은 값은 항상 문자열이다.

다음 장에서는 이 장에서 배운 불리언 값과 비교 연산자를 바탕으로 조건문을 배운다. 조건문을 배우면 “나이가 19 이상이면 성인으로 처리한다”, “재고가 0이면 품절로 표시한다”처럼 조건에 따라 다른 코드를 실행할 수 있게 된다.

---

# 2장 연습문제

## 문제 1. 변수와 자료형

다음 코드의 실행 결과를 예상해 보자.

\`\`\`python
name = "Python"
year = 1991

print(name)
print(year)
print(type(name))
print(type(year))
\`\`\`

## 문제 2. 변수 이름

다음 중 파이썬 변수 이름으로 사용할 수 없는 것을 모두 고르자.

\`\`\`text
1. user_name
2. 2score
3. total_price
4. class
5. user-age
6. is_active
\`\`\`

## 문제 3. 문자열 선언

다음 문장을 문자열 변수 \`message\`에 저장하는 코드를 작성해 보자.

\`\`\`text
파이썬은 재미있습니다.
\`\`\`

## 문제 4. 문자열 인덱싱

다음 코드의 실행 결과를 예상해 보자.

\`\`\`python
text = "Python"

print(text[0])
print(text[-1])
print(text[1:4])
\`\`\`

## 문제 5. 문자열 메서드

다음 코드의 실행 결과를 예상해 보자.

\`\`\`python
email = " USER@EXAMPLE.COM "
clean_email = email.strip().lower()

print(clean_email)
\`\`\`

## 문제 6. 문자열 변경

전화번호 문자열에서 하이픈을 제거하는 코드를 작성해 보자.

\`\`\`python
phone = "010-1234-5678"
\`\`\`

원하는 결과는 다음과 같다.

\`\`\`text
01012345678
\`\`\`

## 문제 7. 수 데이터

다음 값의 자료형을 예상해 보자.

\`\`\`python
a = 10
b = 10.0
c = "10"
\`\`\`

## 문제 8. 숫자 변환

다음 문자열을 정수로 변환한 뒤, 3을 곱해 출력하는 코드를 작성해 보자.

\`\`\`python
price_text = "15000"
\`\`\`

## 문제 9. 연산자

다음 코드의 실행 결과를 예상해 보자.

\`\`\`python
print(10 / 3)
print(10 // 3)
print(10 % 3)
print(2 ** 4)
\`\`\`

## 문제 10. 연산자 우선순위

다음 두 코드의 실행 결과가 어떻게 다른지 예상해 보자.

\`\`\`python
result1 = 10 + 2 * 3
result2 = (10 + 2) * 3

print(result1)
print(result2)
\`\`\`

## 문제 11. 불리언과 비교 연산자

다음 코드의 실행 결과를 예상해 보자.

\`\`\`python
age = 20

print(age >= 19)
print(age == 18)
print(age != 18)
\`\`\`

## 문제 12. 논리 연산자

다음 코드의 실행 결과를 예상해 보자.

\`\`\`python
is_member = True
has_coupon = False

print(is_member and has_coupon)
print(is_member or has_coupon)
print(not has_coupon)
\`\`\`

## 문제 13. \`input()\`의 자료형

다음 코드에서 사용자가 \`25\`를 입력했다고 하자. \`type(age)\`의 결과는 무엇인가?

\`\`\`python
age = input("나이를 입력하세요: ")
print(type(age))
\`\`\`

## 문제 14. 입력값 계산

사용자에게 상품 가격과 수량을 입력받아 총 금액을 출력하는 코드를 작성해 보자. 아직 조건문은 사용하지 않는다.

출력 예시는 다음과 같다.

\`\`\`text
총 금액은 30000원입니다.
\`\`\`

## 문제 15. 코드 오류 찾기

다음 코드는 오류가 발생한다. 이유를 설명하고 올바르게 고쳐 보자.

\`\`\`python
age = 25
message = "나이: " + age

print(message)
\`\`\`

---

# 2장 연습문제 정답 및 해설

## 정답 1

\`\`\`text
Python
1991
<class 'str'>
<class 'int'>
\`\`\`

\`"Python"\`은 문자열이므로 \`str\`이고, \`1991\`은 정수이므로 \`int\`이다.

## 정답 2

사용할 수 없는 변수 이름은 다음과 같다.

\`\`\`text
2. 2score
4. class
5. user-age
\`\`\`

\`2score\`는 숫자로 시작하기 때문에 사용할 수 없다. \`class\`는 예약어이다. \`user-age\`는 \`-\`가 포함되어 있어 변수 이름으로 사용할 수 없다.

## 정답 3

\`\`\`python
message = "파이썬은 재미있습니다."
\`\`\`

작은따옴표를 사용해도 된다.

\`\`\`python
message = '파이썬은 재미있습니다.'
\`\`\`

## 정답 4

\`\`\`text
P
n
yth
\`\`\`

\`text[0]\`은 첫 번째 문자 \`P\`이다. \`text[-1]\`은 마지막 문자 \`n\`이다. \`text[1:4]\`는 1번부터 3번까지의 문자를 가져오므로 \`y\`, \`t\`, \`h\`가 포함된다. 슬라이싱에서 끝 인덱스 4는 포함되지 않는다.

## 정답 5

\`\`\`text
user@example.com
\`\`\`

\`strip()\`이 앞뒤 공백을 제거하고, \`lower()\`가 대문자를 소문자로 바꾼다.

## 정답 6

\`\`\`python
phone = "010-1234-5678"
clean_phone = phone.replace("-", "")

print(clean_phone)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
01012345678
\`\`\`

## 정답 7

\`\`\`text
a: int
b: float
c: str
\`\`\`

\`10\`은 정수, \`10.0\`은 실수, \`"10"\`은 문자열이다.

## 정답 8

\`\`\`python
price_text = "15000"
price = int(price_text)
result = price * 3

print(result)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
45000
\`\`\`

## 정답 9

\`\`\`text
3.3333333333333335
3
1
16
\`\`\`

\`/\`는 일반 나눗셈, \`//\`는 몫, \`%\`는 나머지, \`**\`는 거듭제곱이다.

## 정답 10

\`\`\`text
16
36
\`\`\`

\`result1\`은 곱셈이 먼저 계산되어 \`10 + 6\`이 된다. \`result2\`는 괄호 안의 \`10 + 2\`가 먼저 계산되어 \`12 * 3\`이 된다.

## 정답 11

\`\`\`text
True
False
True
\`\`\`

\`age\`는 20이다. 20은 19 이상이므로 첫 번째 결과는 \`True\`이다. 20은 18과 같지 않으므로 두 번째 결과는 \`False\`, 세 번째 결과는 \`True\`이다.

## 정답 12

\`\`\`text
False
True
True
\`\`\`

\`and\`는 두 값이 모두 참일 때만 참이다. \`or\`는 하나라도 참이면 참이다. \`not\`은 참과 거짓을 반대로 바꾼다.

## 정답 13

\`\`\`text
<class 'str'>
\`\`\`

\`input()\`으로 받은 값은 사용자가 숫자를 입력해도 항상 문자열이다.

## 정답 14

예시 답안은 다음과 같다.

\`\`\`python
price_text = input("상품 가격을 입력하세요: ")
quantity_text = input("수량을 입력하세요: ")

price = int(price_text)
quantity = int(quantity_text)

total_price = price * quantity

print(f"총 금액은 {total_price}원입니다.")
\`\`\`

쉼표를 넣어 출력하고 싶다면 다음처럼 작성할 수 있다.

\`\`\`python
print(f"총 금액은 {total_price:,}원입니다.")
\`\`\`

## 정답 15

오류가 발생하는 이유는 문자열과 정수를 \`+\`로 바로 연결할 수 없기 때문이다.

잘못된 코드:

\`\`\`python
age = 25
message = "나이: " + age
\`\`\`

수정 방법 1: 숫자를 문자열로 변환한다.

\`\`\`python
age = 25
message = "나이: " + str(age)

print(message)
\`\`\`

수정 방법 2: f-string을 사용한다.

\`\`\`python
age = 25
message = f"나이: {age}"

print(message)
\`\`\`

실행 결과는 다음과 같다.

\`\`\`text
나이: 25
\`\`\`
`;export{e as default};